# vcc-ctrgcn environment
# Python 3.10, PyTorch 1.12.1+cu113 (via conda)
# Purpose: CTR-GCN feature extraction using PYSKL
# Frozen from pip freeze 2026-03-31
# NOTE: PyTorch and cudatoolkit installed via conda, not pip
# torch==1.12.1+cu113 (conda: pytorch==1.12.1 cudatoolkit=11.3 -c pytorch -c conda-forge)
# torchvision==0.13.1+cu113 (conda, version-locked with torch)
# NOTE: mmcv-full reinstallation requires: pip install mmcv-full==1.7.0 -f https://download.openmmlab.com/mmcv/dist/cu113/torch1.12.0/index.html
# NOTE: pyskl installed from D:\libs\pyskl via pip install -e --no-deps (per D-04)
mmcv-full==1.7.0
mmdet==2.25.1
mmpose==0.29.0
fvcore==0.1.5.post20221221
decord==0.6.0
numpy==1.26.4
scipy==1.15.3
tqdm==4.67.3
pytest==9.0.2
