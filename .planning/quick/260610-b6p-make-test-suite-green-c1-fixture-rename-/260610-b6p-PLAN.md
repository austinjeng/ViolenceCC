---
quick_id: 260610-b6p
status: in-progress
date: 2026-06-10
---

# Quick Task 260610-b6p: Make the test suite green

**Source:** `.planning/REVIEW-2026-06-10.md` §3 (C1, C2, C3) + §4 RED inventory.
Confirmed red baseline (after Batch A 260610-aqm fixed the queue-count test):
6 failed / 315 passed.

| Red test | Finding |
|----------|---------|
| `test_evaluate_cli.py` ×3 (smoke / bit_identical / done_after_metrics) | **C1** manifest collision |
| `test_run_ablations_tta.py::test_tta_total_runs_is_500` | stale **TTA pin** |
| `test_splits.py::test_split_seed_reproducibility` | **C3** xd_train regen |
| `test_train_integration.py::test_no_test_split_access` | **C3** scanner over-broad |

Plus **C2** (i3d val `shuffle=False`) — a latent bug with no covering test; fixed on
principle (project convention: val loaders MUST `shuffle=True`).

## Task 1 — C1: rename colliding synthetic fixture ids
- **files:** `tests/fixtures/synthetic_eval.py`
- **why:** All 5 anomaly fixture ids are REAL UCF ids present in
  `data/ucf_total_frames.json` (Abuse028=142, Arson011=127, Assault010=1618,
  Fighting003=311, Shooting007=143). evaluate.py's M3 manifest fallback
  (`evaluate.py:330`) keys off the video id → hijacks `n_frames` from the manifest
  → snippet/frame grid mismatch → CLI fails. (`Normal_Videos_001-005` are NOT in
  the manifest, verified — left unchanged.)
- **action:** rename the 5 anomaly ids to clearly-synthetic non-colliding names
  (`SynthAbuse900`/`SynthArson901`/`SynthFighting902`/`SynthAssault903`/
  `SynthShooting904`); category comes from the explicit annotation column, not the
  id, so categories are unaffected. Add a regression-guard comment.
- **verify:** `pytest tests/test_evaluate_cli.py` green.

## Task 2 — C2: i3d val loader shuffle
- **files:** `src/data/loaders.py:346`
- **action:** `shuffle=False` → `shuffle=True` (seeded `g_val` already wired).
- **verify:** py_compile; convention satisfied (mirrors fusion-path fix 0999033).

## Task 3 — C3a: encode xd_train exclusions
- **files:** `scripts/create_splits.py`
- **action:** add `XD_TRAIN_EXCLUSIONS` (2 ids + reasons, insertion order =
  committed header order) and rewrite the `xd_train.txt` write to emit the comment
  header + sorted(train_ids − exclusions). Both excluded videos are enumerated
  from E:/ and land in TRAIN (xd_val/xd_test already reproduce), so filtering only
  the train write reproduces the committed file byte-for-byte.
- **verify:** `python scripts/create_splits.py --verify` → PASSED;
  `pytest tests/test_splits.py::test_split_seed_reproducibility` green.

## Task 4 — C3b: scope the leakage scanner
- **files:** `tests/test_train_integration.py` (`test_no_test_split_access`)
- **action:** skip `src/eval/` and `src/tta/` in the rglob — those subsystems
  legitimately load `*_test.txt` (inference/adaptation). Only 2 files contain
  `_test.txt` and both are eval/tta; the guard targets TRAINING-side leakage.
- **verify:** `pytest tests/test_train_integration.py::test_no_test_split_access` green.

## Task 5 — TTA pin
- **files:** `tests/test_run_ablations_tta.py`
- **action:** update `500` → `680` (20+80+400 + 3×60 backbone queues) + docstring +
  function name.
- **verify:** `pytest tests/test_run_ablations_tta.py` green.

## Done
Full suite green (0 failed). C2 has no covering test (latent) — verified by compile.
