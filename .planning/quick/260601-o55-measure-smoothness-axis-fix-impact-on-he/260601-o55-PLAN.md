---
phase: quick-260601-o55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/_giant_smoothmeasure.yaml
  - src/losses/mil_loss.py
  - tests/test_smoothness_axis.py
  - results/_smoothfix/measurement.md
autonomous: true
requirements: [QUICK-o55]

must_haves:
  truths:
    - "_smoothness penalizes differences between adjacent snippets in time (dim 1), not between videos (dim 0)"
    - "A regression test fails against the old batch-axis behavior and passes against the corrected temporal behavior"
    - "Control (buggy) and fixed retrains both complete full-length eval on the headline UCF Giant config, seed 42"
    - "measurement.md reports baseline/control/fixed AUC+AP plus Δ_smoothness and Δ_vs_reported with a noise-floor verdict"
  artifacts:
    - path: "src/losses/mil_loss.py"
      provides: "Temporal-axis smoothness regularizer"
      contains: "scores_abn[:, 1:]"
    - path: "tests/test_smoothness_axis.py"
      provides: "Axis-distinguishing regression test for _smoothness"
      contains: "_smoothness"
    - path: "results/_smoothfix/measurement.md"
      provides: "Four-row AUC/AP delta table + verdict"
      contains: "Δ_smoothness"
  key_links:
    - from: "configs/_giant_smoothmeasure.yaml"
      to: "src/evaluate.py full-length path"
      via: "paths.snippet_boundaries_dir persisted into config_snapshot.json"
      pattern: "snippet_boundaries_dir"
---

<objective>
Measure whether the confirmed smoothness-axis bug in `src/losses/mil_loss.py` materially changed the headline UCF result, so the orchestrator can decide (with the user) whether a full canonical retrain is warranted.

Method: retrain the headline config twice at seed 42 — once with the CURRENT buggy loss (control) and once after fixing the axis (fixed) — evaluate both full-length, and report the AUC/AP delta. The fixed-minus-control delta is the clean signal (it isolates the loss change from retrain nondeterminism); the fixed-minus-reported delta shows total drift vs the canonical number 0.8249/0.2737.

Purpose: The smoothness term lives in the TRAINING loss, so static inspection cannot tell us its effect on results — only a retrain A/B can.
Output: Code fix + regression test (committed), and a scratch measurement report (NOT committed). No canonical results, tables, figures, or paper are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Confirmed from codebase — executor needs no exploration. -->

src/losses/mil_loss.py — current BUGGY _smoothness (lines 33-49):
  def _smoothness(scores_abn: torch.Tensor, lam: float = 8e-4) -> torch.Tensor:
      arr2 = torch.zeros_like(scores_abn)
      arr2[:-1] = scores_abn[1:]      # shifts dim 0 (video/batch) — BUG
      arr2[-1]  = scores_abn[-1]
      return lam * torch.sum((arr2 - scores_abn) ** 2)
  scores_abn shape is [B_abn, T] where dim 0 = video, dim 1 = time.
  NOTE: tests/test_mil_loss.py has a `_rtfm_smooth_ref` helper (lines 20-25) that
  carries the SAME batch-axis bug. The new regression test must NOT compare against
  that reference — it must assert axis-distinguishing behavior directly.

src/train.py CLI (lines 36-43):
  --config (required)  --seed (int)  --results-dir (str)  --run-name (str)

src/evaluate.py CLI (lines 66-71):
  --run-dir (required)  --split {val,test} (default test)
  Full-length eval is gated on cfg.paths.snippet_boundaries_dir (line 287). evaluate.py
  reads cfg from the run's config_snapshot.json, so a snippet_boundaries_dir added to the
  TRAIN config is persisted into the snapshot and auto-used at eval time. No CLI flag needed.

configs/gated_fusion_giant.yaml — headline UCF config. paths block:
  skeleton_features: "E:/features/ucf/skeleton"
  clip_features:     "E:/features/ucf/siglip2_giant"
  splits_dir:        "data/splits"
  results_dir:       "results"
  (seed: 42 already set at top; train.lam_smooth: 8.0e-4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Control retrain on CURRENT buggy code (baseline sanity)</name>
  <files>configs/_giant_smoothmeasure.yaml, results/_smoothfix/giant_control_s42/ (generated)</files>
  <action>
    Do this BEFORE any code edit so the control reflects the current buggy loss.
    1. Create scratch temp config configs/_giant_smoothmeasure.yaml as a verbatim copy of
       configs/gated_fusion_giant.yaml, adding one line under `paths:`:
         snippet_boundaries_dir: "E:/snippets/ucf"
       This makes evaluate.py produce FULL-LENGTH AUC/AP directly (the boundary dir is
       persisted into config_snapshot.json at train time). Do NOT modify any other field.
       This temp config is scratch — it will NOT be committed.
    2. Train in vcc-main (visible terminal per MEMORY: no headless jobs; ~5 min):
         conda run -n vcc-main python src/train.py --config configs/_giant_smoothmeasure.yaml --seed 42 --results-dir results/_smoothfix --run-name giant_control_s42
    3. Evaluate full-length:
         conda run -n vcc-main python src/evaluate.py --run-dir results/_smoothfix/giant_control_s42 --split test
    4. Read control AUC/AP from results/_smoothfix/giant_control_s42/eval_metrics.json.
       SANITY: control should be CLOSE to the canonical baseline 0.8249 / 0.2737 (confirms
       retrain nondeterminism is small). If control AUC diverges >0.5pp from 0.8249, FLAG it
       in the SUMMARY (nondeterminism larger than expected) — the fixed-minus-control delta
       is still the clean signal, so do not abort on this alone.
    If training or eval FAILS (env/GPU/feature path/boundary JSON missing), STOP and report
    the exact error. Do NOT fabricate numbers and do NOT proceed to Task 2.
  </action>
  <verify>
    <automated>conda run -n vcc-main python -c "import json,sys; m=json.load(open('results/_smoothfix/giant_control_s42/eval_metrics.json')); print('AUC',m.get('auc') or m.get('AUC')); print('AP',m.get('ap') or m.get('AP')); sys.exit(0)"</automated>
  </verify>
  <done>results/_smoothfix/giant_control_s42/eval_metrics.json exists with real full-length AUC and AP recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix smoothness axis (dim 1 / temporal) + axis-distinguishing regression test</name>
  <files>src/losses/mil_loss.py, tests/test_smoothness_axis.py</files>
  <behavior>
    - A single bag [1, T] whose scores ramp in time (e.g. [0,1,2,...]) incurs a NONZERO temporal penalty.
    - Two temporally-FLAT but mutually-different videos, e.g. scores=[[0.2,0.2,0.2],[0.9,0.9,0.9]] (shape [2,3]),
      incur ~0 penalty under the corrected temporal version (each row is flat in time),
      but WOULD be heavily penalized by the old batch-axis version (rows differ across dim 0).
      Assert the corrected _smoothness returns ~0 (< 1e-6 * lam-scale) for this input.
    - Do NOT compare against tests/test_mil_loss.py::_rtfm_smooth_ref — that helper has the same batch-axis bug.
  </behavior>
  <action>
    Edit src/losses/mil_loss.py _smoothness to shift the TEMPORAL axis (dim 1):
        arr2 = torch.zeros_like(scores_abn)
        arr2[:, :-1] = scores_abn[:, 1:]
        arr2[:, -1]  = scores_abn[:, -1]
        return lam * torch.sum((arr2 - scores_abn) ** 2)
    Update the docstring to state it penalizes adjacent-snippet (temporal) differences along
    dim 1 (per the paper Eq. at line 192), and correct the verbatim formula block so it no
    longer documents the batch-axis shift. Keep the lam default 8e-4 and the boundary rule
    (last temporal position diff = 0).
    Create tests/test_smoothness_axis.py implementing the two assertions in <behavior> above,
    importing _smoothness from src.losses.mil_loss. Keep it minimal — no fixtures beyond plain
    torch tensors.
  </action>
  <verify>
    <automated>conda run -n vcc-main python -m pytest tests/test_smoothness_axis.py tests/test_mil_loss.py -q</automated>
  </verify>
  <done>New axis test passes AND existing tests/test_mil_loss.py still passes; src/losses/mil_loss.py uses scores_abn[:, 1:] (dim-1 shift) with an updated docstring.</done>
</task>

<task type="auto">
  <name>Task 3: Fixed retrain (after the fix) + full-length eval</name>
  <files>results/_smoothfix/giant_fixed_s42/ (generated)</files>
  <action>
    Reuse the SAME scratch temp config configs/_giant_smoothmeasure.yaml from Task 1 (do not
    recreate or alter it) so the only difference between control and fixed is the loss code.
    1. Train in vcc-main (visible terminal; ~5 min):
         conda run -n vcc-main python src/train.py --config configs/_giant_smoothmeasure.yaml --seed 42 --results-dir results/_smoothfix --run-name giant_fixed_s42
    2. Evaluate full-length:
         conda run -n vcc-main python src/evaluate.py --run-dir results/_smoothfix/giant_fixed_s42 --split test
    3. Read fixed AUC/AP from results/_smoothfix/giant_fixed_s42/eval_metrics.json.
    If training or eval FAILS, STOP and report the exact error. Do NOT fabricate numbers.
  </action>
  <verify>
    <automated>conda run -n vcc-main python -c "import json,sys; m=json.load(open('results/_smoothfix/giant_fixed_s42/eval_metrics.json')); print('AUC',m.get('auc') or m.get('AUC')); print('AP',m.get('ap') or m.get('AP')); sys.exit(0)"</automated>
  </verify>
  <done>results/_smoothfix/giant_fixed_s42/eval_metrics.json exists with real full-length AUC and AP recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Report the delta (measurement.md + SUMMARY table)</name>
  <files>results/_smoothfix/measurement.md</files>
  <action>
    Write results/_smoothfix/measurement.md with a four-row table using the real numbers from
    Tasks 1 and 3 (do NOT fabricate; pull from the two eval_metrics.json files):

      | Run                                  | AUC     | AP      |
      |--------------------------------------|---------|---------|
      | baseline (canonical, buggy, full-len)| 0.8249  | 0.2737  |
      | control (retrain, buggy)             | X       | X       |
      | fixed   (retrain, corrected)         | Y       | Y       |

    Then compute and state:
      Δ_smoothness  = fixed − control      (clean signal: isolates the loss fix)
      Δ_vs_reported = fixed − 0.8249 (AUC)  / fixed − 0.2737 (AP)
    Add a one-line VERDICT: is |Δ_smoothness| (AUC) within the ~0.3-0.4pp seed-noise floor
    (→ NEGLIGIBLE) or larger (→ MATERIAL)? Also note the control-vs-baseline sanity result
    from Task 1 (and the >0.5pp flag if it triggered).
    Do NOT make the A/B retrain decision and do NOT touch the paper — the orchestrator reviews
    the delta with the user. Reproduce the full table + verdict in the plan SUMMARY too.
  </action>
  <verify>
    <automated>conda run -n vcc-main python -c "t=open('results/_smoothfix/measurement.md',encoding='utf-8').read(); import sys; need=['baseline','control','fixed','Δ_smoothness']; missing=[w for w in need if w not in t]; print('missing:',missing); sys.exit(1 if missing else 0)"</automated>
  </verify>
  <done>results/_smoothfix/measurement.md contains all four rows with real numbers, both deltas, and the noise-floor verdict; same table reproduced in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement → SUMMARY/paper | Reported numbers must come from real eval_metrics.json, never fabricated |
| scratch artifacts → git/canonical | Scratch outputs must not leak into committed results, tables, figures, or paper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o55-01 | Tampering | reported AUC/AP numbers | mitigate | All numbers read programmatically from eval_metrics.json; explicit STOP-and-report on any train/eval failure (Tasks 1 & 3); no fabrication |
| T-o55-02 | Tampering | canonical results/paper | mitigate | All new outputs land under scratch results/_smoothfix/ and configs/_giant_smoothmeasure.yaml; commit scope limited to src/losses/mil_loss.py + tests/test_smoothness_axis.py |
| T-o55-03 | Repudiation | control vs baseline drift | mitigate | Task 1 sanity-flags control divergence >0.5pp from 0.8249 so retrain nondeterminism is recorded, not silently absorbed |
| T-o55-SC | Tampering | package installs | accept | No new packages installed; existing vcc-main env only |
</threat_model>

<verification>
- Task 2 verify: `conda run -n vcc-main python -m pytest tests/test_smoothness_axis.py tests/test_mil_loss.py -q` — new axis test passes AND existing loss tests still pass.
- Both retrains complete in vcc-main with full-length eval producing eval_metrics.json (boundary JSONs at E:/snippets/ucf consumed via the temp config's snippet_boundaries_dir).
- measurement.md has all four rows with real numbers (Task 4 verify).
- Commit contains ONLY src/losses/mil_loss.py + tests/test_smoothness_axis.py. results/_smoothfix/* (gitignored), configs/_giant_smoothmeasure.yaml, and measurement.md are NOT committed.
</verification>

<success_criteria>
- _smoothness shifts dim 1 (temporal); docstring corrected.
- Regression test distinguishes temporal-axis from batch-axis behavior and passes; full existing loss suite still passes.
- Control and fixed retrains (seed 42) both evaluated full-length on the headline UCF Giant config.
- measurement.md + SUMMARY report baseline/control/fixed AUC+AP, Δ_smoothness, Δ_vs_reported, and a one-line negligible-vs-material verdict against the ~0.3-0.4pp seed-noise floor.
- Code fix + test committed: "fix(loss): smoothness regularizer penalizes temporal axis, not batch (+ regression test)". No canonical results/ucf_*, paper/*, tables, or figures touched.
</success_criteria>

<output>
Create `.planning/quick/260601-o55-measure-smoothness-axis-fix-impact-on-he/260601-o55-SUMMARY.md` when done, including the four-row measurement table + verdict.
</output>
