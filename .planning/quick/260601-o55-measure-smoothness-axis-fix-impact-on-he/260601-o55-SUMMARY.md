---
phase: quick-260601-o55
plan: 01
subsystem: testing
tags: [mil-loss, smoothness-regularizer, rtfm, ucf-crime, gated-fusion, axis-bug]

requires:
  - phase: 10-giant-gated-fusion
    provides: headline UCF Giant config + canonical 0.8249/0.2737 baseline
provides:
  - Temporal-axis (dim 1) smoothness regularizer in src/losses/mil_loss.py
  - Axis-distinguishing regression test (tests/test_smoothness_axis.py)
  - A/B measurement of the smoothness-axis bug's impact on the headline UCF result
affects: [thesis-manuscript, mil-loss, canonical-retrain-decision]

tech-stack:
  added: []
  patterns:
    - "Control-vs-fixed A/B retrain at fixed seed to isolate a loss-code change from retrain nondeterminism"

key-files:
  created:
    - tests/test_smoothness_axis.py
    - configs/_giant_smoothmeasure.yaml (scratch, NOT committed)
    - results/_smoothfix/measurement.md (scratch, NOT committed)
  modified:
    - src/losses/mil_loss.py
    - tests/test_mil_loss.py

key-decisions:
  - "Corrected tests/test_mil_loss.py::_rtfm_smooth_ref to the temporal axis (it carried the same batch-axis bug) so test_smoothness_exact validates the fixed behavior"
  - "Headline UCF AUC unaffected (Delta = -0.13pp, within noise floor); UCF AP measurably lower (Delta = -1.23pp)"

patterns-established:
  - "Pattern: scratch temp config adds paths.snippet_boundaries_dir so evaluate.py emits full-length AUC/AP via config_snapshot.json (no CLI flag)"

requirements-completed: [QUICK-o55]

duration: 9min
completed: 2026-06-01
---

# Quick Task 260601-o55: Smoothness-Axis Fix Impact Measurement Summary

**Fixed `_smoothness` to penalize the temporal axis (dim 1) instead of the video/batch axis (dim 0), and measured via a seed-42 control-vs-fixed A/B retrain that the bug left the headline UCF AUC unchanged (-0.13pp, within noise) but lowered UCF AP by 1.23pp.**

## Performance

- **Duration:** ~9 min (excluding ~10 min of training/eval wall time)
- **Started:** 2026-06-01T09:27:31Z
- **Completed:** 2026-06-01T09:36:39Z
- **Tasks:** 4
- **Files modified:** 5 (2 committed, 2 scratch, 1 generated-results tree)

## Accomplishments

- Confirmed and fixed the smoothness-axis bug: `_smoothness` now shifts dim 1 (adjacent snippets in time) rather than dim 0 (different videos in the bag).
- Added 3 axis-distinguishing regression tests that fail against the old batch-axis code and pass against the fix.
- Ran a clean A/B: control (buggy loss) reproduced the canonical baseline EXACTLY (0.8249/0.2737, n_frames=1,097,050), and the fixed retrain measured the loss change in isolation.
- Produced `results/_smoothfix/measurement.md` with the four-row table, both deltas, and a noise-floor verdict.

## Measurement Results (full-length UCF test, seed 42)

| Run                                   | AUC     | AP      |
|---------------------------------------|---------|---------|
| baseline (canonical, buggy, full-len) | 0.8249  | 0.2737  |
| control (retrain, buggy)              | 0.8249  | 0.2737  |
| fixed   (retrain, corrected)          | 0.8236  | 0.2614  |

**Both retrains evaluated full-length: n_frames = 1,097,050** for control and fixed (NOT the truncated 1,010,560), confirming the `snippet_boundaries_dir` took effect and all rows are directly comparable.

**Deltas:**
- **Delta_smoothness = fixed - control** (clean signal): AUC **-0.0013 (-0.13pp)**, AP **-0.0123 (-1.23pp)**
- **Delta_vs_reported = fixed - 0.8249/0.2737**: AUC **-0.0013 (-0.13pp)**, AP **-0.0123 (-1.23pp)**
  (identical to Delta_smoothness because control == baseline exactly)

**Control vs baseline (nondeterminism check):** AUC 0.8249 vs 0.8249 = 0.00pp; AP 0.2737 vs 0.2737 = 0.00pp. The control reproduced the canonical number bit-for-bit — retrain nondeterminism is negligible. No >0.5pp divergence flag triggered.

**Fixed vs reported:** AUC 0.8236 vs 0.8249 (-0.13pp); AP 0.2614 vs 0.2737 (-1.23pp).

**VERDICT:**
- **AUC: NEGLIGIBLE** — |Delta_smoothness(AUC)| = 0.13pp is within the ~0.3-0.4pp seed-noise floor. The thesis headline UCF AUC (82.5%) is unaffected by the bug.
- **AP: borderline-to-MATERIAL** — |Delta_smoothness(AP)| = 1.23pp exceeds the noise floor; the corrected temporal regularizer measurably lowered UCF AP (and lowered best val_loss: 0.2458 fixed vs 0.3270 control). The fix is not a no-op for AP.

The A/B canonical-retrain decision and any paper edits are left to the orchestrator + user (not decided here).

## Task Commits

1. **Task 1: Control retrain on buggy code** - no commit (scratch results under gitignored `results/_smoothfix/`)
2. **Task 2: Fix smoothness axis + regression test** - `f89910b` (fix)
3. **Task 3: Fixed retrain + full-length eval** - no commit (scratch results)
4. **Task 4: Report the delta** - no commit (scratch `measurement.md`)

Per plan constraints, ONLY the code fix + test were committed. Scratch artifacts (`configs/_giant_smoothmeasure.yaml`, `results/_smoothfix/*`, `measurement.md`) are intentionally NOT committed. SUMMARY/STATE/PLAN are not committed for this quick task.

## Files Created/Modified

- `src/losses/mil_loss.py` - `_smoothness` now shifts dim 1 (`scores_abn[:, 1:]`); docstring + formula block corrected to temporal semantics (committed `f89910b`)
- `tests/test_smoothness_axis.py` - 3 axis-distinguishing regression tests (committed `f89910b`)
- `tests/test_mil_loss.py` - corrected `_rtfm_smooth_ref` to the temporal axis (committed `f89910b`; see Deviations)
- `configs/_giant_smoothmeasure.yaml` - scratch verbatim copy of `gated_fusion_giant.yaml` + `snippet_boundaries_dir` (NOT committed)
- `results/_smoothfix/{giant_control_s42,giant_fixed_s42}/` + `measurement.md` - scratch outputs (gitignored / NOT committed)

## Decisions Made

- **Corrected `_rtfm_smooth_ref` rather than deleting/relaxing `test_smoothness_exact`.** The plan flagged that helper as carrying the same batch-axis bug. Once `_smoothness` was fixed, the bit-match test failed against the buggy reference. Fixing the reference to the temporal axis keeps the exactness test meaningful and green. (Rule 1 — see Deviations.)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected `tests/test_mil_loss.py::_rtfm_smooth_ref` to the temporal axis**
- **Found during:** Task 2 (GREEN phase)
- **Issue:** After fixing `_smoothness` to shift dim 1, `test_smoothness_exact` failed (0.0142 vs 0.0125) because it compares the implementation against `_rtfm_smooth_ref`, which still shifted dim 0 — the exact batch-axis bug the plan explicitly identifies (PLAN lines 66-68, 128). The plan's done-criterion ("existing test_mil_loss.py still passes") conflicted with leaving the buggy reference in place.
- **Fix:** Updated `_rtfm_smooth_ref` to shift dim 1 (`arr2[:, :-1] = arr[:, 1:]; arr2[:, -1] = arr[:, -1]`) with a comment noting the correction. The new `tests/test_smoothness_axis.py` does NOT use this helper (per constraint); only `test_smoothness_exact` does.
- **Files modified:** tests/test_mil_loss.py
- **Verification:** `pytest tests/test_smoothness_axis.py tests/test_mil_loss.py -q` -> 14 passed; broader `-k "smooth or mil or loss"` -> 21 passed.
- **Committed in:** `f89910b` (Task 2 commit)

**Commit-scope note:** The plan's commit constraint listed only `src/losses/mil_loss.py` + `tests/test_smoothness_axis.py`. `tests/test_mil_loss.py` was added to the same commit because leaving its buggy reference uncorrected would have broken the test suite on `main`. This is the minimal change required to keep the suite green and is part of the same logical fix.

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The single auto-fix was required for the test suite to stay green after the axis correction and is in-scope (directly caused by the Task 2 change). No scope creep; canonical results, tables, figures, and paper untouched.

## Issues Encountered

None beyond the deviation above. Both retrains and both full-length evals completed in `vcc-main` without env/GPU/feature-path errors. The control reproducing the canonical baseline exactly is a strong sanity signal that the harness and seed determinism are intact.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Code fix + regression test committed (`f89910b`); the temporal-axis smoothness is now correct and protected by tests.
- The orchestrator/user can now decide whether the -1.23pp AP shift warrants a canonical UCF retrain (AUC headline is safe). The clean `Delta_smoothness` is the AP signal to weigh.
- No blockers. Scratch artifacts can be discarded once the A/B decision is made.

## Self-Check: PASSED

- FOUND: src/losses/mil_loss.py (contains `scores_abn[:, 1:]` dim-1 shift)
- FOUND: tests/test_smoothness_axis.py
- FOUND: results/_smoothfix/measurement.md
- FOUND: results/_smoothfix/giant_control_s42/eval_metrics.json
- FOUND: results/_smoothfix/giant_fixed_s42/eval_metrics.json
- FOUND: .planning/quick/.../260601-o55-SUMMARY.md
- FOUND: commit f89910b

---
*Phase: quick-260601-o55*
*Completed: 2026-06-01*
