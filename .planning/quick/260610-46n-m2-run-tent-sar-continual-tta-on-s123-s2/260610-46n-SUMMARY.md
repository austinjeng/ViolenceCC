---
quick_id: 260610-46n
slug: m2-run-tent-sar-continual-tta-on-s123-s2
status: complete
date: 2026-06-09
commit: a5e3f73
---

# Quick Task 260610-46n — Summary

## What was done

Ran the committed continual TENT/SAR protocol on the **s123 and s2024** gated-fusion
source checkpoints (all 4 backbones × 20 corruption conditions) so the paper's
"All TTA results are means over three training seeds" (`main.tex:325`) is now true.
No re-extraction was needed — corrupted caches are seed-independent and all 12
source checkpoints already existed.

- `scripts/run_tta_seeds_m2.py` (new): drives `run_tta_evaluation(..., protocol="continual")`
  over s123/s2024 × {source_only,tent,sar} × 20 conditions, then aggregates the
  3-seed mean entropy delta and writes `summary_3seed.json`. Resumable.
- `scripts/aggregate_tta_table.py` (modified): TENT/SAR delta now read from
  `summary_3seed.json` (3-seed) with seed-42 `summary.json` fallback; docstring +
  footer corrected from "seed 42" to "3-seed {42,123,2024}".
- `results/_tta_rerun_continual/summary_3seed.json` (new, force-added): per-seed +
  3-seed-mean source/tent/sar + ΔTENT/ΔSAR per backbone.

## Result (3-seed mean entropy delta vs Source-Only, over 20 conditions)

| backbone | ΔTENT | ΔSAR |
|---|---|---|
| CLIP ViT-B/16 | +0.021 | +0.024 |
| SigLIP2 Base | −0.082 | −0.077 |
| SigLIP2 SO400M | −0.050 | −0.051 |
| SigLIP2 Giant | +0.011 | +0.012 |

**Worst |Δ| = 0.082pp (SigLIP2-Base) < 0.1pp → the paper's "<0.1pp on every backbone"
holds as a true 3-seed mean.** `main.tex:325` ("three seeds") and `:327` ("<0.1pp")
are now literally true with **no reword**.

## Verification

- 477 condition-runs completed (exit 0, 42.4 min); per-seed s42 source recomputed
  from the per-condition files matches the existing `summary.json` exactly (63.515 …).
- `aggregate_tta_table.py` reproduces Table 3 unchanged: Source 63.7/57.0/58.9/62.8,
  Ours 64.4/58.5/61.1/63.3, ΔOurs +0.67/+1.50/+2.24/+0.42, Mean 60.6→61.8 (+1.21);
  footer now "3-seed mean {42,123,2024}".
- Consistency bonus: 3-seed continual source mean (63.71/57.01/58.87/62.83) reconciles
  with Table-3 Source-Only — the previously-flagged s42-only mismatch was a 1-seed artifact.

## Commit

`a5e3f73` feat(tta): extend continual TENT/SAR entropy comparison to 3 seeds (M2)

## Follow-up (optional, for Austin)

Paper needs no edit. Optionally, the 3-seed ΔTENT/ΔSAR could be stated explicitly in
§4.5 to make the negative result more concrete, but the current "<0.1pp" is accurate.
