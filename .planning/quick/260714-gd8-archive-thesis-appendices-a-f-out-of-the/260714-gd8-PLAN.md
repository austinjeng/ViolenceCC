---
phase: quick-260714-gd8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thesis/main.tex
  - thesis/appendices_archived/appA_rtfm.tex
  - thesis/appendices_archived/appB_per_category.tex
  - thesis/appendices_archived/appC_sweep.tex
  - thesis/appendices_archived/appD_engineering.tex
  - thesis/appendices_archived/appE_reproducibility.tex
  - thesis/appendices_archived/appF_figures.tex
  - thesis/appendices_archived/README.md
  - thesis/chapters/ch01_introduction.tex
  - thesis/chapters/ch04_experimental_setup.tex
  - thesis/chapters/ch05_results_fusion.tex
  - thesis/chapters/ch07_discussion.tex
  - thesis/chapters/ch08_limitations.tex
  - thesis/chapters/ch09_conclusion.tex
  - thesis/PROVENANCE.md
autonomous: true
requirements: [GD8-ARCHIVE, GD8-REWORD, GD8-VERIFY]   # quick task; scope = CONTEXT decisions (LOCKED), no ROADMAP req IDs
must_haves:
  truths:
    - "Thesis builds clean with zero appendices: no ?? in the PDF, no Appendix entries in TOC, no A.x-F.x items in LoF/LoT, References is the last TOC-level section"
    - "All six appendix .tex sources are retrievable at thesis/appendices_archived/ with a README whose restore recipe works from git alone"
    - "Every former appendix pointer in the body is reworded so no sentence implies the reader can find absent material, and no factual claim silently lost its support wording"
    - "Rendered reference list still has exactly 45 entries; front matter untouched"
  artifacts:
    - path: "thesis/appendices_archived/README.md"
      provides: "What/why/how-to-restore, including the verbatim main.tex appendix block"
    - path: "thesis/appendices_archived/appA_rtfm.tex"
      provides: "Archived appendix source (one of six, same filenames as before)"
    - path: "thesis/main.tex"
      provides: "Build with no \\appendix / \\include{appendices/...} lines; 2-line comment pointing at appendices_archived/README.md"
  key_links:
    - from: "thesis/main.tex"
      to: "thesis/chapters/*.tex"
      via: "\\include — chapters only, appendices block deleted"
      pattern: "include\\{chapters/"
    - from: "thesis/appendices_archived/README.md"
      to: "thesis/main.tex"
      via: "verbatim copy of the removed \\appendix block (main.tex:61-70 pre-edit)"
      pattern: "include\\{appendices/appA_rtfm"
---

<objective>
Archive thesis Appendices A–F out of the build (restorably), reword all 19 body cross-references plus the §1.7 roadmap sentence and CGW-delta list so the PDF has zero broken refs and zero pointers to absent material, then rebuild and pass the full verification gate.

Purpose: Author decision 2026-07-14 — the thesis main body stands alone; appendices are preserved retrievably in case they are needed back.
Output: 131pp → ~101–105pp thesis/main.pdf; thesis/appendices_archived/ with README restore recipe; one refactor commit (plus one fallout-fix commit only if the gate forced fixes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260714-gd8-archive-thesis-appendices-a-f-out-of-the/260714-gd8-CONTEXT.md
@thesis/main.tex
@CLAUDE.md

Work in the main tree (no worktree): the build reads tracked sources only.

**Grep-verified cross-reference sites (re-confirmed 2026-07-14; use these, do not re-hunt):**
- ch01_introduction.tex: :181–185 (CGW-delta list), :252 (app:e reproducibility pointer), :270–273 (§1.7 roadmap sentence listing all six appendices) — 8 `app:` refs total
- ch04_experimental_setup.tex: :287 (app:e walkthrough), :318 (app:d pitfalls), :365 (app:a footnote) — 3 refs
- ch05_results_fusion.tex: :263 (app:b deferred per-class verdict), :288–289 (app:c + **`\ref{fig:appc-sweep-ucf}` and `\ref{fig:appc-sweep-xd}` — appendix FIGURE labels the `app:` grep does NOT catch; both must go**), :317 (app:c full grids), :434 (app:b parenthetical), :594 (app:b complete tables) — 5 `app:` refs + 2 fig refs
- ch05_results_fusion.tex:591 — LaTeX comment "…rendered in Appendix B." — the single non-ref word mention (CONTEXT rewording decision 8); update it so it no longer misleads
- ch07_discussion.tex:55 (app:b), ch08_limitations.tex:29 (app:a), ch09_conclusion.tex:86 (app:e) — 1 ref each
- frontmatter/: zero appendix mentions (verified) — do not touch front matter
- 48 labels are defined inside the appendices; all leave the build together, and after the rewordings above NO body ref depends on any of them

**Verbatim block to preserve in the README:** the `%% Appendices` comment header + `\appendix` + six `\include{appendices/...}` lines currently at thesis/main.tex:61–70 (read before deleting; copy exactly).

**Accuracy note for the README table note:** tab_percat_rtfm.tex is `\input` by archived appA (:83) and tab_percat_{ucf,xd}.tex by archived appB (:24–25) — CONTEXT says "appB" but appA inputs the rtfm table; word the README note as "only the archived appendices input these" so it stays true.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive move + main.tex + all rewordings + PROVENANCE note + commit</name>
  <files>thesis/main.tex, thesis/appendices_archived/*.tex (6 moved), thesis/appendices_archived/README.md, thesis/chapters/ch01_introduction.tex, thesis/chapters/ch04_experimental_setup.tex, thesis/chapters/ch05_results_fusion.tex, thesis/chapters/ch07_discussion.tex, thesis/chapters/ch08_limitations.tex, thesis/chapters/ch09_conclusion.tex, thesis/PROVENANCE.md</files>
  <action>
Implement CONTEXT §Archive mechanism + §Cross-reference rewording (decisions 1–9) + §Untouched, in this order:

1. **Capture the verbatim block first**: read thesis/main.tex:61–70 and copy it exactly (needed for the README before the lines are deleted).
2. **Move**: `git mv thesis/appendices/appA_rtfm.tex thesis/appendices_archived/appA_rtfm.tex` and likewise for appB_per_category, appC_sweep, appD_engineering, appE_reproducibility, appF_figures (same filenames). Then delete the stale git-ignored `thesis/appendices/*.aux` artifacts so the emptied directory disappears (git does not track empty dirs; never commit build artifacts per CLAUDE.md).
3. **README**: write thesis/appendices_archived/README.md per CONTEXT §Archive mechanism bullet 2 — what these six files are, why archived (author decision 2026-07-14, quick 260714-gd8), and the exact 4-step restore recipe: (1) `git mv` the six files back to thesis/appendices/; (2) re-insert the preserved verbatim block into main.tex between the bibliography block and `\end{document}`; (3) restore the body cross-references from git history via `git show <parent-of-archive-commit> -- thesis/chapters/` (parent is 484b84b at time of archiving); (4) rebuild with `scripts/build_thesis.ps1 -Clean`. Include the table note: thesis/tables/tab_percat_{ucf,xd,rtfm}.tex stay in place — only the archived appendices (appA inputs tab_percat_rtfm, appB inputs tab_percat_ucf/xd) reference them, harmless.
4. **main.tex**: delete lines 61–70 (the `%% Appendices` header, `\appendix`, six `\include{appendices/...}`); replace with a 2-line LaTeX comment pointing at appendices_archived/README.md for the archived material + restore steps (CONTEXT §Archive mechanism bullet 4).
5. **Rewordings** — apply CONTEXT decisions 1–8 at the grep-verified sites listed in <context>. Principles (CONTEXT): keep every factual claim; remove pointers to absent material; never leave a sentence implying the reader can find something that isn't there. Site→decision mapping: ch01:181–185→decision 1 (rewrite delta items: (a) three-seed ablation grid, Tables 5.1–5.2; (b) per-category/failure analysis §5.7–5.9 + RTFM investigation summarized in §8.1; (c) TTA narrative §6.2–6.3; drop the appendices D–E item); ch01:252→decision 2 (state the measures: fixed seeds {42,123,2024}, committed configs and manifests, tracked metrics files — no pointer); ch01:270–273→decision 3 (delete the appendix sentence entirely; roadmap ends with Chapter 9); ch04 :287/:318/:365→decision 4 (each methodological fact stands alone); ch05 :263/:434/:594→decision 5 (app:b pointers → Table 5.3 / Section 5.8 in-chapter summary, or state "full per-category tables are omitted here"); ch05:288–289+:317→decision 5 (drop the app:c pointers AND the two `\ref{fig:appc-sweep-*}` figure refs — the sweep prose already states the numbers; reword so the heatmap description stands without the figures); ch05:591 comment→decision 8 (update so it no longer claims Appendix-B rendering); ch07:55 + ch09:86→decision 6 (drop/reword parentheticals); ch08:29→decision 7 (replace pointer with the clause: diagnostic investigation localized the gap to the training side — feature dimensionality/MTN/batch mismatches — while the evaluation harness was cleared by an independent sanity anchor; no new claims). Exact wording within these principles is executor discretion (CONTEXT §Claude's Discretion), including comma/word cleanup around dropped parentheticals.
6. **PROVENANCE**: append ONE line to thesis/PROVENANCE.md noting appendices A–F archived to thesis/appendices_archived/ on 2026-07-14 (quick 260714-gd8); do not rewrite any rows (CONTEXT §Untouched). Do NOT edit thesis/references.bib or .planning/THESIS-REVIEW-2026-07-11.md.
7. **Commit** everything above as: `refactor(thesis): archive appendices A-F out of the build (restorable)`.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in appA_rtfm appB_per_category appC_sweep appD_engineering appE_reproducibility appF_figures; do test -f "thesis/appendices_archived/$f.tex"; test ! -f "thesis/appendices/$f.tex"; done; test -f thesis/appendices_archived/README.md; grep -q "include{appendices/appA_rtfm" thesis/appendices_archived/README.md; test "$(grep -c "include{appendices/" thesis/main.tex)" -eq 0; test "$(grep -rn "app:" --include=*.tex thesis/chapters/ thesis/frontmatter/ | wc -l)" -eq 0; test "$(grep -rn "fig:appc" --include=*.tex thesis/chapters/ | wc -l)" -eq 0; grep -q "260714-gd8" thesis/PROVENANCE.md; git log -1 --format=%s | grep -q "archive appendices A-F"'</automated>
  </verify>
  <done>Six .tex files live only under thesis/appendices_archived/ (git mv, history preserved); README contains the verbatim main.tex block + 4-step restore recipe + table note; main.tex has no \appendix or appendices include lines (2-line pointer comment instead); zero `app:` refs and zero `fig:appc` refs remain in chapters/frontmatter .tex; every rewording site handled per its CONTEXT decision with factual claims intact; PROVENANCE has the one-line note; single commit `refactor(thesis): archive appendices A-F out of the build (restorable)` contains all of it.</done>
</task>

<task type="auto">
  <name>Task 2: Clean rebuild + full verification gate (fix fallout until green)</name>
  <files>thesis/main.tex, thesis/chapters/*.tex (only if fallout fixes are needed)</files>
  <action>
Run CONTEXT §Verification gate items 1–5; loop fix→rebuild→re-verify until ALL pass:

1. **Build**: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean` exits 0. Scan thesis/main.log: zero "LaTeX Warning: Reference ... undefined", zero "undefined citations", no "There were undefined references".
2. **PDF gates** (use pymupdf from a scratchpad script or python -c; do NOT commit any script): (a) page count in 101–105 (was 131; Appendix A started at p.102); (b) extracted text contains zero "??" and zero "[?]"; (c) thesis/main.toc has NO Appendix entries and its last chapter-level entry is References; (d) thesis/main.lof and thesis/main.lot contain no A.x–F.x items; (e) NTUST binding order holds — front matter files show no diff (`git diff --stat HEAD -- thesis/frontmatter/` empty) and References is the final section of the document.
3. **Grep gates** (CONTEXT decision 9): `grep -rn "app:" --include=*.tex thesis/chapters/ thesis/frontmatter/` → zero hits; `grep -rin "appendix\|appendices" --include=*.tex thesis/chapters/ thesis/frontmatter/ thesis/main.tex` → every remaining hit (if any) sits inside a LaTeX `%` comment, zero PDF-visible mentions.
4. **Cite coverage**: `grep -c "\\bibitem" thesis/main.bbl` equals 45 (all 45 bib keys were verified cited from the body; the main.tex:51 "45/45" comment stays true — if the count drops, a body \cite was damaged by rewording: restore it).
5. **Fallout policy**: if any gate fails, fix the source, rebuild with `-Clean`, and re-run the ENTIRE gate. If (and only if) fixes were needed, commit them as a follow-up commit `fix(thesis): appendix-archive fallout` (no amend). If everything passed on the first run, make NO additional commit. Never commit build artifacts (main.pdf/.aux/.log/.toc/.lof/.lot/.bbl — all git-ignored per CLAUDE.md).
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(grep -rn "app:" --include=*.tex thesis/chapters/ thesis/frontmatter/ | wc -l)" -eq 0; test "$(grep -c "\\\\bibitem" thesis/main.bbl)" -eq 45; ! grep -qi "appendix" thesis/main.toc; grep -qi "references" thesis/main.toc; ! grep -q "undefined" thesis/main.log; python -c "import fitz,sys; d=fitz.open(\"thesis/main.pdf\"); n=d.page_count; t=chr(10).join(p.get_text() for p in d); sys.exit(0 if 101<=n<=105 and \"??\" not in t and \"[?]\" not in t else 1)"; git status --porcelain thesis/ | grep -v "\.pdf\|\.aux\|\.log\|\.toc\|\.lof\|\.lot\|\.bbl\|\.blg\|\.fls\|\.fdb_latexmk\|\.out\|\.synctex" | wc -l | grep -qx "0"'</automated>
  </verify>
  <done>build_thesis.ps1 -Clean exits 0 with a clean log; PDF is ~101–105 pages with zero ??/[?]; TOC ends at References with no Appendix entries; LoF/LoT free of A.x–F.x; grep gates pass (zero app: refs, zero PDF-visible appendix mentions); main.bbl renders exactly 45 references; front matter untouched; working tree clean apart from git-ignored build artifacts; fallout commit exists only if fixes were required.</done>
</task>

</tasks>

<verification>
- `git log --oneline -2` shows the refactor commit (and optionally one fallout-fix commit) on main.
- `git show --stat HEAD` (or HEAD~1 if fallout commit exists) lists renames appendices/→appendices_archived/ for all six .tex files (history-preserving move).
- Restore recipe is self-contained: README names the six files, the verbatim main.tex block, the parent commit (484b84b) for chapter-ref restoration, and the rebuild command.
- All CONTEXT rewording decisions 1–8 traceable: each of the 19 former ref sites + ch05:591 comment reads cleanly with its factual claim preserved.
</verification>

<success_criteria>
- Thesis PDF builds clean at ~101–105 pages with appendices fully absent (TOC/LoF/LoT/body) and zero broken references.
- Appendices A–F retrievable from thesis/appendices_archived/ via the README recipe alone.
- 45/45 citation coverage intact; front matter and references.bib untouched; headlines (UCF 82.5 / XD 78.7) unaffected (prose-only rewording, no numbers changed).
- Exactly one content commit (`refactor(thesis): archive appendices A-F out of the build (restorable)`), plus `fix(thesis): appendix-archive fallout` only if the gate forced fixes.
</success_criteria>

<output>
Create `.planning/quick/260714-gd8-archive-thesis-appendices-a-f-out-of-the/260714-gd8-SUMMARY.md` when done.
</output>
