---
phase: quick-260611-66v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/figures/fig_architecture.tex
  - paper/main.tex
  - paper/figures/fig_architecture.png   # deleted (git rm)
  - paper/figures/fig_architecture.html  # deleted (git rm)
autonomous: true
requirements: []   # quick task — no roadmap REQ IDs; scope = approved design in orchestrator context
quick_id: 260611-66v
must_haves:
  truths:
    - "paper/main.pdf builds clean via build_paper.ps1 -Clean with Figure 1 rendered from native TikZ (vector, document fonts) instead of the raster PNG"
    - "Figure 1 content matches paper prose: visual snippet feature is 2d-dim (1024-3072-d), late fusion is equal-weight SCORE AVERAGING, gated fusion shows Eqs (2)-(4) math incl. residual, NO TENT/SAR block, dashed test-time reliability-reweighting annotation referencing sec:disc"
    - "Rendered figure page is visually clean: no text overlap, no arrow collisions, text readable at print size, figure spans the text width"
    - "Orphaned fig_architecture.png and fig_architecture.html are removed from git"
  artifacts:
    - path: "paper/figures/fig_architecture.tex"
      provides: "Standalone tikzpicture (no document wrapper) for Figure 1"
      min_lines: 80
    - path: "paper/main.tex"
      provides: "tikz preamble + \\input of the new figure + corrected caption"
      contains: "\\input{figures/fig_architecture}"
  key_links:
    - from: "paper/main.tex"
      to: "paper/figures/fig_architecture.tex"
      via: "\\input inside the figure* environment"
      pattern: "input\\{figures/fig_architecture\\}"
    - from: "paper/figures/fig_architecture.tex"
      to: "main.tex \\label{sec:disc} (Sec 3.6, line ~189)"
      via: "\\ref{sec:disc} in the test-time annotation node"
      pattern: "ref\\{sec:disc\\}"
---

<objective>
Rebuild CGW '26 paper Figure 1 (architecture overview) as a native TikZ vector figure, replacing the unreadable 2100x680 raster PNG, correcting content drift against the current paper prose, and visually verifying the rebuilt PDF page.

Purpose: Figure 1 is the paper's anchor figure; the current PNG has too-small text at print size and three content errors (visual dims "512-1536d" instead of 2d-dim 1024-3072-d; late fusion described as "Concatenation + FC projection" instead of score averaging; obsolete TENT/SAR + "LayerNorm adapted by TTA" blocks that contradict the rewritten TTA story).
Output: paper/figures/fig_architecture.tex (TikZ), updated paper/main.tex (preamble, figure block, caption), deleted PNG/HTML sources, verified clean rebuild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md  (Paper / LaTeX Build conventions — MUST rebuild with -Clean after any LaTeX edit; NEVER commit build artifacts)
@paper/main.tex  (figure* block at lines 111-116; content sources: §3.1-3.2 skeleton path lines 120-134, §3.3 visual path lines 136-147, §3.4 fusion Eqs (1)-(4) lines 149-171, §3.5 MIL lines 173-186, §3.6 sec:disc lines 188-195)

Environment facts (verified during planning — do not re-discover):
- `pdftoppm` is on PATH at C:/Users/Austin/AppData/Local/Programs/MiKTeX/miktex/bin/x64/pdftoppm (use for PDF→PNG page rendering; pypdfium2/fitz/magick are NOT installed — do not install anything).
- Both paper/figures/fig_architecture.png and .html are git-tracked → plain `git rm` works for both.
- acmart already loads graphicx (\resizebox available). tikz is NOT yet loaded.
- Paper is currently 10 pages; main.log after build contains "Output written on main.pdf (10 pages".
- Old SVG palette hex anchors (keep hue families, muted): skeleton green #27ae60/#d5f5e3, visual blue #2980b9/#ebf5fb, fusion orange #f39c12/#fef9e7 (gated accent #e74c3c), prediction purple #8e44ad/#f5eef8, neutral #2c3e50/#f8f9fa.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TikZ figure source, integrate into main.tex, remove orphaned raster sources</name>
  <files>paper/figures/fig_architecture.tex, paper/main.tex, paper/figures/fig_architecture.png (delete), paper/figures/fig_architecture.html (delete)</files>
  <action>
**1a. Create `paper/figures/fig_architecture.tex`** — file contains ONLY a `tikzpicture` (no \documentclass / \begin{document}; it is \input inside the existing figure* environment). Implement the approved design exactly:

- **Geometry:** design at natural width ~7.0-8.0in (acmart figure* text width is ~7in) and height ~2.4-2.8in so the \resizebox safety clamp scales by ≥0.85 and \footnotesize/\scriptsize text stays readable. Aspect ~2.6-2.9:1. Use absolute coordinates (in) for predictable layout.
- **Four labeled column zones** (\scriptsize gray zone labels across the top, optional light dashed vertical separators): Input → Frozen Feature Extraction → Trainable Fusion → Prediction.
- **Define tikzset styles once** (box/badge/arrow/dashed-arrow) with `font=\footnotesize` (\scriptsize for secondary lines, \tiny for badges); `rounded corners`, `align=center`. Arrows: `-{Stealth[length=2mm]}` via arrows.meta. Define muted colors with \definecolor from the hex anchors in <context> (e.g. lighten fills, darken borders slightly for print).
- **Skeleton stream (top, green family):** Video Frames (shared input node, neutral) → "RTMPose\\COCO-17 keypoints, top-2 persons" → "CTR-GCN\\NTU-120, 4 streams (j/b/jm/bm)" with a small "frozen" badge → dashed-border feature node "256-d\\skeleton snippet feat.".
- **Visual stream (bottom, blue family):** same Video Frames node → "Visual Backbone\\CLIP ViT-B/16 or SigLIP2\\(Base / SO400M / Giant)" with "frozen" badge → "~1 FPS frame features" → "mean+max concat\\snippet pooling" → dashed-border feature node "2d-dim (1024-3072-d)\\visual snippet feat.". (MUST say 2d-dim 1024-3072-d — the old figure's "512-1536d" was wrong.)
- **Fusion module (center-right, orange family, visually highlighted as the ONLY trainable part):** an outer rounded container titled "Gated Fusion (trainable)" containing three stacked \footnotesize math/text rows:
  1. "project + LN per modality: $\hat{\mathbf{s}}=\mathrm{LN}_s(W_s\mathbf{s}{+}\mathbf{b}_s)$, $\hat{\mathbf{v}}=\mathrm{LN}_v(W_v\mathbf{v}{+}\mathbf{b}_v)$"
  2. "gate: $\mathbf{g}=\sigma(W_g[\hat{\mathbf{s}}\,\|\,\hat{\mathbf{v}}])$"
  3. "fuse: $\mathbf{f}=\mathrm{LN}_f(\mathbf{g}\odot\hat{\mathbf{s}}+(1-\mathbf{g})\odot\hat{\mathbf{v}}+\hat{\mathbf{s}}+\hat{\mathbf{v}})$"
  Plus a small detached baseline note (white/neutral box, \scriptsize): "Late fusion baseline (score averaging): $a_{\text{late}}=\tfrac{1}{2}h_s(\mathbf{s})+\tfrac{1}{2}h_v(\mathbf{v})$". (MUST be score averaging — old figure's "Concatenation + FC projection" was wrong.)
  Do NOT include any TENT/SAR or "LayerNorm adapted by TTA" block — REMOVED per approved design.
- **Test-time annotation (dashed, on the visual-stream arrow into the fusion module):** small dashed node + dashed arrow: "test-time: visual stream scaled by reliability weight $w$ (Sec.~\ref{sec:disc})". \ref{sec:disc} resolves because the file is \input into main.tex.
- **Prediction (right, purple family):** "MIL Head\\FC + sigmoid\\top-$k$ snippet selection" with a "trainable" badge → "Anomaly score $\in[0,1]$". Add a dashed training-signal arrow labeled "MIL ranking loss" pointing back into the MIL head.
- **Badges:** "frozen" text badges (\tiny, tinted rect) on CTR-GCN and Visual Backbone; "trainable" emphasis (badge and/or thicker accent border) on the fusion container and MIL head. Use the TEXT "frozen" — do NOT use a Unicode snowflake glyph (pdflatex will not render it).
- Do NOT hardcode equation numbers like "Eq. (3)" in node text (the methodology equations are unlabeled; numbers could drift). Math content itself is the reference.

**1b. Edit `paper/main.tex`:**
- Preamble: after `\usepackage{graphicx}` (line 26), add `\usepackage{tikz}` and `\usetikzlibrary{positioning,arrows.meta,fit,calc}` (load after the class defaults; no known acmart conflict).
- In the figure* block (line ~113): replace `\includegraphics[width=\textwidth]{figures/fig_architecture.png}` with `\resizebox{\textwidth}{!}{\input{figures/fig_architecture}}`.
- Rewrite the caption (line ~114) to match the corrected content. It must mention: two parallel FROZEN streams (RTMPose→CTR-GCN 256-d skeleton snippet features; ~1 FPS frames from a frozen CLIP/SigLIP2 backbone aggregated by concatenated mean+max pooling into a $2d$-dimensional, 1024-3072-d representation); gated fusion = the only trained component (per-dimension learned gate with residual retention of both projected modalities, three LayerNorms); late fusion shown as an equal-weight score-averaging baseline; the dashed test-time reliability-reweighting annotation (Sec.~\ref{sec:disc}); MIL head trained with MIL ranking loss. Keep `\label{fig:architecture}` unchanged.

**1c. Remove orphans:** `git rm paper/figures/fig_architecture.png paper/figures/fig_architecture.html` (both verified git-tracked). Do not stage anything else yet.
  </action>
  <verify>
    <automated>grep -c "input{figures/fig_architecture}" paper/main.tex returns 1; grep -c "usepackage{tikz}" paper/main.tex returns 1; grep -c "includegraphics.*fig_architecture" paper/main.tex returns 0; grep -c "ref{sec:disc}" paper/figures/fig_architecture.tex returns 1; grep -ci "documentclass" paper/figures/fig_architecture.tex returns 0; git status --short shows both raster files staged as deleted (D)</automated>
  </verify>
  <done>fig_architecture.tex exists as a bare tikzpicture implementing all content corrections (2d-dim 1024-3072-d, score-averaging late fusion, no TENT/SAR, sec:disc annotation, frozen/trainable badges); main.tex loads tikz, inputs the figure via \resizebox, caption rewritten; PNG+HTML git-rm'ed.</done>
</task>

<task type="auto">
  <name>Task 2: Build clean, render figure page, visually verify and iterate</name>
  <files>paper/figures/fig_architecture.tex (layout iterations only), paper/main.tex (only if pagination fix needed)</files>
  <action>
**2a. Build** per CLAUDE.md convention: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_paper.ps1 -Clean` from repo root. Must exit 0. Then check `paper/main.log`: zero "LaTeX Error" / "Undefined references"; page count via the "Output written on main.pdf (N pages" line — expect 10 pages (the old PNG rendered ~2.27in tall; the new figure at 2.4-2.8in may reflow). If page count grows to 11, reduce figure height (tighten vertical spacing / cap at ~2.4in) and rebuild until back to 10.

**2b. Render the figure page:** the figure* [t] floats near the start of §3 — render the candidate pages rather than assuming: `pdftoppm -png -r 150 -f 2 -l 4 paper/main.pdf _tmp_fig1page` (writes _tmp_fig1page-2/3/4.png in repo root). Identify the page containing Figure 1.

**2c. Visual inspection with the Read tool** on that PNG. Check ALL of:
- no text overlapping node borders or other text; no arrow passing through node text; arrowheads land on box edges,
- all labels legible at this print-equivalent scale (compare against body text on the same page — node text should be roughly caption-sized, not smaller than ~70% of it),
- the figure spans the full text width without distortion,
- math renders correctly (gate/fusion equations, $a_{\text{late}}$, $w$),
- the four zone labels read left→right; frozen/trainable badges visible; fusion module visually emphasized.

**2d. Iterate:** adjust TikZ coordinates/spacing/font sizes and rebuild (always via `build_paper.ps1 -Clean`) until every check passes. Then delete the temp renders: `rm _tmp_fig1page-*.png` (leave the pre-existing untracked `_tmp_paperfig-1.png` alone — not ours).
  </action>
  <verify>
    <automated>build_paper.ps1 -Clean exits 0; grep on paper/main.log: "Output written on main.pdf (10 pages" present, no "LaTeX Error", no "undefined references" (case-insensitive)</automated>
    <human-check>Final rendered figure-page PNG inspected via Read tool by the executor (acts as visual gate): no overlap/collision, readable fonts, full text width — iterate until clean before proceeding</human-check>
  </verify>
  <done>PDF builds clean at 10 pages with 0 errors/undefined refs; rendered Figure 1 page passes all visual checks; temp render PNGs removed.</done>
</task>

<task type="auto">
  <name>Task 3: Commit</name>
  <files>paper/figures/fig_architecture.tex, paper/main.tex, deletions of paper/figures/fig_architecture.{png,html}</files>
  <action>
Stage exactly: `paper/figures/fig_architecture.tex`, `paper/main.tex`, and the two deletions (already staged by git rm). NEVER stage build artifacts (paper/main.pdf, *.aux/*.bbl/*.log/*.fdb_latexmk/*.synctex.gz — all git-ignored, but verify nothing slips in via `git status --short` before committing). Do not stage unrelated untracked files (scripts/_tmp_*, _tmp_paperfig-1.png, etc.).

Commit message: `feat(paper): rebuild Figure 1 as native TikZ vector figure`
Body: note the three content corrections (visual dims 512-1536d → 2d-dim 1024-3072-d; late fusion concatenation+FC → equal-weight score averaging; obsolete TENT/SAR blocks → test-time reliability-reweighting annotation per sec:disc) and the PNG/HTML source removal.
  </action>
  <verify>
    <automated>git show --stat HEAD lists exactly 4 paths (2 modified/added, 2 deleted); git status --short shows paper/ clean; git log -1 --format=%s matches "feat(paper): rebuild Figure 1 as native TikZ vector figure"</automated>
  </verify>
  <done>Single commit on main containing the TikZ figure, main.tex integration, and raster-source deletions; no build artifacts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Local LaTeX authoring + build; no network, no new packages, no untrusted input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q66v-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this task (pdftoppm + MiKTeX already present); install nothing |
| T-q66v-01 | Tampering | git history | mitigate | Stage explicit paths only; verify with git status before commit so no artifacts/unrelated files enter history |
</threat_model>

<verification>
- `grep -c "input{figures/fig_architecture}" paper/main.tex` == 1 and `grep -c "fig_architecture.png" paper/main.tex` == 0
- `git ls-files paper/figures/` lists fig_architecture.tex and NOT fig_architecture.png / fig_architecture.html
- Fresh `build_paper.ps1 -Clean` build: exit 0, 10 pages, 0 LaTeX errors, 0 undefined references
- Figure content audit against prose: 256-d skeleton (§3.2 line 134), 2d-dim 1024-3072-d visual (§3.3 line 147), gate/fusion math identical to lines 165/169, late fusion = score averaging (line 155), reliability weight $w$ annotation cites sec:disc (line 189)
</verification>

<success_criteria>
- Figure 1 is a vector TikZ figure in document fonts, readable at print size, replacing the raster PNG
- All three content-drift errors corrected; no obsolete TTA blocks; test-time reweighting annotation present
- Paper rebuilds clean (10pp, 0 errors/undefined refs); headlines/tables untouched (no numeric content changed)
- One clean commit; no build artifacts or unrelated files committed
</success_criteria>

<output>
Create `.planning/quick/260611-66v-rebuild-paper-figure-1-architecture-over/260611-66v-SUMMARY.md` when done (use the summary template; record final figure dimensions, iteration count, and the rendered-page visual check result).
</output>
