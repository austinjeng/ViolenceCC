---
phase: quick-260611-66v
plan: 01
subsystem: paper
tags: [latex, tikz, figure, cgw26]
requires: []
provides:
  - "paper/figures/fig_architecture.tex — native TikZ vector source for Figure 1"
  - "paper/main.tex — tikz preamble, \\resizebox+\\input figure block, corrected caption"
affects: [paper-build]
tech-stack:
  added: ["tikz (positioning, arrows.meta, fit, calc libraries) in paper preamble"]
  patterns: ["figure \\input inside \\resizebox{\\textwidth}{!}{...} for safety clamp"]
key-files:
  created:
    - paper/figures/fig_architecture.tex
  modified:
    - paper/main.tex
  deleted:
    - paper/figures/fig_architecture.png
    - paper/figures/fig_architecture.html
decisions:
  - "Gate equation includes +b_g (matches main.tex line 165 exactly) even though the plan's node-text spec omitted it — verification block required math identical to Eqs (2)-(4)"
  - "Badges kept at \\tiny — rendered crisply as vector text and legible in the 300dpi inspection; no iteration needed"
  - "Test-time annotation placed below the visual stream with a curved dashed arrow into the fusion module's south-east edge (clears visfeat node by design)"
metrics:
  duration: ~15 min
  completed: 2026-06-11
  tasks: 3
  iterations: 1 (visual gate passed on first build)
---

# Quick Task 260611-66v: Rebuild Paper Figure 1 (Architecture Overview) Summary

Figure 1 rebuilt as a native TikZ vector figure in document fonts (8.0in x 3.0in natural, \resizebox'd to \textwidth at ~0.875 scale), replacing the unreadable 2100x680 raster PNG and fixing three content-drift errors.

## What Was Done

### Task 1: TikZ figure source + main.tex integration + orphan removal
- Created `paper/figures/fig_architecture.tex` (120 lines): bare tikzpicture, absolute inch coordinates, muted palette derived from the old SVG hue anchors, four zone labels (Input / Frozen Feature Extraction / Trainable Fusion / Prediction).
  - Skeleton stream (green): Video frames -> RTMPose (COCO-17, top-2 persons) -> CTR-GCN (NTU-120, j/b/jm/bm, "frozen" badge) -> dashed 256-d skeleton snippet feature.
  - Visual stream (blue): Video frames -> Visual backbone (CLIP ViT-B/16 or SigLIP2 Base/SO400M/Giant, "frozen" badge) -> ~1 FPS frame features -> mean+max concat pooling -> dashed **2d-dim (1024--3072-d)** visual snippet feature (was wrongly "512-1536d" in the PNG).
  - Gated Fusion container (orange, thick border, bold title + red "(trainable)") with the three Eq (2)-(4) rows: project+LN per modality, gate (incl. +b_g), fuse with residual s-hat + v-hat.
  - Detached neutral note: **late fusion = equal-weight score averaging** a_late = 1/2 h_s(s) + 1/2 h_v(v) (was wrongly "Concatenation + FC projection").
  - **No TENT/SAR or "LayerNorm adapted by TTA" blocks** — replaced by a dashed red test-time annotation "visual stream scaled by reliability weight w (Sec.~\ref{sec:disc})" feeding the fusion module.
  - Prediction (purple): MIL head (FC + sigmoid, top-k, "trainable" badge) -> Anomaly score in [0,1], with a dashed "MIL ranking loss" training-signal arrow back into the MIL head.
- `paper/main.tex`: added `\usepackage{tikz}` + `\usetikzlibrary{positioning,arrows.meta,fit,calc}` after graphicx; figure block now `\resizebox{\textwidth}{!}{\input{figures/fig_architecture}}`; caption rewritten to match the corrected content (frozen streams, 256-d skeleton, 2d-dim 1024-3072-d visual, gated fusion + MIL head as only trained components, score-averaging late-fusion baseline, sec:disc reweighting annotation); `\label{fig:architecture}` unchanged.
- `git rm` of the orphaned `fig_architecture.png` and `fig_architecture.html`.

### Task 2: Build + visual verification
- `build_paper.ps1 -Clean`: exit 0, **10 pages** (unchanged), 0 LaTeX errors, 0 undefined references. All 10 overfull-hbox warnings are pre-existing body-prose hyphenation (lines 78-410), none from the figure.
- Figure floats to the top of **page 3**. Rendered page + 3 zoomed crops (300 dpi, via pdftoppm to %TEMP%, deleted after) inspected with the Read tool:
  - No text overlap, no arrow-through-text, arrowheads land on box edges.
  - All equations render correctly (project/gate/fuse rows, a_late, w); `\ref{sec:disc}` resolves to "Sec. 3.6".
  - Figure spans the full text width; node text ~caption-sized; badges small but crisp vector text.
  - Zone labels read left->right; fusion module visually emphasized (thick orange border, bold title, red accent).
- **Visual gate passed on the first build — zero layout iterations needed.**

### Task 3: Commit
- Single commit `3e9c492` with exactly 4 paths (1 added, 1 modified, 2 deleted); paper/ tree clean afterward; no build artifacts staged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Content fidelity] Gate equation includes the bias term**
- **Found during:** Task 1
- **Issue:** The plan's node-text spec for the gate row omitted `+\mathbf{b}_g`, but the plan's verification block requires the figure math to be identical to main.tex line 165, which includes it.
- **Fix:** Figure gate row reads $\mathbf{g}=\sigma(W_g[\hat{\mathbf{s}}\,\|\,\hat{\mathbf{v}}]{+}\mathbf{b}_g)$.
- **Files modified:** paper/figures/fig_architecture.tex
- **Commit:** 3e9c492

No other deviations — plan executed as written.

## Verification Results

- `grep -c "input{figures/fig_architecture}" paper/main.tex` = 1; no `includegraphics.*fig_architecture` remains
- `git ls-files paper/figures/` lists fig_architecture.tex; PNG/HTML gone
- Fresh `-Clean` build: exit 0, 10 pages, 0 errors, 0 undefined refs
- Content audit vs prose: 256-d skeleton (line 134) OK; 2d-dim 1024-3072-d (line 147) OK; gate/fuse math = lines 165/169 OK; late fusion = score averaging (line 155) OK; w annotation cites sec:disc (line 189) OK; no TENT/SAR block OK

## Known Stubs

None.

## Threat Flags

None — local LaTeX authoring only; no new packages, no network, explicit-path staging (T-q66v-01 mitigated).

## Self-Check: PASSED

- paper/figures/fig_architecture.tex exists (120 lines, >= 80 min_lines)
- fig_architecture.png / fig_architecture.html removed from working tree and git
- Commit 3e9c492 exists with exactly 4 paths
- Working tree clean except this SUMMARY.md (left uncommitted for the orchestrator's docs commit)
