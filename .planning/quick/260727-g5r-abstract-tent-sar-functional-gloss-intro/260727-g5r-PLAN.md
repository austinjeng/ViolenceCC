---
quick_id: 260727-g5r
description: Abstract TENT/SAR functional gloss — introduce them as two representative entropy-minimization TTA methods (EN+ZH abstracts)
date: 2026-07-27
mode: quick
planner: Fable (session directive — orchestrator plans, Opus subagent executes, orchestrator verifies)
---

# Quick Task 260727-g5r: TENT/SAR functional gloss in both abstracts

## Decision (user-approved via AskUserQuestion, 2026-07-27)

Option chosen: **functional gloss** — introduce TENT/SAR as "two representative
entropy-minimization TTA methods", no acronym expansions, no citations in the
abstract. Rationale: standard treatment for named methods (cf. CLIP/CTR-GCN
unexpanded in same abstract); formal expansions already live in the notation
index (notation.tex:32,34) which follows the abstracts in binding order; first
body use (ch01:67) carries the \cite commands.

One deliberate deviation from the approved preview: the preview spelled out
"test-time adaptation methods", but the immediately preceding sentence defines
"(TTA)", so the edit uses "TTA methods" — spelling it out again right after
defining the abbreviation is a style wart. Flag to author in the report.

## Edits (exact, 2 files)

### 1. thesis/frontmatter/abstract.tex (lines 39-41)

OLD:
```
Entropy-minimization TTA (TENT, SAR),
restricted by the architecture to the fusion head's LayerNorm parameters,
changes AUC by less than $0.1$ points on every backbone
```
NEW:
```
Two representative entropy-minimization TTA methods (TENT, SAR),
restricted by the architecture to adapting only the fusion head's LayerNorm
parameters, change AUC by less than $0.1$ points on every backbone
```
(Verb agreement: "changes" → "change" — subject is now plural "methods".)

### 2. thesis/frontmatter/abstract_zh.tex (line 14)

OLD (fragment):
```
以熵最小化為基礎之 TENT 與 SAR 受架構限制僅能調整融合頭之 LayerNorm 參數
```
NEW (fragment):
```
以熵最小化為基礎之兩種代表性 TTA 方法（TENT 與 SAR）受架構限制僅能調整融合頭之 LayerNorm 參數
```
(ZH wording was author-approved 2026-07-14; this change was re-approved by the
author via the AskUserQuestion preview. Full-width parentheses, per house style.)

## Tasks

1. **Apply both edits + clean rebuild** — one Opus agent: Edit both files exactly
   as above, then `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean`.
   Nothing else may change.
2. **Verify (orchestrator)** — diff shows only the two intended hunks; build exit 0,
   no LaTeX errors/undefined refs; page count ~95; headline numbers (82.5/78.7,
   +1.2/+13.2, 0.1) untouched; commit + STATE row + docs commit.

## Verification gates

- [ ] git diff: exactly 2 files, only the planned lines changed
- [ ] "changes AUC" no longer present in abstract.tex; "change AUC" present
- [ ] ZH line contains 兩種代表性 TTA 方法（TENT 與 SAR）
- [ ] Clean build exit 0, no `!` errors, no undefined refs, ~95pp
- [ ] No number drift anywhere (abstract headlines byte-identical elsewhere)
