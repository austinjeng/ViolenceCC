---
quick_id: 260610-2s1
slug: fix-m1-include-visual-only-in-the-multi-
status: in-progress
date: 2026-06-09
---

# Quick Task 260610-2s1: Fix M1 — Visual Only multi-seed reporting

## Problem

`paper/main.tex:217` states only Gated Fusion is reported as a 3-seed mean±std and that
"All other variants report single-seed results (seed 42)". This contradicts the table
captions (`:225`, `:244`), the figure caption (`:281`), the printed cells (Visual Only
shown as e.g. `82.4±0.2`, `76.8±1.0`), and the result data — Visual Only IS a 3-seed
mean±std (clip_only runs exist at seeds 42/123/2024 for every backbone × dataset).

Confirmed by the 2026-06-09 audit (dimension `hyperparams`, MEDIUM, adversarially verified).
Root cause: Visual Only was single-seed at the 2026-06-07 review and was later upgraded to
3 seeds for the tables/figure, but line 217's prose was not updated.

## Task

1. Edit `paper/main.tex:217`:
   - Multi-seed sentence: "Gated Fusion configuration" → "Gated Fusion and Visual Only configurations".
   - Single-seed sentence: enumerate the true single-seed variants — "All other variants
     (Late Fusion, GF 2-Person, GF Mean-Only, Skeleton Only) report single-seed results (seed 42)".
   - verify: the edited sentence is consistent with captions `:225`, `:244`, `:281`.
2. Recompile a fresh PDF: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_paper.ps1 -Clean`.
   - verify: build succeeds; `paper/main.pdf` regenerated; 0 LaTeX errors, 0 undefined refs.
   - done: PDF reflects the edit, no stale artifacts.

## Scope

One-line LaTeX prose edit. No numeric/table/equation change. No code change.
Build artifacts (`paper/main.pdf`, `*.aux/*.bbl/...`) are git-ignored — not committed.
