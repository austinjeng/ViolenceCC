---
quick_id: 260610-2s1
slug: fix-m1-include-visual-only-in-the-multi-
status: complete
date: 2026-06-09
commit: 48cee29
---

# Quick Task 260610-2s1 — Summary

## What changed

`paper/main.tex:217` — one sentence reworded to fix audit finding **M1** (MEDIUM, adversarially verified in the 2026-06-09 full review).

- Before: "For the **Gated Fusion configuration**, we report mean and standard deviation over three random seeds (42, 123, 2024)… **All other variants report single-seed results (seed 42)** for consistency."
- After: "For the **Gated Fusion and Visual Only configurations**, we report mean and standard deviation over three random seeds (42, 123, 2024)… **All other variants (Late Fusion, GF 2-Person, GF Mean-Only, Skeleton Only) report single-seed results (seed 42)** for consistency."

## Why

The old prose classed Visual Only as single-seed, contradicting:
- table captions `:225`, `:244` ("Gated Fusion and Visual Only rows show mean±std over three seeds"),
- figure caption `:281` ("Visual Only and Gated Fusion bars are three-seed means"),
- the printed cells (e.g. `82.4±0.2`, `76.8±1.0`), and
- the data — `clip_only` runs exist at seeds 42/123/2024 for every backbone × dataset, and the printed cells match the 3-seed means, not the seed-42 values.

Root cause: Visual Only was single-seed at the 2026-06-07 review and was upgraded to 3 seeds for the tables/figure, but line 217 was never updated.

## Verification

- `build_paper.ps1 -Clean` → `paper/main.pdf` rebuilt, **10 pages**, **0 LaTeX errors**, **0 undefined references/citations** (checked `main.log`).
- Edited sentence present in source (grep confirms).
- No numeric, table, equation, or code change. Build artifacts git-ignored (not committed).

## Commit

`48cee29` fix(paper): include Visual Only in multi-seed reporting sentence (M1)
