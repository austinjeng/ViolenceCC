---
quick_id: 260623-9tu
slug: low-batch-code-hygiene
date: 2026-06-23
status: in-progress
type: quick
---

# Quick Task 260623-9tu — Low-severity batch #2 (code-hygiene group)

From review-2026-06-22.html. Each fix gated behind (a) a **full green pytest run** and (b) an
**independent adversarial check** before being marked resolved.

## Findings + fixes
- **C2-1** `src/data/dataset.py:138` — MILFeatureDataset silently drops unloadable/<64-frame videos.
  Fix: after building `video_ids`, if any dropped, `warnings.warn` with the dropped count (mirrors i3d_dataset's "k/n available" pattern).
- **C1-1** `src/losses/mil_loss.py` — top-k over {finite,-inf} → NaN when a bag has <k real snippets.
  Fix: fail-loud guard — raise `ValueError` if any bag's real-snippet count < k (enforces the documented all-ones-mask invariant). Never fires in production (sample-with-replacement → T=32 all-ones).
- **C1-2** `tests/test_mil_loss.py` — no test for the `N_real<k` edge. Fix: add a regression test pinning the contract (raises a clear error, not silent NaN).
- **C4-1** `src/evaluate.py:442` — `--split val` mil_loss=0.0 stub emitted via `print()`. Fix: use `warnings.warn(...)` per the CLAUDE.md eval-stub convention (warnings already imported).
- **C4-3** `src/train.py:246` `validate()` — returns `inf` silently when every val batch is single-class. Fix: emit a `warnings.warn` the first time it would return inf (degenerate val split visible at runtime). Add `import warnings`.
- **C1-3** `src/models/registry.py:82` + `src/models/gated_fusion.py:59` — typo'd hyperparameters swallowed with only a logger.warning. Fix: `raise ValueError` on truly-unexpected kwargs (not declared AND not in the `_KNOWN_EXTRA_KWARGS` replay allow-list). VERIFIED SAFE: every tracked config's model-block keys are declared constructor args (incl. proj_dim/alpha) or allow-listed (variant), so the raise never fires on current configs; a future typo (e.g. `droput`) now fails loud.
- **C3-2** `scripts/pri5_tta_breakdown.py` — mislabels the disc_reweight method as "CORAL-TTA"/"CORAL R1" in docstring/caption/stdout (CORAL is the rejected baseline). Fix: rename method labels to disc-reweight / discriminative-reliability reweighting; keep the `_coral_derisk` DIRECTORY path (its real name) but rename the path variable for clarity.

## Verify
- `conda run -n vcc-main python -m pytest -q` → 321 passed, 0 failed (the green gate).
- Adversarial check per finding (workflow) → mark resolved only those that PASS.
- Update review-2026-06-22.html + STATE for passing findings.
