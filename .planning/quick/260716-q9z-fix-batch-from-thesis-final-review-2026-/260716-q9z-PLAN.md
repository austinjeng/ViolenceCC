---
phase: quick-260716-q9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thesis/chapters/ch02_related_work.tex
  - thesis/chapters/ch03_methodology.tex
  - thesis/chapters/ch04_experimental_setup.tex
  - thesis/chapters/ch05_results_fusion.tex
  - thesis/chapters/ch06_results_tta.tex
  - thesis/chapters/ch07_discussion.tex
  - thesis/frontmatter/notation.tex
  - paper/sota_comparison_full.tex
  - .planning/quick/260716-q9z-fix-batch-from-thesis-final-review-2026-/260716-q9z-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [H1, H3, H4, M1, M2, M3, M4, M5, M6, M7, M8, M9, M10, M11, M12, M14]

must_haves:
  truths:
    - "ch05 no longer claims a 'majority' of cells favor default over 2-person; it states the half/half split (worse in 3 of 4 XD backbones)"
    - "The bolded 'This work' row (82.5) sits directly above EventVAD (82.03) in tab:sota-full in BOTH thesis/chapters/ch07_discussion.tex and paper/sota_comparison_full.tex; every cell value byte-identical (position-only change)"
    - "The frontmatter Symbols table can break across pages: thesis/main.log contains zero 'Overfull \\vbox' warnings and folio VIII no longer overprints a table row"
    - "All 13 open MEDIUM findings (M1-M12, M14) are fixed per the review's suggested fixes, or a data-verified deviation recorded in the SUMMARY; M15 and every LOW finding untouched"
    - "thesis/main.pdf rebuilds clean via build_thesis.ps1 -Clean: 0 errors, 0 undefined references/citations, 97-99 pages, headlines 82.5 and 78.7 present in the rendered text"
    - "No experiment number changed anywhere in the diff (H3 is row reordering only)"
  artifacts:
    - path: "thesis/frontmatter/notation.tex"
      provides: "Symbols section split into two tabular blocks (break point after the lambda_1/lambda_2 row)"
      contains: "Symbols (continued)"
    - path: "thesis/chapters/ch05_results_fusion.tex"
      provides: "H1 + M5 + M6 + M7 + M8 corrected prose"
    - path: "thesis/chapters/ch07_discussion.tex"
      provides: "H3 row swap + M11 + M12 + M14 corrected prose"
    - path: "paper/sota_comparison_full.tex"
      provides: "H3 mirror row swap in the standalone fragment"
    - path: ".planning/quick/260716-q9z-fix-batch-from-thesis-final-review-2026-/260716-q9z-SUMMARY.md"
      provides: "per-finding fix record incl. any deviations"
  key_links:
    - from: "thesis/frontmatter/notation.tex"
      to: "thesis/main.log"
      via: "page-breakable Symbols table removes the 47.58pt Overfull \\vbox on frontmatter p.8"
      pattern: "Overfull \\\\vbox"
    - from: "thesis/chapters/ch07_discussion.tex tab:sota-full caption ('sorted by UCF AUC descending')"
      to: "table body row order"
      via: "RTFM 84.30 > This work 82.5 > EventVAD 82.03 > LAVAD 80.28"
    - from: "thesis/chapters/ch04_experimental_setup.tex:94-96"
      to: "thesis/chapters/ch07_discussion.tex ~:542"
      via: "coordinated M14 replacement of the 'AP calibration dependence' framing"
    - from: "thesis/chapters/ch06_results_tta.tex:61"
      to: "Figure 6.1 caption + ch06:333 JPEG concession"
      via: "M9 'for the visual-language stream' qualifier"
---

<objective>
Apply the fix batch from the FINAL-version thesis review (.planning/THESIS-FINAL-REVIEW-2026-07-16.md):
3 blockers (H1, H3, H4) + 13 open MEDIUM findings (M1-M12, M14).

Purpose: clear every committee-catchable factual/internal contradiction before submission. These are
editorial/factual corrections ONLY — no experiment number may change. Headlines 82.5 (UCF AUC) /
78.7 (XD AP) and all table values stay byte-identical, with the single exception of the H3 row
REORDERING (values unchanged, position only).

EXCLUDED (do not touch): M15 (acknowledgments placeholder — author-only), M13 (already fixed in
3c275e4), all 25 LOW findings (incl. L8's broader ch04/ch05 metric-framing rewrite and ch05:375).

Output: 8 edited LaTeX sources, clean thesis rebuild, SUMMARY, STATE row, one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/THESIS-FINAL-REVIEW-2026-07-16.md   (canonical source — full detail + verified facts per finding)
@CLAUDE.md                                      (LaTeX build conventions)

**Planner-verified anchors (current tree, post-3c275e4 — some review line numbers shifted by +/-1;
anchor edits on TEXT, not line numbers):**

| Finding | File | Current anchor |
|---|---|---|
| H1 | ch05_results_fusion.tex:160-161 | "worse} than the default for the majority of backbone$\times$dataset cells" |
| H3 | ch07_discussion.tex:253-254 | EventVAD row then This-work row; row above is RTFM 84.30 (line 252) |
| H3-mirror | paper/sota_comparison_full.tex:120-121 | same pair; EventVAD line carries a trailing `% RE-VERIFY` comment |
| H4 | notation.tex:44-98 | single 21-row Symbols tabular; lambda_1/lambda_2 row at 78-79; TTA symbols start at p_clip (line 80) |
| M1 | ch02_related_work.tex:66 | "the sub-million-parameter fusion head of this thesis" |
| M2 | ch03_methodology.tex:114 | duplicate four-stream sentence + closing "(On UCF-Crime a single snippet thus pools..." parenthetical |
| M3 | ch03_methodology.tex:303 (score $s$), 355-416 (gate $s$, dual $\sigma$), Notation para 50-58 | see symbol map below |
| M4 | ch04_experimental_setup.tex:385-387 | "runs at ${\sim}30$~ms per frame (about 21.6 FPS end-to-end, 99.3\% of it model inference)" |
| M5 | ch05_results_fusion.tex:149-154 | "measured the same ordering with larger margins ... confirms its direction on every backbone" |
| M6 | ch05_results_fusion.tex:341-343 | "its width exceeds the spread of the entire published leaderboard" |
| M7 | ch05_results_fusion.tex:541-542 | "Explosion007 (4.2\% anomalous) shows the same pattern." |
| M8 | ch05_results_fusion.tex:543-545 and 594-596 | "are exactly the categories..." / "The weak tail is exactly the..." |
| M9 | ch06_results_tta.tex:61-62 | "Gaussian noise at high severities is the deepest failure mode, while brightness shift..." |
| M10 | ch06_results_tta.tex:410-412 | "The per-video episodic protocol shows the problem is not adaptation volume either: ... (continual protocol) ..." |
| M11 | ch07_discussion.tex:273-274 | "Omitted from the main comparison only because" |
| M12 | ch07_discussion.tex:402-403 | "up to $+13.2$ points on the hardest corruption" |
| M14 | ch07_discussion.tex:542-544 + ch04_experimental_setup.tex:93-97 | "AP's calibration dependence" / "more sensitive to score calibration" |

**M3 symbol-usage map (planner-verified by grep — re-confirm with a quick grep before editing):**
- Bare `$s$` as SNIPPET SCORE: ONLY ch03:303 (binary entropy). Nowhere else in any chapter.
- Bare `$s$` as SKELETON-RELIABILITY GATE: ch03:359, 377, 381-382, 390-397, 416; ch06:356
  ("skeleton-trust gate $s$"); notation.tex:90, 94, 101-105 (frontmatter Remark ALREADY
  disambiguates gate-$s$ vs vector-$\mathbf{s}$ and dual-$\sigma$, and claims "both conventions
  follow the notation paragraph of Chapter 3" — which is currently FALSE because ch03's Notation
  paragraph, lines 50-58, never mentions them).
- `$\sigma$` as sigmoid: ch03:196 (gate equation). `$\sigma$` as std-dev: ch03:357, 359, 412-413.
  NOT used in ch06 (grep confirms).
→ Renaming the GATE (report's example, $r_{\mathrm{skel}}$) would cascade into ch06 + notation.tex.
  The constraint mandates a minimal, non-cascading rename, so: rename the ENTROPY SCORE instead
  (single line, and it aligns with the chapter's own declared convention "Scalar $a$ denotes a
  snippet anomaly score", ch03:53-54), plus a Notation-paragraph note for the remaining overloads
  (the report's own alternative for $\sigma$). Record this deviation in the SUMMARY.

**M4 data check (planner-verified):** results/backbone_bench_combined.csv RTMPose-m + YOLOX row
lists "30.1 ms/f" AND "33.3 f/s"; the thesis Table tab:efficiency-backbones (ch04:370) shows
"30.07 ms/frame" (no throughput column). 21.6 FPS is the separate production extraction-loop
measurement (ch04:221, ch03 ~:88; STATE.md: "~46ms/frame (21.6 FPS on RTX 4090)"). The report's
suggested decoupled sentence is consistent with all three sources — apply it as written.

**H3 order check (planner-verified):** after the swap both tables read RTFM 84.30 → This work
82.5 → EventVAD 82.03 → LAVAD 80.28 — strictly descending, satisfying the caption's claim.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the three blockers — H1 (false majority claim), H3 (SOTA row order, thesis + fragment), H4 (notation table overflow)</name>
  <files>thesis/chapters/ch05_results_fusion.tex, thesis/chapters/ch07_discussion.tex, paper/sota_comparison_full.tex, thesis/frontmatter/notation.tex</files>
  <action>
**H1 — ch05_results_fusion.tex ~:160.** Replace the clause
"the 2-person aggregation is \emph{worse} than the default for the majority of backbone$\times$dataset cells"
with the report's fix:
"the 2-person aggregation is \emph{worse} than the default in half of the backbone$\times$dataset cells, including three of the four XD-Violence backbones"
(verified split: better in 4 — UCF Base/SO400M/Giant, XD SO400M; worse in 4 — UCF CLIP, XD CLIP/Base/Giant).
Leave the rest of the sentence (seed variance, post-hoc selection) untouched.

**H3 — ch07_discussion.tex :253-254.** Move the entire
"\textbf{This work (dual-modal fusion)} & \textbf{2026} & \textbf{(thesis)} & \textbf{82.5} & \textbf{78.7} & ..." line
so it sits ABOVE the "EventVAD$^{k}$~\cite{shao2025eventvad} & 2025 & ACM MM'25 & 82.03 & 64.04 & ..." line.
Pure line swap — every character of both rows unchanged. Resulting order: RTFM 84.30 → This work 82.5 → EventVAD 82.03 → LAVAD 80.28.

**H3 mirror — paper/sota_comparison_full.tex :120-121.** Apply the same swap in the full table.
The EventVAD line carries a trailing "% RE-VERIFY: EventVAD XD = 64.04 (AP)..." comment — it MUST stay
attached to the EventVAD row. Do NOT touch the fair-subset table (line ~200) — EventVAD is not in it.
This fragment is standalone (\documentclass{standalone}, not \input by paper/main.tex or the thesis),
so the CLAUDE.md build_paper.ps1 convention is NOT triggered (paper/main.tex is not edited in this batch).

**H4 — thesis/frontmatter/notation.tex :44-98.** The Symbols section is one unbreakable
\begin{center}\begin{tabular}{@{}lp{10.5cm}@{}} spanning 21 rows → 47.58pt Overfull \vbox on frontmatter
p.8, folio VIII overprinting the "$w$ & routing scalar..." row. Split it into TWO tables per the report:
- First table: keep rows $\mathbf{s}$ (line 51) through "$\lambda_1$, $\lambda_2$ & sparsity and
  temporal-smoothness regularization weights" (lines 78-79), then \bottomrule\end{tabular}\end{center}.
- Second table: a new \begin{center}\begin{tabular}{@{}lp{10.5cm}@{}} block with the same
  "Symbol & Meaning" header, preceded by a short unnumbered lead-in line
  (e.g. \noindent\textit{Symbols (continued --- test-time adaptation and reweighting).}),
  containing rows $p_{\mathrm{clip}}$ (line 80) through $\rho$ (line 95).
Row ORDER and row TEXT unchanged — split only. Keep the Remark paragraph (lines 100-105) after the
second table. LaTeX can now break the page between the two center blocks; verification of the
Overfull \vbox disappearing happens in Task 3's clean rebuild.
  </action>
  <verify>
    <automated>cd /d/ViolenceCC && grep -c "majority of backbone" thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && bash -c 'tw=$(grep -n "This work (dual-modal fusion)" thesis/chapters/ch07_discussion.tex | cut -d: -f1); ev=$(grep -n "EventVAD\$\^{k}" thesis/chapters/ch07_discussion.tex | head -1 | cut -d: -f1); [ "$tw" -lt "$ev" ]' && bash -c 'tw=$(grep -n "This work (dual-modal fusion)" paper/sota_comparison_full.tex | cut -d: -f1); ev=$(grep -n "EventVAD\$\^{k}" paper/sota_comparison_full.tex | head -1 | cut -d: -f1); [ "$tw" -lt "$ev" ]' && [ "$(grep -c 'begin{tabular}' thesis/frontmatter/notation.tex)" = "3" ] && grep -q '82\.5.*78\.7' thesis/chapters/ch07_discussion.tex</automated>
  </verify>
  <done>ch05 states the half/half split (no "majority"); This-work row precedes EventVAD in both tab:sota-full copies with all cell values byte-identical; notation.tex has 3 tabular environments (1 abbreviations + 2 symbols) with unchanged row text/order.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 13 open MEDIUM findings (M1-M12, M14) across ch02-ch07</name>
  <files>thesis/chapters/ch02_related_work.tex, thesis/chapters/ch03_methodology.tex, thesis/chapters/ch04_experimental_setup.tex, thesis/chapters/ch05_results_fusion.tex, thesis/chapters/ch06_results_tta.tex, thesis/chapters/ch07_discussion.tex</files>
  <action>
Apply each fix from THESIS-FINAL-REVIEW-2026-07-16.md's "Suggested fix" (deviations only where the
planner verified the data requires it — record every deviation in the SUMMARY). Do NOT fix anything
labeled LOW, and do NOT touch M15, ch05:375, or paper/main.tex.

**M1 — ch02:66.** "a design goal shared by the sub-million-parameter fusion head of this thesis"
→ "a design goal shared by the lightweight (0.50M--1.02M parameter) fusion head of this thesis"
(matches ch01:105, ch04:379, conclusion).

**M2 — ch03:114.** Delete TWO redundant fragments from the long line-114 paragraph:
(a) the sentence "The four-stream features are combined by weighted averaging (joint and bone
weighted 1.0, motion streams 0.5) to form the final 256-d skeleton representation." (duplicate of
lines 108-112), and (b) the closing parenthetical "(On UCF-Crime a single snippet thus pools
skeleton motion over ${\sim}20$~s---far longer than the few-second action clips on which CTR-GCN
was pre-trained---a plausible factor in the weaker UCF skeleton-only result reported below.)"
(duplicate of lines 116-122, and its "reported below" pointer is wrong — the result is in Ch.5).
Everything else in the paragraph stays. (Note: the SURVIVING first mention at lines 109-110 wraps
across the source lines as "combined by weighted" / "averaging with weights 1.0..." — the deleted
duplicate is the only single-line occurrence of "combined by weighted averaging".)

**M3 — ch03, minimal non-cascading fix (recorded deviation from the report's $r_{\mathrm{skel}}$
example, which would cascade into ch06:356 + notation.tex:90/94/102 — see symbol map in context).**
(a) Line 303: rename the binary-entropy snippet score from bare $s$ to the chapter's declared score
symbol $a$: "binary entropy, $-(a\log a + (1-a)\log(1-a))$ per snippet anomaly score $a$, since the
MIL head outputs..." — this is the ONLY score-usage of bare $s$ in the thesis; after it, bare $s$
means the skeleton-reliability gate exclusively, killing the §3.7.1-vs-§3.7.3 collision.
(b) Notation paragraph (ch03:50-58): append two sentences disambiguating the remaining overloads,
e.g.: "Two symbols are context-overloaded in this chapter: $\sigma(\cdot)$ denotes the logistic
sigmoid in the gate equation (Section~\ref{sec:meth-fusion}) but the standard deviation pooled over
a corruption condition in the test-time reweighting method (Section~\ref{sec:disc}); and the plain
scalar $s$ denotes the skeleton-reliability gate of Section~\ref{sec:disc}, distinct from the bold
skeleton feature vector $\mathbf{s}$." This also makes the frontmatter Remark's existing claim
(notation.tex:105, "both conventions follow the notation paragraph of Chapter 3") true.
Do NOT touch the gate-$s$ usages (ch03:359-416, ch06:356) or notation.tex rows. Re-grep
`grep -n '\$s\$\|s\\log' thesis/chapters/*.tex` before editing to confirm the map still holds.

**M4 — ch04:383-387.** Replace "pose estimation runs at ${\sim}30$~ms per frame (about 21.6 FPS
end-to-end, 99.3\% of it model inference), an order of magnitude slower per frame than CLIP
embedding." with the report's decoupled sentence: "pose estimation costs ${\sim}30$~ms per frame in
isolation (Table~\ref{tab:efficiency-backbones}), and the full extraction loop sustains about
21.6~FPS on single-person scenes (Chapter~\ref{ch:03}), 99.3\% of that time being model
inference---an order of magnitude slower per frame than CLIP embedding." (Planner verified this is
the data-consistent version: table = 30.07 ms/frame isolated benchmark; 21.6 FPS = production loop.)

**M5 — ch05:149-154.** Replace "---measured the same ordering with larger margins ($-0.90$~AP for
2-person concatenation and $-2.32$~AP for mean-only pooling), and the final 3-seed grid confirms its
direction on every backbone." with the report's fix, precision-adjusted to the verified data (XD
2-person deltas: CLIP $-2.1$, Base $-1.3$, Giant $-0.9$, SO400M $+0.5$):
"---measured the same default-wins ordering, with a much larger mean-only margin ($-2.32$~AP); the
final 3-seed grid confirms the mean-only direction on every backbone, while the 2-person cost on
XD-Violence spans the three non-SO400M backbones." (The report's own wording "concentrated in CLIP
and SigLIP2 Base" omits Giant's $-0.9$; record the adjustment in the SUMMARY. Keep the literal
token "default-wins" — it is a verification anchor.)

**M6 — ch05:341-343.** "its width exceeds the spread of the entire published leaderboard on this
benchmark." → "its width rivals the spread of the entire published leaderboard on this benchmark
and exceeds the spread of the modern frozen-feature methods it is compared against in
Chapter~\ref{ch:07}." (Leaderboard spread 75.41-89.44 = 14.03pp > CI width 12.39pp; frozen-feature
subset 84.30-89.44 = 5.14pp < 12.39pp.)

**M7 — ch05:541-542.** "Explosion007 (4.2\% anomalous) shows the same pattern." →
"Explosion007 (4.2\% anomalous) is likewise a short-event miss, though there the head saturates the
whole video near the ceiling rather than near zero, blending the two regimes." (Cited source
pri9_failure_cases.md: Explosion007 means 0.992/0.993 — ceiling saturation, not near-0.000.)

**M8 — ch05, two reciprocal spots.**
(a) :543-545: "are exactly the categories where snippet pooling costs the most" → "are among the
categories where snippet pooling costs the most" (Shooting is 95.67, 6th strongest — "exactly" is false).
(b) :594-596: "The weak tail is exactly the abrupt, short-event category set identified by the
failure analysis (Section~\ref{sec:failure}): categories whose events..." → "The weak tail overlaps
the abrupt, short-event category set identified by the failure analysis
(Section~\ref{sec:failure})---RoadAccidents and Explosion appear in both, though Robbery, a
sustained-event category, also sits in the weak tail: categories whose events..." Splice so the
following "categories whose events are brief..." clause still reads grammatically.

**M9 — ch06:61-62.** "Gaussian noise at high severities is the deepest failure mode, while
brightness shift leaves the stream comparatively intact." → "Gaussian noise at high severities is
the deepest failure mode for the visual-language stream (in absolute fused AUC several JPEG cells
sit lower still; Section~\ref{sec:disc-results}), while brightness shift leaves the stream
comparatively intact." (Matches the Fig. 6.1 caption's canonical qualifier and the chapter's own
concession at ch06:333. The verify gate is wrap-tolerant, so normal line re-flow is fine.)

**M10 — ch06:410-412.** "The per-video episodic protocol shows the problem is not adaptation volume
either: even unlimited per-condition adaptation (continual protocol) moves nothing once the
reachable parameters are limited to LN affines." → "The continual protocol shows the problem is not
adaptation volume either: even unlimited per-condition adaptation moves nothing once the reachable
parameters are limited to LN affines." (§6.2 establishes the episodic protocol was structurally
incapable of showing anything; drop the now-redundant parenthetical.)

**M11 — ch07:273-274.** "Omitted from the main comparison only because its trained per-category
mixture-of-experts head exceeds the single-GPU-class regime." → "Omitted from the fair-subset
comparison (Table~\ref{tab:sota-fair}) only because its trained per-category mixture-of-experts
head exceeds the single-GPU-class regime." (GS-MoE IS row 1 of the main table; confirm the label
tab:sota-fair exists via grep before referencing.)

**M12 — ch07:402-403.** "up to $+13.2$ points on the hardest corruption" → "up to $+13.2$ points in
the condition that most collapses the visual-language stream" (thesis-canonical phrase per
PROVENANCE.md row 12; matches ch01:223, ch06:304/331, conclusion).

**M14 — coordinated two-spot fix.**
(a) ch07:542-544: "the residual Giant sensitivity suggests that AP's calibration dependence and
XD-Violence's imbalanced categories still create a challenging optimization landscape" → "the
residual Giant sensitivity suggests that AP's sensitivity to the ordering among the highest-ranked
frames under XD-Violence's low anomaly prevalence, together with its imbalanced categories, still
creates a challenging optimization landscape" (AP is rank-based/monotone-invariant — "calibration
dependence" is technically wrong and contradicts §7.7.2 and §7.4).
(b) ch04:94-96: minimally align the AP clause ONLY: "AP weights precision across the recall range
and is therefore more sensitive to score calibration---a distinction that matters..." → "AP weights
precision across the recall range, depends on class prevalence, and concentrates weight on the
highest-ranked frames, making it more sensitive to small ranking changes among top-scored
frames---a distinction that matters...". Do NOT restructure the preceding AUC sentence (that is L8
scope, out of batch), and leave the similar phrasing at ch05:375 and in paper/main.tex untouched
(L8 / paper scope) — note both leftovers in the SUMMARY.
  </action>
  <verify>
    <automated>cd /d/ViolenceCC && grep -c "sub-million-parameter" thesis/chapters/ch02_related_work.tex | grep -qx 0 && grep -c 'combined by weighted averaging' thesis/chapters/ch03_methodology.tex | grep -qx 0 && grep -q 'averaging with weights 1.0' thesis/chapters/ch03_methodology.tex && tr '\n' ' ' < thesis/chapters/ch03_methodology.tex | grep -Ec 'thus +pools +skeleton +motion +over' | grep -qx 0 && grep -Fc 's\log s' thesis/chapters/ch03_methodology.tex | grep -qx 0 && grep -Fq 'a\log a' thesis/chapters/ch03_methodology.tex && grep -c '21.6 FPS end-to-end' thesis/chapters/ch04_experimental_setup.tex | grep -qx 0 && grep -c 'ordering with larger margins' thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && grep -q 'default-wins' thesis/chapters/ch05_results_fusion.tex && grep -c 'width exceeds the spread of the entire published leaderboard' thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && grep -c 'shows the same' thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && grep -c 'are exactly' thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && grep -c 'is exactly the' thesis/chapters/ch05_results_fusion.tex | grep -qx 0 && tr '\n' ' ' < thesis/chapters/ch06_results_tta.tex | grep -Eq 'deepest +failure +mode +for +the +visual-language +stream' && grep -c 'episodic protocol shows the problem is not adaptation volume' thesis/chapters/ch06_results_tta.tex | grep -qx 0 && grep -c 'Omitted from the main comparison' thesis/chapters/ch07_discussion.tex | grep -qx 0 && grep -c 'hardest corruption' thesis/chapters/ch07_discussion.tex | grep -qx 0 && grep -c 'calibration dependence' thesis/chapters/ch07_discussion.tex | grep -qx 0 && grep -c 'is therefore more sensitive to' thesis/chapters/ch04_experimental_setup.tex | grep -qx 0</automated>
  </verify>
  <done>All 13 MEDIUM findings' offending phrases are gone and replaced per the report (or recorded deviation); every replacement grep-gate above passes; git diff shows NO changed digits in any table cell or reported metric; M15/LOW anchor phrases (e.g. ch05:375, "analyses", BN row) untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full clean rebuild, verification gates, SUMMARY, STATE row, commit</name>
  <files>.planning/quick/260716-q9z-fix-batch-from-thesis-final-review-2026-/260716-q9z-SUMMARY.md, .planning/STATE.md</files>
  <action>
1. **Clean rebuild:** run
   `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean`
   (from repo root D:\ViolenceCC). Must exit 0.

2. **Gates against thesis/main.log and thesis/main.pdf:**
   - 0 LaTeX errors; 0 undefined references/citations:
     `grep -Ei "(Reference|Citation).*undefined|There were undefined" thesis/main.log` → 0 matches.
     (NOTE: "LaTeX Font Warning: Font shape ... undefined" for the C70/bkai CJK font is a
     pre-existing, accepted warning — L24, out of batch — do NOT count it; hence the pattern above,
     not a bare grep for "undefined".)
   - H4 gate: `grep -F 'Overfull \vbox' thesis/main.log` → 0 matches (the 47.58pt frontmatter-p.8
     warning is GONE; there were no other Overfull \vbox warnings before this batch).
   - Page count ~97: expect 97-99 pages (the H4 split may add one frontmatter page). Check via
     `python -c "import fitz; d=fitz.open('thesis/main.pdf'); print(d.page_count)"`
     (fallback interpreter if fitz missing: C:/Anaconda/envs/vcc-main/python.exe).
   - Headlines present in rendered text:
     `python -c "import fitz; t=''.join(p.get_text() for p in fitz.open('thesis/main.pdf')); print('82.5' in t and '78.7' in t)"` → True.
   - Number-integrity: `git diff` over the 8 edited .tex files contains no changed digit in any
     table cell or reported metric (H3 = pure line moves; all other diffs are prose).

3. **Fragment check (H3 mirror):** paper/sota_comparison_full.tex is a standalone fragment with no
   tracked build (NOT \input by paper/main.tex or the thesis; its PDF is gitignored). Attempt a
   standalone compile as a courtesy check: `cd paper && latexmk -pdf sota_comparison_full.tex`
   (exit 0 expected; delete/leave untracked build artifacts — never commit them). If the standalone
   toolchain is awkward, a source-only edit is acceptable — the change is a pure row swap — record
   which path was taken in the SUMMARY.

4. **SUMMARY:** write 260716-q9z-SUMMARY.md in this quick-task directory: per-finding table
   (finding → file → old phrase → new phrase), the two recorded deviations (M3 minimal rename of the
   entropy score to $a$ + Notation-paragraph note instead of the gate rename; M5 precision wording),
   the M14 leftovers deliberately NOT touched (ch05:375, paper/main.tex — L8/paper scope), and the
   gate results (page count, log greps, headline check).

5. **STATE row:** append a row to the "Quick Tasks Completed" table in .planning/STATE.md
   (id 260716-q9z, date 2026-07-16, commit hash, directory link) and update the "Last activity" line.

6. **Commit** (sources + planning docs only — NEVER build artifacts):
   `fix(thesis): final-review batch — blockers H1/H3/H4 + 13 MEDIUM (M1-M12, M14) [260716-q9z]`
   including the 8 .tex files, this PLAN, the SUMMARY, and STATE.md.
  </action>
  <verify>
    <automated>cd /d/ViolenceCC && grep -cF 'Overfull \vbox' thesis/main.log | grep -qx 0 && ! grep -Eiq "(Reference|Citation).*undefined|There were undefined" thesis/main.log && python -c "import fitz; d=fitz.open('thesis/main.pdf'); t=''.join(p.get_text() for p in d); assert 97 <= d.page_count <= 99, d.page_count; assert '82.5' in t and '78.7' in t; print('PASS', d.page_count)" && test -f .planning/quick/260716-q9z-fix-batch-from-thesis-final-review-2026-/260716-q9z-SUMMARY.md && git log -1 --oneline | grep -q 260716-q9z</automated>
  </verify>
  <done>Clean rebuild exits 0 with 0 errors / 0 undefined refs; no Overfull \vbox anywhere in main.log; page count 97-99; headlines 82.5/78.7 in the PDF text; SUMMARY + STATE row written; single commit contains exactly the 8 sources + planning docs (no build artifacts).</done>
</task>

</tasks>

<verification>
- Every finding ID in scope (H1, H3, H4, M1-M12, M14) has a corresponding diff hunk; M13/M15 and all
  25 LOW findings have ZERO diff hunks (spot-check LOW anchors survive: "analyses" ch02:88, BN row
  notation.tex:19, ch05:375 calibration phrasing, "NORM/" line-break ch06:406, UR-DMU/MGFN order in
  tab:sota-fair).
- `git diff --word-diff` over the batch shows no numeric token changed (only moved, for H3).
- thesis/main.log: no Overfull \vbox, no undefined refs/citations; build_thesis.ps1 -Clean exit 0.
- Both tab:sota-full copies read ... RTFM 84.30 → **This work 82.5** → EventVAD 82.03 → LAVAD 80.28.
</verification>

<success_criteria>
- 3 blockers + 13 MEDIUM findings fixed exactly per THESIS-FINAL-REVIEW-2026-07-16.md (2 recorded,
  data-verified deviations: M3 minimal rename, M5 precision wording).
- No experiment number changed; headlines 82.5/78.7 intact; H3 is position-only.
- Thesis rebuilds clean at 97-99pp with the notation-page overflow eliminated.
- One commit; SUMMARY + STATE updated; M15 remains the only open blocker (author-only).
</success_criteria>

<output>
Create `.planning/quick/260716-q9z-fix-batch-from-thesis-final-review-2026-/260716-q9z-SUMMARY.md` when done.
</output>
