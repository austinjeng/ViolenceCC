---
id: 260804-2x3
slug: store-2026-08-03-sci-journal-placement-s
description: Store 2026-08-03 SCI journal placement survey as durable project record
date: 2026-08-04
status: complete
type: docs
---

# Quick Task 260804-2x3 — Summary

Persisted the 2026-08-03 SCI journal placement survey (advisor-requested) as a tracked
project record. Documentation only — no source, paper, thesis, or results changes.

## Delivered

| File | Size | Contents |
|---|---|---|
| `.planning/JOURNAL-SURVEY-2026-08-03.md` | ~19 KB | Source of record — 11 sections |
| `.knowledge/journal-survey-2026-08-03.html` | ~56 KB | Rendered artifact, self-contained |
| `.planning/STATE.md` | +1 row | Quick Tasks Completed |

## Provenance

20 research agents over two workflows: discovery `wf_a0ebe9a4-808` (9 agents, 1.10M
tokens, 774 tool calls) and adversarial verification `wf_c783879f-d77` (11 agents, 1.31M
tokens, 784 tool calls). 133 journals screened, 20 assessed in depth. The verification
pass was instructed to refute rather than confirm and overturned **nine** first-pass
claims, all itemized in §11 of the record.

All metrics are 2025 JIFs from the JCR 2026 edition (released 17 June 2026), with
per-category JCR ranks read from JCR profiles — not Scimago, and not the "best category"
headline aggregators print.

## Findings that change existing project assumptions

1. **MTAP is no longer SCI** — removed from WoS Core in Clarivate's Oct 2024 update; no
   JIF, no quartile. It is **15 of Prof. Yang's 40** DBLP-indexed journal papers (37.5%),
   including one published *after* the delisting. The lab default venue no longer produces
   an SCI paper. Needs raising with the advisor.
2. **82.5% UCF AUC is not disqualifying.** IET Image Processing accepted **80.86%** in 2025;
   Sensors accepted 82.33%; CVIU accepted a paper framed as "a novel baseline" at 85.30%.
   What sinks papers in this band is presenting as a competitive detector and losing.
3. **Q1 is not uniformly closed.** TCSVT/TIP/TMM/PR are shut, but Neurocomputing (Q1 CS-AI,
   8-day desk decision, $0) is a real ~12–18% shot and ESWA/EAAI reach 30–40% with an
   application-first reframe.
4. **No deadline exists.** NTUST IM graduate regulations (both current versions) contain
   zero occurrences of 期刊/發表/SCI — no master's journal requirement. CGW '26 is
   **non-archival** by written CFP policy, so no duplicate-publication rule is triggered.
5. **Prior-art blocker:** Chen et al., "Cross-modal attention fusion of RGB and skeleton for
   multimodal-driven video anomaly detection", *Pattern Recognition* 179:113815 (2026),
   DOI 10.1016/j.patcog.2026.113815. Existence confirmed; **its numbers could not be
   retrieved** — ScienceDirect blocked every route. Must be pulled via the NTUST library
   and differentiated before submitting anywhere.

## Recommended ladder (recorded, not acted on)

Image and Vision Computing → CVIU → JVCIR, all $0 via the Elsevier subscription route and
linked by the Article Transfer Service. Alternatives: SIViP (advisor's active SCIE venue,
6 papers, 10-page cap) or IEEE Access ($1,728 at the NTUST rate, novelty explicitly not
required).

## Verification performed

- HTML rendered under a real browser via `playwright-cli` over `http://127.0.0.1:8731`
  (`file:` is blocked by the tool). Confirmed: `document.characterSet` = UTF-8, no
  mojibake, CJK glyphs (期刊/發表) intact, 10 sections, 7 tables, 49 body rows, 25 verdict
  chips, **no horizontal body overflow**.
- Dark theme verified by computed style, not by eyeballing screenshots: body background
  `rgb(251,251,252)` → `rgb(19,21,25)` and h1 color inverts when `data-theme="dark"` is
  set. An initial pair of screenshots came out byte-identical due to stale page state; that
  was a measurement artifact, not a styling bug, and was chased down rather than assumed.
- Self-containment: zero external asset references (`grep` for remote js/css/font/image
  URLs returns 0), so the file opens offline.
- Added the standalone document wrapper (`<!DOCTYPE>`, `<meta charset="UTF-8">`, viewport,
  minimal reset, explicit `</head><body>`) that the Artifact runtime injects at publish time
  but a local file does not get — matching the `.knowledge/tta-to-reweighting.html`
  precedent.

## Deviations from the standard quick workflow

- **Executed by the orchestrator, not delegated to `gsd-planner`/`gsd-executor`.** The
  deliverable *is* research output held only in the orchestrator's context; a fresh
  executor would have had to reconstruct ~2.4M tokens of verified findings it cannot see.
- **No worktree isolation.** `workflow.use_worktrees` is unset in this project's config and
  the tree carries substantial unrelated uncommitted changes; the commit stages only the
  four paths above.
- Transient artifacts (`shot-*.png` written to the repo root by `playwright-cli`, a
  localhost static server) were removed before staging; `git status` was re-checked to
  confirm only the intended paths were left.

## Follow-ups (not done here)

- Pull and read *Pattern Recognition* 179:113815 via the NTUST library.
- Email the library re: Wiley coverage of IET titles, an IEEE deposit account, and any
  remaining Springer 2026 quota; ask Prof. Yang about NSTC APC funds and his IEEE
  membership grade.
- Raise the MTAP delisting with the advisor.
- The ranked manuscript upgrades in §10 — matched-protocol baseline re-runs first, since
  that single item moves the odds most and serves every venue simultaneously.
