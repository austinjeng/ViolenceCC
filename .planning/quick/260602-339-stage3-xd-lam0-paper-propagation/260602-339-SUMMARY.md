---
quick_id: "260602-339"
slug: stage3-xd-lam0-paper-propagation
date: "2026-06-02"
status: complete
tasks_completed: 2
tasks_total: 2
commits:
  - "6599414 chore: regenerate XD lam=0 artifacts from canonical"
  - "2992ce5 docs: propagate XD lam=0 numbers into paper + author/typo fixes"
verification: PASSED (5/5 dimensions, 0 mismatches)
---

# Quick Task 260602-339 Summary — Stage 3: XD lam=0 paper propagation

Final stage of the smoothness resolution. Dataset-dependent decision was already
locked (commit e4781fb): **XD canonical = lam_smooth 0** (retrained, kept),
**UCF canonical = lam_smooth 8e-4** (frozen). This task propagated the new XD
numbers through the artifact pipeline and into the paper, leaving UCF untouched.

User decisions this session (via AskUserQuestion):
- **UCF = disclose-and-keep** (no retrain; one-sentence footnote; Table 1 frozen).
- **XD abstract headline = plain Gated Fusion SO400M 78.7%** (parallel to the UCF gated headline).

## Task 1 — Regenerate data + figures from canonical [COMPLETE, 6599414]

The Stage-2 retrain overwrote leaf run dirs but never re-aggregated, so
`results-index.csv` → phase10 CSVs → `tables_generated.tex` → figures were all
stale at lam=8e-4. `recompute_fulllength_ucf.py` only updates `ucf_*` index rows,
so a new XD updater was needed.

- **`scripts/_update_xd_index_lam0.py`** (new): XD analog of the UCF index updater.
  Updates `auc`/`ap` for exactly the 29 `xd_*` runs in `results/_lam0_done.txt`
  from each run's `eval_metrics.json`; leaves `ucf_*`, `xd_rtfm_i3d_*`, and TTA
  rows untouched. Backup-once (`.pre_xd_lam0.bak`), atomic replace, asserts 29/29.
- Ran the regeneration chain (vcc-main): updater → `generate_phase10_charts.py`
  → `generate_latex_tables.py` → `generate_pub_figures.py`.
- Result: index 29 XD rows refreshed (e.g. `xd_gated_fusion_so400m_s42` ap
  0.7192→0.7974), phase10 CSVs + XD charts regenerated, `tables_generated.tex`
  XD Table 2 now lam=0, `fig_backbone_comparison.pdf` + `fig_gating_distribution.pdf`
  regenerated. `fig_temporal_scores`/`fig_tta_comparison`/`comparison_auc` reverted
  (UCF/TTA — no data change, only non-deterministic re-render).

## Task 2 — Propagate into main.tex + recompile [COMPLETE, 2992ce5]

All XD numbers copied verbatim from regenerated `tables_generated.tex`; prose
recomputed. UCF Table 1 + all UCF prose frozen.

- **Table 2 (XD):** gated SO400M **78.7±0.9** now leads (was Base 74.7); Giant
  is now the wide-variance outlier (**±2.8**, was 1.6); GF Mean-Only SO400M
  **79.9** (best XD cell, brushes the 80% gate); Best Δ recomputed.
- **Abstract:** XD headline 74.7 (Base) → **78.7 (SO400M)**.
- **§4.3:** skeleton 41.3→40.9; late-vs-gated CLIP gap 5.5→**12.1pp**.
- **§4.4:** visual SO400M 77.6→77.2 (still leads); redistribution reframed
  (SO400M leads both; Base 2nd-visual→last-gated); **seed-stability narrative
  INVERTS** — Base/SO400M 2.9/2.8→~0.9, Giant→2.8; high-XD-variance was partly
  a smoothness artifact.
- **§5/§6/Conclusion:** XD skeleton 40.9; limitations seed sensitivity reframed
  to Giant-only; Conclusion SO400M 77.2 visual + 78.7 gated.
- **Methods:** dataset-dependent λ₂ note (8e-4 UCF / 0 XD) in §4.2 (`sec:impl`);
  loss eq labeled `eq:loss`; smoothness term **retained** in Eq.(5) (UCF uses it);
  §3.5 now defers λ₂ to §4.2. **UCF disclosure footnote** (prior smoothness impl,
  ≤0.13pp AUC).
- **Author block:** `Universiy`→`University` typo (2×). (Affiliations had been
  filled to NTUST / Wei-Han Jeng + Chuan-Kai Yang earlier this session.)
- Recompiled `build_paper.ps1 -Clean` → **9 pages, 0 undefined refs/citations**,
  both new labels resolved.

## Verification — PASSED (5/5, workflow wf_748bc1bb-1bb)

Independent adversarial pass (5 parallel agents, re-deriving from canonical, not
trusting the edits):
1. **Table 2 vs canonical** — every cell/bold/Δ matches to 1 decimal; gated stds
   re-derived (CLIP 0.89, Base 0.92, SO400M 0.91, Giant 2.80).
2. **Inline vs `tables_generated.tex`** — Table 1 + Table 2 identical.
3. **Prose consistency** — all XD prose numbers supported; 12.1=76.5−64.4 exact;
   UCF prose unchanged.
4. **UCF frozen** — no UCF Table 1/prose change vs 9eb23e0; index diff is xd_*-only.
5. **LaTeX internal** — 0 undefined refs; eq:loss retains smoothness term; sec:impl
   states λ₂=8e-4 UCF / 0 XD + footnote; §4.1 C1 reframe still holds; abstract = 78.7.

## Deviations from standard quick workflow

- **Executed on the main tree (no worktree), orchestrator-direct (no spawned
  gsd-executor).** Canonical `results/` is gitignored → absent from a git worktree,
  which would break every regeneration script; and the task held publication-critical
  context (exact λ=0 numbers, regeneration mechanism) already verified in this
  session. GSD guarantees preserved: PLAN.md + SUMMARY.md, two atomic task commits,
  STATE.md tracking. Verification was delegated to an independent multi-agent workflow.

## Out of scope / untouched
- UCF retrain (disclose-and-keep). i3d XD index rows (left at 8e-4). Untracked
  scratch (`scripts/_check_citations.py`, `scripts/_run_tta_backbone_batch.py`,
  `.playwright-cli/`, `CGW2026_Latex_Paper_Template/`, `pipeline-docs.html`) — left as-is.

## Self-Check: PASSED
