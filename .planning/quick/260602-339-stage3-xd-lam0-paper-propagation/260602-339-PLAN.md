---
quick_id: "260602-339"
slug: stage3-xd-lam0-paper-propagation
date: "2026-06-02"
status: in-progress
description: >
  Stage 3 of the smoothness resolution — propagate XD-Violence lam_smooth=0
  canonical numbers into the CGW '26 paper. Dataset-dependent decision already
  landed (commit e4781fb): XD canonical = lam_smooth 0, UCF canonical =
  lam_smooth 8e-4 (frozen). User decisions this session: UCF = disclose-and-keep
  (no retrain); XD abstract headline = plain Gated Fusion SO400M 78.7%.
must_haves:
  truths:
    - "Every XD number in main.tex traces to a canonical results/xd_*/eval_metrics.json (lam_smooth=0)."
    - "UCF Table 1 + all UCF numbers are byte-for-byte unchanged (frozen)."
    - "results-index.csv, phase10 CSVs, tables_generated.tex, and the 2 XD figures all regenerated from canonical via scripts, not hand-edited."
    - "Paper recompiles clean (0 undefined refs/citations) via build_paper.ps1 -Clean."
  artifacts:
    - "scripts/_update_xd_index_lam0.py (XD analog of recompute_fulllength_ucf index updater)"
    - "results/results-index.csv (29 xd_* rows updated)"
    - "paper/tables_generated.tex (XD Table 2 = lam=0)"
    - "paper/figures/fig_backbone_comparison.pdf + fig_gating_distribution.pdf (regenerated)"
    - "paper/main.tex (Table 2, abstract, §4.3/4.4/5/6/Conclusion, smoothness note, UCF footnote, typo fix)"
---

# Quick Task 260602-339: Stage 3 — XD lam_smooth=0 paper propagation

## Background (verified this session)

- **On-disk canonical confirmed:** XD = lam_smooth 0 (e.g. xd_gated_fusion_so400m_s42 AP 0.7974, `.pre_lam0.bak` intact); UCF = lam_smooth 8e-4 (ucf_gated_fusion_giant_s42 AUC 0.8249). Dataset-dependent state is real.
- **Earlier review fixes already landed in main.tex (verified):** C1 leakage reframe (line 198), C2 hparams (line 204: AdamW/lr1e-4/k3/batch16/50ep), H1 UCF full-length (82.5/83.0), H2/H3 equations (score-avg late Eq.1, residual-highway gated Eq.4). The memory's "STILL OPEN" list is stale.
- **The break:** Stage-2 retrain overwrote only leaf run dirs. `results-index.csv` + `tables_generated.tex` are STALE (still lam=8e-4 XD). Pipeline `run dirs → results-index.csv → phase10_charts/{backbone_comparison_4way,seed_stability_4way}.csv → tables_generated.tex → main.tex inline` must be re-run for XD. `recompute_fulllength_ucf.py` updates only `ucf_*` index rows — no XD equivalent exists.

## Regeneration chain (source of truth = results-index.csv)

1. canonical `results/xd_*/eval_metrics.json` (lam=0) — DONE by Stage 2 retrain
2. `results-index.csv` — needs XD `auc`/`ap` update (cols 6,7) for the 29 `xd_*` runs in `results/_lam0_done.txt`. **NOT** i3d XD rows (left at 8e-4).
3. `generate_phase10_charts.py` → `phase10_charts/backbone_comparison_4way.csv` + `seed_stability_4way.csv` (aggregates 3-seed gated mean±std from index)
4. `generate_latex_tables.py` → `tables_generated.tex`
5. `generate_pub_figures.py` → `fig_backbone_comparison.pdf` (from CSVs), `fig_gating_distribution.pdf` (from per-run eval_metrics per_category, already lam=0). UCF/TTA figs re-render identically.

Conda: `C:/Anaconda/envs/vcc-main/python.exe`.

## Task 1 — Regenerate data + figures from canonical [data, commit]

- **files:** `scripts/_update_xd_index_lam0.py` (new), `results/results-index.csv`, `results/phase10_charts/*.csv`, `paper/tables_generated.tex`, `paper/figures/fig_backbone_comparison.pdf`, `paper/figures/fig_gating_distribution.pdf`
- **action:** Write `_update_xd_index_lam0.py` to update auc/ap for exactly the `xd_*` runs listed in `results/_lam0_done.txt` from each run's `eval_metrics.json`. Run it. Then run generate_phase10_charts → generate_latex_tables → generate_pub_figures.
- **verify:** index xd_gated_fusion_so400m_s42 ap≈0.7974; tables_generated.tex XD gated row shows SO400M ≈78.7, CLIP ≈76.5 (not 71.0/74.7); 2 XD figures' mtimes updated. UCF index rows untouched (diff shows only xd_* rows changed).
- **done:** `git commit` (chore/data): index + CSVs + tables + figures.

## Task 2 — Propagate into main.tex + recompile [paper, commit]

UCF Table 1 + all UCF prose FROZEN. Edits (XD only) — numbers copied from regenerated `tables_generated.tex`, prose recomputed:

1. **Table 2** (lines 235-247): replace 6 XD rows with lam=0; re-bold best-per-row; recompute Best Δ (vs CLIP).
2. **Abstract** (line 50): "74.7\% AP on XD-Violence (SigLIP2 Base)" → "78.7\% AP on XD-Violence (SigLIP2 SO400M)".
3. **§4.3 Ablation** (lines 250-261): XD skeleton-only 41.3→~40.9; XD late-CLIP vs gated-CLIP gap (65.5 vs 71.0 = 5.5pp) → new (~64.4 vs ~76.5); visual-only XD prose.
4. **§4.4 Backbone Comparison** (lines 276-282): "XD favors SO400M visual" (77.6→~77.2, still leads); "fusion redistributes...Base 74.7 competitive gated" → SO400M now leads XD gated (~78.7); **Seed stability ¶ INVERTS**: Base/SO400M std 2.9/2.8 → ~0.9; Giant 1.6 → ~2.8 (now the wide one). Note the high-XD-variance narrative was partly a smoothness artifact.
5. **§5 Discussion** (lines 334-338): dataset-specific prefs (visual-only SO400M still leads, holds); late-fusion XD skeleton standalone 41.3→~40.9.
6. **§6 Limitations** (line 353): XD seed sensitivity "std up to 2.9%" → ~0.9% for 3/4 backbones (Giant ~2.8%); weaken/reframe.
7. **Conclusion** (line 376): XD visual-only 77.6→~77.2; add gated SO400M ~78.7.
8. **Dataset-dependent smoothness note** (§3.5 / §4.2): λ2=8e-4 for UCF, λ2=0 for XD (temporal smoothness over-regularizes AP on XD's heterogeneous sources). KEEP the smoothness term in Eq.(5) — UCF uses it.
9. **UCF disclosure footnote** (§4.2): UCF numbers used a prior smoothness implementation differing ≤0.13pp AUC from the released temporal-smoothing code (within UCF seed noise).
10. **Typo fix:** "National Taiwan Universiy" → "University" (2× in author blocks, uncommitted).

- **verify:** `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_paper.ps1 -Clean` → main.pdf, 0 undefined refs/citations.
- **done:** `git commit` (docs/paper): main.tex.

## Task 3 — Adversarial verification [workflow, no commit]

Parallel independent verifiers: (a) every XD number in main.tex Table 2 + abstract + prose vs canonical eval_metrics.json; (b) UCF Table 1 + UCF prose unchanged vs HEAD~2; (c) tables_generated.tex XD matches main.tex inline; (d) build log clean; (e) no stray lam_smooth/smoothness-term contradiction in loss eq. Fix any mismatch, re-commit, re-verify.

## Out of scope / frozen
- UCF retrain (user: disclose-and-keep). UCF Table 1 frozen.
- i3d XD rows in index (left at 8e-4, not retrained).
- Untracked scratch scripts `_check_citations.py`, `_run_tta_backbone_batch.py` (leftovers; leave untracked).
