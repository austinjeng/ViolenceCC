---
status: complete
quick_id: 260729-5l3
description: Exploratory XD re-grid — k×lr×3 seeds under final SO400M gated-fusion config
date: 2026-07-29
commits:
  - 4632e17 (feat: re-grid harness script)
---

# Quick 260729-5l3 — Summary

## What was done

1. **Harness** (`scripts/_tmp_regrid_so400m_xd.py`, commit `4632e17`): 27-spec queue (k ∈ {1,2,3} × lr ∈ {1e-4, 7e-4, 1e-3} × seeds {42,123,2024}) over the final headline config `configs/gated_fusion_xd_so400m.yaml`, reusing `run_ablations.RunSpec/run_queue` with an isolated results root (`outputs/regrid_so400m_xd/`) so the tracked `results/results-index.csv` is never opened. Anchor-first ordering; dry-run verified against an exact 27-line expected block.
2. **Probe:** anchor cell (lr 1e-4, k=3, s42) reproduced the tracked headline run bit-for-bit (AP 0.7974129371935831) before the batch was released.
3. **Batch:** 27/27 runs succeeded overnight (~4 min/run, RTX 4090), zero failures.
4. **Analysis:** `260729-5l3-REPORT.md`. Headline finding: **the historical Appendix C ridge is fully inverted under the final config** — the shipped (lr 1e-4, k=3) cell is the best cell in the grid; the historical winner (1e-3, k=2), formerly +3.72 pp, now −3.27 pp. Appendix C's non-adoption disposition is empirically vindicated.

## Constraints honored

- NO changes to `thesis/`, `paper/`, `results/` (byte-identical), `.knowledge/`. Pure exploratory.
- All run outputs under untracked `outputs/regrid_so400m_xd/`.

## Deviations from standard flow

- Executor ran **without worktree isolation** (orchestrator decision: single additive file, sequential flow, avoids Windows worktree-cleanup failure modes during an unattended overnight run).
- GPU jobs ran as harness-tracked **background shells**, not a visible terminal — explicit user instruction mid-session ("run the GPU jobs directly… I need to get some sleep") overriding the standing visible-terminal preference for this task.
- SUMMARY written by orchestrator post-batch (executor's scope ended at the committed harness), since results are the deliverable.
