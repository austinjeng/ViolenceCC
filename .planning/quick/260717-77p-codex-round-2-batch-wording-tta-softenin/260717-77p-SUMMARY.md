---
phase: quick-260717-77p
plan: 01
subsystem: thesis + analysis + gpu-job-setups
tags: [codex-round-2, thesis-wording, tta-softening, paired-bootstrap, keypoint-drift, learned-late-fusion, consistency-rerun]
requires: [CODEX-REVIEW-VERIFICATION-2026-07-17.md, results/*/eval_scores.npz]
provides:
  - "Thesis wording/disclosure batch (C1/C2/C4/C5/C6) — zero experiment-number changes"
  - "scripts/paired_bootstrap_fusion.py + results/paired_bootstrap/{paired_bootstrap.csv,summary.json} (24 rows, tracked)"
  - "ch05 paired-bootstrap paragraph + PROVENANCE.md row 25"
  - "3 ready-to-run GPU jobs: keypoint drift (A), learned late fusion (B), consistency rerun (C) — NO launches"
affects: [thesis-defense-readiness, ch04-footnote, ch06-caveats, tab:sota-fair]
key-files:
  created:
    - scripts/paired_bootstrap_fusion.py
    - scripts/measure_keypoint_drift.py
    - scripts/run_ucf_consistency_rerun.py
    - scripts/compare_consistency_rerun.py
    - configs/late_fusion_learned{,_siglip2,_so400m,_giant,_xd,_xd_siglip2,_xd_so400m,_xd_giant}.yaml
    - results/paired_bootstrap/paired_bootstrap.csv
    - results/paired_bootstrap/summary.json
  modified:
    - thesis/chapters/ch0{1,3,4,5,6,7,9}*.tex
    - thesis/preamble.tex, thesis/references.bib, thesis/PROVENANCE.md
    - thesis/figures/fig_gate_by_category_{ucf,xd}.png
    - paper/sota_comparison_full.tex
    - scripts/generate_phase6_charts.py, scripts/run_ablations.py, tests/test_run_ablations.py
decisions:
  - "Per-task commits (3) instead of the plan's single end commit — orchestrator constraint takes precedence"
  - "results/paired_bootstrap/{csv,json} git-tracked via `git add -f` (Phase-13 C7-3 policy; PROVENANCE requires tracked sources)"
  - "Fig 5.4 regenerated at (6,3.6)in canvas, 15-16pt fonts (~7.1-7.5pt effective at 2.83in) — print-legible AND holds the 95±2 page gate"
  - "tab:sota-fair given a SHORT caption for the List of Tables (the enlarged caption was pushing the front matter +1 page)"
metrics:
  duration: ~50 min
  completed: 2026-07-17
  commits: 3 (d44a5b4, ddabb1a, 6dd2abf)
  final-page-count: 97 (gate 93-97)
---

# Quick 260717-77p: Codex Round-2 Batch (wording + bootstrap + GPU-job setups) Summary

**One-liner:** Thesis moved to disclosure-precise per the Codex round-2 verification — 13 wording/presentation fixes with zero number changes, a new honest paired video-level bootstrap (24 configs, all sanity-gated against eval_metrics.json), and three validated-but-unlaunched GPU jobs (keypoint drift, learned-scalar late fusion, version-locked consistency rerun) ready for visible-terminal execution.

## Task 1 — Thesis wording batch (commit d44a5b4)

All 13 items applied; no experiment number changed anywhere (digit diff audited):

1. **C1 footnote** (ch04) rewritten: legacy batch-axis formula described; provenance commits `e5ab412`/`f89910b` + `tests/test_smoothness_axis.py` cited; mixture disclosed (all s42 + all-3-seed Gated Fusion rows legacy incl. 82.5±0.4 headline; s123/s2024 of remaining rows corrected at identical λ₂=8e-4); explicit "stds should not be read as pure seed variance" (NO std-bound claim); −0.13pp A/B cited; reported as run.
2. **C2 64×64 disclosure** restored to ch04 dataset description ("distributed as" scoped to the frame re-distribution, not Sultani's release; XD unaffected).
3. ch03 "medium-resolution surveillance frames" → scoped to XD native-resolution vs UCF 64×64 re-distribution.
4. **apples-to-apples → regime-matched** everywhere (ch07 ×5 + fragment ×6, comments included) + protocol-note sentence in the tab:sota-fair caption (254/290 cohort + 64×64/~3FPS; comparability scoped to training regime, not evaluation protocol). Fragment recompiles standalone (latexmk exit 0, artifacts cleaned).
5. **TTA softening**: ch06 "we argue it is not a tuning artifact"; single-LR disclosure (TENT/SAR run only at 1×10⁻³, no sweep; source comment pins run_tta_seeds_m2.py:54); "structural limit" scoped to the evaluated continual protocol + 1,536-param LN surface; ch06 §6.6 "even unlimited adaptation moves nothing" → "full-stream continual adaptation leaves the metric unchanged (<0.1 points)"; ch09 RQ3 softened to argued/scoped form; ch01 already hedged ("We argue") — left as-is.
6. **Fourth honest caveat** (ch06): strategy search scored on the reported 20 test conditions; mitigations (LOCO killed CORAL; gains reproduced on unseen backbones/severities/seeds); mirrored as a third scope cap in ch07 sec:lim-tta.
7. **C5 attribution** ×3: exact replacements applied (ch01 "indicating that a learned combination---here, input-dependent gating---"; ch09 "learned gating (the mechanism tested here)"; ch07 "consistent with its lack of any learned way").
8. **GF 2-Person definition** corrected (preserves two per-person 256-d features → 512-d concat; default = top-2 persons mean-pooled); aligned "2-person aggregation" mentions → "per-person concatenation" (ch05 ×3).
9. **Fig 6.2 caption**: TENT/SAR "omitted here because deltas < 0.1 points (Table tab:tent-sar-null)".
10. **"clear leader" softened ×4** (ch01:205, Fig 5.1 caption, ch05 §backbones ×2) → "highest observed mean", with 78.7±0.9 vs Giant 76.8±2.8 overlap noted (existing numbers only).
11. **PDF metadata**: \hypersetup with exact titlepage title, author "Wei-Han Jeng", abstract keywords (ASCII).
12. **Carreira bib** fixed: `Jo{\~a}o` — PDF renders "J. Carreira", "ao Carreira" absent.
13. **Fig 5.4 regenerated** print-legible via CPU-ONLY fusion-head forward over cached features (CUDA hidden with `CUDA_VISIBLE_DEVICES=-1`; gate ranges identical: UCF [0.0369,0.9394], XD [0.0235,0.9660]; per-category means logged and spot-checked vs old PNG — data unchanged). Checksums differ from baseline (6cab01b9→11d35b05, e57e0762→543fd360).

## Task 2 — Paired bootstrap (commit ddabb1a)

- `scripts/paired_bootstrap_fusion.py`: B=2000 video-level paired resamples (numpy default_rng(0)), gated−visual per (dataset, backbone, seed), GT mirrors `_build_frame_arrays` via `frame_labels`/`xd_frame_labels` with n_frames from the saved arrays. **Mandatory sanity gate passed for all 48 run dirs** (sklearn recompute vs eval_metrics.json within 1e-3) + fast weighted AUC/AP asserted sklearn-exact (<1e-9) at multiplicity 1. Degenerate-resample redraw rule implemented (0 redraws occurred).
- **Actual results (per-config P(Δ>0) min–max across seeds):**
  | config | P(Δ>0) range | note |
  |---|---|---|
  | ucf_clip | 0.386 – 0.838 | all CIs straddle 0 |
  | ucf_siglip2 | 0.3405 – 0.848 | all CIs straddle 0 |
  | ucf_so400m | 0.4125 – 0.838 | all CIs straddle 0 |
  | ucf_giant | 0.377 – 0.7585 | all CIs straddle 0 |
  | xd_clip | 1.0 – 1.0 | significant on all 3 seeds (+1.4/+1.7/+2.7 pts) |
  | xd_siglip2 | 0.013 – 0.9845 | sign flips with seed (s42 significantly −1.3) |
  | xd_so400m | 0.561 – 1.0 | CIs exclude 0 on 2/3 seeds |
  | xd_giant | 0.0 – 0.9905 | sign flips with seed (s42 significantly −2.1) |
- ch05 §Statistical Robustness paragraph reports exactly this, framed per the thesis position (evidence remains cross-config consistency); PROVENANCE.md row 25 added; CSV+summary.json tracked (`git add -f`).

## Task 3 — GPU-job deliverables, NO launches (commit 6dd2abf)

- **(A) measure_keypoint_drift.py**: stratified 53-video UCF test sample (14 categories, default_rng(0), clean-cache-filtered); gaussian s3/s4/s5 + brightness s5 via `corruption.apply_corruption` through `extract_skeletons.load_ucf_frames` (no formula duplication); px-dev / bbox-scale OKS (κ=0.1, formula documented) / conf-drop vs `E:/skeletons/ucf/*.pkl`; stated criterion printed (PASS iff OKS≥0.85 AND drop≤0.10); resumable CSV; `--list-only` validated (212 measurements enumerated, zero inference, no files created). Runs in vcc-skeleton, visible terminal, ~1.5–2.5 GPU-h.
- **(B) learned_late_fusion queue**: 8 configs cloned byte-preserving (only header filename + alpha line differ; YAML-parse verified: dims/λ₂ inherited); `QUEUES["learned_late_fusion"]` = 24 RunSpecs with learned-tagged unique names (`*_late_fusion_learned_s*`, `*_late_fusion_{siglip2,so400m,giant}_learned_s*`); `--dry-run` emits exactly 24 `[dry-run] would run` lines; test pin 334→358 + queue assert; pytest 17 passed. ~2–3 GPU-h when the user runs it.
- **(C) run_ucf_consistency_rerun.py + compare_consistency_rerun.py**: `--list-only` enumerates **exactly 29 legacy cells** (all 21 UCF s42 + all gated-fusion s123/s2024 across 4 backbones — matches the C1 evidence), via `git merge-base --is-ancestor f89910b <snapshot sha>`; retrains from the snapshot's own config dict into `results/_consistency_rerun/` only (path-prefix assert = T-77p-01 mitigation); scoped clean-worktree guard; resumable; provenance block written into each rerun snapshot. Compare script: per-cell delta vs row 3-seed `statistics.stdev` + DECISION-GATE verdict (PASS→footnote deltas; FAIL→adopt rerun numbers). ~2.5 GPU-h (29 × <5 min; ch04 head-only timing — the addendum's 5–6 h was conservative). `results/_consistency_rerun/` does NOT exist.

## Final gate battery — 9/9 PASS

1. Clean rebuild: exit 0, 0 errors, 0 undefined refs, 0 Overfull \vbox.
2. Pages **97** (93–97 gate) + headlines 82.5/78.7 present.
3. PDF metadata title/author/keywords non-empty.
4. "ao Carreira" absent / "Carreira" present in extracted text.
5. 0 "apples-to-apples" in PDF text and all .tex sources (tr-flattened); fragment standalone compile exit 0.
6. Fig 5.4 checksums differ from baseline; 150-dpi render of PDF p.64 confirmed legible (15–16pt fonts on 6-in canvas ≈ 7.1–7.5pt effective at 2.83-in render width).
7. pytest test_run_ablations 17 passed; dry-run exactly 24 learned lines; bootstrap CSV 24 rows.
8. No canonical results/<run>/ dir created or modified; results/paired_bootstrap/ is the only new results/ path (worktree tracked + main-repo copy); results/_consistency_rerun/ absent in both roots.
9. git status clean after commits — only intended files changed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Paired-bootstrap AP produced NaN under resampling**
- **Found during:** Task 2, first full run
- **Issue:** zero-weight tie groups (videos with multiplicity 0) caused 0/0 in the weighted-AP precision term; NaN propagated to all 12 XD deltas
- **Fix:** safe divide (`np.divide(..., where=tot>0)`); mathematically exact since zero-weight groups have zero recall increment; full rerun produced NaN-free CSV
- **Files:** scripts/paired_bootstrap_fusion.py — **Commit:** ddabb1a

**2. [Rule 3 - Blocking] Page-count gate: 96 (baseline) → 99 after edits**
- **Fix (3 levers, all within this task's own additions):** short caption `\caption[...]` for tab:sota-fair (its enlarged caption was duplicating into the List of Tables, +1 front-matter page); tightened my own added prose in ch06/ch07 (disclosure content preserved); Fig 5.4 canvas (6,4.5)→(6,3.6) with fonts unchanged. Final: 97pp.
- **Commits:** d44a5b4 / ddabb1a

**3. [Rule 3 - Blocking] `CUDA_VISIBLE_DEVICES=""` did not hide the GPU on this host**
- **Fix:** used `CUDA_VISIBLE_DEVICES=-1` + in-driver `assert not torch.cuda.is_available()` before the Fig 5.4 CPU forward (authorized CPU-only forward; no GPU launch occurred)

**4. [Rule 3 - Blocking] Job C scripts needed `--results-root` for zero-GPU validation from the worktree**
- **Fix:** added optional `--results-root` override (mirrors the existing `run_ablations.py --results-root` pattern); defaults unchanged for the user's main-repo run

### Process deviations

- **Per-task commits (3) instead of the plan's single end commit** — orchestrator constraint ("commit each atomically") takes precedence over the plan's Task-3 single-commit instruction.
- **Learned configs: header filename comment updated** alongside the alpha line (strict byte-identity kept for every other byte; avoids a wrong-filename header).
- **Bootstrap outputs written to BOTH roots:** computed against `D:/ViolenceCC/results` (worktree has no npz), canonical copy at `D:/ViolenceCC/results/paired_bootstrap/`, tracked copy committed inside the worktree so it merges.

## Known Stubs

None — no placeholder values or unwired paths introduced. The three GPU-job scripts are deliberately unlaunched deliverables (that is their spec, not a stub).

## Threat Flags

None — no new network endpoints, auth paths, or trust-boundary changes. T-77p-01 (rerun output-path containment) and T-77p-02 (bootstrap provenance) mitigations implemented as planned; T-77p-SC upheld (zero new packages installed).

## User's next steps (visible terminal, in any order)

1. `C:/Anaconda/envs/vcc-skeleton/python.exe scripts/measure_keypoint_drift.py` (~1.5–2.5 h) → ch04 sentence + table row per the printed PASS/FAIL.
2. `python scripts/run_ablations.py --queue learned_late_fusion` (vcc-main, ~2–3 h) → one table row / footnote defusing "why no learned-weight late fusion?".
3. `python scripts/run_ucf_consistency_rerun.py` then `python scripts/compare_consistency_rerun.py` (~2.5 h) → footnote deltas or escalation per the DECISION-GATE.

## Self-Check: PASSED

14/14 checks (11 artifact files incl. tracked provenance CSVs + 3 commits d44a5b4/ddabb1a/6dd2abf) verified on disk and in git.
