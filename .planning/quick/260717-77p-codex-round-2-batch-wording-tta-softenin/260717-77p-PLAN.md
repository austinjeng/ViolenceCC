---
phase: quick-260717-77p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thesis/chapters/ch01_introduction.tex
  - thesis/chapters/ch03_methodology.tex
  - thesis/chapters/ch04_experimental_setup.tex
  - thesis/chapters/ch05_results_fusion.tex
  - thesis/chapters/ch06_results_tta.tex
  - thesis/chapters/ch07_discussion.tex
  - thesis/chapters/ch09_conclusion.tex
  - thesis/preamble.tex
  - thesis/references.bib
  - thesis/PROVENANCE.md
  - thesis/figures/fig_gate_by_category_ucf.png
  - thesis/figures/fig_gate_by_category_xd.png
  - paper/sota_comparison_full.tex
  - scripts/generate_phase6_charts.py
  - scripts/paired_bootstrap_fusion.py
  - scripts/measure_keypoint_drift.py
  - scripts/run_ucf_consistency_rerun.py
  - scripts/compare_consistency_rerun.py
  - scripts/run_ablations.py
  - tests/test_run_ablations.py
  - configs/late_fusion_learned.yaml
  - configs/late_fusion_learned_siglip2.yaml
  - configs/late_fusion_learned_so400m.yaml
  - configs/late_fusion_learned_giant.yaml
  - configs/late_fusion_learned_xd.yaml
  - configs/late_fusion_learned_xd_siglip2.yaml
  - configs/late_fusion_learned_xd_so400m.yaml
  - configs/late_fusion_learned_xd_giant.yaml
autonomous: true
requirements: [CODEX-C1, CODEX-C2, CODEX-C4, CODEX-C5, CODEX-C6, CODEX-D3, CODEX-JOB-C]

must_haves:
  truths:
    - "Compiled thesis contains zero 'apples-to-apples' occurrences; fair-subset table is titled 'regime-matched' with a protocol-note sentence"
    - "ch04 footnote describes the legacy(s42+all-GF)/corrected(s123-s2024) seed mixture, cites the -0.13pp A/B, and does NOT claim stds bound the implementation effect"
    - "TTA categorical claims are argued/scoped and the single-LR (1e-3, no sweep) disclosure is present in ch06"
    - "ch06 Honest caveats has a FOURTH item (selection on reported test conditions) mirrored by one sentence in ch07 sec:lim-tta"
    - "ch05 contains a paired-bootstrap paragraph whose numbers are copied verbatim from results/paired_bootstrap/paired_bootstrap.csv (24 rows = 8 configs x 3 seeds)"
    - "Three GPU-job deliverables exist and validate WITHOUT any GPU launch (list-only/dry-run/pytest only)"
    - "Thesis rebuilds clean via build_thesis.ps1 -Clean: 0 errors, 0 undefined refs, page count 95+/-2, headlines 82.5/78.7 present, PDF metadata non-empty, references render 'J. Carreira'"
    - "No experiment number anywhere in the thesis changes except the NEW paired-bootstrap numbers"
    - "No canonical results/<run>/ dir is created or modified; results/paired_bootstrap/ (and later, when the user runs Job C, results/_consistency_rerun/) are the only new results/ paths"
  artifacts:
    - path: "scripts/paired_bootstrap_fusion.py"
      provides: "Paired video-level bootstrap (B=2000) of gated-minus-visual deltas from saved eval_scores.npz"
    - path: "results/paired_bootstrap/paired_bootstrap.csv"
      provides: "24 rows: per (dataset,backbone,seed) mean delta, 2.5/97.5 percentiles, P(delta>0)"
    - path: "scripts/measure_keypoint_drift.py"
      provides: "D3 prep run: RTMPose keypoint drift under gaussian s3/s4/s5 + brightness s5 vs clean cache (deliverable only)"
    - path: "scripts/run_ucf_consistency_rerun.py"
      provides: "Job C: version-locked rerun of legacy-trained UCF cells into results/_consistency_rerun/ (deliverable only)"
    - path: "scripts/compare_consistency_rerun.py"
      provides: "Per-cell delta table + DECISION-GATE verdict vs row seed-std (deliverable only)"
    - path: "configs/late_fusion_learned_xd_giant.yaml"
      provides: "One of 8 learned-alpha late-fusion configs (alpha: learned)"
    - path: "thesis/figures/fig_gate_by_category_ucf.png"
      provides: "Regenerated print-legible gate-by-category figure (data unchanged)"
  key_links:
    - from: "scripts/paired_bootstrap_fusion.py"
      to: "src/evaluate.py"
      via: "GT reconstruction reusing frame_labels/xd_frame_labels + data/ucf_total_frames.json manifest path, validated by asserting recomputed full-dataset AUC/AP matches each run's eval_metrics.json"
      pattern: "frame_labels|xd_frame_labels"
    - from: "thesis/chapters/ch05_results_fusion.tex"
      to: "results/paired_bootstrap/paired_bootstrap.csv"
      via: "paragraph numbers copied from CSV; PROVENANCE.md row added"
      pattern: "paired"
    - from: "scripts/run_ablations.py"
      to: "configs/late_fusion_learned_xd_giant.yaml"
      via: "QUEUES[\"learned_late_fusion\"] RunSpecs referencing the 8 new configs"
      pattern: "learned_late_fusion"
    - from: "thesis/preamble.tex"
      to: "thesis/frontmatter/titlepage.tex"
      via: "\\hypersetup pdftitle/pdfauthor copied from titlepage strings (author 'Wei-Han Jeng')"
      pattern: "hypersetup"
---

<objective>
Codex round-2 batch (canonical source: .planning/CODEX-REVIEW-VERIFICATION-2026-07-17.md, incl.
the 2026-07-17 addendum): (T1) thesis wording/presentation fixes across C1/C2/C4/C5/C6, (T2) paired
video-level bootstrap analysis + honest ch05 addition, (T3) three resumable GPU-job setup scripts —
deliverables only, NO GPU launches — then a full clean rebuild + gate battery.

Purpose: move the thesis from "conditionally defense-ready" to disclosure-precise without changing
any experiment number, and hand Austin three ready-to-run GPU jobs (keypoint drift, learned-scalar
late fusion, consistency rerun) for visible-terminal execution before the defense.

Output: edited thesis sources + regenerated Fig 5.4 PNGs + fixed bib + PDF metadata; new
scripts/paired_bootstrap_fusion.py + results/paired_bootstrap/paired_bootstrap.csv + ch05 paragraph
+ PROVENANCE.md row; scripts/measure_keypoint_drift.py, 8 learned-alpha configs +
QUEUES["learned_late_fusion"], scripts/run_ucf_consistency_rerun.py + compare_consistency_rerun.py;
clean 95±2pp thesis build passing all gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CODEX-REVIEW-VERIFICATION-2026-07-17.md   <!-- canonical evidence per claim; READ the addendum too -->
@.planning/STATE.md
@CLAUDE.md

Environment facts (verified this session — do not rediscover):
- vcc-main python: `C:/Anaconda/envs/vcc-main/python.exe` (has sklearn, numpy, fitz 1.28.0; system python has NO sklearn). Use it for all analysis scripts, pytest, and PDF-text gates. Do NOT use `conda run` (cp950 UnicodeEncodeError on Windows).
- Thesis build: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean` (non-interactive). Build artifacts are git-ignored; never commit them.
- eval_scores.npz format (src/evaluate.py:485-488, test path): npz keyed by video id -> float32 PER-FRAME score array (`frames_map`). Both UCF and XD run dirs have it. GT construction lives in `_build_frame_arrays` (src/evaluate.py:184-356) which calls `frame_labels`/`parse_annotations` (src/eval/ucf_annotations) and `xd_frame_labels`/`parse_xd_annotations` (src/eval/xd_annotations), with annotation fallbacks at data/annotations/{ucf,xd}_temporal.txt and the UCF full-length manifest data/ucf_total_frames.json.
- Results dir naming: `{ds}_{model}[_{bb}]_s{seed}`; visual-only model is `clip_only`; backbone suffixes: (none)=CLIP ViT-B/16, `siglip2`=Base, `so400m`, `giant`. Index: results/results-index.csv.
- Clean skeleton cache: `E:/skeletons/{dataset}/{video_id}.pkl`, PYSKL pickle: keypoint [M=2,T,V=17,C=2] pixel coords + keypoint_score [M=2,T,V=17] (scripts/extract_skeletons.py header). UCF frames: E:/UCF_crime_dataset/{Train|test}/... 64x64 PNGs.
- late_fusion learned mode: src/models/late_fusion.py constructor `alpha: str = "equal"` (~line 32), `self.alpha_logit = nn.Parameter(torch.zeros(1))` under `alpha == "learned"` (~line 53-55). Config key: `alpha: equal` at configs/late_fusion.yaml:20. The 8 existing late-fusion configs: late_fusion{,_siglip2,_so400m,_giant}.yaml + late_fusion_xd{,_siglip2,_so400m,_giant}.yaml.
- run_ablations.py: `QUEUES["pri1_ablations_seeds"]` at line ~311 is the clone pattern; RunSpec signature is RunSpec(dataset, model, seed, config_path[, variant_suffix]). tests/test_run_ablations.py:123 pins `len(all_run_names) == 334` (update to 358 after adding 24). Dry-run emit format (line ~732): `[dry-run] would run {run_name}`.
- Fig 5.4 generator: scripts/generate_phase6_charts.py:946-1034 `collect_gate_and_features(dataset_name, config_path, checkpoint_path)` loads the GatedFusion checkpoint and runs a fusion-head-only forward over CACHED features (device auto-selects CUDA at ~line 968); the plot lands as `D01_gate_by_category_{tag}.png` under the charts output dir, NOT thesis/figures/.
- English author string: "Wei-Han Jeng" (thesis/frontmatter/titlepage.tex:27); English title on titlepage.tex ~lines 20-26; English keywords at thesis/frontmatter/abstract.tex:51.
- Line anchors in this plan were verified 2026-07-17 but ALWAYS re-anchor on text, not line numbers.

CRITICAL verify-gate rule (lesson from quick 260716-q9z): phrases can span source line breaks, making
naive grep gates vacuous. Every phrase gate below must run on tr-flattened text
(`tr '\n' ' ' < file | grep ...`) or on extracted PDF text — never bare multi-word grep on .tex sources.
Never use bare `grep -c token == 0` on unfiltered files where the token could appear in comments.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thesis wording + presentation batch (13 items, zero number changes)</name>
  <files>thesis/chapters/ch01_introduction.tex, thesis/chapters/ch03_methodology.tex, thesis/chapters/ch04_experimental_setup.tex, thesis/chapters/ch05_results_fusion.tex, thesis/chapters/ch06_results_tta.tex, thesis/chapters/ch07_discussion.tex, thesis/chapters/ch09_conclusion.tex, thesis/preamble.tex, thesis/references.bib, paper/sota_comparison_full.tex, scripts/generate_phase6_charts.py, thesis/figures/fig_gate_by_category_ucf.png, thesis/figures/fig_gate_by_category_xd.png</files>
  <action>
Before editing: record baseline `md5sum thesis/figures/fig_gate_by_category_*.png` for the item-13 gate. Editorial edits must change NO experiment numbers anywhere.

**1. C1 footnote rewrite (ch04_experimental_setup.tex ~247-252).** Replace the existing smoothness footnote ("The reported UCF-Crime numbers were produced with an earlier implementation...") with a footnote that states ALL of: (a) the earlier implementation penalized score differences across the batch/video axis rather than temporally adjacent snippets (formula documented in tests/test_smoothness_axis.py:4-6; original at git e5ab412, fixed in f89910b — recoverable from repository history); (b) WHICH runs used it: all seed-42 UCF runs and all three seeds of the Gated Fusion rows (including the 82.5±0.4 headline); (c) seeds 123/2024 of the remaining UCF table rows were trained after the correction with the released temporal implementation at identical lambda_2 = 8e-4, so those rows combine two loss implementations and their standard deviations should not be read as pure seed variance; (d) a direct A/B on the headline configuration (seed 42) measured −0.13pp AUC; (e) numbers are reported as run. PROHIBITED: any claim that the stds "bound" or "empirically bound" the implementation effect (round-2 concession 2 — mixed-row stds have low power).

**2. C2 64×64 disclosure (ch04 ~31-34).** In the UCF-Crime dataset description, adapt archived thesis/appendices_archived/appD_engineering.tex:128-132: the UCF frame re-distribution used provides 64×64-pixel PNGs pre-sampled at ~3 FPS; CLIP/SigLIP2 preprocessing upsamples to the backbone input size and RTMPose operates on these frames; this input fidelity is lower than the native-resolution video used by published pipelines; clarify that "distributed as" refers to this re-distribution, not the original Sultani et al. video release. Note XD is decoded from native video (unaffected).

**3. ch03_methodology.tex ~76.** Fix "medium-resolution surveillance frames" to match the 64×64 UCF reality (reword, or scope the phrase to XD).

**4. "apples-to-apples" → "regime-matched" (ch07 + fragment).** In the tab:sota-fair caption (ch07_discussion.tex ~314-323) rename "apples-to-apples" to "regime-matched" AND add one protocol-note sentence: the evaluation cohort (254/290) and 64×64/~3 FPS inputs differ from published pipelines, so comparability is scoped to the training regime, not the evaluation protocol. Update the prose "apples-to-apples subset" (ch07 ~410) to "regime-matched subset". Then flatten-grep ALL of thesis/ and paper/sota_comparison_full.tex for remaining occurrences (the fragment has its own copy — update it too). The fragment is \documentclass{standalone}: after editing, compile-check it in isolation with latexmk (quick 260716-q9z precedent), then delete its build artifacts (PDF is git-ignored).

**5. TTA softening + single-LR disclosure (C4 round-2, ch06/ch09/ch01).** (a) ch06_results_tta.tex ~174-183: change "it is not a tuning artifact" to argued form ("we argue this is not a tuning artifact" or equivalent) and ADD one disclosure sentence: both TENT and SAR ran at their reference learning rate 1e-3 (scripts/run_tta_seeds_m2.py:54) with no learning-rate sweep, so the categorical reading rests on the rank-invariance mechanism. (b) ch06 ~182-183: scope "a structural limit of the most widely used TTA family" to the evaluated continual protocol + the 1,536-parameter LayerNorm adaptation surface; keep "structural" only as the argued interpretation. (c) ch06 ~413-415: replace "even unlimited per-condition adaptation moves nothing" with "full-stream continual adaptation leaves the metric unchanged ($<0.1$ points)". (d) ch09_conclusion.tex ~47-52 RQ3: soften "a structural consequence of LayerNorm's lack of recalibratable batch statistics, not a tuning failure" to the same argued/scoped form. (e) ch01_introduction.tex ~210-216: check whether the "tuning artifact" sentence already sits under a "We argue" hedge; if yes leave it, if no add the hedge.

**6. C4 fourth honest caveat.** ch06 "Honest caveats" list (~367-383, currently 3 items): ADD a fourth item — the six-strategy search and finalist comparison were scored on the same 20 UCF-Crime-C test conditions reported in the results table, so residual researcher-selection risk remains even though every candidate was label-free/tuning-free with knobs fixed by stated principles; mitigations: the leave-one-condition-out check eliminated the CORAL candidate, and the adopted method's gains reproduced on backbones/severities/seeds not used during screening. Mirror ONE sentence in ch07 sec:lim-tta subsection (~576).

**7. C5 attribution softening (3 sentences, exact replacements).** ch01 ~197-198: "establishing that learned, input-dependent gating is what allows" → "indicating that a learned combination --- here, input-dependent gating --- is what allows". ch09 ~27-28: "only realized through learned, input-dependent gating" → "only realized through learned gating (the mechanism tested here)". ch07 ~43-44: "underperforms precisely because it has no input-dependent way" → "consistent with its lack of any learned way".

**8. GF 2-Person definition fix.** ch05_results_fusion.tex ~28-30 falsely claims the variant replaces "the default single-person aggregation". Correct: the default retains the top-2 persons per frame and mean-pools them (ch03:79, ch03:119; scripts/extract_ctrgcn.py:241-245); the variant preserves the two per-person 256-d CTR-GCN features and CONCATENATES them to 512-d (configs/gated_fusion_2person.yaml `skel_agg: concat`). Grep thesis/chapters/ for other definitions of the variant (ch04, table captions, "single-person") and align them all.

**9. Fig 6.2 caption (ch06 ~250-260).** The caption claims TENT/SAR are shown but thesis/figures/fig_tta_comparison.pdf plots only Source-Only and Ours. Reword: TENT/SAR are omitted from the figure because their deltas are below 0.1 points on every backbone (Table~\ref{tab:tent-sar-null} — verify the actual label name of the TENT/SAR table before citing; use the label that exists in ch06).

**10. "clear leader" softening ×3.** ch01 ~205, ch05 ~180-181 (Fig 5.1 caption), ch05 ~220 → "highest observed mean" or equivalent; where numeric context fits, note Giant's ±2.8 band overlaps SO400M's mean (78.7±0.9 vs 76.8±2.8). Numbers already in the thesis — do not introduce new ones.

**11. PDF metadata.** thesis/preamble.tex currently loads \usepackage[bookmarksnumbered,hidelinks]{hyperref} with no metadata. Add \hypersetup{pdftitle={...}, pdfauthor={...}, pdfkeywords={...}} using the EXACT English thesis title from thesis/frontmatter/titlepage.tex (~lines 20-26), author "Wei-Han Jeng" (titlepage.tex:27), and the English keyword list from thesis/frontmatter/abstract.tex:51 — read those strings, do not invent. Use plain ASCII/English strings (avoid CJK in metadata to keep hyperref happy).

**12. Carreira bib fix.** thesis/references.bib:149: change author from {Jo\~{a}o Carreira and Andrew Zisserman} to {Jo{\~a}o Carreira and Andrew Zisserman} (the current form breaks IEEEtranN abbreviation → PDF renders "J. ao Carreira").

**13. Fig 5.4 regeneration (print-legible fonts, data unchanged, CPU-only forward authorized).** scripts/generate_phase6_charts.py:946-1034 `collect_gate_and_features` loads the GatedFusion checkpoint and runs a fusion-head-only forward over CACHED features (device auto-selects CUDA at ~line 968); it writes `D01_gate_by_category_{tag}.png` under the charts output dir, NOT thesis/figures/. There are no CSV/npz plot inputs to reuse. Procedure: (i) force a deterministic CPU-only forward — set CUDA_VISIBLE_DEVICES='' for the invocation or patch/parameterize device='cpu' for this run; this is a fusion-head-only forward on cached features (minutes on CPU) and does NOT violate the no-GPU-launch rule; (ii) modify ONLY font sizing / canvas dimensions in the D01 gate-by-category plotting section so in-figure text reaches ≥7pt effective at the 2.83in render width (bump font sizes and/or shrink figure canvas so fonts scale up); (iii) regenerate ONLY the gate-by-category charts and confirm plotted values are unchanged — spot-check a handful of plotted per-category gate means against the current PNGs' values, or accept determinism-by-fixed-checkpoint (same checkpoint, eval mode, deterministic forward, no dropout at eval); (iv) copy/rename the regenerated outputs to thesis/figures/fig_gate_by_category_ucf.png and thesis/figures/fig_gate_by_category_xd.png. Fallback if the CPU forward is impractical: leave the PNGs untouched and instead stack the two subfigures full-width (\textwidth each) in ch05 .tex. Verify new checksums differ from the recorded baseline (unless fallback used).

Finish with an intermediate build (`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean`) to catch LaTeX errors early; fix any introduced errors. Do not commit yet.
  </action>
  <verify>
    <automated>for f in thesis/chapters/*.tex paper/sota_comparison_full.tex; do tr '\n' ' ' < "$f" | grep -o "apples-to-apples"; done | wc -l  # must be 0; AND tr '\n' ' ' < thesis/chapters/ch06_results_tta.tex | grep -c "1e-3"  # >=1 (single-LR disclosure); AND tr '\n' ' ' < thesis/chapters/ch04_experimental_setup.tex | grep -cE '64[^0-9]{0,12}times[^0-9]{0,12}64|64x64'  # >=1 (C2 resolution disclosure — bare "64" is vacuous, ch04 already says "64-frame"); AND grep -c "Jo{\\\\~a}o" thesis/references.bib # ==1; AND grep -c "hypersetup" thesis/preamble.tex # >=1; AND md5sum thesis/figures/fig_gate_by_category_*.png differs from baseline (or fallback documented)</automated>
  </verify>
  <done>All 13 items applied; flatten-grep shows 0 "apples-to-apples" in any .tex source; ch04 contains the 64×64 resolution phrase (tr-flattened `grep -cE '64[^0-9]{0,12}times[^0-9]{0,12}64|64x64'` >= 1); ch04 footnote contains the mixture description (s42+GF legacy, s123/s2024 corrected, −0.13pp A/B) and no "bound"-type std claim; ch06 has 4 honest-caveat items + single-LR sentence; fragment compiles standalone via latexmk exit 0; intermediate thesis build exits 0; no experiment number changed (git diff of .tex files shows no digit edits outside the specified sentences/captions/footnote).</done>
</task>

<task type="auto">
  <name>Task 2: Paired video-level bootstrap (zero-GPU) + honest ch05 addition</name>
  <files>scripts/paired_bootstrap_fusion.py, results/paired_bootstrap/paired_bootstrap.csv, results/paired_bootstrap/summary.json, thesis/chapters/ch05_results_fusion.tex, thesis/PROVENANCE.md</files>
  <action>
**Script (scripts/paired_bootstrap_fusion.py).** For each (dataset, backbone) in {ucf, xd} × {clip, siglip2(base), so400m, giant} and each seed in {42, 123, 2024}: resolve the gated dir (`{ds}_gated_fusion[_{bb}]_s{seed}`) and visual-only dir (`{ds}_clip_only[_{bb}]_s{seed}`) — confirm exact names against results/results-index.csv and fail loudly on a missing dir. Load per-video FRAME-score arrays from each dir's eval_scores.npz (npz key = video id → float32 frame scores). Guard: assert the two npz files share identical video-id key sets.

Ground truth: reuse the evaluation pipeline's GT — import `parse_annotations`/`frame_labels` (src/eval/ucf_annotations) and `parse_xd_annotations`/`xd_frame_labels` (src/eval/xd_annotations) and mirror exactly how `_build_frame_arrays` (src/evaluate.py:184-356) calls them, building each video's label array to n_frames = len(saved frame-score array) — the saved arrays already encode the H1/M3 full-length logic (incl. the data/ucf_total_frames.json manifest), so lengths must match by construction. Do NOT reimplement label semantics. MANDATORY sanity gate inside the script: for every run dir, recompute the full-dataset headline metric (UCF: sklearn roc_auc_score over concatenated frames; XD: average_precision_score) from the loaded npz + rebuilt labels and assert it matches that run's eval_metrics.json headline value to within 1e-3 — abort if any mismatch (this proves GT reconstruction is exact).

Paired bootstrap: B=2000 video-level resamples with a fixed RNG seed (numpy default_rng(0), documented in the script header). Per iteration: resample video ids WITH replacement once, compute the dataset's headline metric for BOTH models on the identical resample, record delta = gated − visual. Degenerate resamples (single-class) are skipped with a resample redraw (document the rule). Output: results/paired_bootstrap/paired_bootstrap.csv with one row per (dataset, backbone, seed): mean delta, 2.5th/97.5th percentiles, P(delta>0), n_videos, B; plus results/paired_bootstrap/summary.json (per-config 3-seed aggregates). The script writes ONLY under results/paired_bootstrap/ — never into canonical run dirs.

Run it: `C:/Anaconda/envs/vcc-main/python.exe scripts/paired_bootstrap_fusion.py` (CPU-only; NO conda run). Expect ~24 configs × 2 models sanity checks + 24 × 2000 metric evaluations — if runtime is excessive, vectorize per-video concatenation with precomputed index arrays, do not lower B.

**ch05 addition (~316-356, Statistical Robustness section).** Add a short paragraph (mini-table ONLY if it earns its space) reporting the paired-bootstrap results HONESTLY from the ACTUAL computed CSV — no pre-drafted numbers; the expected shape per the internal single-config probe is nonsignificant per-config (P(delta>0)~0.6), but write whatever the CSV says. Frame it consistently with the thesis's existing position: per-configuration gains are individually within seed variance; the evidence for complementarity is cross-configuration consistency (the sign test); the paired bootstrap makes that explicit at the per-config level. These are the ONLY new numbers permitted in the thesis.

**Provenance.** Add a row to thesis/PROVENANCE.md following its existing row format, sourcing the new ch05 numbers to scripts/paired_bootstrap_fusion.py + results/paired_bootstrap/paired_bootstrap.csv.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import csv;rows=list(csv.DictReader(open('results/paired_bootstrap/paired_bootstrap.csv')));assert len(rows)==24, len(rows);print('24 rows OK')"; AND the script's own eval_metrics.json sanity asserts passed during the run (exit 0); AND tr '\n' ' ' < thesis/chapters/ch05_results_fusion.tex | grep -c "bootstrap" # increased vs pre-edit; AND grep -c "paired_bootstrap" thesis/PROVENANCE.md # >=1</automated>
  </verify>
  <done>CSV exists with exactly 24 data rows (8 configs × 3 seeds) + summary.json; every per-run recomputed AUC/AP matched eval_metrics.json within 1e-3 (script exit 0, asserts silent); ch05 paragraph added with numbers verbatim from the CSV (spot-check 2 values against CSV); PROVENANCE.md row added in the existing format; no file inside any canonical results/<run>/ dir created or modified.</done>
</task>

<task type="auto">
  <name>Task 3: GPU-job setup scripts (NO launches) + final gate battery</name>
  <files>scripts/measure_keypoint_drift.py, configs/late_fusion_learned.yaml, configs/late_fusion_learned_siglip2.yaml, configs/late_fusion_learned_so400m.yaml, configs/late_fusion_learned_giant.yaml, configs/late_fusion_learned_xd.yaml, configs/late_fusion_learned_xd_siglip2.yaml, configs/late_fusion_learned_xd_so400m.yaml, configs/late_fusion_learned_xd_giant.yaml, scripts/run_ablations.py, tests/test_run_ablations.py, scripts/run_ucf_consistency_rerun.py, scripts/compare_consistency_rerun.py</files>
  <action>
ABSOLUTE RULE: these are deliverables. Do NOT launch training, extraction, or any GPU inference. Validation is limited to py_compile, --help, --list-only/--dry-run enumeration, and pytest. Every script gets a usage header documenting: run in a VISIBLE terminal (user preference — no headless background jobs), the exact env activation, expected runtime, and resumability.

**(A) scripts/measure_keypoint_drift.py** (D3 prep; runs in the SKELETON env — rtmlib/onnxruntime, no torch). Stratified sample of ~50 UCF test videos (stratify by category from the test tree at E:/UCF_crime_dataset/test/<Category>/). Conditions: gaussian s3/s4/s5 + brightness s5 — import the EXACT corruption functions from scripts/corruption.py (do not duplicate formulas). Reuse RTMPose extraction utilities from scripts/extract_skeletons.py (import its detector/pose init and per-frame inference helpers; corrupt frames in-memory before inference). Compare against the clean skeleton cache at E:/skeletons/ucf/{video_id}.pkl (PYSKL pickle: keypoint [2,T,17,2] pixel coords, keypoint_score [2,T,17]): per condition compute mean per-joint pixel deviation, an OKS-style similarity (bbox-scale-normalized, document the formula), and mean keypoint-confidence drop. Output: CSV (one row per video×condition) + printed per-condition summary with an explicit PASS/FAIL readout against a STATED negligibility criterion printed in the output (default: PASS if mean OKS ≥ 0.85 AND mean confidence drop ≤ 0.10 for the condition; state it as the criterion in the header). Resumable: skip videos whose rows already exist in the CSV. Include a --list-only flag that enumerates the stratified sample WITHOUT any inference (used for validation now). Usage header: skeleton env activation (mirror how scripts/extract_skeletons.py documents it, e.g. the vcc-skeleton python exe), expected runtime estimate.

**(B) Learned-scalar late-fusion queue.** Verify src/models/late_fusion.py's learned mode (constructor `alpha` string ~line 32; `alpha_logit` nn.Parameter ~line 55). Create 8 config files by cloning each existing late_fusion*.yaml counterpart and switching `alpha: equal` → `alpha: learned` (keep everything else byte-identical): configs/late_fusion_learned.yaml, _siglip2, _so400m, _giant, and the 4 xd variants (clone late_fusion_xd*.yaml). Add `QUEUES["learned_late_fusion"]` to scripts/run_ablations.py cloning the structure of QUEUES["pri1_ablations_seeds"] (line ~311): 8 configs × seeds {42,123,2024} = 24 RunSpecs. Inspect how RunSpec's variant suffix builds run names and choose suffixes so all 24 run names are unique and clearly learned-tagged (e.g. variant "learned", "siglip2_learned", ...). Validate with `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue learned_late_fusion --dry-run` — exactly 24 names, all configs resolve. Update the queue-count pin in tests/test_run_ablations.py (line ~123: 334 → 358; quick 260610-aqm precedent) and add an `assert len(QUEUES["learned_late_fusion"]) == 24`; run `C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_run_ablations.py -x`.

**(C) scripts/run_ucf_consistency_rerun.py + scripts/compare_consistency_rerun.py** (Job C, round-2 addendum). Rerun script: (1) enumerate legacy-trained UCF cells programmatically — every results/ucf_*/config_snapshot.json whose recorded git sha does NOT have fix f89910b as an ancestor (`git merge-base --is-ancestor f89910b <sha>`; non-zero exit = legacy); expected ≈ all UCF *_s42 runs + gated-fusion s123/s2024 (~29 runs); print the list (expose as --list-only for validation now). (2) Retrain each with CURRENT HEAD code and the SAME config/seed into results/_consistency_rerun/<runname>/ — NEVER writing into canonical results/<runname>/ (assert the output path prefix before any write). (3) Clean-worktree guard: abort unless `git status --porcelain` is clean for paths under src/ scripts/ configs/ (path-scoped check; untracked .planning/ and results/ noise allowed); record {sha, scoped_dirty: false} into each rerun's config snapshot. (4) Resumable: skip runs whose results/_consistency_rerun/<runname>/eval_metrics.json exists. Usage header: visible terminal, resumable, expected runtime with derivation "≈29 runs × <5 min/run ≈ 2.5 GPU-h" (reconciles with ch04:383 head-only training time; the addendum's 5-6h estimate was conservative). Compare script: reads canonical + _consistency_rerun eval_metrics.json pairs, emits a per-cell delta table (rerun − canonical) with each row's canonical seed-std (from the row's 3-seed sibling runs) and a DECISION-GATE verdict line: PASS if every |delta| ≤ its row's seed std, else list escalation cells; writes results/_consistency_rerun/comparison.csv + a markdown summary. Validate both with py_compile + --help; validate the rerun script's --list-only enumeration (zero GPU — reads snapshots + git only) and sanity-check the count is ~29 and includes ucf_gated_fusion_giant_s123/s2024.

**FINAL GATES (run all; fix and re-run on any failure):**
1. Clean rebuild: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean` — exit 0, 0 LaTeX errors, 0 undefined references (the pre-existing C70/bkai CJK font-shape warning is allowed), 0 Overfull \vbox in the log.
2. Page count 95±2 and headlines: `C:/Anaconda/envs/vcc-main/python.exe -c "import fitz;d=fitz.open('thesis/main.pdf');t=' '.join(p.get_text() for p in d);assert 93<=d.page_count<=97,d.page_count;assert '82.5' in t and '78.7' in t;print('pages',d.page_count,'headlines OK')"`.
3. PDF metadata non-empty: fitz doc.metadata title/author/keywords all non-empty strings.
4. References render "J.~Carreira": extract text of the references pages via fitz; assert "ao Carreira" ABSENT and "Carreira" present.
5. No "apples-to-apples" in extracted PDF text NOR in any .tex source (tr-flatten pipeline from Task 1); fragment paper/sota_comparison_full.tex compiles standalone exit 0.
6. Fig 5.4: new PNG checksums differ from Task-1 baseline (or the documented .tex-stacking fallback was applied); spot-render one page containing Figure 5.4 via fitz pixmap at 150 dpi and confirm labels are legible (human-readable size — record the effective font assessment in the summary).
7. `C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_run_ablations.py -x` passes; --dry-run outputs exactly 24 `[dry-run] would run` learned lines; bootstrap CSV has 24 rows.
8. No canonical results/ run dir modified: results/paired_bootstrap/ is the only new results/ path this session; results/_consistency_rerun/ does NOT exist yet (created only when the user runs the script).
9. `git status` shows only intended files changed (thesis sources, figures, scripts, configs, tests, PROVENANCE, .planning).

Then commit everything (single commit, message like `fix(thesis)+feat(scripts): codex round-2 batch — wording/TTA softening, paired bootstrap, GPU-job setups (260717-77p)`), per repo convention excluding build artifacts and results/ (git-ignored).
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue learned_late_fusion --dry-run | grep -c '\[dry-run\] would run .*learned' # ==24 (anchored on the emit prefix at run_ablations.py:732 — bare grep "learned" could match banner/config-path lines); AND C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_run_ablations.py -x # pass; AND C:/Anaconda/envs/vcc-main/python.exe scripts/run_ucf_consistency_rerun.py --list-only # exits 0, prints ~29 runs, no GPU; AND C:/Anaconda/envs/vcc-main/python.exe -c "import fitz;d=fitz.open('thesis/main.pdf');m=d.metadata;assert m['title'] and m['author'] and m['keywords'];t=' '.join(p.get_text() for p in d);assert 'ao Carreira' not in t and 'Carreira' in t;assert 'apples-to-apples' not in t;assert '82.5' in t and '78.7' in t;assert 93<=d.page_count<=97;print('ALL PDF GATES OK')"</automated>
  </verify>
  <done>All three GPU-job deliverables exist with visible-terminal usage headers and validate WITHOUT GPU (py_compile/--help/--list-only/--dry-run/pytest only); nothing was trained or extracted; all 9 final gates pass; single commit lands with only intended files; results/_consistency_rerun/ absent; SUMMARY.md written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts → results/ artifacts | Analysis scripts read local run artifacts (npz/json); no untrusted network input |
| rerun script → canonical results/ | Job C must never write into canonical run dirs (path-prefix assert) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77p-01 | Tampering | run_ucf_consistency_rerun.py output paths | mitigate | assert output prefix results/_consistency_rerun/ before any write; clean-worktree scoped guard |
| T-77p-02 | Repudiation | ch05 bootstrap numbers | mitigate | PROVENANCE.md row + in-script eval_metrics.json sanity asserts (1e-3) |
| T-77p-03 | Info disclosure | PDF metadata | accept | title/author/keywords are public thesis facts |
| T-77p-SC | Tampering | package installs | accept | NO new packages installed anywhere in this plan (all deps pre-verified in vcc-main/vcc-skeleton) |
</threat_model>

<verification>
- Zero "apples-to-apples" in .tex sources (tr-flattened) and in extracted PDF text; fragment compiles standalone.
- ch04 footnote: mixture disclosed, −0.13pp A/B cited, no std-bound claim (flatten-grep for "bound" near the footnote must be absent); ch04 64×64 resolution phrase present (tr-flattened grep -cE '64[^0-9]{0,12}times[^0-9]{0,12}64|64x64' >= 1).
- ch06: 4 honest caveats; single-LR 1e-3 disclosure; argued/scoped structural claims; ch09 RQ3 + ch01 hedges consistent.
- paired_bootstrap.csv: 24 rows; ch05 paragraph numbers match CSV; PROVENANCE row present.
- Deliverables: --dry-run emits exactly 24 `[dry-run] would run` learned lines, pytest green, --list-only ≈29 legacy cells, no GPU work performed, results/_consistency_rerun/ absent.
- Build: exit 0, 0 undefined refs, 0 Overfull \vbox, 95±2 pages, headlines 82.5/78.7, metadata non-empty, "J. Carreira" renders correctly.
- No canonical results/<run>/ dir created or modified; results/paired_bootstrap/ is the only new results/ path.
</verification>

<success_criteria>
- All 13 T1 items applied with zero experiment-number changes; all T2/T3 artifacts exist and validate; all 9 final gates pass; one commit; SUMMARY.md written to .planning/quick/260717-77p-codex-round-2-batch-wording-tta-softenin/260717-77p-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/quick/260717-77p-codex-round-2-batch-wording-tta-softenin/260717-77p-SUMMARY.md` when done.
</output>
