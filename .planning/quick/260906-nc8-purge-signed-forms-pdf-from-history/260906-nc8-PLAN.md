---
phase: quick-260906-nc8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - thesis/frontmatter/approval.tex
  - thesis/frontmatter/recommendation.tex
  - "git index/history: thesis/frontmatter/signed_forms.pdf stripped from all commits (filter-repo rewrite + force push)"
autonomous: true
requirements: [QUICK-260906-NC8]

must_haves:
  truths:
    - "thesis/frontmatter/signed_forms.pdf is absent from every commit reachable from refs/heads/main and refs/remotes/origin/main"
    - "origin/main on GitHub contains the rewritten history (force-pushed) and the old blob path 404s via the API"
    - "Local copy of signed_forms.pdf survives in thesis/frontmatter/, untracked and gitignored"
    - "thesis builds locally WITH the signed scans (IfFileExists true branch) and compiles for public cloners WITHOUT the PDF (placeholder fallback branch)"
    - "Repo is PUBLIC again after the purge (prerequisite for Zenodo GitHub integration)"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore entry for thesis/frontmatter/signed_forms.pdf under the personal-documents section"
      contains: "thesis/frontmatter/signed_forms.pdf"
    - path: "thesis/frontmatter/approval.tex"
      provides: "IfFileExists wrapper: signed scan page 1 if present, pre-260804 placeholder page otherwise"
      contains: "IfFileExists"
    - path: "thesis/frontmatter/recommendation.tex"
      provides: "IfFileExists wrapper: signed scan page 2 if present, pre-260804 placeholder page otherwise"
      contains: "IfFileExists"
  key_links:
    - from: "git history (585 commits)"
      to: "thesis/frontmatter/signed_forms.pdf"
      via: "git filter-repo --invert-paths"
      pattern: "git rev-list --objects main | grep -c signed_forms.pdf == 0"
---

<objective>
Purge `thesis/frontmatter/signed_forms.pdf` (signed 推薦書/審定書 committee scans, 1.7MB, personal info) from the entire git history while the repo is private, then re-publicize the repo so Zenodo's GitHub integration can see it.

Context: The 2026-08-31 purge (e0afb1f) removed only the root copy `審定書和推薦書.pdf`. A second copy under `thesis/frontmatter/` (added 2026-08-04 by quick-260804-o0k, commit 3333028) was missed and is tracked at HEAD. The repo was briefly public on 2026-09-01 with this file exposed; it was flipped back to private within minutes. This task completes the purge before the repo goes public permanently.

Output: Rewritten history force-pushed to origin/main with the PDF stripped from all commits; local signed copy preserved untracked; thesis build falls back to the pre-260804 unsigned placeholder pages when the PDF is absent; repo set to public.
</objective>

<tasks>

## Task 1: LaTeX fallback + gitignore + untrack

- Wrap `\includepdf` in `thesis/frontmatter/approval.tex` and `recommendation.tex` in `\IfFileExists{frontmatter/signed_forms.pdf}{...}{<pre-260804 placeholder page>}` (placeholder content recovered from `git show 3333028^`).
- Add `thesis/frontmatter/signed_forms.pdf` to the personal-documents section of `.gitignore`.
- `git rm --cached thesis/frontmatter/signed_forms.pdf` and commit.
- Verify: file untracked but present on disk; `git ls-files` no longer lists it.

## Task 2: History rewrite + force push

- Back up the PDF outside the repo (scratchpad) — filter-repo deletes the working-tree copy.
- Remove the stale clean worktree `D:/ViolenceCC_rerun_wt` (detached at d2e4c2b, 0 dirty files; filter-repo aborts on extra worktrees).
- `pip install git-filter-repo`; run `git filter-repo --invert-paths --path thesis/frontmatter/signed_forms.pdf --force`.
- Restore the PDF to `thesis/frontmatter/` (now untracked+ignored); re-add `origin` remote (filter-repo strips it); `git push --force origin main`.
- Verify: `git rev-list --objects main | grep signed_forms` empty locally for main and origin/main; GitHub API returns 404 for the old path on HEAD; commit 3333028 no longer an ancestor of main.

## Task 3: Build verification + re-publicize

- Rebuild thesis (`build_thesis.ps1`) with the PDF present → signed pages embedded (local path).
- Temporarily rename the PDF, rebuild → placeholder pages compile cleanly (public-cloner path); restore PDF.
- `gh repo edit --visibility public`.
- Write SUMMARY.md, update STATE.md Quick Tasks table, commit docs, push (normal push).

</tasks>

<execution_note>
Executed directly in the main session context rather than via a worktree-isolated gsd-executor: git filter-repo rewrites every ref and cannot run safely from (or with) secondary worktrees, and a force push of rewritten history warrants direct step-by-step verification. Sequential execution on the main tree is the workflow-sanctioned fallback for exactly this case.
</execution_note>
