---
phase: quick-260804-o0k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thesis/frontmatter/signed_forms.pdf
  - thesis/frontmatter/recommendation.tex
  - thesis/frontmatter/approval.tex
  - thesis/preamble.tex
autonomous: true
requirements: [QUICK-260804-o0k]

must_haves:
  truths:
    - "thesis/main.pdf page 3 is the signed 指導教授推薦書 scan (advisor signature dated 2026-07-30), no grey placeholder text"
    - "thesis/main.pdf page 4 is the signed 學位考試委員審定書 scan (committee signatures + chair stamp), no grey placeholder text"
    - "Clean rebuild exits 0 with main.log reporting 95 pages, 0 LaTeX errors, 0 undefined references"
    - "The original upload 審定書和推薦書.pdf remains untouched at the repo root (untracked, user's file)"
  artifacts:
    - path: "thesis/frontmatter/signed_forms.pdf"
      provides: "2-page signed scan (page 1 = 審定書, page 2 = 推薦書), tracked by git"
    - path: "thesis/frontmatter/recommendation.tex"
      provides: "推薦書 page via pdfpages"
      contains: "\\includepdf[pages=2]{frontmatter/signed_forms.pdf}"
    - path: "thesis/frontmatter/approval.tex"
      provides: "審定書 page via pdfpages"
      contains: "\\includepdf[pages=1]{frontmatter/signed_forms.pdf}"
    - path: "thesis/preamble.tex"
      provides: "pdfpages package load"
      contains: "\\usepackage{pdfpages}"
  key_links:
    - from: "thesis/frontmatter/recommendation.tex"
      to: "thesis/frontmatter/signed_forms.pdf"
      via: "\\includepdf pages=2 (scan page 2 is the 推薦書)"
      pattern: "includepdf\\[pages=2\\]"
    - from: "thesis/frontmatter/approval.tex"
      to: "thesis/frontmatter/signed_forms.pdf"
      via: "\\includepdf pages=1 (scan page 1 is the 審定書)"
      pattern: "includepdf\\[pages=1\\]"
    - from: "thesis/main.tex"
      to: "frontmatter include order"
      via: "unchanged: recommendation (line 21) BEFORE approval (line 22)"
      pattern: "input\\{frontmatter/recommendation\\}"
---

<objective>
Replace the two grey placeholder frontmatter pages (指導教授推薦書 / 學位考試委員審定書) in the thesis with the signed scans from the user's uploaded PDF, keeping the 95-page count and NTUST R4 binding order intact.

Purpose: Closes pre-binding revision item "unsigned 推薦書/審定書 front matter" (one of the four items open before binding, tracked in project memory / DEFENSE-RUNS context).
Output: thesis/frontmatter/signed_forms.pdf (tracked), rewritten recommendation.tex + approval.tex using pdfpages, updated preamble.tex, clean 95pp thesis/main.pdf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@thesis/main.tex
@thesis/preamble.tex
@thesis/frontmatter/recommendation.tex
@thesis/frontmatter/approval.tex

**Verified facts (do not re-derive):**
- Upload: `D:\ViolenceCC\審定書和推薦書.pdf` (repo root, untracked, 1,743,631 bytes, 2 pages). Page 1 = 學位考試委員審定書 (signed, chair stamp). Page 2 = 指導教授推薦書 (signed 2026-07-30). Content verified to match this thesis (鄭暐瀚 / M11309108 / 資訊管理系, correct bilingual title).
- **Page-order crossing:** `main.tex` inputs `frontmatter/recommendation` (line 21) BEFORE `frontmatter/approval` (line 22), but the scan has 審定書 first. Therefore recommendation.tex embeds scan **page 2** and approval.tex embeds scan **page 1**. Do NOT reorder main.tex.
- `thesis/preamble.tex` has no `pdfpages` (verified by grep); `graphicx` is loaded at line 31, `hyperref` last at line 65. `pdfpages` must be added (MiKTeX auto-installs it via `--enable-installer` in thesis/.latexmkrc if not present locally — a CTAN auto-install during the build is expected and fine).
- `git check-ignore thesis/frontmatter/signed_forms.pdf` exits 1 → NOT ignored (only `thesis/main.pdf` and paper/ PDFs are ignored). Plain `git add` works; `git add -f` NOT needed.
- latexmk runs with thesis/ as working directory (build_thesis.ps1), so `\includepdf` paths are relative to `thesis/` → use `frontmatter/signed_forms.pdf`.
- `\includepdf` defaults: issues its own page break and uses `pagecommand={\thispagestyle{empty}}`, so the inserted pages carry no page number — matching the current placeholders and R4 (Roman numbering starts at 中文摘要 via `\pagenumbering{Roman}` on main.tex line 23, AFTER approval). Expect a harmless pdfpages/hyperref warning about links in included pages being discarded; it is not an error.
- Current clean build: 95 pages, 0 errors. Placeholders are exactly one page each, so the 1:1 replacement keeps 95pp.
- Frontmatter page positions in main.pdf: p.1 cover, p.2 titlepage, p.3 recommendation (推薦書), p.4 approval (審定書), p.5 中文摘要 (Roman I).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy scan into thesis tree and switch both placeholder pages to pdfpages</name>
  <files>thesis/frontmatter/signed_forms.pdf, thesis/preamble.tex, thesis/frontmatter/recommendation.tex, thesis/frontmatter/approval.tex</files>
  <action>
  1. Copy the upload to an ASCII path (avoids CJK-path issues in LaTeX/latexmk): in Bash, `cp './審定書和推薦書.pdf' 'thesis/frontmatter/signed_forms.pdf'`. If the CJK filename fails to resolve in the shell, fall back to globbing the only root-level PDF (`ls ./*.pdf` shows exactly one) and copy that. Leave the original root file untouched — it is the user's upload.
  2. In `thesis/preamble.tex`, add `\usepackage{pdfpages}` on a new line directly after the `graphicx` load (line 31), with a one-line comment noting it embeds the signed 推薦書/審定書 scans. Keep it before `hyperref` (line 65). Touch nothing else in the preamble.
  3. Rewrite `thesis/frontmatter/recommendation.tex` (use Write tool, full replacement): keep the `%%` header-comment style but update it to say the page is now the signed advisor recommendation form embedded from scan page 2 of `frontmatter/signed_forms.pdf` (source: repo-root upload 審定書和推薦書.pdf, signed 2026-07-30). Body is a single line: `\includepdf[pages=2]{frontmatter/signed_forms.pdf}`. Remove the CJK block, `\thispagestyle{empty}`, and `\clearpage` — `\includepdf` handles page breaks and empty page style itself.
  4. Rewrite `thesis/frontmatter/approval.tex` identically in style, embedding scan page 1: `\includepdf[pages=1]{frontmatter/signed_forms.pdf}` with a header comment noting it is the signed committee approval form (scan page 1). NOTE the deliberate crossing: main.tex order is recommendation→approval, scan order is approval→recommendation; the pages= values above encode this. Do NOT edit thesis/main.tex.
  </action>
  <verify>
    <automated>ls -la thesis/frontmatter/signed_forms.pdf (size 1,743,631 bytes) && grep -c 'includepdf\[pages=2\]{frontmatter/signed_forms.pdf}' thesis/frontmatter/recommendation.tex (=1) && grep -c 'includepdf\[pages=1\]{frontmatter/signed_forms.pdf}' thesis/frontmatter/approval.tex (=1) && grep -c 'usepackage{pdfpages}' thesis/preamble.tex (=1) && git check-ignore thesis/frontmatter/signed_forms.pdf; echo $? (must print 1 = not ignored)</automated>
  </verify>
  <done>signed_forms.pdf present in thesis/frontmatter/ at the exact upload size; both .tex files contain only their header comment + one \includepdf line with the crossed page mapping; preamble loads pdfpages; root upload unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Clean rebuild and assert 95 pages / 0 errors / 0 undefined refs</name>
  <files>thesis/main.pdf (build artifact, git-ignored — never commit)</files>
  <action>
  Run the non-interactive clean build from the repo root:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean`
  The script already fails (nonzero exit) on LaTeX `!` error lines and undefined references/citations, so exit code 0 covers those two gates. MiKTeX may auto-install the pdfpages CTAN package mid-build — that is expected, let it finish. Then independently confirm the page count from the log: `grep 'Output written' thesis/main.log` must report `(95 pages`. If the count differs, the scan pages did not replace 1:1 — inspect main.log around the \includepdf lines before touching anything else.
  </action>
  <verify>
    <automated>build_thesis.ps1 -Clean exits 0 && grep -o 'Output written on main.pdf (95 pages' thesis/main.log returns a match</automated>
  </verify>
  <done>Build exits 0; thesis/main.log shows exactly 95 pages, no `!` error lines, no undefined references (script-enforced).</done>
</task>

<task type="auto">
  <name>Task 3: Visually verify the two scan pages land in the right slots, then commit</name>
  <files>none (verification renders go to scratchpad; commit covers Task 1 files)</files>
  <action>
  1. Write a small throwaway PyMuPDF script to the session scratchpad directory (Write tool, NOT a heredoc) that opens `thesis/main.pdf` and renders pages 1–5 (1-based) to PNGs at ~80 dpi in the scratchpad. Run it read-only in the vcc-main env: `conda run -n vcc-main python <scratchpad>/render_pages.py`.
  2. Read the PNGs and confirm: p.1 cover and p.2 titlepage unchanged; **p.3 = signed 指導教授推薦書** (advisor form, signature dated 2026-07-30, no grey placeholder text); **p.4 = signed 學位考試委員審定書** (committee form, signatures + chair stamp); p.5 = 中文摘要 with Roman numeral I. If p.3/p.4 are swapped, the pages= mapping in Task 1 was applied backwards — fix and rebuild.
  3. Commit exactly the four source files (never thesis/main.pdf, which is git-ignored):
  `git add thesis/preamble.tex thesis/frontmatter/recommendation.tex thesis/frontmatter/approval.tex thesis/frontmatter/signed_forms.pdf`
  `git commit -m "docs(quick-260804-o0k): replace 推薦書/審定書 placeholders with signed scans"` (append the standard Co-Authored-By trailer). Leave the root 審定書和推薦書.pdf untracked and untouched.
  </action>
  <verify>
    <automated>conda run -n vcc-main python render script exits 0; git show --stat HEAD lists exactly the 4 files; git status shows 審定書和推薦書.pdf still untracked</automated>
    <human-check>Rendered p.3/p.4 PNGs show the signed forms in the correct order (Claude reads the PNGs itself; no user action needed)</human-check>
  </verify>
  <done>Pages 3 and 4 of main.pdf are visually confirmed as the signed 推薦書 and 審定書 respectively; the four source files are committed; root upload untouched; pre-binding item "unsigned 推薦書/審定書 front matter" is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Local LaTeX build embedding a user-provided local PDF; no network input, no runtime code paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | signed_forms.pdf embedded via pdfpages | accept | User's own verified signed scan (content matched to this thesis by orchestrator); byte-size check in Task 1 confirms faithful copy |
| T-quick-02 | Tampering | MiKTeX auto-install of pdfpages (CTAN) | accept | Standard CTAN package via MiKTeX's own installer; not an npm/pip/cargo install, legitimacy gate N/A |
</threat_model>

<verification>
- Build exits 0 via `scripts/build_thesis.ps1 -Clean` (script self-enforces 0 LaTeX errors and 0 undefined refs from main.log).
- `thesis/main.log` reports `Output written on main.pdf (95 pages`.
- Rendered pages 3–4 of thesis/main.pdf are the signed 推薦書 (scan p.2) and 審定書 (scan p.1) respectively; pages 1–2 and 5 unchanged.
- `git show --stat HEAD` lists exactly: thesis/preamble.tex, thesis/frontmatter/recommendation.tex, thesis/frontmatter/approval.tex, thesis/frontmatter/signed_forms.pdf.
- Root `審定書和推薦書.pdf` untouched and untracked.
</verification>

<success_criteria>
- The two grey placeholder pages are gone; the signed scans render in the correct binding order (推薦書 p.3, 審定書 p.4) with no page numbers, and Roman numbering still begins at 中文摘要 (p.5 = I).
- Total page count remains 95; build clean (0 errors, 0 undefined refs).
- signed_forms.pdf is tracked by git without `-f`; build artifacts are not committed.
- Pre-binding revision item #3 (unsigned 推薦書/審定書 front matter) is closed.
</success_criteria>

<output>
Create `.planning/quick/260804-o0k-replace-thesis-frontmatter-placeholder-p/260804-o0k-SUMMARY.md` when done.
</output>
