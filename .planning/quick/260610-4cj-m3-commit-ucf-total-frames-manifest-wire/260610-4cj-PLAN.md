---
quick_id: 260610-4cj
slug: m3-commit-ucf-total-frames-manifest-wire
status: complete
date: 2026-06-09
commit: 4ddda56
---

# Quick Task 260610-4cj: Fix M3 — UCF headline reproducible from git (manifest + fallback)

## Problem (audit M3, MEDIUM, adversarially verified; prior-known memory 1635)

The released UCF **82.5% AUC** headline is the full-length recompute (each video's
snippet scores expanded to its TRUE length `total_frames*10`, recovering the trailing
partial 64-PNG window). That true length lived ONLY in per-video boundary JSONs at the
off-repo machine path `E:/snippets/ucf`. The committed `config_snapshot.json` carries no
`snippet_boundaries_dir`, so a fresh `python src/evaluate.py --run-dir results/ucf_gated_fusion_giant_s42`
fell back to the truncated snippet-grid length → **83.2%**, not the released 82.5%.
The headline is the LOWER, more-honest number; the gap is purely a reproducibility/provenance
hole vs the paper's "codebase … will be released to support reproducibility" claim.

Decision: **Option A** (user-chosen) — commit a tiny manifest + wire a fallback so the
headline reproduces from git alone (vs B disclosure-only, C commit-all-boundaries, D accept).

## Task

1. `scripts/build_ucf_frames_manifest.py` (new) — distil `{video_id: total_frames}` for all
   UCF videos from the off-repo boundary JSONs into `data/ucf_total_frames.json` (~45KB).
   - verify: manifest covers all 254 evaluated giant-s42 videos (0 missing).
2. `src/evaluate.py` — add a manifest fallback tier between the boundaries-dir lookup and the
   truncated fallback. Backward-safe: when `snippet_boundaries_dir` IS set, the per-video JSON
   still takes precedence (byte-identical behavior).
   - verify: a fresh evaluate.py on the committed snapshot (no boundaries dir) yields auc=0.824904.
3. `scripts/recompute_fulllength_ucf.py` — same fallback in `_load_boundaries_total_frames`;
   relax the startup guard to proceed when the boundaries dir is absent but the manifest exists.
   - verify: recompute gate PASS new_auc≈0.8249 with a nonexistent boundaries dir.
4. No paper edit (Option A makes it reproducible; disclosure note optional).

## Scope

New 45KB committed manifest + generator + 2 small backward-safe code fallbacks. No numeric
change to any canonical artifact (verification ran read-only / into temp dirs).
