# Full Project Review — 2026-06-10

**Scope:** CGW '26 paper ↔ code alignment, bug/pitfall hunt, improvement scouting.
**Method:** 52-agent workflow (`wf_8e280df5-ca4`): 11 finders (5 paper-alignment + 6 bug-hunt), every HIGH/MEDIUM finding verified by 2 independent adversarial skeptics (refute lens + reproduce lens), 4 improvement scouts + feasibility critic. All numeric claims recomputed from primary artifacts (`results/results-index.csv`, TTA JSONs, `eval_scores.npz`).
**Verdict:** 0 HIGH, 9 unique confirmed MEDIUM (none touch headline numbers), ~21 LOW, 0 findings refuted. Headlines UCF 82.5 / XD 78.7 intact.

**UPDATE 2026-06-10:** ALL confirmed findings RESOLVED (P1–P7 + C1–C3 + stale pins/scanner)
across quick tasks `260610-aqm` (Pri-1 queue), `260610-b6p` (suite-green: C1/C2/C3 + pins),
`260610-bed` (paper P1–P7). Test suite **321 passed / 0 failed**; paper rebuilds clean
(10pp, 0 errors); headlines UCF 82.5 / XD 78.7 intact.

**Pri-1 + zero-GPU analyses also DONE (2026-06-10):** GPU `pri1_ablations_seeds` 52/52 OK
(commit 3aac394); the paper's ablation Tables 1/2 + prose now report 3-seed mean±std
(commit 33591ac) — single-seed artifacts corrected (UCF GF Mean-Only 83.0→82.5, XD 79.9→78.4,
skeleton 71.8→68.8±2.8; headlines unchanged). Zero-GPU Pri 5/6/7/9 analyses complete
(commit d9c04aa, `.planning/ANALYSIS-2026-06-10-zero-gpu.md`); Pri-5 mini-table added to the
paper. **Remaining:** §4 LOW items; §5 Pri 2/4/8 (GPU-track TTA improvements) + Pri 3 (XD
smoothing, headline-changing, needs discussion); MAYBE tier; and the
`fig_backbone_comparison` regeneration follow-up (stale chart CSV).
Status legend: `[ ]` open · `[x]` fixed · `[~]` won't fix / accepted — update markers as items are resolved.

---

## 1. Verified clean (recomputed, exact match)

- Tables 1, 2, 3: every cell + every Best-Δ recomputed from `results-index.csv` / TTA JSONs. UCF 82.5 (=82.54), XD 78.7 (=78.72), 83.0 mean-only (=82.98), 71.8 skeleton (=71.79), 40.9 XD skeleton (=40.94). All ±std are sample std (ddof=1) and match.
- TENT/SAR `<0.1pp` claim: TRUE 3-seed; max 3-seed-mean |Δ| = 0.082 (Base), max per-seed |Δ| = 0.094. 1,536 adapted LN params confirmed.
- disc_reweight Table 3 column: +0.67/+1.50/+2.24/+0.42, mean +1.21; "every per-backbone gain positive on every seed" = 12/12 (min +0.27); Gaussian "+4.8 average" = +4.826; Base "−3.2 on one seed" = −3.202 (s42 gn_5, correctly labeled single-seed).
- Complementarity: gated−visual mean +0.64 → "+0.6"; 8/8 positive; sign test p = 0.0078 ≈ 0.008; "up to +2.1 AP" (SO400M +2.13); 12.1pp CLIP late-vs-gated XD gap (12.08); ">10 AP points on every backbone" (10.8–12.8).
- Seed-stability prose: UCF GF stds 0.28/0.08/0.31/0.36 → "0.1–0.4"; XD GF 0.89/0.92/0.91/2.80; XD visual-only 0.99–2.17 → "1.0–2.2".
- Loss equations ↔ `mil_loss.py`; gate equations ↔ `gated_fusion.py`; hyperparameters ↔ configs; disc_reweight §3.6 ↔ `src/tta/disc_reweight.py` incl. transductive disclosure; param counts 0.50M/1.02M.

---

## 2. Confirmed MEDIUM — paper (7)

### P1. Stale "+13.9" TTA peak (seed-42-only value in a 3-seed paper)
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:50` (abstract), `:333` (§4.5), `:386` (conclusion)
- **Problem:** Paper states all TTA results are 3-seed means (:325), but +13.9 is the seed-42-only delta (SO400M gaussian_5 = +13.92). 3-seed mean = **+13.2** (+13.92/+12.34/+13.31). Max single-seed on SO400M = +14.93 (s123 gn_3); across backbones +15.14 (Base s123 gn_1). No consistent definition reproduces 13.9.
- **Provenance:** `.planning/PAPER-TTA-REVISION-DRAFT.md:8` (seed-42 era draft). The adjacent "+0.8 mean" WAS updated to +1.21; this one was missed.
- **Fix:** "up to +13.2" in all three places (or keep 13.9 with explicit seed-42 qualifier). Rebuild via `build_paper.ps1 -Clean`.

### P2. Discussion §5 "TTA and the LayerNorm barrier" is stale pre-rewrite text
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:348`
- **Problem:** Repeats the BN/LN "distributional memory" mechanism story the project itself disproved; contradicts §4.5's corrected explanation (frozen gate/head + LN-affine updates too small to reorder a rank-based metric; shift lives upstream in frozen backbone features); discusses TTA as a pure negative while ignoring the paper's own positive method (disc_reweight); "future TTA approaches may need prompt tuning/adapters" ignores that §4.5 already presents a working alternative.
- **Fix:** Rewrite paragraph to match §4.5's mechanism; correct the TENT description (uses current-batch stats, adapts affine only); reference disc_reweight as the demonstrated path.

### P3. "SO400M trained on 400M+ samples" — factually wrong (400M = parameters)
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:142` (backbone list), `:289` (§4.4), `:344` (Discussion)
- **Problem:** SO400M = **S**hape-**O**ptimized ~**400M-parameter** ViT (SoViT-400M, "Getting ViT in Shape"). All three SigLIP2 variants train on the same WebLI corpus — which line 145 itself states, so the paper self-contradicts. The ":289/:344 'broader data distribution (400M+ samples)'" causal explanation for SO400M's XD lead is unsupported.
- **Fix:** `:142` → "a shape-optimized ~400M-parameter SigLIP2 encoder (SoViT-400M), producing 1152-d features." `:289/:344` → replace broader-data rationale with a capacity/shape-optimization framing (or drop the explanation).

### P4. `:344` says SO400M optimal for "XD-Violence visual-only" — contradicts Table 2
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:344` vs Table 2 (`:251`), `:289`, `:384`
- **Problem:** XD visual-only 3-seed means: CLIP 74.56, Base 74.50, SO400M 76.59, **Giant 76.82** (Table 2 itself bolds Giant). SO400M leads only **under gated fusion** (78.7) — exactly the framing §4.4 and the Conclusion use. Leftover from the pre-06-09 single-seed artifact ("SO400M leads XD-visual" was noise); the fix was propagated everywhere except this Discussion sentence.
- **Fix:** "(Giant for UCF-Crime, SO400M for XD-Violence **under gated fusion**)".

### P5. XD-Violence category codes are wrong
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:206`; ground truth `src/eval/xd_annotations.py:42-58`, `data/annotations/xd_temporal.txt`
- **Problem:** Paper claims "Fighting (B1), Shooting (B2), Riot (B3), Abuse (B4), Car Accident (B5), Explosion (B6)". The dataset has **no B3**. Official mapping: B1 Fighting, B2 Shooting, **B4 Riot, B5 Abuse, B6 Car Accident, G Explosion**.
- **Fix:** Correct the codes or drop the parentheticals.

### P6. "none in XD-Violence" below 64 frames — contradicted by the repo's own split file
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:134`; `data/splits/xd_train.txt:1-2`
- **Problem:** One 34-frame XD training video (`v=Gm73TwtUyGY__#1_label_G-0-0`) excluded for the 64-frame minimum; one more excluded as corrupt MP4 (missing moov atom). Training uses 3,952 of 3,954 official train videos.
- **Fix:** "(172 videos in UCF-Crime, one in XD-Violence)" + optionally disclose the corrupt-video exclusion.

### P7. Trailing-frame coverage claim false for XD
- [x] **Fixed (2026-06-10)**
- **Where:** `paper/main.tex:217` vs `src/evaluate.py:263-269` (XD branch) and `:330-355` + `snippet_to_frame.py:60-62` (UCF pad path)
- **Problem:** "the final snippet's score is repeated to cover any trailing frames" is true on UCF (full-length grid, pad-with-last) but the XD path sets `n_frames = len(scores)*64` and **drops trailing partial windows**.
- **Fix:** Qualify: UCF repeats the final snippet score over trailing frames of the full-length grid; XD evaluates on the snippet-aligned portion.

---

## 3. Confirmed MEDIUM — code (3)

### C1. M3 commit (4ddda56) broke 3 committed evaluate-CLI tests
- [x] **Fixed (2026-06-10)**
- **Where:** `src/evaluate.py:330-332` (manifest fallback) + `tests/fixtures/synthetic_eval.py:43-53` (fixture ids) → `tests/test_evaluate_cli.py` (`test_evaluate_cli_smoke`, `test_evaluate_bit_identical_rerun`, `test_done_after_metrics`)
- **Problem:** The committed UCF total-frames manifest hijacks synthetic fixtures whose video id (`Abuse028`) collides with a real UCF id — full-length fallback engages, snippet/frame counts mismatch, tests fail.
- **Fix:** Rename fixture ids to non-colliding names (e.g. `SynthAbuse900`) + matching fixture annotations; or add a manifest-vs-scores sanity guard in `evaluate.py`.

### C2. i3d val loader uses `shuffle=False` — single-class-batch bias persists in RTFM-I3D path
- [x] **Fixed (2026-06-10)**
- **Where:** `src/data/loaders.py:338-349`, consumed by `src/train.py:249-280` (`validate_i3d`)
- **Problem:** The exact bug fixed for the fusion path (commit 0999033; project convention: val loaders MUST `shuffle=True`) persists in the `xd_i3d` path. `validate()` skips single-class batches → biased val loss → affects early stopping/selection for the two xd_i3d baseline rows in results-index. Headlines unaffected.
- **Fix:** `shuffle=True` (seeded `g_val` generator already passed). Add provenance note for the 2 xd_i3d rows.

### C3. `xd_train.txt` not regenerable from `create_splits.py`
- [x] **Fixed (2026-06-10)**
- **Where:** `data/splits/xd_train.txt:1-2` (manual `# Excluded:` lines) vs `scripts/create_splits.py`; `tests/test_splits.py:103-127` currently RED
- **Problem:** The 2 manual exclusions aren't encoded in the generator, so verify mode / `test_split_seed_reproducibility` fail; byte-identical regeneration impossible.
- **Fix:** Encode exclusions in `create_splits.py` (e.g. `XD_TRAIN_EXCLUSIONS = {video_id: reason}`) and emit the same comment lines.

---

## 4. LOW findings (notable; severity not adversarially verified)

**Paper/figures:**
- [ ] `d_v` never defined; silently equals 2d (mean+max concat), not backbone embed dim (`main.tex:151` vs `:147`, `:350`)
- [ ] "GF 2-Person" and "GF Mean-Only" variants never defined in text (`:217`, tables, `:287`, `:359`, `:384`)
- [ ] `:350` "only the projection and gating layers are learned" understates trainable set (MIL head included in the quoted parameter counts)
- [ ] Table 3 TTA eval uses truncated UCF snippet grid, not Table 1's full-length frame grid (`src/tta/evaluate_tta.py:355-364` vs `evaluate.py:313-344`). Internally consistent within Table 3, but an undisclosed protocol difference between tables
- [ ] `:325` "adapts across the entire corruption stream" overstates — continual protocol is per-condition streams, not the 20-condition sequence
- [ ] Figure 5 (`fig_tta_comparison.pdf`): generator docstring still carries retracted "≤0.004pp" bound; verify the figure actually shows the TENT/SAR bars its caption discusses (`scripts/generate_pub_figures.py:423-433`, caption `:319`)
- [ ] UCF annotation interval start off-by-one vs Sultani MATLAB convention — measured impact 0.002pp, cosmetic (`src/eval/ucf_annotations.py:85-93`)

**TTA code:**
- [ ] `sar.py:92-110` omits official SAR's second-pass entropy re-filtering and EMA model-recovery reset (`self.ema` is dead state). Behaviorally irrelevant here (deltas ≈0) but a reviewer could contest the "SAR" label — consider footnote or fidelity fix
- [ ] `tent.py:104` / `sar.py:60-62` episodic reset assigns plain dict to `optimizer.state` — crashes for stateful optimizers; safe today only because production uses momentum-free SGD
- [ ] `tent.py:102` / `sar.py:58` / `evaluate_tta.py:624` use `load_state_dict(strict=False)` — deviates from project strict=True convention (verified harmless today; the dicts are LN-only partial states, so a naive strict=True would break — needs assert-keys hardening instead if touched)
- [ ] `evaluate_tta.py:401-404` log line prints method twice, omits intended field

**Scripts/infra:**
- [ ] Re-running `generate_latex_tables.py` silently clobbers the "STALE — DO NOT USE" disclaimer in `tables_generated.tex` and regenerates the superseded TENT/SAR Table 3 (file not `\input` by main.tex, but a footgun)
- [ ] TTA rows in results-index silently drop `backbone` field (`run_ablations.py:756` vs `csv_logger.py` schema)
- [ ] `create_splits.py:67` UCF split membership depends on `iterdir()` filesystem ordering — "byte-reproducible (seed=42)" only on original machine. CAUTION: committed splits are the source of truth; sorting the iterator would CHANGE the published splits — document rather than "fix"
- [ ] Model constructors swallow unknown kwargs via `**unused` — typo'd hyperparameters fall back to defaults silently (`gated_fusion.py:43` etc., `registry.py:44-56`). NOTE: may be load-bearing for config_snapshot replay (extra keys like strategy/cache_variant) — check before tightening; warn-log is the safe middle ground
- [ ] Hardcoded `D:/ViolenceCC`, `E:/...` paths in `create_splits.py:33-37`, `tests/test_splits.py:18`, `evaluate_tta.py` (documented/overridable; relocatability nit)

**Test suite currently RED (~10 failures) — inventory:**
- [x] 3 × M3 fixture collision (see C1)
- [x] 1 × split regeneration (see C3)
- [x] 2 × stale queue-count pins: `test_run_ablations.py:122` (expects 266, queues now 282), `test_run_ablations_tta.py:114` (expects 500, now 680) — verify growth is intentional, then update pins
- [x] ~3 × `test_train_integration.py:268-316` C3 no-test-split scanner false-positives on legitimate eval/TTA modules — scope scanner to training modules

---

## 5. Improvement ideas (28 scouted → prioritized by feasibility critic)

Constraints honored: frozen cached features, single RTX 4090, solo researcher, near-final paper.
Items marked **(verified)** were recomputed from repo artifacts during the review, not estimated.

### RECOMMEND tier

| Pri | Idea | Expected gain | Cost | Notes |
|----|------|--------------|------|-------|
| 1 | **3-seed the single-seed ablation rows** (26 configs: Late Fusion, GF 2-Person, GF Mean-Only ×8, Skeleton Only ×2) | Protects load-bearing single-seed numbers 83.0 UCF (`:237,:287,:359,:384`) & 79.9 XD (`:256`) | ~2–3 GPU-h, 1 day | This exact failure mode (single-seed artifact) killed two results in the 06-09 audit. Also trains skeleton-only s123/s2024 needed by Pri 2 |
| 2 | **Reliability-routed score-level ensemble to trained skeleton-only head** (TTA) | +1–2pp on TTA mean (lifts +1.21 toward +2–3); converts the Base −3.2 disclosure into a positive | 2–3 days | Base gaussian_4/5 bottleneck is the zero-VL plateau (60.6/60.8) vs skeleton-only 71.8 AUC with byte-identical clean features (gaussian/brightness reuse clean skeleton cache; `SKELETON_REEXTRACT_TYPES` covers only motion_blur/jpeg). Keep R1 gate verbatim; disclose auxiliary head honestly |
| 3 | **Snippet-window temporal smoothing of XD test scores (w=64)** | **+0.57pp XD AP (verified:** 78.72 → 79.29 recomputed from `eval_scores.npz`, all 3 seeds**)** | 0.5 day | Keep raw number primary; show w-plateau [48,96] to preempt post-hoc-tuning objection; ~0 on UCF (don't apply). Verify XD snippet stride truly = 64 before claiming "native window" |
| 4 | **Streaming/causal w estimation for disc_reweight** | Upgrades claim class transductive → online; paper names this limitation twice (`:333`, `:363`) | 1–1.5 days | Routed signal is 2 scalars/condition — converges fast over ~1.5k-snippet streams. Run on Pri-2 winner to avoid doing twice |
| 5 | **Per-corruption-type TTA breakdown table/heatmap** | Substantiates the +4.8 / +13.x / −3.2 prose numbers that currently live nowhere checkable; ImageNet-C reporting convention | 0.5–1 day, 0 GPU | Data already on disk (`results/_tta_rerun_continual/summary_3seed.json`, `r1full_*.json` per_condition). Mini-table in paper, heatmap in thesis |
| 6 | **Video-level bootstrap CIs for headline numbers** | **(verified probe:** test-set CI width ~12.7pp vs 0.4pp seed std — test uncertainty ≈30× training noise**)** | 0.5–1 day, CPU only | Defense insurance for "is 82.5 vs 81.2 meaningful?" Frame as "all published VAD numbers share this uncertainty; we quantify it" |
| 7 | **Per-class UCF complementarity analysis** (skeleton-relevant vs no-human classes) | Upgrades central "motion not captured by appearance" claim (`:342`) from statistical to mechanistic | 1 day, 0 GPU | `per_category.csv` already in every UCF run dir; matched-seed visual-only runs exist. Use 3-seed means; tiny per-class n (Abuse n=2) needs disclaimer |
| 8 | **JPEG-family rescue diagnostic** (skeleton-only on cached corrupted skeletons, step 1 only) | 0 or unlocks +1.5–3pp on 20-cond mean | 0.5 day | Fused AUC under JPEG = 37–48 (below chance) — largest untouched pool; R1 is exactly 0.0 there by gate construction (skel_rel=0). Corrupted skeleton caches exist (`E:/features/ucf/skeleton_jpeg_*`). Pre-registered kill criterion; never tune on test deltas |
| 9 | **Qualitative failure-case analysis** (per-video AUC ranking → 2-3 failure figures + score histograms) | Defense insurance — paper has 1 success case, 0 failures; "show me where it fails" is a near-certain defense question | 1 day, 0 GPU | `video_auc` (0.927) / `snippet_auc` (0.834) already in eval_metrics.json — report in thesis. Shares loader with Pri 6/7 |

### MAYBE tier (discuss before committing)

| Pri | Idea | Gain | Cost | Blocker |
|----|------|------|------|---------|
| 10 | Wilcoxon over 24 paired bootstraps (complementarity) | Rigor upgrade of p≈0.008 sign test | 1–2 days | Single-config probe not significant (P(>0)=0.599); aggregate may land marginal; paper already hedges correctly |
| 11 | **Lightweight temporal module** (TCN / 1-layer transformer over snippet seq) | +1–3pp — biggest ceiling (CLIP-TSA ablation +1.3–1.8 on frozen features) | days + full cascade | Changes headline architecture → re-run Tables 1-2 + entire TTA section. Thesis-branch only, after RECOMMEND tier. Keep LN inside block to preserve LN-TTA story |
| 12 | Val video-AUC checkpoint selection A/B (vs val MIL loss) | +0.2–0.8pp, possibly null | ~15 GPU-min | If it wins, every checkpoint changes → same cascade problem. Thesis robustness note |
| 13 | **Text-prompt alignment branch** (VadCLIP-lite / MIL-Align on cached joint-space features) | +1–3pp (VadCLIP dual-branch ablation anchor) | 3–5 days | Joint-space mean caches exist on disk for all 4 backbones, both datasets. Largest implementation surface; SigLIP2 sigmoid scale needs calibration. Flagship thesis extension; de-risk on CLIP first |
| 14 | Feature-magnitude top-k (+optional MGFN loss), loss-level only | Unknown; weakest-anchored | 1–2 days | Tap residual BEFORE `ln_fused` (`gated_fusion.py:76`) — LN destroys magnitude. Config-flag impl, no cascade. Kill fast if first 3-seed run null |
| 15 | Disclosed ensembling rows | Honest pre-registered version: only +0.18pp XD **(verified)** | 1 day | Eye-catching +1.9pp version requires test-informed member selection — not honestly reportable. Thesis "upper bound" row only |
| 16 | lr/k sweep transfer to headline backbones | Unknown; the 106-run sweep was ViT-B-only | ~4–5 GPU-h | Wait for Pri-1 results first; tuned-per-backbone headline destroys fixed-hparam virtue (`:215`) |
| 17 | Pseudo-label self-training (TPWNG/LAP-lite) | Soft +1–2pp anchors | 2–3 days | Confirmation-bias risk; val split too weak for threshold selection. After 11/13 |
| 18 | Cross-dataset table (UCF↔XD) | New robustness analysis | 1–2 days, <0.5 GPU-h | Snippet-duration confound (20s vs 2.7s) — frame as "what transfers", never as benchmark |

### SKIP tier (with reasons)

- **Audio third stream (XD):** changes thesis narrative to tri-modal; does nothing for UCF/TTA; UR-DMU ablation shows ~0.1pp when visual is strong. Scope creep.
- **XD-Violence-C:** 4–7 days of VL extraction to demonstrate a predictable no-op (XD routing target = skeleton at 40.9 AP vs fused 76–79 — router correctly does nothing). Make the modality-availability point in one prose sentence instead.
- **Calibrated routing curve on synthetic train corruption:** subsumed by Pri 2; the feature-space-noise→pixel-corruption transfer assumption failed repeatedly in S1/S2/S3/S5/S6.
- **Gated diagonal-NORM stacking:** NORM negative on 2/4 backbones; component about to be superseded by Pri 2.
- **XD sweep revival (parked Phase 4d):** deferral memo concluded gap to VadCLIP is architectural; sweep dilutes fixed-hparam strength; val MIL loss correlates weakly with test AP. RTFM repro gap (65.70 vs 77.81 published, verified) = thesis-appendix material only.

**Positioning context (no action):** gap to 84+ frozen-CLIP methods ≈ (a) zero temporal modeling (`gated_fusion.py:66-79` strictly per-snippet) + (b) unused text tower. Realistic frozen-feature ceiling ~85–86 UCF AUC, not 88+.

---

## 6. Suggested execution order

1. **Quick batch (≤1 day):** P1–P7 paper edits + rebuild (`build_paper.ps1 -Clean`); C1 fixture rename; C2 i3d shuffle; C3 split-exclusion encoding; stale test-pin updates. Clears all confirmed findings + makes suite green.
2. **Week 1:** Pri 1 (3-seed ablations) → Pri 3 (XD smoothing) → Pri 5 (per-corruption table).
3. **Week 2:** Pri 2 (skeleton-route TTA) → Pri 4 (streaming variant) → Pri 8 (JPEG diagnostic).
4. **Thesis-side, anytime:** Pri 6, 7, 9 (zero-GPU analyses).
5. **Discuss before starting:** MAYBE tier — especially 11 (temporal module) and 13 (text branch) as thesis-extension chapters.

---

*Generated from workflow `wf_8e280df5-ca4` (52 agents, 2 adversarial verifiers per HIGH/MED finding, ~3.9M tokens). Full agent transcripts: `C:\Users\Austin\.claude\projects\D--ViolenceCC\48cbfd0f-da84-45d8-b894-d08b7eb18a58\subagents\workflows\wf_8e280df5-ca4\`.*
