---
phase: 11-thesis-manuscript
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tta/evaluate_tta.py
  - scripts/run_ablations.py
  - scripts/analyze_tta_best.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "evaluate_tta.py accepts --backbone flag and loads backbone-specific corrupted feature directories"
    - "Corrupted visual features exist for siglip2, siglip2_so400m, and siglip2_giant on UCF test split (60 extraction runs)"
    - "TTA evaluation results exist for all 4 backbones (source_only + best TENT + best SAR per backbone)"
    - "A summary CSV aggregates TTA results across backbones for paper table generation"
  artifacts:
    - path: "src/tta/evaluate_tta.py"
      provides: "Backbone-parameterized TTA evaluation"
      contains: "--backbone"
    - path: "scripts/analyze_tta_best.py"
      provides: "Best TTA config selection from 500 CLIP runs + cross-backbone summary"
    - path: "results/tta_backbone/summary.csv"
      provides: "Aggregated TTA results across 4 backbones for paper Table 3"
  key_links:
    - from: "src/tta/evaluate_tta.py"
      to: "E:/features/ucf/{prefix}_{corruption}_{severity}/"
      via: "BACKBONE_FEATURE_PREFIX mapping"
      pattern: "BACKBONE_FEATURE_PREFIX"
    - from: "scripts/run_ablations.py"
      to: "src/tta/evaluate_tta.py"
      via: "subprocess invocation with --backbone flag"
      pattern: "--backbone"
---

<objective>
Extend TTA evaluation to all 4 backbone variants (D-07), extract corrupted visual features for 3 additional backbones, run reduced TTA grid, and aggregate results for paper inclusion.

Purpose: D-07 requires TTA experiments on ALL backbone variants. Currently only CLIP has TTA results (500 runs). This plan adds backbone parameterization to evaluate_tta.py, extracts corrupted features for SigLIP2/SO400M/Giant, and runs source_only + best TENT + best SAR configs per backbone.
Output: Backbone-parameterized TTA infrastructure + 180 new TTA evaluation runs + summary CSV for paper table.
</objective>

<execution_context>
@.planning/phases/11-thesis-manuscript/11-CONTEXT.md
@.planning/phases/11-thesis-manuscript/11-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backbone-parameterize evaluate_tta.py and create TTA best-config analyzer</name>
  <files>src/tta/evaluate_tta.py, scripts/analyze_tta_best.py, scripts/run_ablations.py</files>
  <read_first>
    - src/tta/evaluate_tta.py (current hardcoded clip_ prefix at line 94, feature loading at lines 82-108, argparse at lines 57-70, source_run at line 116 of run_ablations.py)
    - scripts/run_ablations.py (TTARunSpec class at line 105, queue definitions at lines 442-454, _run_tta at the dispatch section)
    - scripts/extract_clip.py (BACKBONE_CONFIGS dict at lines 119-147 for output_subdir mapping)
  </read_first>
  <action>
    1. Add --backbone argument to evaluate_tta.py parse_args() with choices matching extract_clip.py BACKBONE_CONFIGS keys: clip-vit-b-16, siglip2-base, siglip2-so400m, siglip2-giant. Default: clip-vit-b-16.

    2. Add BACKBONE_FEATURE_PREFIX dict mapping backbone keys to feature subdirectory prefixes: clip-vit-b-16 -> clip, siglip2-base -> siglip2, siglip2-so400m -> siglip2_so400m, siglip2-giant -> siglip2_giant. (Mirrors extract_clip.py output_subdir values.)

    3. Add BACKBONE_SOURCE_RUNS dict mapping backbone keys to their s42 gated fusion run directories: clip-vit-b-16 -> ucf_gated_fusion_s42, siglip2-base -> ucf_gated_fusion_siglip2_s42, siglip2-so400m -> ucf_gated_fusion_so400m_s42, siglip2-giant -> ucf_gated_fusion_giant_s42.

    4. Modify _load_test_video_features() signature to accept backbone parameter. Replace hardcoded clip_dir = feature_root / f"clip_{corruption_type}_{severity}" with prefix = BACKBONE_FEATURE_PREFIX[backbone]; clip_dir = feature_root / f"{prefix}_{corruption_type}_{severity}". Skeleton loading remains unchanged (backbone-independent).

    5. Thread backbone parameter through run_tta_evaluation() and the call to _load_test_video_features() at line 263. Add backbone to the eval_metrics.json payload.

    6. Add backbone field to TTARunSpec in run_ablations.py (default "clip-vit-b-16"). Update run_name property to include backbone prefix when not clip (e.g., siglip2_tent_gaussian_noise_3_lr0.001). Update source_run default to use BACKBONE_SOURCE_RUNS[backbone]. Add 3 new queues: tta_backbone_source, tta_backbone_tent, tta_backbone_sar that generate specs for the 3 non-CLIP backbones using fixed best LR/rho (placeholder values 1e-3 for lr, 0.05 for rho -- will be updated after analyze_tta_best.py runs).

    7. Create scripts/analyze_tta_best.py: reads all 500 results/tta/*/eval_metrics.json files, finds best TENT lr (highest mean AUC across all 20 conditions) and best SAR lr+rho (highest mean AUC across all 20 conditions). Outputs the winning configs to stdout and to results/tta/best_configs.json with keys best_tent_lr, best_sar_lr, best_sar_rho. When invoked with --all-backbones, also writes results/tta_backbone/summary.csv with columns: Backbone, Source_Only_AUC, TENT_AUC, SAR_AUC, Best_Delta.
  </action>
  <verify>
    <automated>python -c "from src.tta.evaluate_tta import BACKBONE_FEATURE_PREFIX, BACKBONE_SOURCE_RUNS; assert len(BACKBONE_FEATURE_PREFIX) == 4; assert 'siglip2-giant' in BACKBONE_SOURCE_RUNS; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - evaluate_tta.py contains BACKBONE_FEATURE_PREFIX dict with 4 entries (clip, siglip2, siglip2_so400m, siglip2_giant values)
    - evaluate_tta.py contains BACKBONE_SOURCE_RUNS dict with 4 entries mapping to ucf_gated_fusion_{backbone}_s42 patterns
    - evaluate_tta.py parse_args includes --backbone with 4 choices
    - _load_test_video_features accepts backbone parameter and uses BACKBONE_FEATURE_PREFIX for clip_dir construction
    - eval_metrics.json payload includes backbone field
    - run_ablations.py TTARunSpec has backbone field
    - scripts/analyze_tta_best.py exists and produces best_configs.json from existing 500 CLIP TTA results
  </acceptance_criteria>
  <done>evaluate_tta.py backbone-parameterized, TTARunSpec extended, analyze_tta_best.py created and produces best TTA configs from existing data</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Extract corrupted features for 3 backbones and run TTA evaluation grid</name>
  <what-built>Task 1 created the backbone-parameterized TTA infrastructure and the best-config analyzer. Now the user must run GPU-intensive jobs in visible terminals (per user memory: no headless processes).</what-built>
  <how-to-verify>
    The user must execute the following in visible terminal windows using the vcc-main conda environment. Total estimated time: ~93 minutes GPU.

    Step 1: Analyze existing CLIP TTA results to find best configs (~10 seconds):
    ```
    conda activate vcc-main
    python scripts/analyze_tta_best.py
    ```
    This outputs best_tent_lr, best_sar_lr, best_sar_rho values and writes results/tta/best_configs.json.

    Step 2: Extract corrupted visual features for 3 additional backbones (~78 min total). Run each backbone sequentially in the same terminal. For each of the 3 backbones (siglip2-base, siglip2-so400m, siglip2-giant), extract 20 corruption conditions (4 types x 5 severities) on UCF test split:
    ```
    python scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-base --corruption-type gaussian_noise --corruption-severity 1
    ```
    (Repeat for all 60 combinations: 3 backbones x 4 corruption types x 5 severities. A helper script or loop command will be provided in the plan summary if Task 1 includes a batch extraction helper.)

    Step 3: Run TTA evaluation grid (~15 min). After updating run_ablations.py queue LR/rho values from best_configs.json:
    ```
    python scripts/run_ablations.py --queue tta_backbone_source --no-preflight
    python scripts/run_ablations.py --queue tta_backbone_tent --no-preflight
    python scripts/run_ablations.py --queue tta_backbone_sar --no-preflight
    ```

    Step 4: Generate TTA summary CSV:
    ```
    python scripts/analyze_tta_best.py --all-backbones --output results/tta_backbone/summary.csv
    ```

    Expected outcome: results/tta_backbone/summary.csv exists with 4 rows (one per backbone) and columns: Backbone, Source_Only_AUC, TENT_AUC, SAR_AUC, Best_Delta.
  </how-to-verify>
  <resume-signal>Type "tta-complete" with the summary.csv path, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- evaluate_tta.py --backbone flag works with all 4 backbone keys
- Corrupted feature directories exist under E:/features/ucf/ for siglip2_*, siglip2_so400m_*, siglip2_giant_* (20 dirs each)
- results/tta_backbone/summary.csv has TTA mean AUC for all 4 backbones
</verification>

<success_criteria>
TTA experiments complete for all 4 backbone variants per D-07. Summary data ready for paper Table 3 (D-29).
</success_criteria>

<output>
Create `.planning/phases/11-thesis-manuscript/11-01-SUMMARY.md` when done
</output>
