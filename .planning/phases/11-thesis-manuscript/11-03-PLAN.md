---
phase: 11-thesis-manuscript
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - scripts/generate_pub_figures.py
  - paper/figures/fig_architecture.png
  - paper/figures/fig_temporal_scores.pdf
  - paper/figures/fig_backbone_comparison.pdf
  - paper/figures/fig_gating_distribution.pdf
  - paper/figures/fig_tta_comparison.pdf
autonomous: false
requirements: []

must_haves:
  truths:
    - "Architecture diagram shows the full pipeline from video input to anomaly scores (D-14, D-27.1)"
    - "Temporal score plot shows a video where gated fusion clearly outperforms single-modality (D-27.2)"
    - "Backbone comparison bar chart shows 4-way grouped bars for both datasets (D-27.3)"
    - "Gating weight distribution shows category-specific modality preferences (D-27.4)"
    - "All figures are publication-quality: 300 DPI, ACM column width (3.33in or 7in), minimum 7pt font, grayscale-friendly (D-28)"
  artifacts:
    - path: "scripts/generate_pub_figures.py"
      provides: "Publication-quality figure generation with ACM presets"
      contains: "ACM_COL_WIDTH"
    - path: "paper/figures/fig_architecture.png"
      provides: "Pipeline architecture diagram"
    - path: "paper/figures/fig_temporal_scores.pdf"
      provides: "Temporal anomaly score comparison plot"
    - path: "paper/figures/fig_backbone_comparison.pdf"
      provides: "4-way backbone grouped bar chart"
    - path: "paper/figures/fig_gating_distribution.pdf"
      provides: "Gate weight analysis by category"
  key_links:
    - from: "scripts/generate_pub_figures.py"
      to: "results/phase10_charts/backbone_comparison_4way.csv"
      via: "pandas CSV read for bar chart data"
      pattern: "backbone_comparison_4way"
    - from: "scripts/generate_pub_figures.py"
      to: "results/tta_backbone/summary.csv"
      via: "pandas CSV read for TTA comparison data"
      pattern: "tta_backbone"
---

<objective>
Generate all publication-quality figures for the paper (D-27, D-28) and create the architecture diagram (D-14). All figures must meet ACM sigconf formatting requirements: 300 DPI, proper column widths, grayscale-friendly, minimum 7pt fonts.

Purpose: D-28 explicitly requires regenerating all charts as publication-quality -- not reusing Phase 6/10 charts (150 DPI, screen-sized). These figures are prerequisites for the full paper draft in Plan 04.
Output: 4-5 publication-quality PDF/PNG figures in paper/figures/, plus the generation script.
</objective>

<execution_context>
@.planning/phases/11-thesis-manuscript/11-CONTEXT.md
@.planning/phases/11-thesis-manuscript/11-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/11-thesis-manuscript/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create publication-quality figure generation script and generate data figures</name>
  <files>scripts/generate_pub_figures.py, paper/figures/fig_temporal_scores.pdf, paper/figures/fig_backbone_comparison.pdf, paper/figures/fig_gating_distribution.pdf, paper/figures/fig_tta_comparison.pdf</files>
  <read_first>
    - scripts/generate_phase10_charts.py (existing chart generation pattern, run name mapping at lines 59+, style settings at lines 33-52; adapt patterns but override DPI/sizing/fonts for publication)
    - results/phase10_charts/backbone_comparison_4way.csv (4-way comparison data: columns Dataset, Variant, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP, Giant_AUC, Giant_AP, plus delta columns)
    - results/phase10_charts/seed_stability_4way.csv (seed data: columns Dataset, Seed, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP, Giant_AUC, Giant_AP)
    - results/tta_backbone/summary.csv (TTA cross-backbone results from Plan 01)
    - results/phase6_charts/A_temporal/ (existing temporal score plots for reference -- select best fusion-advantage video)
    - results/phase6_charts/D_gating/ (existing gating distribution plots for reference data source)
  </read_first>
  <action>
    Create scripts/generate_pub_figures.py with ACM sigconf presets (per RESEARCH.md Pattern 3):
    - ACM_COL_WIDTH = 3.33 inches, ACM_TEXT_WIDTH = 7.0 inches, PUB_DPI = 300
    - Font family serif (Times New Roman / DejaVu Serif), sizes: axes 8pt, ticks 7pt, legend 7pt, title 9pt
    - Grayscale-friendly palette with hatching patterns: CLIP = dark (#2c3e50, no hatch), SigLIP2 = medium gray (#7f8c8d, ///), SO400M = light gray (#bdc3c7, ...), Giant = red accent (#e74c3c, xxx)
    - All figures saved as PDF (vector) except architecture diagram (PNG)

    Generate 4 figures:

    Figure 2 (fig_temporal_scores.pdf): Temporal anomaly score plot. Read existing Phase 6 temporal plot data to identify the video where gated fusion most clearly outperforms single-modality baselines. Plot frame-level scores for skeleton-only, visual-only, and gated fusion on the same axes. Shade ground-truth anomaly interval. Single-column width (3.33in). Use CLIP backbone (primary results). Source data from the results run directories (ucf_gated_fusion_s42, ucf_skeleton_only_s42, ucf_clip_only_s42) -- load eval_scores.npz files for per-video frame scores.

    Figure 3 (fig_backbone_comparison.pdf): 4-way backbone grouped bar chart. Read backbone_comparison_4way.csv. Create side-by-side subplots for UCF (AUC) and XD (AP). Group bars by variant (Visual-Only, Late Fusion, Gated Fusion), with 4 bars per group (one per backbone). Use hatching per RESEARCH.md pattern. Full text width (7in). Include error bars for Gated Fusion from seed_stability_4way.csv.

    Figure 4 (fig_gating_distribution.pdf): Gating weight distribution by anomaly category. Load gate values from Gated Fusion model inference on test videos (existing analysis from Phase 6 D_gating data). Show violin or box plot of gate weights grouped by UCF crime categories and XD violence categories. Single-column width. Demonstrates category-specific modality preference (D-27.4).

    Figure 5 (fig_tta_comparison.pdf): TTA comparison across backbones. Read results/tta_backbone/summary.csv. Create grouped bar chart showing source-only vs TENT vs SAR mean AUC for each of the 4 backbones. Single-column width. Include delta annotations showing improvement from source to best TTA method.

    Run the script to generate all 4 figures to paper/figures/.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; figs=Path('paper/figures'); expected=['fig_temporal_scores.pdf','fig_backbone_comparison.pdf','fig_gating_distribution.pdf','fig_tta_comparison.pdf']; missing=[f for f in expected if not (figs/f).exists()]; assert not missing, f'Missing: {missing}'; sizes=[(figs/f).stat().st_size for f in expected]; assert all(s > 5000 for s in sizes), f'Suspiciously small: {dict(zip(expected,sizes))}'; print('OK: all figures exist')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_pub_figures.py exists with ACM_COL_WIDTH=3.33 and PUB_DPI=300
    - paper/figures/fig_temporal_scores.pdf exists, single-column width, shows 3 score curves + GT shading
    - paper/figures/fig_backbone_comparison.pdf exists, full-text width, shows grouped bars with hatching
    - paper/figures/fig_gating_distribution.pdf exists, single-column width, shows category-grouped distributions
    - paper/figures/fig_tta_comparison.pdf exists, single-column width, shows 4-backbone TTA comparison
    - All figures use serif font family, 300 DPI, grayscale-distinguishable patterns
  </acceptance_criteria>
  <done>4 publication-quality data figures generated as PDF in paper/figures/</done>
</task>

<task type="auto">
  <name>Task 2: Create architecture diagram as HTML/SVG and export to PNG</name>
  <files>paper/figures/fig_architecture.html, paper/figures/fig_architecture.png</files>
  <read_first>
    - .planning/phases/11-thesis-manuscript/11-CONTEXT.md (D-14: created as HTML/SVG, exported to PDF)
    - .planning/phases/11-thesis-manuscript/11-RESEARCH.md (Architecture Patterns section, system architecture diagram showing pipeline flow)
  </read_first>
  <action>
    Create paper/figures/fig_architecture.html containing an inline SVG pipeline diagram showing the ViolenceCC dual-modal fusion framework. The diagram must show:

    Left side (input): Video frames icon
    Top path: Frame -> RTMPose skeleton extraction -> COCO-17 keypoints -> CTR-GCN backbone (frozen) -> 256-d skeleton features
    Bottom path: Frame -> Visual backbone (CLIP/SigLIP2, frozen) -> snippet-level pooling -> visual features (512-1536d depending on backbone)
    Center: Fusion module block showing Late Fusion (concatenation) and Gated Fusion (learned gate with LayerNorm) as alternative paths
    Right: MIL head -> anomaly score (0-1)
    Annotation: "MIL Ranking Loss" label on the training signal
    Annotation: "TTA (TENT/SAR)" label on LayerNorm parameters with a dotted border indicating adapted parameters

    Style: Clean academic diagram. White background. Use black/dark gray lines and fills. Labeled boxes for each component. Arrows showing data flow. Full-width (7 inches = 672px at 96 DPI base, will be captured at 2x for 300 DPI equivalent). Sans-serif font (Arial/Helvetica) for consistency with common architecture diagrams.

    Use playwright-cli to capture the HTML as a PNG at 2x device scale factor (effectively 192 DPI capture of a 7-inch layout = ~1344px wide, which at 300 DPI print = 4.48in -- adjust HTML layout width to 2100px at 1x so 2x capture gives 4200px = 7in at 600 DPI, more than sufficient).

    Capture command: npx playwright screenshot paper/figures/fig_architecture.html paper/figures/fig_architecture.png --full-page --device-scale-factor 2

    If playwright-cli is unavailable, fall back to creating the SVG directly and note that the user should open in a browser and screenshot at high resolution.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('paper/figures/fig_architecture.png'); assert p.exists(); assert p.stat().st_size > 50000, f'Too small: {p.stat().st_size}'; print(f'OK: {p.stat().st_size} bytes')"</automated>
  </verify>
  <acceptance_criteria>
    - paper/figures/fig_architecture.html exists with inline SVG showing the dual-modal pipeline
    - paper/figures/fig_architecture.png exists and is larger than 50KB (high-resolution capture)
    - Diagram shows both skeleton and visual paths converging at fusion
    - Diagram labels RTMPose, CTR-GCN, CLIP/SigLIP2, Late Fusion, Gated Fusion, MIL Ranking Loss, TTA
    - White background suitable for academic paper inclusion
  </acceptance_criteria>
  <done>Architecture diagram created as HTML/SVG and exported to PNG for paper inclusion as Figure 1</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 5 publication figures generated: (1) architecture diagram PNG, (2) temporal score PDF, (3) backbone comparison PDF, (4) gating distribution PDF, (5) TTA comparison PDF. All at 300 DPI with ACM column widths.</what-built>
  <how-to-verify>
    1. Open paper/figures/fig_architecture.png -- verify pipeline flow is clear, all components labeled, readable at column width
    2. Open paper/figures/fig_temporal_scores.pdf -- verify 3 curves visible, GT interval shaded, axis labels readable
    3. Open paper/figures/fig_backbone_comparison.pdf -- verify 4 backbone groups distinguishable (hatching patterns work in grayscale)
    4. Open paper/figures/fig_gating_distribution.pdf -- verify category groups visible, distributions show variation
    5. Open paper/figures/fig_tta_comparison.pdf -- verify all 4 backbones shown, delta annotations present
    6. Check any figure would be legible when printed in black and white (D-28 grayscale-friendly)
  </how-to-verify>
  <resume-signal>Type "figures-approved" or describe specific changes needed per figure</resume-signal>
</task>

</tasks>

<verification>
- All 5 figures exist in paper/figures/ as PDF or PNG
- scripts/generate_pub_figures.py can regenerate data figures if results change
- Figures are publication-quality (300 DPI, proper sizing, grayscale-friendly)
</verification>

<success_criteria>
All 4-5 figures per D-27 are generated at publication quality per D-28, ready for LaTeX includegraphics in the paper draft.
</success_criteria>

<output>
Create `.planning/phases/11-thesis-manuscript/11-03-SUMMARY.md` when done
</output>
