---
phase: 11-thesis-manuscript
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - paper/main.tex
  - scripts/generate_latex_tables.py
autonomous: false
requirements: []

must_haves:
  truths:
    - "main.tex contains complete prose for all sections: Introduction, Related Work, Methodology, Experiments, Discussion, Limitations and Future Work, Conclusion (D-12)"
    - "Two separate results tables exist: UCF-Crime (AUC primary) and XD-Violence (AP primary), each showing 4 backbones x 6 variants with deltas (D-21, D-22, D-25)"
    - "Gated Fusion rows show mean +/- std from 3 seeds (D-23)"
    - "TTA results table shows source-only vs TENT vs SAR across all 4 backbones (D-29)"
    - "All figures are included via includegraphics with correct sizing (D-38)"
    - "All citations resolve to references.bib entries (D-37)"
    - "Paper is a single .tex file with no input splitting (D-35)"
    - "Paper is 8-10 pages in ACM sigconf format (D-11)"
    - "Central contribution framed as full pipeline study, not SOTA chasing (D-01)"
    - "No external baseline comparison table (D-06)"
  artifacts:
    - path: "paper/main.tex"
      provides: "Complete workshop paper draft"
      min_lines: 500
      contains: "\\begin{abstract}"
    - path: "scripts/generate_latex_tables.py"
      provides: "Programmatic LaTeX table generation from CSVs"
      contains: "backbone_comparison_4way"
  key_links:
    - from: "paper/main.tex"
      to: "paper/references.bib"
      via: "\\cite{} commands"
      pattern: "cite"
    - from: "paper/main.tex"
      to: "paper/figures/"
      via: "\\includegraphics"
      pattern: "includegraphics"
    - from: "scripts/generate_latex_tables.py"
      to: "results/phase10_charts/backbone_comparison_4way.csv"
      via: "pandas CSV read"
      pattern: "backbone_comparison_4way"
---

<objective>
Write the complete CGW '26 workshop paper in a single pass (D-36) -- all prose, tables, figures, and citations integrated into paper/main.tex. Generate results tables programmatically from CSV data to avoid transcription errors.

Purpose: This is the primary deliverable of Phase 11. The paper presents the ViolenceCC dual-modal fusion framework, four-backbone comparison, and TTA investigation as a cohesive workshop contribution (D-01). Must be ready for professor review by June 1 (D-18).
Output: Complete 8-10 page paper draft in paper/main.tex + table generation script.
</objective>

<execution_context>
@.planning/phases/11-thesis-manuscript/11-CONTEXT.md
@.planning/phases/11-thesis-manuscript/11-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-thesis-manuscript/11-02-SUMMARY.md
@.planning/phases/11-thesis-manuscript/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate LaTeX tables programmatically from CSV data</name>
  <files>scripts/generate_latex_tables.py</files>
  <read_first>
    - results/phase10_charts/backbone_comparison_4way.csv (column structure: Dataset, Variant, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP, Giant_AUC, Giant_AP, plus delta columns)
    - results/phase10_charts/seed_stability_4way.csv (columns: Dataset, Seed, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP, Giant_AUC, Giant_AP)
    - results/tta_backbone/summary.csv (TTA cross-backbone results)
    - .planning/phases/11-thesis-manuscript/11-RESEARCH.md (Pattern 2: Publication-Quality Table Formatting -- booktabs, textbf best, delta arrows, 1 decimal, table* for full width)
  </read_first>
  <action>
    Create scripts/generate_latex_tables.py that reads the CSV data files and outputs LaTeX table code to stdout (and optionally to a file). The script generates 3 tables:

    Table 1 (UCF-Crime Ablation): Read backbone_comparison_4way.csv, filter Dataset==UCF. Columns: Variant | CLIP AUC | SigLIP2 AUC | SO400M AUC | Giant AUC | Best Delta. Use table* (full width). Format values as XX.X% (multiply by 100, 1 decimal per D-26). Bold the best value per row (D-24). Delta column shows signed arrow format: $\uparrow$+X.X or $\downarrow$-X.X comparing best backbone to CLIP baseline (D-25). For Gated Fusion row, replace single-seed values with mean +/- std from seed_stability_4way.csv (D-23). Variant display names: Skeleton Only, Visual Only, Late Fusion, Gated Fusion, GF 2-Person, GF Mean-Only.

    Table 2 (XD-Violence Ablation): Same structure as Table 1 but filter Dataset==XD, use AP as primary metric instead of AUC. Delta compares best backbone AP to CLIP AP.

    Table 3 (TTA Results): Read results/tta_backbone/summary.csv. Columns: Backbone | Source-Only AUC | TENT AUC | SAR AUC | Best Delta. 4 rows (one per backbone). Format same conventions. Bold best method per backbone row.

    Output format: print the raw LaTeX code for each table, separated by clear comments, ready to paste into main.tex. Also write to paper/tables_generated.tex for reference.

    Run the script and capture the output for use in Task 2.
  </action>
  <verify>
    <automated>python scripts/generate_latex_tables.py > paper/tables_generated.tex 2>&1 && python -c "t=open('paper/tables_generated.tex').read(); assert 'toprule' in t; assert 'textbf' in t; assert 'uparrow' in t or 'downarrow' in t; print(f'OK: {len(t)} chars, tables generated')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_latex_tables.py exists and runs without error
    - Output contains 3 table environments (table or table*)
    - All numeric values are formatted as XX.X (1 decimal place, per D-26)
    - Best values per row are wrapped in \textbf{} (per D-24)
    - Delta columns use $\uparrow$ or $\downarrow$ arrows (per D-25)
    - Gated Fusion rows show mean$\pm$std format (per D-23)
    - Uses booktabs (\toprule, \midrule, \bottomrule)
    - No manually typed numeric values -- all computed from CSV data
  </acceptance_criteria>
  <done>LaTeX tables generated programmatically from CSV data, ready for inclusion in main.tex</done>
</task>

<task type="auto">
  <name>Task 2: Write complete paper draft in single pass</name>
  <files>paper/main.tex</files>
  <read_first>
    - paper/main.tex (current scaffold from Plan 02 with section headings and placeholders)
    - paper/references.bib (all BibTeX keys for \cite{} commands)
    - paper/tables_generated.tex (LaTeX table code from Task 1)
    - results/phase10_charts/backbone_comparison_4way.csv (numeric results for inline discussion)
    - results/phase10_charts/seed_stability_4way.csv (seed stability numbers)
    - results/tta_backbone/summary.csv (TTA numbers for discussion)
    - .planning/phases/11-thesis-manuscript/11-CONTEXT.md (all D-XX decisions governing content, framing, structure)
    - .planning/phases/11-thesis-manuscript/11-RESEARCH.md (key numeric results at lines 304-331, BibTeX keys, paper structure pattern at lines 149-192)
    - .planning/STATE.md (accumulated context: key decisions, performance metrics, critical pitfalls)
  </read_first>
  <action>
    Write the complete paper draft in paper/main.tex as a single pass (D-36). Formal academic style with proper hedging (D-34). Single .tex file, no \input splitting (D-35). All LaTeX formatting handled including cross-references, citations, table/figure numbering (D-37).

    ABSTRACT: ~150 words. State the problem (weakly supervised VAD), approach (dual-modal skeleton-visual fusion), key contribution (four-backbone comparison + TTA on LN), main results (best AUC/AP numbers), and finding (backbone choice matters; LN-based TTA shows limited gains). Per D-42, Claude decides whether abstract is drafted first or last -- write it as a summary after all sections are complete, then place it at the top.

    SECTION 1 - INTRODUCTION (~1 page, D-12):
    - Open with VAD problem significance (surveillance scale, weakly supervised paradigm)
    - Identify gap: existing CLIP-based VAD ignores skeletal motion; backbone sensitivity unstudied
    - State contributions: (1) dual-modal skeleton-visual fusion framework (D-03: skeleton as core modality), (2) four-backbone visual comparison, (3) first LN-based TTA evaluation in VAD
    - Per D-01: frame as full pipeline study, not SOTA claim
    - Per D-04: MIL ranking loss is adopted recipe, not novel contribution

    SECTION 2 - RELATED WORK (~1 page, D-12):
    - 2.1 Video Anomaly Detection: Sultani2018 MIL paradigm, RTFM2021 magnitude learning, VadCLIP2024 as CLIP-based context (D-05: cited for context only, not baseline). Note weakly supervised vs unsupervised distinction.
    - 2.2 Multi-Modal Fusion for Video Understanding: Skeleton-based approaches (Doshi2022, CTR-GCN2021), visual-language models (CLIP2021, SigLIP2023/2025). Gap: no prior work combines skeleton GCN with VL models for VAD.
    - 2.3 Test-Time Adaptation: TENT2021 entropy minimization, SAR2023 stable adaptation. Note BN assumption in original works; our LN transfer is novel territory.

    SECTION 3 - METHODOLOGY (~1.5 pages, D-12, D-13):
    - 3.1 Skeleton Extraction with RTMPose: COCO-17 keypoints, RTMPose-m balanced mode, per-frame extraction, PreNormalize2D to [-1,1]
    - 3.2 CTR-GCN Feature Extraction: Frozen NTU120 pretrained backbone, 4-stream weighted concat (j:b:jm:bm = 1.0:1.0:0.5:0.5), 64-frame snippets, output 256-d per snippet
    - 3.3 Visual-Language Feature Extraction: Four backbone variants (CLIP ViT-B/16, SigLIP2 ViT-B/16, SigLIP2 SO400M, SigLIP2 Giant-opt) all frozen. 1 FPS sampling, mean+max pooling, output dimensionality varies (512-1536d). Mention backbone swap is clean (same extraction pipeline, different model).
    - 3.4 Fusion Mechanisms: Late Fusion (concatenation + projection) and Gated Fusion (learned gate on skeleton vs visual, 3 LayerNorm layers). Emphasize LN choice as enabling TTA.
    - 3.5 MIL Training with Ranking Loss: Adopted from Sultani/RTFM (D-04). Top-k selection, sparsity+smoothness regularization. Briefly state hyperparameter values found via sweep (D-30: report final values, not search process).

    SECTION 4 - EXPERIMENTS (~4 pages, D-12):
    - 4.1 Datasets (D-15): UCF-Crime (1900 videos, 13 categories, AUC metric) and XD-Violence (4754 videos, 6 categories, AP metric). Training/val/test splits. 15% stratified validation holdout.
    - 4.2 Implementation Details (D-32): Hardware (RTX 4090, 24GB), training hyperparameters (lr, epochs, early stopping patience=10), seeds {42, 123, 2024}. Brief computational cost paragraph.
    - 4.3 Ablation Study: Insert Table 1 (UCF) and Table 2 (XD) from generated LaTeX. Discuss: skeleton-only as weak standalone (71.8% UCF AUC) but complementary in fusion (D-41: frame to strengthen complementarity argument). Visual-only strong. Late Fusion sometimes hurts. Gated Fusion recovers and exceeds.
    - 4.4 Backbone Comparison: Reference Table 1 and Table 2 backbone columns. Key finding per D-40: dataset-specific backbone divergence -- Giant-opt leads UCF (83.6% GF Mean-Only AUC), SO400M leads XD (74.8% GF 2-Person AP). Include backbone comparison bar chart (Figure 3). 3-seed stability analysis per D-23.
    - 4.5 TTA Results (D-29): Insert Table 3. Discuss LN-based TTA showing marginal improvements. Frame as an honest negative finding -- BN-to-LN transfer does not straightforwardly apply. Compare across backbones. Reference TTA comparison figure (Figure 5).
    - Include Figure 2 (temporal scores) and Figure 4 (gating distribution) at appropriate locations in 4.3/4.4.

    SECTION 5 - DISCUSSION:
    - Synthesize: fusion works but backbone choice matters more than fusion architecture
    - Dataset-specific behavior: UCF favors larger models, XD benefits from balanced representations
    - Skeleton complementarity: despite weak standalone performance, skeleton features consistently enable fusion gains
    - Gating analysis: reference Figure 4 for modality preference patterns

    SECTION 6 - LIMITATIONS AND FUTURE WORK (D-16):
    - UCF AUC below 83% target (82.3% observed) -- MISS-ACCEPTED
    - XD AP seed sensitivity (1.08% std exceeds 0.5% gate)
    - TTA limited effectiveness on LN -- further investigation needed
    - Single dataset pair (UCF-Crime + XD-Violence) -- generalizability unknown
    - Future: end-to-end fine-tuning, additional modalities, larger backbone scale

    SECTION 7 - CONCLUSION (~0.5 page):
    - Summarize contribution and key findings concisely
    - Restate that backbone choice and multi-modal fusion are both important design axes

    ACKNOWLEDGMENTS: Standard academic acknowledgment of advisor and compute resources.

    FIGURES: Include all 5 figures at appropriate locations using \includegraphics with \columnwidth or \textwidth sizing. Figure 1 (architecture, full width at top of methodology), Figure 2 (temporal, column width in ablation), Figure 3 (backbone bars, full width in backbone comparison), Figure 4 (gating, column width in discussion), Figure 5 (TTA, column width in TTA results).

    TABLES: Paste generated LaTeX table code from paper/tables_generated.tex into appropriate sections. Tables 1-2 in 4.3 Ablation Study, Table 3 in 4.5 TTA Results.

    D-31: Highlight best configuration per dataset explicitly in results text.
    D-06: No external baseline comparison table anywhere.
    D-05: VadCLIP cited in Related Work context only.
  </action>
  <verify>
    <automated>python -c "t=open('paper/main.tex',encoding='utf-8').read(); checks={'abstract':'begin{abstract}','intro':'section{Introduction}','related':'section{Related Work}','method':'section{Methodology}','exper':'section{Experiments}','discuss':'section{Discussion}','limits':'section{Limitations','concl':'section{Conclusion}','bib':'bibliography{references}','fig1':'fig_architecture','fig3':'fig_backbone','table':'begin{table'}; missing=[k for k,v in checks.items() if v not in t]; assert not missing, f'Missing: {missing}'; lines=t.count(chr(10)); assert lines > 400, f'Only {lines} lines -- too short for 8-10 pages'; cites=t.count('cite{'); assert cites >= 10, f'Only {cites} citations'; assert '\input{' not in t, 'D-35 violation: \input splitting detected'; print(f'OK: {lines} lines, {cites} citations, no \input splitting')"</automated>
  </verify>
  <acceptance_criteria>
    - paper/main.tex is a single .tex file (no \input commands) per D-35
    - Contains \begin{abstract} with ~150 word abstract
    - Contains all 7 \section commands per D-12
    - Contains all methodology \subsection commands per D-13
    - Contains all experiments \subsection commands including TTA Results per D-29
    - Contains at least 10 \cite{} commands referencing keys from references.bib
    - Contains 5 \includegraphics commands for all figures
    - Contains 3 table/table* environments with booktabs formatting
    - Tables use \textbf for best values, arrow deltas, and mean+/-std for Gated Fusion
    - No \input{} file splitting (D-35)
    - No external baseline comparison table (D-06)
    - VadCLIP referenced only in Related Work, not as comparison baseline (D-05)
    - MIL ranking loss described as adopted from Sultani/RTFM, not as novel contribution (D-04)
    - File has 400+ lines (minimum for 8-10 page ACM sigconf paper)
    - Separate Limitations and Future Work section exists (D-16)
  </acceptance_criteria>
  <done>Complete 8-10 page workshop paper draft ready for Overleaf compilation and professor review</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete CGW '26 workshop paper draft in paper/main.tex with all sections, tables, figures, and citations. Ready for Overleaf upload and compilation.</what-built>
  <how-to-verify>
    1. Upload paper/ directory contents (main.tex, references.bib, acmart.cls, ACM-Reference-Format.bst, figures/) to Overleaf
    2. Compile with pdflatex -- verify no compilation errors
    3. Check page count is 8-10 pages in ACM sigconf format
    4. Verify all figures render correctly (no missing image warnings)
    5. Verify all citations resolve (no [?] markers in bibliography)
    6. Skim each section for academic tone, logical flow, and completeness
    7. Verify tables have correct numbers (spot-check 2-3 values against backbone_comparison_4way.csv)
    8. Send to professor for review per D-18 (June 1 deadline)
  </how-to-verify>
  <resume-signal>Type "paper-approved" or list revision requests from professor/self-review</resume-signal>
</task>

</tasks>

<verification>
- paper/main.tex compiles on Overleaf without errors
- Paper is 8-10 pages in ACM sigconf format
- All 5 figures and 3 tables render correctly
- All citations resolve to references.bib entries
- No [?] markers in compiled PDF
</verification>

<success_criteria>
Complete CGW '26 workshop paper draft delivered per D-18 professor deadline (June 1). All D-01 through D-39 locked decisions honored. Paper presents the full pipeline study (D-01) with four-backbone comparison and TTA investigation in a cohesive 8-10 page ACM sigconf format document.
</success_criteria>

<output>
Create `.planning/phases/11-thesis-manuscript/11-04-SUMMARY.md` when done
</output>
