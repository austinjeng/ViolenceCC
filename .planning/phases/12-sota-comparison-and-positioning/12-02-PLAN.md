---
phase: 12-sota-comparison-and-positioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/main.tex
  - paper/references.bib
autonomous: false
requirements: [N/A-thesis-enrichment]
must_haves:
  truths:
    - "Section 2 Related Work quotes the benchmark numbers for the already-cited methods (Sultani 75.41; RTFM 84.30/77.81; VadCLIP 88.02/84.51) with correct attribution caveats"
    - "main.tex contains a condensed SOTA comparison table with a Setting column (fair-subset rows + a few top anchors), OR the documented page-budget fallback (3-4-row mini-table)"
    - "Every new cite in the added paper content resolves to a references.bib entry (no undefined-citation warning in main.log)"
    - "The paper still compiles cleanly via build_paper.ps1 -Clean and the page count is recorded (page-budget decision documented)"
    - "Headlines UCF 82.5 / XD 78.7 and all existing results in main.tex are unchanged"
  artifacts:
    - path: "paper/main.tex"
      provides: "Section 2 number-backfill prose + condensed comparison table with Setting column"
      contains: "Setting"
    - path: "paper/references.bib"
      provides: "New BibTeX entries for every newly cited method (condensed-table anchors + fair-subset methods)"
      contains: "@"
  key_links:
    - from: "paper/main.tex"
      to: "paper/references.bib"
      via: "every cite resolves to a bib key"
      pattern: "cite\\{"
    - from: "paper/main.tex"
      to: ".planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md"
      via: "every quoted number sourced from the verified artifact"
      pattern: "84\\.51|88\\.02"
---

<objective>
Make the live CGW '26 paper edits that close the reviewer's "no head-to-head SOTA comparison" gap: (deliverable 4) backfill Section 2 Related Work with benchmark numbers for the already-cited methods, (deliverable 5) add a CONDENSED comparison table with a mandatory Setting column under a strict page budget with a documented fallback, and (deliverable 6, paper side) add the supporting references.bib entries so every new cite resolves. Rebuild clean and confirm page count.

Purpose: The professor's exact complaint is that prior benchmark numbers were not quoted and there is no head-to-head positioning. This plan closes that gap inside the page-limited workshop paper, honestly (Setting column, no overclaim), without any new GPU runs.

Output: edited paper/main.tex (Section 2 prose + condensed table), edited paper/references.bib (new entries), a clean build_paper.ps1 -Clean compile, and a recorded page-count + page-budget decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md
@.planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md
@paper/main.tex
@paper/references.bib

<interfaces>
<!-- Existing references.bib keys (do NOT duplicate; reuse these in the Section 2 backfill). Extracted from paper/references.bib. -->
Existing bib keys relevant here:
  sultani2018ucfcrime  (Sultani et al., CVPR 2018 -- UCF 75.41)
  tian2021rtfm         (RTFM, ICCV 2021 -- UCF 84.30 / XD 77.81)
  wu2020xdviolence     (Wu et al., ECCV 2020 -- XD-Violence dataset; source of Sultani's re-impl XD number)
  wu2024vadclip        (VadCLIP, AAAI 2024 -- UCF 88.02 / XD 84.51)
Key naming convention (from references.bib header): firstauthorYYYYkeyword
ACM acmart sigconf doc: table = single column; table* = two-column span. main.tex already loads booktabs and multirow.
</interfaces>

<notes>
- WRITING phase: "tests" are clean-compile (build exit 0), grep assertions on main.tex, and main.log checks (no undefined-citation warning, no overfull-by-large-pt errors), plus number-traceability against 12-RESEARCH-sota.md.
- This plan OWNS paper/main.tex and paper/references.bib exclusively (no overlap with Plan 01, which owns only paper/sota_comparison_full.tex) -> Plan 01 and Plan 02 run in parallel in Wave 1.
- references.bib in THIS plan must cover BOTH the condensed-table methods AND the fair-subset methods cited in Section 2 prose, so the paper is self-consistent. (Plan 01's standalone artifact carries its own citation strategy and does not depend on this bib.)
- Source of record for EVERY number: 12-RESEARCH-sota.md (Section 1 full table, Section 2 fair-subset, Section 8 citation appendix with author lists/venues for bib entries). Section 6 = mandatory re-verify list -- flag any included re-verify number with a `% RE-VERIFY` comment.
- Guardrails (12-CONTEXT.md + 12-RESEARCH-sota.md Section 4 DO / Section 5 DO-NOT) are acceptance criteria: Setting column on every comparison table; no SOTA/outperforms language; TTA scoped to robustness; correct attribution (Sultani XD = Wu et al. 2020; RTFM 78.27 XD = VadCLIP's table not RTFM's own); cite only verified numbers; MGFN XD = 79.19 (I3D) not 80.11; no XD number for STPrompt/FDPN; EventVAD/LAVAD XD = AP not AUC.
- The paper currently builds; existing PDF is paper/main.pdf. Existing Section 6 "Performance gaps" prose (main.tex ~line 383) already gestures at this -- the condensed table + Section 2 numbers make it concrete. Do NOT rewrite existing results/Section 6 claims beyond what the deliverables require (surgical-changes rule).
- Page budget: build script auto-opens the PDF (Invoke-Item) and exits with latexmk's code. Read page count from main.log's "Output written on main.pdf (N pages" line (or pdfinfo). The paper targets ~10 pages and is page-limited.
- IMPORTANT: this plan is autonomous:false because Task 3 is a checkpoint:human-verify on the rendered table + page count, and Task 2 makes the page-budget tradeoff (full condensed table vs mini-table fallback) when the measured page count leaves no clean margin.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add references.bib entries for all newly cited methods + backfill Section 2 with verified benchmark numbers</name>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (Section 8 citation appendix = author lists / venues / canonical numbers for each method; Section 1 full table; Section 6 re-verify list)
    - paper/references.bib (existing keys + naming convention firstauthorYYYYkeyword; do NOT duplicate sultani2018ucfcrime/tian2021rtfm/wu2020xdviolence/wu2024vadclip)
    - paper/main.tex lines 88-106 (Section 2 Related Work -- the exact prose to backfill: Sultani/RTFM/VadCLIP sentences ~line 92)
    - .planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md (Guardrails)
  </read_first>
  <files>paper/references.bib, paper/main.tex</files>
  <action>
    First, in paper/references.bib, add new BibTeX entries (following the existing firstauthorYYYYkeyword convention, mimicking existing entry formatting) for every method that will be cited by the Section 2 backfill and the condensed table in Task 2. At minimum the fair-subset methods not already present: CLIP-TSA (Joo et al., ICIP 2023), UR-DMU (Zhou, Yu and Yang, AAAI 2023), MGFN (Chen et al., AAAI 2023), Light-WVAD (Wang, Zhou and Guan, Neurocomputing 2024 -- NOTE correct authors, NOT "Sun et al."); plus the top SOTA anchors chosen for the condensed table (VadCLIP already exists; add DSANet (Yin et al., AAAI 2026) and/or PI-VAD (Majhi et al., CVPR 2025) per the anchor set decided in Task 2). Pull exact author lists / venues / years from 12-RESEARCH-sota.md Section 8. Do NOT invent DOIs you cannot source; an inproceedings/misc entry with author+title+booktitle/eprint+year is sufficient and matches existing arXiv misc entries.
    Then backfill Section 2 Related Work in paper/main.tex (the VAD subsection, ~lines 90-92): insert the verified benchmark numbers into the existing prose for the already-cited methods -- Sultani et al. 75.41% UCF AUC; RTFM 84.30% UCF / 77.81% XD AP; VadCLIP 88.02% UCF / 84.51% XD AP. Each number must carry the correct metric label (UCF = frame-level ROC-AUC; XD = AP) and correct attribution caveat where relevant (do NOT attribute an XD number to Sultani's 2018 paper -- Sultani's mention is UCF-only here; if XD context is needed, attribute to Wu et al. via wu2020xdviolence). Keep edits surgical -- extend existing sentences, do not rewrite the subsection.
    Flag any included number that is on the Section 6 re-verify list with a `% RE-VERIFY` comment (the three Section 2 backfill numbers are all VERIFIED high-confidence per Section 8, so likely none need flagging here; confirm against Section 6).
  </action>
  <verify>
    <automated>cd D:/ViolenceCC && powershell -NoProfile -Command "if ((Select-String -Path paper/main.tex -Pattern '88\.02' -Quiet) -and (Select-String -Path paper/main.tex -Pattern '84\.51' -Quiet) -and (Select-String -Path paper/main.tex -Pattern '75\.41' -Quiet) -and (Select-String -Path paper/main.tex -Pattern '84\.30' -Quiet) -and (Select-String -Path paper/main.tex -Pattern '77\.81' -Quiet)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - paper/references.bib gains a new entry for each newly-cited method; grep for each chosen bib key returns exactly one `@...{key,` (no duplicate keys, no duplicate of the existing 4 keys).
    - Each new bib entry's author list/venue/year matches 12-RESEARCH-sota.md Section 8 (manual cross-check; Light-WVAD authors = Wang, Zhou and Guan, NOT Sun et al.).
    - Section 2 main.tex now contains literally `75.41`, `84.30`, `77.81`, `88.02`, `84.51` (grep all five present).
    - Each backfilled number has its metric named nearby (grep that "AUC" and "AP" appear in the edited Section 2 sentences) and no XD number is attributed to Sultani's 2018 paper (manual check of the edited sentence).
    - No existing Section 2 sentence is deleted or rewritten beyond inserting the numbers (diff is additive/surgical).
  </acceptance_criteria>
  <done>references.bib has entries for every new method; Section 2 quotes the five verified benchmark numbers with correct metric labels and attribution; edits are surgical.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the page budget, then add the condensed SOTA comparison table (Setting column) or the documented fallback</name>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (Section 2 fair-subset table = the condensed table's core rows; Section 1 = top anchors to add; Section 6 re-verify list)
    - paper/main.tex lines 226-261 (existing booktabs table style: table* / caption / label / toprule-midrule-bottomrule) and lines 379-383 (Section 6 "Performance gaps" -- natural placement context)
    - scripts/build_paper.ps1 (build path; -Clean does full wipe+rebuild; exits with latexmk's code; PDF auto-opens via Invoke-Item)
    - .planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md (page-budget guardrail: reclaim ~half page OR fall back to 3-4-row mini-table; Setting column mandatory)
  </read_first>
  <files>paper/main.tex</files>
  <action>
    STEP A -- measure the budget. Run scripts/build_paper.ps1 -Clean for a baseline build, then read the final page count from paper/main.log (the "Output written on main.pdf (N pages" line). Record N.
    STEP B -- choose table size (the page-budget tradeoff). If N leaves clean room for a full condensed table, use: the 7 fair-subset rows from research-artifact Section 2 (CLIP-TSA, UR-DMU, MGFN-I3D, Light-WVAD, RTFM, This work, Sultani) PLUS ~3 top anchors (VadCLIP 88.02/84.51, DSANet 89.44/86.95, PI-VAD 90.33/85.37), each with the Setting column. Otherwise fall back to a 3-4-row mini-table (This work 82.5/78.7 + CLIP-TSA 87.58/82.19 + RTFM 84.30/77.81 + VadCLIP 88.02/84.51) and DOCUMENT the fallback in the SUMMARY and a `% PAGE-BUDGET FALLBACK` comment in main.tex.
    STEP C -- add the table near Section 6 "Performance gaps" (or end of Section 2) in the existing booktabs style. Columns = Method | Venue | UCF AUC | XD AP | Setting (Setting encodes Frozen?/Text?/Audio?/frame-vs-video compactly, e.g. "Frozen, no-text, frame" / "Frozen, text, frame" / "Frozen, audio+text, frame"). This-work row bolded at 82.5/78.7, never bare head-to-head -- the Setting column makes the regime explicit. State metric definitions in the caption (UCF = frame ROC-AUC; XD = AP). Apply ALL attribution guardrails: MGFN XD = 79.19 (I3D) if included; correct Sultani/RTFM attribution; EventVAD/LAVAD XD = AP if included; label any audio-visual row. Add `% RE-VERIFY` comments on any included row whose number is on the Section 6 re-verify list.
    Add 1-2 sentences of surrounding prose tying the table to the honest claim: competitiveness ONLY within the constrained frozen/visual-only/single-GPU regime -- NO "SOTA"/"comparable to SOTA"/"outperforms" language. TTA stays scoped to robustness.
    STEP D -- rebuild. Run scripts/build_paper.ps1 -Clean again; confirm exit 0 and re-read the page count; confirm the table did not push the paper over the page limit (if it did, switch to the mini-table fallback and rebuild).
  </action>
  <verify>
    <automated>cd D:/ViolenceCC && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_paper.ps1 -Clean > build_log.txt 2>&1; $code = $LASTEXITCODE; $pages = (Select-String -Path paper/main.log -Pattern 'Output written on main\.pdf \((\d+) page' | Select-Object -First 1).Matches.Groups[1].Value; Write-Output "exit=$code pages=$pages"; $undef = Select-String -Path paper/main.log -Pattern 'Citation .* undefined' -Quiet; $hasSetting = Select-String -Path paper/main.tex -Pattern 'Setting' -Quiet; if (($code -eq 0) -and (-not $undef) -and $hasSetting) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - build_paper.ps1 -Clean exits 0; paper/main.pdf is produced.
    - paper/main.log shows the "Output written on main.pdf (N pages" line and N is recorded in the SUMMARY; N does NOT exceed the workshop page limit (record the limit and the chosen table-size decision).
    - main.log has NO "Citation ... undefined" warning and NO `??` undefined references (every new cite resolves).
    - main.tex contains a comparison table whose header includes `Setting`; the This-work row shows `82.5` and `78.7` in bold.
    - If MGFN is included its XD cell = `79.19` (not 80.11); STPrompt/FDPN have NO XD number anywhere; EventVAD/LAVAD (if included) XD = AP value (64.04 / 62.01).
    - Zero overclaim tokens in the added prose/caption (grep -i for `state-of-the-art`, `\bSOTA\b`, `comparable to SOTA`, `outperform` over the diff returns 0, except inside an explicit "we do not claim SOTA" framing if used).
    - Headlines unchanged: existing Tables 1-3 and all existing numbers (82.5 / 78.7 / 68.8 / 40.8 etc.) are byte-identical to before this plan (diff touches only the new bib entries, new Section 2 numbers, and the new table + its prose).
  </acceptance_criteria>
  <done>Page count measured and recorded; condensed table (or documented mini-table fallback) with Setting column added; paper rebuilds clean with no undefined citations; no overclaim; headlines unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the rendered condensed table, page count, and honest framing</name>
  <what-built>The condensed SOTA comparison table and Section 2 number-backfill are in paper/main.tex, references.bib has the new entries, and build_paper.ps1 -Clean produced a clean PDF at the recorded page count.</what-built>
  <how-to-verify>
    1. Open paper/main.pdf (the build auto-opens it via Invoke-Item).
    2. Confirm the condensed comparison table renders with a visible Setting column and the This-work row (82.5 / 78.7) is bolded and clearly in-context (not a bare head-to-head against VadCLIP/DSANet/PI-VAD).
    3. Confirm the paper page count is at or under the workshop page limit (the executor recorded N in the SUMMARY).
    4. Skim Section 2 -- confirm the Sultani 75.41 / RTFM 84.30/77.81 / VadCLIP 88.02/84.51 numbers read naturally with correct metric labels.
    5. Confirm the framing reads as honest competitiveness within the constrained regime -- no "SOTA"/"outperforms" claim.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what to fix (table size, placement, wording, page overflow).</resume-signal>
</task>

</tasks>

<verification>
- references.bib has a resolvable entry for every newly cited method; no duplicate keys.
- Section 2 quotes the five verified numbers with correct metric labels + attribution.
- A condensed comparison table with a Setting column (or the documented mini-table fallback) is in main.tex; This-work row 82.5/78.7 bolded, never bare head-to-head.
- build_paper.ps1 -Clean exits 0; main.log has no undefined citations; page count recorded and within limit.
- Zero overclaim language; headlines and existing tables unchanged.
- Plan touches only paper/main.tex and paper/references.bib (clean ownership vs Plan 01).
</verification>

<success_criteria>
The live CGW '26 paper now closes the reviewer's SOTA-comparison gap: Section 2 quotes the benchmark numbers, a condensed honest comparison table (Setting column) is present within the page budget, every citation resolves, and the headlines are untouched.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research artifact -> paper | Numbers transcribed from 12-RESEARCH-sota.md into citeable LaTeX; the risk is a mis-transcribed or mis-attributed (un-verified) number reaching the published paper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure (false claim) | condensed table / Section 2 numbers | mitigate | Every number cross-checked against a VERIFIED row in 12-RESEARCH-sota.md Section 1/2/8; re-verify-list numbers flagged with `% RE-VERIFY`; Plan 03 runs a full traceability audit |
| T-12-02 | Tampering (silent overclaim) | added prose/caption | mitigate | Overclaim-language grep in acceptance criteria; Setting column mandatory; honest-regime framing enforced; Plan 03 re-scans |
| T-12-03 | Repudiation (broken build shipped) | paper build | mitigate | build_paper.ps1 -Clean must exit 0 with no undefined citations before the checkpoint; Plan 03 re-runs the clean build as the final gate |

No npm/pip/cargo installs in this phase -> no package-legitimacy (T-12-SC) gate required.
</threat_model>

<output>
Create `.planning/phases/12-sota-comparison-and-positioning/12-02-SUMMARY.md` when done.
</output>
