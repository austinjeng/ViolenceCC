---
phase: 12-sota-comparison-and-positioning
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - .planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md
autonomous: true
requirements: [N/A-thesis-enrichment]
must_haves:
  truths:
    - "The paper rebuilds cleanly from scratch (build_paper.ps1 -Clean exits 0) with no undefined citations and within the page limit"
    - "Every number in both the standalone artifact and the paper traces to a VERIFIED row in 12-RESEARCH-sota.md, or is explicitly RE-VERIFY-flagged"
    - "A full overclaim-language scan over all phase-12 deliverables finds zero violations"
    - "The headline anchors UCF 82.5 / XD 78.7 (and the single-stream baselines) are unchanged across the whole repo"
  artifacts:
    - path: ".planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md"
      provides: "Audit report: clean-build result + page count, number-traceability table, overclaim scan, re-verify checklist status"
      contains: "Traceability"
  key_links:
    - from: ".planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md"
      to: ".planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md"
      via: "every cited number audited against the verified artifact and its re-verify list"
      pattern: "RE-VERIFY|VERIFIED"
---

<objective>
Final verification of the SOTA-comparison phase: confirm the paper rebuilds cleanly, audit every cited number against the verified research artifact (and its mandatory re-verify list), scan all deliverables for overclaim language, and confirm the headline anchors are unchanged. Produce a single VERIFICATION.md audit report.

Purpose: This phase introduces externally-cited numbers into a thesis-grade paper; the failure mode is a mis-transcribed, mis-attributed, or un-verified number, or accidental overclaim, slipping through. This plan is the goal-backward gate that proves the phase goal — an HONEST, VERIFIED comparison with zero overclaim and untouched headlines — actually holds across both the standalone artifact (Plan 01) and the paper (Plan 02).

Output: `.planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md` with a pass/fail audit, plus any blocking issues surfaced for fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md
@.planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md
@.planning/phases/12-sota-comparison-and-positioning/12-01-SUMMARY.md
@.planning/phases/12-sota-comparison-and-positioning/12-02-SUMMARY.md
@paper/main.tex
@paper/references.bib
@paper/sota_comparison_full.tex

<notes>
- WRITING phase: "tests" are a clean build (exit 0), grep/regex assertions, log checks (no undefined citations, no fatal LaTeX errors), and number-traceability against 12-RESEARCH-sota.md.
- This plan is the final gate; it depends on BOTH Plan 01 (standalone artifact) and Plan 02 (paper edits) being complete, so it runs in Wave 2.
- It writes ONLY the audit report (12-VERIFICATION.md). It does NOT edit paper sources. If the audit finds a defect, it records it as a FAIL with the exact location/fix and the orchestrator routes a gap-closure plan; do NOT silently patch.
- Re-verify list = 12-RESEARCH-sota.md Section 6 (15 items). Every such number that was actually cited must be either (a) flagged with `% RE-VERIFY` in the LaTeX, AND (b) carried into the VERIFICATION.md re-verify checklist as a manual-check action for the student. Numbers on the list that were NOT cited need no flag.
- Overclaim tokens to scan: case-insensitive `state-of-the-art`, `\bSOTA\b`, `comparable to SOTA`, `outperform(s|ed|ing)?`, `beats SOTA`, `surpass`. The only allowed appearance is inside an explicit negation ("we do NOT claim state-of-the-art"); list any such allowed instance explicitly.
- Headline anchors to confirm unchanged: UCF 82.5, XD 78.7, skeleton-only 68.8/40.8, visual-only CLIP 81.2/74.6, SigLIP2-Giant 82.4/76.8. Compare against git (these existed before this phase in main.tex Tables 1-2).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean-rebuild + number-traceability + overclaim + headline-integrity audit; write VERIFICATION.md</name>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (Section 1 full table, Section 2 fair-subset, Section 6 re-verify list, Section 8 citation appendix -- the source of truth for the audit)
    - .planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md (Guardrails + success criteria to audit against)
    - paper/main.tex (Section 2 backfill + condensed table to audit)
    - paper/references.bib (new entries to confirm resolvable)
    - paper/sota_comparison_full.tex (standalone artifact to audit)
    - .planning/phases/12-sota-comparison-and-positioning/12-01-SUMMARY.md and 12-02-SUMMARY.md (what each plan claims it did, incl. the page-budget decision)
  </read_first>
  <files>.planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md</files>
  <action>
    Run the four audits and record results in a new `12-VERIFICATION.md`:
    (1) CLEAN BUILD: run scripts/build_paper.ps1 -Clean; capture exit code and the "Output written on main.pdf (N pages" line from paper/main.log; grep main.log for "Citation .* undefined" and "Reference .* undefined" (must be none); record N and confirm N is within the workshop page limit recorded by Plan 02. Also confirm the standalone artifact still compiles (latexmk on paper/sota_comparison_full.tex, or document toolchain-absent per Plan 01).
    (2) NUMBER TRACEABILITY: extract every UCF AUC / XD AP value cited in BOTH paper/main.tex (the new table + Section 2 numbers) AND paper/sota_comparison_full.tex, and for each build a row in a Traceability table: Number | Where cited | Method | Matches a VERIFIED value in 12-RESEARCH-sota.md? (Section 1/2/8 line) | RE-VERIFY-flagged?. Any number that does NOT match a verified value, or is on the Section 6 re-verify list but NOT `% RE-VERIFY`-flagged, is a FAIL.
    (3) OVERCLAIM SCAN: case-insensitive grep across paper/main.tex (the diff region), paper/sota_comparison_full.tex for the overclaim tokens (see notes). Record every hit; each must be either zero or an explicit negation -- otherwise FAIL.
    (4) HEADLINE INTEGRITY: confirm UCF 82.5 / XD 78.7 / 68.8 / 40.8 / 81.2 / 74.6 / 82.4 / 76.8 are present and that Tables 1-3 in main.tex are unchanged vs git HEAD before this phase (git diff on the table line ranges shows only additive new-table/new-prose/new-bib changes, no edits to existing result cells).
    Write 12-VERIFICATION.md with: overall PASS/FAIL, the four audit sections, the Traceability table, and a copy of the Section 6 re-verify checklist annotated with which items were cited (and therefore need the student's manual re-check) vs not cited. If anything FAILs, list each defect with file:line and the exact fix so a gap-closure plan can act.
  </action>
  <verify>
    <automated>cd D:/ViolenceCC && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_paper.ps1 -Clean > build_log.txt 2>&1; $code = $LASTEXITCODE; $undef = Select-String -Path paper/main.log -Pattern 'Citation .* undefined','Reference .* undefined' -Quiet; $auditExists = Test-Path .planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md; if (($code -eq 0) -and (-not $undef) -and $auditExists) { exit 0 } else { Write-Output "exit=$code undef=$undef audit=$auditExists"; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - build_paper.ps1 -Clean exits 0; paper/main.pdf produced; main.log has NO "Citation ... undefined" and NO "Reference ... undefined".
    - paper/sota_comparison_full.tex compiles standalone (or toolchain-absence documented).
    - 12-VERIFICATION.md exists and contains a section literally titled with "Traceability" and a row for every cited UCF/XD number.
    - Every number in the Traceability table is marked as matching a VERIFIED value in 12-RESEARCH-sota.md (or, if on the Section 6 re-verify list, is `% RE-VERIFY`-flagged in the LaTeX AND carried into the audit's re-verify checklist). Zero un-traceable numbers.
    - Overclaim scan section lists every token hit; total disallowed hits = 0 (allowed negations listed explicitly).
    - Headline-integrity section confirms 82.5 / 78.7 / 68.8 / 40.8 / 81.2 / 74.6 / 82.4 / 76.8 all present and existing Tables 1-3 cells unchanged vs git.
    - Overall verdict line is PASS (or FAIL with a precise defect list including file:line and fix).
  </acceptance_criteria>
  <done>12-VERIFICATION.md written with a PASS verdict (clean build within page limit, full number-traceability, zero overclaim, headlines intact), or a precise FAIL defect list for gap closure.</done>
</task>

</tasks>

<verification>
- Clean rebuild exits 0, no undefined citations, page count within limit.
- Every cited number in main.tex and sota_comparison_full.tex traces to a VERIFIED row in 12-RESEARCH-sota.md or is RE-VERIFY-flagged.
- Overclaim scan = zero disallowed hits across all deliverables.
- Headlines 82.5 / 78.7 and single-stream baselines unchanged; existing Tables 1-3 untouched.
- 12-VERIFICATION.md captures all four audits + the annotated re-verify checklist.
</verification>

<success_criteria>
The phase goal is provably met: an honest, fully-verified SOTA comparison ships in both the paper and the standalone artifact, with zero overclaim, every number traceable (and the student's manual re-check items enumerated), a clean within-budget paper build, and untouched headlines.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deliverables -> publication | This is the last gate before the cited numbers are considered shippable; an un-audited number or overclaim crossing here reaches the reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information disclosure (un-verified number ships) | full audit | mitigate | Number-traceability table cross-checks every cited value against 12-RESEARCH-sota.md; un-traceable -> FAIL + gap closure |
| T-12-05 | Tampering (overclaim ships) | overclaim scan | mitigate | Case-insensitive token scan across all deliverables; any disallowed hit -> FAIL |
| T-12-06 | Repudiation (headline silently changed) | headline integrity | mitigate | git diff confirms existing result cells (82.5/78.7 etc.) are byte-identical; any change -> FAIL |

No package installs -> no T-12-SC gate.
</threat_model>

<output>
Create `.planning/phases/12-sota-comparison-and-positioning/12-03-SUMMARY.md` when done (in addition to 12-VERIFICATION.md).
</output>
