---
phase: 12-sota-comparison-and-positioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/sota_comparison_full.tex
autonomous: true
requirements: [N/A-thesis-enrichment]
must_haves:
  truths:
    - "A self-contained LaTeX fragment exists that compiles standalone and renders the full SOTA table, the fair-subset table, and the 5 positioning paragraphs"
    - "Every number in both tables traces to a VERIFIED row in 12-RESEARCH-sota.md"
    - "Every comparison table carries a Setting column (E2E?/Text?/Audio?/frame-vs-video metric)"
    - "Zero overclaim language ('SOTA', 'comparable to SOTA', 'outperforms') appears anywhere in the artifact"
    - "This work's anchors (UCF 82.5 / XD 78.7 / skel 68.8/40.8 / visual-only 81.2/74.6 / SigLIP2-Giant 82.4/76.8) appear unchanged"
  artifacts:
    - path: "paper/sota_comparison_full.tex"
      provides: "Standalone, drop-in thesis fragment: full table + fair-subset table + 5 paragraphs"
      contains: "Setting"
  key_links:
    - from: "paper/sota_comparison_full.tex"
      to: ".planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md"
      via: "every tabulated number sourced from the verified artifact"
      pattern: "82\\.5|78\\.7"
---

<objective>
Produce the standalone, reusable thesis artifact that captures the verified-SOTA analysis while it is fresh: a self-contained LaTeX fragment file `paper/sota_comparison_full.tex` containing (1) the full ~20-method SOTA comparison table sorted by UCF AUC with a mandatory Setting column and per-number footnotes, (2) the 7-row fair-subset (apples-to-apples) table, and (3) the five positioning paragraphs. It is NOT a new thesis scaffold — it is a drop-in fragment for the later expanded thesis (Phase 11 deferred the full thesis manuscript).

Purpose: The professor's review gap ("the thesis lacks comparison with current SOTA") demands a full, honest, verified comparison. This artifact is the full-treatment home for it (deliverables 1-3), kept zero-GPU and sourced entirely from the verified research artifact, so the analysis is preserved verbatim for incorporation into the expanded thesis.

Output: `paper/sota_comparison_full.tex` — a fragment that ALSO compiles standalone for verification (via a thin standalone wrapper / `\documentclass{standalone}` or a self-contained check harness), with zero overclaim language and every number traceable to a VERIFIED row in 12-RESEARCH-sota.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md
@.planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md
@paper/main.tex

<notes>
- This is a WRITING/ANALYSIS phase: "tests" are LaTeX clean-compile checks, grep assertions, and number-traceability checks against 12-RESEARCH-sota.md — NOT unit tests.
- The artifact is a STANDALONE fragment. It must NOT be `\input` by main.tex (that is Plan 02's condensed table). It must NOT touch references.bib (Plan 02 owns that file). For the standalone-compile check, embed a tiny self-contained filecontents bib stub OR use plain `[N]`-style manual citation labels so the fragment compiles with zero external bib dependency. Whichever you choose, document it at the top of the file in a comment so a future thesis author knows how to wire it to the real references.bib.
- 12-RESEARCH-sota.md is the SOURCE OF RECORD for every number. Section 1 = full table; Section 2 = fair-subset table + "read of the fair subset"; Section 3 = the 5 positioning paragraphs (P1-P5, draft-ready); Section 6 = the mandatory re-verify list; Section 8 = per-method citation appendix.
- The CONTEXT guardrails (12-CONTEXT.md §Guardrails + 12-RESEARCH-sota.md §4 DO / §5 DO-NOT) are NON-NEGOTIABLE acceptance criteria.
- Metric definitions stated ONCE, prominently: UCF-Crime = frame-level ROC-AUC (%); XD-Violence = Average Precision / AUPRC (%).
- Build the fragment with `\input{figures/fig_architecture}`-style conventions NOT applicable here; mimic the booktabs style already used in main.tex (`\toprule`/`\midrule`/`\bottomrule`, `\begin{tabular}`). main.tex already loads `booktabs` and `multirow`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the full ~20-method SOTA comparison table (sorted by UCF AUC, with Setting column + footnotes)</name>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (Section 1 = full table with all rows + the 14 caveat footnotes; Section 6 = re-verify list; Section 8 = citation appendix)
    - .planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md (Guardrails block)
    - paper/main.tex lines 226-261 (existing booktabs table style to mimic: \toprule/\midrule/\bottomrule, \caption, \label)
  </read_first>
  <files>paper/sota_comparison_full.tex</files>
  <action>
    Create `paper/sota_comparison_full.tex`. Begin with a header comment block stating: (a) this is a standalone drop-in thesis fragment, NOT \input by main.tex; (b) the citation strategy chosen for the standalone compile (embedded filecontents bib stub OR manual [N] labels) and how to rewire it to the real references.bib later; (c) that every number is sourced from 12-RESEARCH-sota.md and the §6 re-verify items are flagged inline.
    Build a standalone-compilable wrapper so the fragment can be checked in isolation: either `\documentclass{standalone}` (with `varwidth`) or a minimal `article`/`acmart`-free preamble loading `booktabs`, `multirow`, `graphicx`. Guard it so the same file can be `\input` into the thesis body later (e.g. wrap the preamble+document in a `\ifdefined` standalone-check guard, OR keep the wrapper in a sibling `_check` file — document whichever pattern you use).
    State metric definitions ONCE near the top (UCF = frame-level ROC-AUC %, XD = AP %).
    Build the full table from 12-RESEARCH-sota.md Section 1: columns = Method | Year | Venue | UCF AUC | XD AP | Setting (encode E2E?/Text?/Audio?/frame-vs-video as a compact Setting cell, e.g. "Frozen, no-text, frame" / "MLLM LoRA, text, frame" / "Frozen, audio+text, frame" / "frozen, text, VIDEO-level"). Sort rows by UCF AUC descending, with the `This work` row placed at its true position (82.5) and visually marked (bold) — never as a bare head-to-head; the Setting column makes the regime explicit.
    Reproduce the per-number caveat footnotes from 12-RESEARCH-sota.md Section 1 (the †ᵃ…†ⁿ list) for every caveated value. For EVERY entry that appears on the Section 6 re-verify list, add an inline `% RE-VERIFY: <reason>` LaTeX comment next to that row so it is flagged before final citation.
    Apply attribution guardrails exactly: Sultani's XD number is Wu et al. ECCV 2020 (omit from a Sultani row or footnote it); MGFN XD row uses the I3D value 79.19 (NOT 80.11 VideoSwin); do NOT list an XD number for STPrompt or FDPN; cite EventVAD/LAVAD XD as AP (64.04 / 62.01), never the ROC-AUC; label audio-visual rows (HyperVD, PiercingEye, Ghadiya, HL-Net) as audio.
    Do NOT paste the full RESEARCH table body verbatim into this action — read it from 12-RESEARCH-sota.md Section 1 and transcribe faithfully into LaTeX.
  </action>
  <verify>
    <automated>cd D:/ViolenceCC && powershell -NoProfile -Command "Select-String -Path paper/sota_comparison_full.tex -Pattern 'Setting' -Quiet; if (Select-String -Path paper/sota_comparison_full.tex -Pattern '82\.5' -Quiet) {exit 0} else {exit 1}"</automated>
  </verify>
  <acceptance_criteria>
    - `paper/sota_comparison_full.tex` exists and the full table contains a column header literally containing `Setting`.
    - The `This work` row shows UCF `82.5` and XD `78.7` (grep both present).
    - Rows are sorted by UCF AUC descending (manual check: top row is the highest verified UCF AUC, e.g. GS-MoE 91.58 — flagged RE-VERIFY).
    - Every Section-6 re-verify entry that is tabulated has a `% RE-VERIFY` comment on its row (grep `% RE-VERIFY` returns >= 8 matches, matching the count of re-verify items actually included).
    - No `% RE-VERIFY`-free table row cites a number absent from 12-RESEARCH-sota.md Section 1/Section 8 (manual cross-check: each UCF/XD value matches a VERIFIED canonical value in the artifact).
    - MGFN XD cell = `79.19` (not 80.11); STPrompt and FDPN have NO XD number (cell = N/A or dash); EventVAD XD = `64.04`, LAVAD XD = `62.01`.
    - Zero overclaim tokens in the file (grep -i for `\bSOTA\b`, `comparable to SOTA`, `state-of-the-art`, `outperform` returns 0 in prose/caption — the phrase may appear only inside a neutral "we do NOT claim SOTA" framing if present; if so, document it).
  </acceptance_criteria>
  <done>Full ~20-method table renders in LaTeX with a Setting column, sorted by UCF AUC, This-work bolded at 82.5/78.7, all caveat footnotes present, every re-verify number flagged, attribution guardrails honored.</done>
</task>

<task type="auto">
  <name>Task 2: Add the fair-subset (apples-to-apples) table and the 5 positioning paragraphs, then verify standalone compile</name>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (Section 2 = fair-subset table (7 rows) + "Read of the fair subset" prose; Section 3 = the five positioning paragraphs P1-P5 verbatim-draft)
    - .planning/phases/12-sota-comparison-and-positioning/12-CONTEXT.md (Guardrails: TTA scoped to robustness only; no overclaim; competitiveness ONLY within the constrained frozen/visual-only/single-GPU regime)
    - paper/sota_comparison_full.tex (the file from Task 1 — extend it; do NOT re-create the preamble)
  </read_first>
  <files>paper/sota_comparison_full.tex</files>
  <action>
    Extend `paper/sota_comparison_full.tex` (do not duplicate the preamble/wrapper).
    Add the fair-subset table from 12-RESEARCH-sota.md Section 2 with EXACTLY these 7 rows: CLIP-TSA (87.58 / 82.19), UR-DMU (86.97 / 81.66), MGFN-I3D (86.98 / 79.19), Light-WVAD (84.7 / 77.3), RTFM (84.30 / 77.81), This work (82.5 / 78.7, bold), Sultani et al. (75.41 / — ). Columns = Method | Year | UCF AUC | XD AP | Why it qualifies. Include a one-line caption stating the subset definition (frozen features, no backbone fine-tuning, no text/prompt branch, visual-only RGB or RGB+skeleton, single-GPU-class head) and the metric definitions reminder. This table is also a comparison table, so its scope/Setting is captured by the caption's subset definition (acceptable in lieu of a per-row Setting column since every row is, by construction, the same setting — state this explicitly in the caption).
    Add the five positioning paragraphs from 12-RESEARCH-sota.md Section 3 (P1 deliberate-constraint frame; P2 fair-subset competitiveness; P3 orthogonal contribution; P4 reproducibility/transparency; P5 training-free ≠ low-compute). Transcribe the draft prose faithfully; keep the anchors exactly (82.5 / 78.7; skeleton 68.8/40.8; visual-only CLIP 81.2/74.6; SigLIP2-Giant 82.4/76.8; TTA mean +1.2% / up to +13.2%). Ensure P3 and P5 keep the TTA claim scoped to corrupted-AUC / robustness, never clean-headline AUC.
    Do NOT paste the full paragraph prose into this action — read P1-P5 from 12-RESEARCH-sota.md Section 3 and transcribe.
    After writing, attempt a standalone compile of the fragment to prove it is self-contained and error-free.
  </action>
  <verify>
    <automated>cd D:/ViolenceCC/paper && powershell -NoProfile -ExecutionPolicy Bypass -Command "$mk = Get-ChildItem -Path $env:LOCALAPPDATA -Recurse -Filter latexmk.exe -ErrorAction SilentlyContinue | Select-Object -First 1; if (-not $mk) { Write-Output 'NO_LATEXMK'; exit 0 }; & $mk.FullName -pdf -interaction=nonstopmode sota_comparison_full.tex | Out-Null; if (Test-Path sota_comparison_full.pdf) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - The fair-subset table has exactly 7 data rows with the methods CLIP-TSA, UR-DMU, MGFN, Light-WVAD, RTFM, This work, Sultani (grep each method name present).
    - MGFN row XD = `79.19`; CLIP-TSA XD = `82.19` (NOT 82.17, NOT 94.02); This-work row = `82.5` / `78.7` bold.
    - All five paragraphs present (grep for distinctive phrases: "deliberately constrained", "apples-to-apples"/"comparable regime", "orthogonal", "three random seeds"/"reproduc", "training-free"); each is non-empty.
    - TTA mentions in P3/P5 are scoped to corrupted-AUC/robustness (grep that "+1.2" and "+13.2" appear only in robustness context; no claim of clean-AUC improvement).
    - Standalone compile: either latexmk produces `sota_comparison_full.pdf` (exit 0, PDF exists) OR latexmk is absent and the harness prints `NO_LATEXMK` (in which case the executor MUST run the project build path `scripts/build_paper.ps1` once to confirm a TeX toolchain exists, then document that the fragment was syntax-checked). If a PDF is produced, `sota_comparison_full.log` shows NO `! ` fatal LaTeX errors and NO unresolved-reference `??` (if the manual-label citation strategy is used, there are no \cite at all, so `??` cannot occur).
    - Zero overclaim language across the whole file (re-run the overclaim grep from Task 1 over the final file).
    - Anchors unchanged: grep confirms `68.8`, `40.8`, `81.2`, `74.6`, `82.4`, `76.8` all present in the paragraphs.
  </acceptance_criteria>
  <done>Fair-subset table (7 rows) + 5 positioning paragraphs added; fragment compiles standalone (or is toolchain-verified if latexmk absent); zero overclaim; all anchors intact; TTA scoped to robustness.</done>
</task>

</tasks>

<verification>
- `paper/sota_comparison_full.tex` exists, contains both tables (Setting column on the full table; subset-definition caption on the fair-subset table) and all 5 paragraphs.
- Every tabulated number traces to a VERIFIED row in 12-RESEARCH-sota.md (Section 1/2/8); every re-verify item is `% RE-VERIFY`-flagged.
- Fragment compiles standalone to a PDF (or is toolchain-verified).
- Zero overclaim language; headline anchors 82.5 / 78.7 and all single-stream baselines unchanged.
- File does NOT modify references.bib and is NOT \input by main.tex (file ownership clean for parallel execution with Plan 02).
</verification>

<success_criteria>
The standalone thesis artifact is committed, self-contained, compiles, and every number is honest and traceable — ready to drop into the expanded thesis with no rework.
</success_criteria>

<output>
Create `.planning/phases/12-sota-comparison-and-positioning/12-01-SUMMARY.md` when done.
</output>
