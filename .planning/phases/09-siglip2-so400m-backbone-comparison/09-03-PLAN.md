---
phase: 09-siglip2-so400m-backbone-comparison
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified: []
autonomous: false
requirements:
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "SO400M ablation results for all 8 main runs (clip_only, late_fusion, gated_fusion x2 datasets + 2person x2 + clip_mean x2) are recorded in results/results-index.csv"
    - "3-seed stability runs (seeds 42, 123, 2024) for Gated Fusion on both UCF and XD with SO400M features are recorded in results/results-index.csv"
    - "All 14 SO400M runs have so400m or so400m_* cache_variant tags in results-index.csv"
  artifacts:
    - path: "results/results-index.csv"
      provides: "SO400M ablation results alongside CLIP and SigLIP2-base results"
      contains: "so400m"
  key_links:
    - from: "scripts/run_ablations.py"
      to: "results/results-index.csv"
      via: "phase9_* queue execution"
      pattern: "phase9_"
---

<objective>
Run the full SO400M ablation matrix: 8 main variant runs + 6 seed stability runs = 14 total experiments.

Purpose: Produce the AUC/AP numbers for all SO400M model variants and seed stability, completing the data needed for the three-way backbone comparison table. Uses the same evaluation harness, hyperparameters, and seed set as CLIP and SigLIP2-base ablations for controlled comparison.

Output: 14 new rows in results/results-index.csv with so400m cache_variant tags.
</objective>

<execution_context>
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-01-SUMMARY.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run Phase 9 ablation queues (14 experiments)</name>
  <read_first>
    scripts/run_ablations.py (phase9_* queue definitions to confirm they exist and reference correct configs)
  </read_first>
  <what-built>Plans 01-02 created the SO400M configs, queues, and extracted features. The ablation queues are ready to run.</what-built>
  <how-to-verify>
    Run the 6 Phase 9 queues in a visible terminal (vcc-main conda env). Each queue trains and evaluates its RunSpecs sequentially. Estimated ~5-8 min per run, ~70-112 min total for 14 runs.

    Recommended order (main variants first, then pooling ablations, then seeds):

    Step 1 - UCF main (3 runs, ~15-24 min):
    conda activate vcc-main
    python scripts/run_ablations.py --queue phase9_ucf_main

    Step 2 - XD main (3 runs, ~15-24 min):
    python scripts/run_ablations.py --queue phase9_xd_main

    Step 3 - UCF pooling ablations (2 runs, ~10-16 min):
    python scripts/run_ablations.py --queue phase9_ucf_pooling

    Step 4 - XD pooling ablations (2 runs, ~10-16 min):
    python scripts/run_ablations.py --queue phase9_xd_pooling

    Step 5 - UCF seed stability (2 runs, ~10-16 min):
    python scripts/run_ablations.py --queue phase9_ucf_seeds

    Step 6 - XD seed stability (2 runs, ~10-16 min):
    python scripts/run_ablations.py --queue phase9_xd_seeds

    After all queues complete, verify results are recorded:
    python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); so=df[df['run_name'].str.contains('so400m')]; print(f'SO400M runs: {len(so)}'); print(so[['run_name','auc','ap']].to_string())"
  </how-to-verify>
  <acceptance_criteria>
    - results/results-index.csv contains exactly 14 new rows with "so400m" in the run_name
    - Expected run_names present: ucf_clip_only_so400m_s42, ucf_late_fusion_so400m_s42, ucf_gated_fusion_so400m_s42, xd_clip_only_so400m_s42, xd_late_fusion_so400m_s42, xd_gated_fusion_so400m_s42, ucf_gated_fusion_so400m_2person_s42, xd_gated_fusion_so400m_2person_s42, ucf_gated_fusion_so400m_clip_mean_s42, xd_gated_fusion_so400m_clip_mean_s42, ucf_gated_fusion_so400m_s123, ucf_gated_fusion_so400m_s2024, xd_gated_fusion_so400m_s123, xd_gated_fusion_so400m_s2024
    - All 14 runs have non-zero AUC and AP values (no placeholder metrics)
    - No training errors or NaN losses in queue output
  </acceptance_criteria>
  <resume-signal>Paste the SO400M results summary output, or type "ablations complete" if all 14 runs finished successfully</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Features -> Model | Feature .npy files loaded from E: drive into training pipeline |
| Model -> Results CSV | Trained model evaluation metrics appended to results-index.csv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | results-index.csv | accept | Research-local CSV; integrity verified by re-runnable ablation queues |
| T-09-06 | Repudiation | Ablation results | mitigate | Fixed seeds (42, 123, 2024) ensure reproducibility; wandb disabled but results CSV is append-only |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
After user confirms ablation completion:
- Count SO400M rows in results-index.csv == 14
- Verify all AUC/AP values are non-zero and in reasonable range (0.5-1.0 for AUC, 0.3-1.0 for AP)
</verification>

<success_criteria>
- 14 SO400M ablation results recorded in results-index.csv
- Results include 3-seed stability for Gated Fusion on both datasets
- No training failures or placeholder metrics
</success_criteria>

<output>
Create .planning/phases/09-siglip2-so400m-backbone-comparison/09-03-SUMMARY.md when done
</output>
