---
phase: 09-siglip2-so400m-backbone-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified: []
autonomous: false
requirements:
  - EVAL-02

must_haves:
  truths:
    - "SO400M mean+max features exist for all UCF-Crime videos at E:/features/ucf/siglip2_so400m/ with shape [N_snippets, 2304]"
    - "SO400M mean+max features exist for all XD-Violence videos at E:/features/xd/siglip2_so400m/ with shape [N_snippets, 2304]"
    - "SO400M mean-only features exist for UCF at E:/features/ucf/siglip2_so400m_mean/ with shape [N_snippets, 1152]"
    - "SO400M mean-only features exist for XD at E:/features/xd/siglip2_so400m_mean/ with shape [N_snippets, 1152]"
  artifacts:
    - path: "E:/features/ucf/siglip2_so400m/"
      provides: "UCF SO400M mean+max features"
    - path: "E:/features/xd/siglip2_so400m/"
      provides: "XD SO400M mean+max features"
    - path: "E:/features/ucf/siglip2_so400m_mean/"
      provides: "UCF SO400M mean-only features"
    - path: "E:/features/xd/siglip2_so400m_mean/"
      provides: "XD SO400M mean-only features"
  key_links:
    - from: "scripts/extract_clip.py"
      to: "E:/features/*/siglip2_so400m/"
      via: "--backbone siglip2-so400m flag"
      pattern: "backbone.*siglip2-so400m"
---

<objective>
Extract SigLIP2 SO400M features for all UCF-Crime and XD-Violence videos.

Purpose: Produce the cached .npy feature files that the ablation configs from Plan 01 reference. Uses the same snippet boundaries and temporal alignment as CLIP and SigLIP2-base extractions for controlled comparison. Estimated ~1.1 hours total at ~152 img/s on RTX 4090 with batch_size=16.

Output: 4 feature directories populated on E: drive (siglip2_so400m and siglip2_so400m_mean for both datasets).
</objective>

<execution_context>
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Extract SO400M features for UCF-Crime and XD-Violence</name>
  <read_first>
    scripts/extract_clip.py (lines 119-134 for BACKBONE_CONFIGS to confirm siglip2-so400m entry exists)
  </read_first>
  <what-built>Plan 01 added siglip2-so400m to BACKBONE_CONFIGS. The extraction script is ready to use with --backbone siglip2-so400m.</what-built>
  <how-to-verify>
    Run these 4 extraction commands in a visible terminal (vcc-main conda env). Each produces one feature directory. Estimated total time: ~1.1 hours.

    Step 1 - UCF mean+max (~9 min):
    conda activate vcc-main
    python scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-so400m --batch-size 16
    python scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-so400m --batch-size 16

    Step 2 - UCF mean-only (~9 min):
    python scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-so400m --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-so400m --batch-size 16 --pool mean

    Step 3 - XD mean+max (~59 min):
    python scripts/extract_clip.py --dataset xd --split train --backbone siglip2-so400m --batch-size 16
    python scripts/extract_clip.py --dataset xd --split test --backbone siglip2-so400m --batch-size 16

    Step 4 - XD mean-only (~59 min):
    python scripts/extract_clip.py --dataset xd --split train --backbone siglip2-so400m --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset xd --split test --backbone siglip2-so400m --batch-size 16 --pool mean

    After extraction completes, verify feature dimensions:
    python -c "import numpy as np, glob; files=glob.glob('E:/features/ucf/siglip2_so400m/*.npy'); f=np.load(files[0]); print(f'UCF mean+max shape: {f.shape}, expect dim=2304')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/ucf/siglip2_so400m_mean/*.npy'); f=np.load(files[0]); print(f'UCF mean-only shape: {f.shape}, expect dim=1152')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/xd/siglip2_so400m/*.npy'); f=np.load(files[0]); print(f'XD mean+max shape: {f.shape}, expect dim=2304')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/xd/siglip2_so400m_mean/*.npy'); f=np.load(files[0]); print(f'XD mean-only shape: {f.shape}, expect dim=1152')"
  </how-to-verify>
  <acceptance_criteria>
    - E:/features/ucf/siglip2_so400m/ contains .npy files for all UCF videos (1728 train+test)
    - E:/features/xd/siglip2_so400m/ contains .npy files for all XD videos (4752 train+test)
    - E:/features/ucf/siglip2_so400m_mean/ contains .npy files for all UCF videos
    - E:/features/xd/siglip2_so400m_mean/ contains .npy files for all XD videos
    - UCF mean+max files have shape [N_snippets, 2304]
    - UCF mean-only files have shape [N_snippets, 1152]
    - XD mean+max files have shape [N_snippets, 2304]
    - XD mean-only files have shape [N_snippets, 1152]
    - No extraction errors or zero-vector warnings in output
  </acceptance_criteria>
  <resume-signal>Paste the 4 dimension verification outputs, or type "extracted" if all look correct</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model download -> local cache | First-time SO400M weight download from HuggingFace Hub (~2.3 GB) |
| Video frames -> features | Frame decoding and model inference on local GPU |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Model weights | accept | open-clip uses HuggingFace Hub with checksum verification; model already verified in research phase |
| T-09-04 | Denial of Service | GPU VRAM | accept | Batch_size=16 uses ~4.6 GB of 24 GB; safe margin |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
After user confirms extraction:
- Count files in each directory matches expected video counts
- Spot-check feature dimensions (2304 for mean+max, 1152 for mean-only)
</verification>

<success_criteria>
- All 4 feature directories populated with correct-dimension .npy files
- No missing videos or extraction errors
</success_criteria>

<output>
Create .planning/phases/09-siglip2-so400m-backbone-comparison/09-02-SUMMARY.md when done
</output>
