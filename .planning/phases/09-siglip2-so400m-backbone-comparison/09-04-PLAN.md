---
phase: 09-siglip2-so400m-backbone-comparison
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - scripts/generate_phase9_charts.py
autonomous: true
requirements:
  - EVAL-04

must_haves:
  truths:
    - "A three-way comparison table (CLIP vs SigLIP2 ViT-B/16-256 vs SigLIP2 SO400M) exists as CSV for all ablation rows on both datasets"
    - "Delta columns show CLIP-vs-SO400M and SigLIP2-vs-SO400M differences for AUC and AP"
    - "3-seed stability table includes SO400M alongside CLIP and SigLIP2 with mean and std"
    - "Grouped bar charts visualize the three-way comparison for AUC and AP"
  artifacts:
    - path: "scripts/generate_phase9_charts.py"
      provides: "Three-way comparison table and chart generation"
      contains: "SO400M"
    - path: "results/phase9_charts/backbone_comparison_3way.csv"
      provides: "Three-way comparison CSV"
    - path: "results/phase9_charts/seed_stability_3way.csv"
      provides: "Three-way seed stability CSV"
    - path: "results/phase9_charts/comparison_auc_3way.png"
      provides: "AUC comparison bar chart"
    - path: "results/phase9_charts/comparison_ap_3way.png"
      provides: "AP comparison bar chart"
    - path: "results/phase9_charts/seed_stability_3way.png"
      provides: "Seed stability comparison chart"
  key_links:
    - from: "scripts/generate_phase9_charts.py"
      to: "results/results-index.csv"
      via: "pd.read_csv for run_name lookup"
      pattern: "results-index.csv"
    - from: "scripts/generate_phase9_charts.py"
      to: "results/phase9_charts/"
      via: "OUT_DIR output path"
      pattern: "phase9_charts"
---

<objective>
Create the three-way backbone comparison analysis script and generate thesis-ready tables and charts.

Purpose: Produce the final CLIP vs SigLIP2 ViT-B/16-256 vs SigLIP2 SO400M comparison table, delta analysis, seed stability table, and grouped bar charts. This is the thesis deliverable that demonstrates backbone sensitivity analysis across both datasets. Leave Phase 8's generate_phase8_charts.py untouched -- create a new script for the three-way comparison.

Output: generate_phase9_charts.py script, 2 CSV tables (comparison + seed stability), 3 PNG charts (AUC bars, AP bars, seed stability).
</objective>

<execution_context>
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-RESEARCH.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-03-SUMMARY.md
@scripts/generate_phase8_charts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three-way comparison script generate_phase9_charts.py</name>
  <files>scripts/generate_phase9_charts.py</files>
  <read_first>
    scripts/generate_phase8_charts.py (full file, 243 lines -- the structural analog),
    results/results-index.csv (first 5 lines for column names and format)
  </read_first>
  <action>
    Create scripts/generate_phase9_charts.py following the Phase 8 script structure exactly, extended from two-column to three-column comparison. Key structural changes:

    1. Header: Update docstring to "Phase 9 CLIP vs SigLIP2 ViT-B/16-256 vs SigLIP2 SO400M Three-Way Backbone Comparison"

    2. OUT_DIR: Set to PROJECT_ROOT / "results" / "phase9_charts"

    3. COMPARISON_MAP: Extend tuples from 2-element (clip_name, sig_name) to 3-element (clip_name, sig_name, so400m_name). The SO400M run_names follow the pattern {dataset}_{variant}_so400m_s42. Map entries:
       - ("ucf", "Skeleton Only"): ("ucf_skeleton_only_s42", None, None) -- backbone-independent
       - ("ucf", "CLIP/SigLIP2/SO400M Only"): ("ucf_clip_only_s42", "ucf_clip_only_siglip2_s42", "ucf_clip_only_so400m_s42")
       - ("ucf", "Late Fusion"): ("ucf_late_fusion_s42", "ucf_late_fusion_siglip2_s42", "ucf_late_fusion_so400m_s42")
       - ("ucf", "Gated Fusion"): ("ucf_gated_fusion_s42", "ucf_gated_fusion_siglip2_s42", "ucf_gated_fusion_so400m_s42")
       - ("ucf", "GF 2-Person"): ("ucf_gated_fusion_2person_s42", "ucf_gated_fusion_siglip2_2person_s42", "ucf_gated_fusion_so400m_2person_s42")
       - ("ucf", "GF Mean-Only"): ("ucf_gated_fusion_clip_mean_s42", "ucf_gated_fusion_siglip2_clip_mean_s42", "ucf_gated_fusion_so400m_clip_mean_s42")
       - Same pattern for XD (6 entries with "xd" prefix and XD run_names)

    4. SEED_MAP: Extend from 2-element to 3-element tuples. Add SO400M seed run_names:
       - ("ucf", 42): (..., "ucf_gated_fusion_so400m_s42")
       - ("ucf", 123): (..., "ucf_gated_fusion_so400m_s123")
       - ("ucf", 2024): (..., "ucf_gated_fusion_so400m_s2024")
       - ("xd", 42): (..., "xd_gated_fusion_so400m_s42")
       - ("xd", 123): (..., "xd_gated_fusion_so400m_s123")
       - ("xd", 2024): (..., "xd_gated_fusion_so400m_s2024")

    5. build_comparison_table: Add SO400M_AUC, SO400M_AP columns. Add delta columns: Delta_CLIP_SO400M_AUC, Delta_CLIP_SO400M_AP, Delta_SigLIP2_SO400M_AUC, Delta_SigLIP2_SO400M_AP. Follow the same idx.loc lookup pattern as Phase 8, with a third branch for so400m_name.

    6. build_seed_table: Add SO400M_AUC, SO400M_AP columns alongside CLIP and SigLIP2. Compute mean/std across 3 seeds for each backbone on each dataset.

    7. plot_comparison_bars: Change from 2 grouped bars to 3 grouped bars per x-tick. Use bar width ~0.25 (was 0.35). Three colors: "#3B82F6" (CLIP), "#EF4444" (SigLIP2 ViT-B/16), "#10B981" (SO400M). Labels: "CLIP ViT-B/16", "SigLIP2 ViT-B/16", "SigLIP2 SO400M". Bar positions: x - width, x, x + width.

    8. plot_seed_stability: Extend from 2 backbone bars to 3. Same color scheme as comparison bars. Show mean+std error bars for each backbone.

    9. main(): Output files:
       - backbone_comparison_3way.csv
       - seed_stability_3way.csv
       - comparison_auc_3way.png
       - comparison_ap_3way.png
       - seed_stability_3way.png

    Use identical import block, style constants (DPI, FIG_STD, etc.), sns.set_theme, and PROJECT_ROOT/sys.path pattern from Phase 8 script. Use matplotlib Agg backend.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import ast; ast.parse(open('scripts/generate_phase9_charts.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_phase9_charts.py exists and parses without syntax errors
    - COMPARISON_MAP has 12 entries (6 UCF + 6 XD) with 3-element tuples
    - SEED_MAP has 6 entries with 3-element tuples
    - build_comparison_table produces columns: Dataset, Variant, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP, plus 4 delta columns
    - plot_comparison_bars renders 3 grouped bars per x-tick (not 2)
    - OUT_DIR is results/phase9_charts (not phase8_charts)
  </acceptance_criteria>
  <done>Three-way comparison script created with correct structure and data mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Generate three-way comparison tables and charts</name>
  <files>
    results/phase9_charts/backbone_comparison_3way.csv,
    results/phase9_charts/seed_stability_3way.csv,
    results/phase9_charts/comparison_auc_3way.png,
    results/phase9_charts/comparison_ap_3way.png,
    results/phase9_charts/seed_stability_3way.png
  </files>
  <read_first>
    scripts/generate_phase9_charts.py (full file to confirm structure before execution),
    results/results-index.csv (to verify SO400M run_names are present)
  </read_first>
  <action>
    1. Verify all 14 SO400M run_names exist in results/results-index.csv before running the script. If any are missing, report which ones.

    2. Run the chart generation script:
       C:/Anaconda/envs/vcc-main/python.exe scripts/generate_phase9_charts.py

    3. Verify outputs exist in results/phase9_charts/:
       - backbone_comparison_3way.csv (12 rows: 6 UCF + 6 XD variants)
       - seed_stability_3way.csv (4 rows: 2 datasets x 2 stats (mean, std))
       - comparison_auc_3way.png
       - comparison_ap_3way.png
       - seed_stability_3way.png

    4. Print the three-way comparison table to console for review.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/phase9_charts/backbone_comparison_3way.csv'); assert 'SO400M_AUC' in df.columns; assert len(df)>=10; print(df.to_string())"</automated>
  </verify>
  <acceptance_criteria>
    - backbone_comparison_3way.csv has CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, SO400M_AUC, SO400M_AP columns
    - All non-Skeleton-Only rows have populated SO400M columns (no NaN for SO400M metrics where SO400M was run)
    - seed_stability_3way.csv has mean and std for all 3 backbones on both datasets
    - All 3 PNG chart files exist and are non-empty (file size > 10 KB)
    - Delta columns correctly show arithmetic differences between backbone pairs
  </acceptance_criteria>
  <done>Three-way comparison tables and charts generated. Phase 9 deliverables complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| results-index.csv -> analysis script | Script reads CSV and produces derived tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | Charts/CSVs | accept | Research outputs contain only aggregate metrics (AUC/AP), no sensitive data |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- backbone_comparison_3way.csv exists with all 3 backbone columns populated
- seed_stability_3way.csv exists with mean/std for all 3 backbones
- 3 PNG chart files exist in results/phase9_charts/
- Print comparison table shows reasonable AUC/AP values for SO400M
</verification>

<success_criteria>
- Three-way comparison table with CLIP, SigLIP2, and SO400M metrics for all variants on both datasets
- Seed stability table with 3-seed mean+std for Gated Fusion across all 3 backbones
- Bar chart PNGs suitable for thesis inclusion
</success_criteria>

<output>
Create .planning/phases/09-siglip2-so400m-backbone-comparison/09-04-SUMMARY.md when done
</output>
