---
phase: 09-siglip2-so400m-backbone-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_clip.py
  - configs/clip_only_so400m.yaml
  - configs/clip_only_xd_so400m.yaml
  - configs/late_fusion_so400m.yaml
  - configs/late_fusion_xd_so400m.yaml
  - configs/gated_fusion_so400m.yaml
  - configs/gated_fusion_xd_so400m.yaml
  - configs/gated_fusion_so400m_2person.yaml
  - configs/gated_fusion_xd_so400m_2person.yaml
  - configs/gated_fusion_so400m_clip_mean.yaml
  - configs/gated_fusion_xd_so400m_clip_mean.yaml
  - scripts/run_ablations.py
  - tests/test_extract_siglip2.py
  - tests/test_models.py
  - tests/test_run_ablations.py
autonomous: true
requirements:
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "BACKBONE_CONFIGS contains siglip2-so400m with embed_dim 1152"
    - "10 SO400M YAML configs exist with correct clip_dim values (2304 for mean+max, 1152 for mean-only)"
    - "6 phase9_* queues exist in run_ablations.py with 14 total RunSpecs"
    - "All tests pass including updated assertions for 3 backbones and 252 total run_names"
  artifacts:
    - path: "scripts/extract_clip.py"
      provides: "siglip2-so400m backbone config entry"
      contains: "siglip2-so400m"
    - path: "configs/gated_fusion_so400m.yaml"
      provides: "SO400M gated fusion UCF config"
      contains: "clip_dim: 2304"
    - path: "configs/gated_fusion_so400m_clip_mean.yaml"
      provides: "SO400M mean-only UCF config"
      contains: "clip_dim: 1152"
    - path: "scripts/run_ablations.py"
      provides: "Phase 9 ablation queues"
      contains: "phase9_ucf_main"
    - path: "tests/test_extract_siglip2.py"
      provides: "SO400M config validation tests"
      contains: "siglip2-so400m"
    - path: "tests/test_models.py"
      provides: "clip_dim=2304 forward-pass tests"
      contains: "test_clip_dim_2304"
    - path: "tests/test_run_ablations.py"
      provides: "Phase 9 queue assertions"
      contains: "phase9_ucf_main"
  key_links:
    - from: "configs/gated_fusion_so400m.yaml"
      to: "E:/features/ucf/siglip2_so400m"
      via: "clip_features path"
      pattern: "clip_features.*siglip2_so400m"
    - from: "scripts/run_ablations.py"
      to: "configs/*_so400m*.yaml"
      via: "RunSpec config_path"
      pattern: "configs/.*so400m"
---

<objective>
Add SigLIP2 SO400M backbone support to the extraction, training, and ablation infrastructure.

Purpose: Prepare all code, configs, queues, and tests so that feature extraction and ablation runs can proceed without any code changes. This is entirely mechanical -- one new BACKBONE_CONFIGS entry, 10 YAML configs mirroring Phase 8 SigLIP2 configs with SO400M dimensions (clip_dim 2304/1152), 6 phase9_* queues with 14 RunSpecs, and test updates for the third backbone.

Output: Modified extract_clip.py, run_ablations.py, 3 test files; 10 new YAML config files. All tests green.
</objective>

<execution_context>
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-RESEARCH.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SO400M backbone config, create 10 YAML configs, add 6 phase9 queues</name>
  <files>
    scripts/extract_clip.py,
    configs/clip_only_so400m.yaml,
    configs/clip_only_xd_so400m.yaml,
    configs/late_fusion_so400m.yaml,
    configs/late_fusion_xd_so400m.yaml,
    configs/gated_fusion_so400m.yaml,
    configs/gated_fusion_xd_so400m.yaml,
    configs/gated_fusion_so400m_2person.yaml,
    configs/gated_fusion_xd_so400m_2person.yaml,
    configs/gated_fusion_so400m_clip_mean.yaml,
    configs/gated_fusion_xd_so400m_clip_mean.yaml,
    scripts/run_ablations.py
  </files>
  <read_first>
    scripts/extract_clip.py (lines 119-134 for BACKBONE_CONFIGS dict),
    configs/clip_only_siglip2.yaml (full analog for clip_only_so400m.yaml),
    configs/clip_only_xd_siglip2.yaml (full analog),
    configs/late_fusion_siglip2.yaml (full analog),
    configs/late_fusion_xd_siglip2.yaml (full analog),
    configs/gated_fusion_siglip2.yaml (full analog for gated_fusion_so400m.yaml),
    configs/gated_fusion_xd_siglip2.yaml (full analog),
    configs/gated_fusion_siglip2_2person.yaml (full analog),
    configs/gated_fusion_xd_siglip2_2person.yaml (full analog),
    configs/gated_fusion_siglip2_clip_mean.yaml (full analog),
    configs/gated_fusion_xd_siglip2_clip_mean.yaml (full analog),
    scripts/run_ablations.py (lines 185-213 for Phase 8 queue pattern)
  </read_first>
  <action>
    1. In scripts/extract_clip.py, add a third entry to BACKBONE_CONFIGS after the siglip2-base entry (before the closing brace on line 134):
       - Key: "siglip2-so400m"
       - model_name: "ViT-SO400M-16-SigLIP2-256"
       - pretrained: "webli"
       - embed_dim: 1152
       - output_subdir: "siglip2_so400m"
       - mean_subdir: "siglip2_so400m_mean"

    2. Create 10 YAML config files. Each mirrors its Phase 8 siglip2 analog exactly, with these substitutions:
       - Header comment: "Phase 9" instead of "Phase 8", "SO400M" instead of "SigLIP2"
       - clip_features path: replace "siglip2" with "siglip2_so400m" (or "siglip2_mean" with "siglip2_so400m_mean" for mean-only configs)
       - clip_dim: 2304 for mean+max configs (clip_only, late_fusion, gated_fusion, 2person); 1152 for mean-only configs (clip_mean)
       - wandb tags: replace "phase8" with "phase9" and "siglip2" with "so400m"

       Config dimension mapping:
       - clip_only_so400m.yaml: clip_dim=2304, clip_features=E:/features/ucf/siglip2_so400m
       - clip_only_xd_so400m.yaml: clip_dim=2304, clip_features=E:/features/xd/siglip2_so400m
       - late_fusion_so400m.yaml: clip_dim=2304, clip_features=E:/features/ucf/siglip2_so400m
       - late_fusion_xd_so400m.yaml: clip_dim=2304, clip_features=E:/features/xd/siglip2_so400m
       - gated_fusion_so400m.yaml: clip_dim=2304, clip_features=E:/features/ucf/siglip2_so400m
       - gated_fusion_xd_so400m.yaml: clip_dim=2304, clip_features=E:/features/xd/siglip2_so400m
       - gated_fusion_so400m_2person.yaml: clip_dim=2304, clip_features=E:/features/ucf/siglip2_so400m, skel_dim=512, skeleton_features=E:/features/ucf/skeleton_2person, skel_agg=concat
       - gated_fusion_xd_so400m_2person.yaml: clip_dim=2304, clip_features=E:/features/xd/siglip2_so400m, skel_dim=512, skeleton_features=E:/features/xd/skeleton_2person, skel_agg=concat
       - gated_fusion_so400m_clip_mean.yaml: clip_dim=1152, clip_features=E:/features/ucf/siglip2_so400m_mean
       - gated_fusion_xd_so400m_clip_mean.yaml: clip_dim=1152, clip_features=E:/features/xd/siglip2_so400m_mean

       All other fields (seed, dataset, shared_dim, head_hidden, dropout, train hyperparameters) remain identical to the analog. Do NOT change shared_dim from 256 or proj_dim from 512.

    3. In scripts/run_ablations.py, add 6 phase9_* queues after the Phase 8 queues (before the closing brace of the QUEUES dict on line 213). Follow the exact Phase 8 pattern with cache_variant substitutions:
       - phase9_ucf_main: 3 RunSpecs (clip_only/late_fusion/gated_fusion, seed 42, so400m configs, cache_variant "so400m")
       - phase9_xd_main: 3 RunSpecs (same variants, XD configs, cache_variant "so400m")
       - phase9_ucf_pooling: 2 RunSpecs (2person as "so400m_2person", clip_mean as "so400m_clip_mean")
       - phase9_xd_pooling: 2 RunSpecs (same pattern, XD configs)
       - phase9_ucf_seeds: 2 RunSpecs (seeds 123, 2024, gated_fusion_so400m.yaml, cache_variant "so400m")
       - phase9_xd_seeds: 2 RunSpecs (seeds 123, 2024, gated_fusion_xd_so400m.yaml, cache_variant "so400m")

       Add a comment "# Phase 9 SigLIP2 SO400M backbone comparison queues" before the first queue.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import sys; sys.path.insert(0,'.'); from scripts.extract_clip import BACKBONE_CONFIGS; assert 'siglip2-so400m' in BACKBONE_CONFIGS; cfg=BACKBONE_CONFIGS['siglip2-so400m']; assert cfg['embed_dim']==1152; assert cfg['model_name']=='ViT-SO400M-16-SigLIP2-256'; print('BACKBONE_CONFIGS OK')"</automated>
  </verify>
  <acceptance_criteria>
    - BACKBONE_CONFIGS has exactly 3 keys: "clip-vit-b-16", "siglip2-base", "siglip2-so400m"
    - siglip2-so400m entry: model_name="ViT-SO400M-16-SigLIP2-256", pretrained="webli", embed_dim=1152, output_subdir="siglip2_so400m", mean_subdir="siglip2_so400m_mean"
    - 10 new YAML files exist in configs/ with correct clip_dim values (2304 for 8 mean+max configs, 1152 for 2 mean-only configs)
    - No YAML config references "siglip2/" or "siglip2_mean/" paths (must use "siglip2_so400m/" and "siglip2_so400m_mean/")
    - 6 phase9_* queues exist in run_ablations.py with 14 total RunSpecs
    - All configs retain shared_dim=256, proj_dim=512, same train hyperparameters as Phase 8 analogs
  </acceptance_criteria>
  <done>SO400M backbone entry, 10 YAML configs, and 6 ablation queues are created. BACKBONE_CONFIGS import succeeds with correct dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Update test files for SO400M backbone assertions</name>
  <files>
    tests/test_extract_siglip2.py,
    tests/test_models.py,
    tests/test_run_ablations.py
  </files>
  <read_first>
    tests/test_extract_siglip2.py (full file, 76 lines),
    tests/test_models.py (lines 72-88 for CLIPProj 1536 tests, line 162 for LateFusion 1536, lines 216-222 for GatedFusion 1536),
    tests/test_run_ablations.py (lines 32-96 for queue assertions, line 96 for total count 238)
  </read_first>
  <action>
    1. In tests/test_extract_siglip2.py:
       a. Update test_backbone_configs_keys (line 20): change expected set to {"clip-vit-b-16", "siglip2-base", "siglip2-so400m"}
       b. Add test_so400m_config_values function (after test_siglip2_config_values): assert model_name=="ViT-SO400M-16-SigLIP2-256", pretrained=="webli", embed_dim==1152, output_subdir=="siglip2_so400m", mean_subdir=="siglip2_so400m_mean"
       c. Add SO400M branch to test_expected_dim_mean_pool: elif backbone_key == "siglip2-so400m": assert expected_dim == 1152
       d. Add SO400M branch to test_expected_dim_meanmax_pool: elif backbone_key == "siglip2-so400m": assert expected_dim == 2304
       e. Add test_so400m_output_subdir function: assert output_subdir=="siglip2_so400m" and mean_subdir=="siglip2_so400m_mean"
       f. Update module docstring to mention SO400M alongside CLIP and SigLIP2

    2. In tests/test_models.py, add 4 new test functions following the Phase 8 pattern:
       a. test_clip_dim_2304: CLIPProj(clip_dim=2304), input torch.randn(2, 32, 2304), assert output shape (2, 32) and values in [0, 1]
       b. test_clip_dim_2304_projection_layer: CLIPProj(clip_dim=2304), assert clip_proj.in_features==2304, clip_proj.out_features==512
       c. test_late_fusion_clip_dim_2304: LateFusion(skel_dim=256, clip_dim=2304, proj_dim=512, alpha="equal"), input skel randn(2,32,256) + clip randn(2,32,2304), assert output shape (2,32) and [0,1]
       d. test_gated_fusion_clip_dim_2304: GatedFusion(skel_dim=256, clip_dim=2304, shared_dim=256), input skel randn(2,32,256) + clip randn(2,32,2304), assert output shape (2,32) and [0,1]

    3. In tests/test_run_ablations.py:
       a. Add phase9 queue names to test_help_has_expected_queues tuple (line 44-46): "phase9_ucf_main", "phase9_xd_main", "phase9_ucf_pooling", "phase9_xd_pooling", "phase9_ucf_seeds", "phase9_xd_seeds"
       b. Add phase9 length assertions to test_queue_definitions (after line 91): assert len for each of the 6 phase9 queues (3, 3, 2, 2, 2, 2)
       c. Add spot check: QUEUES["phase9_ucf_main"][2].run_name == "ucf_gated_fusion_so400m_s42"
       d. Add spot check: QUEUES["phase9_xd_seeds"][0].run_name == "xd_gated_fusion_so400m_s123"
       e. Update total unique run_names assertion (line 96): change 238 to 252
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_extract_siglip2.py tests/test_models.py tests/test_run_ablations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_extract_siglip2.py passes with SO400M config validation (3 backbone keys, 1152 embed_dim, correct subdirs)
    - pytest tests/test_models.py passes with clip_dim=2304 forward-pass tests for CLIPProj, LateFusion, GatedFusion
    - pytest tests/test_run_ablations.py passes with 6 phase9 queue assertions and total unique run_names == 252
    - No existing tests broken (CLIP and SigLIP2-base assertions unchanged)
  </acceptance_criteria>
  <done>All 3 test files updated and passing. SO400M backbone validated at config, model, and queue levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config -> Model | YAML clip_dim must match feature .npy shape at runtime |
| Config -> Filesystem | clip_features paths must point to correct SO400M subdirectories, not SigLIP2-base dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | YAML configs | mitigate | Test assertions verify clip_dim values (2304/1152); feature path validated by dataset loader shape check at training time |
| T-09-02 | Information Disclosure | Feature paths | accept | Feature files are local research data on E: drive, no PII |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies already verified in prior phases |
</threat_model>

<verification>
Run the full test suite:
C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_extract_siglip2.py tests/test_models.py tests/test_run_ablations.py -x -q

All tests must pass including new SO400M assertions.
</verification>

<success_criteria>
- BACKBONE_CONFIGS has 3 entries with correct SO400M dimensions
- 10 YAML configs exist with correct clip_dim and feature paths
- 6 phase9_* queues with 14 RunSpecs in run_ablations.py
- All tests pass (extract, models, ablations)
</success_criteria>

<output>
Create .planning/phases/09-siglip2-so400m-backbone-comparison/09-01-SUMMARY.md when done
</output>
