# Phase 4 Plan 06 — HUMAN-UAT Gate Log

Tracks empirical gates that cannot be unit-tested. Each entry: context, automated verify, human judgment.

---

## Task 1 — Pooling cache re-extraction (completed)

**Status:** PASS

- `E:/features/ucf/skeleton_2person/` — 1728 `.npy` files, shape `[N, 2, 256]` float32.
  - Train split: 1254 existing (skip) + 0 new (pre-recovered from partial extraction on 2026-04-15).
  - Val split: 220 processed + 22 failed (video too short, `< 64 frames`).
  - Test split: 254 processed + 36 failed (video too short, `< 64 frames`).
  - Total coverage matches baseline skeleton/: 1728 / 1728 video IDs identical.
- `E:/features/ucf/clip_mean/` — 1728 `.npy` files, shape `[N, 512]` float32.
  - Train split: 1254 processed + 114 failed (video too short).
  - Val split: 220 processed + 22 failed.
  - Test split: 254 processed + 36 failed.
  - Total coverage matches baseline clip/: 1728 / 1728 video IDs identical.
- Verifiers:
  - `scripts/verify_pooling_caches.py --cache clip_mean` → PASS: 1728, FAIL: 0, exit 0.
  - `scripts/verify_pooling_caches.py --cache skeleton_2person` → PASS: 1728, FAIL: 0, exit 0.

---

## EVAL-01 (RTFM XD-I3D) Gate — BLOCKED by Architectural Gap

**Status:** HALTED — Rule 4 architectural deviation surfaced

**Config:** `configs/rtfm_i3d.yaml`
**Run dir:** `results/xd_i3d_rtfm_i3d_s42/` (partial, removed for clean retry)
**AP:** not measured (training failed at loader-construction time)
**Gate:** >= 0.7681 — **CANNOT EVALUATE**

### What happened

1. **Preflight** (`scripts/wandb_preflight.py`) exited 2 — no `WANDB_API_KEY` and no `~/.netrc`.
2. **Rule 3 fallback applied** (plan Step A Option C): set `wandb.mode: disabled` in `configs/rtfm_i3d.yaml` + passed `--no-preflight` to `run_ablations.py`.
3. **Training subprocess failed with `KeyError: 'skeleton_features'`** in `src/data/loaders.py:104`.

### Architectural root cause

Plan 04-03's SUMMARY claims D-36 was implemented: "`train.py + evaluate.py dispatch via cfg['dataset'] == 'xd_i3d' → I3DFeatureDataset`".

**Empirical finding:** This dispatch does NOT exist in code.

- `src/train.py` — no `xd_i3d` branch; the train loop unconditionally consumes `nor_batch["skel"]`, `nor_batch["clip"]`, `nor_batch["mask"]` (MIL skel+clip contract from Phase 3).
- `src/data/loaders.py:102-113` — `build_dataloaders()` unconditionally constructs `MILFeatureDataset(skel_dir=paths["skeleton_features"], clip_dir=paths["clip_features"], ...)`. Since `configs/rtfm_i3d.yaml` intentionally omits `skeleton_features` / `clip_features` (per D-36), this raises `KeyError`.
- `src/data/i3d_dataset.I3DFeatureDataset` IS implemented, but is not wired into the training path. Only `src/eval/test_loader.py:103-112` imports it.
- `src/evaluate.py:172-174` — the xd_i3d label-construction branch is a documented stub ("Plan 04-03 will wire the xd_i3d label construction from the Wu et al. annotations"). All labels defaulted to zero.

### Why this is Rule 4, not Rule 1/2/3

The fix requires significant structural changes to the training pipeline:

1. Add a `build_dataloaders_i3d()` (or equivalent dispatch) that constructs `I3DFeatureDataset` with a single `feature_dir` parameter. D-19 requires 5-crop expansion in train (5× effective bags) and 5-crop averaging in test, which is incompatible with `MILFeatureDataset`'s paired skel+clip structure.
2. Add a new `train_one_epoch_i3d(...)` that consumes `[B, T, 1024]` i3d tensors (or `[B*5, T, 1024]` 5-crop expanded) rather than paired skel+clip+mask.
3. Add a new `validate_i3d(...)` matching the above signature.
4. Adjust `mil_ranking_loss` call — the MIL contract and `n_normal` split still apply, but inputs are i3d-only.
5. Wire the xd_i3d label construction in `src/evaluate.py:_build_frame_arrays` using the actual Wu et al. XD-Violence annotations (not all-zero stubs), otherwise every `AUC` / `AP` value is meaningless.

This is **not** a missing column or a simple null check — it is a whole second training code path parallel to the fusion MIL pipeline, plus a ground-truth annotation wiring step.

### Proposed paths forward (researcher decision required)

**Option A: Insert a pre-Task-2 implementation plan (recommended).** Insert a new plan `04-06a` before the empirical queue to fully implement D-36 in train.py + build_dataloaders + evaluate.py's xd_i3d label construction. Estimated 1-2 days of TDD. Only after 04-06a completes does the RTFM gate become runnable.

**Option B: Drop the RTFM gate entirely and execute only the UCF-only phase 4 main ablations.** Per D-01 the full Phase 4 scope is UCF-only; the RTFM gate was retargeted to XD-I3D per D-03, but if the xd_i3d training path is incomplete, we can treat EVAL-01 as deferred-to-Phase-4b and run Tasks 3-5 (UCF main + pooling + 3-seed) immediately. Risk: the RTFM gate catches silent C4-expansion bugs and architecture-impl bugs; losing it increases the chance of shipping a broken UCF pipeline undetected.

**Option C: Rescope EVAL-01 to UCF (the PRD's original gate).** Revert D-03 back to the PRD-original gate ("RTFM baseline reproduction on UCF-Crime — target 84.30% AUC within +/-1%"). This would let the existing MILFeatureDataset-based pipeline serve the gate run. Trade-off: no RTFM XD-I3D reproduction, but Phase 4b can still re-establish it after XD skel+CLIP features finish.

**Recommendation:** Option A. The thesis's dual-modal contribution is contingent on RTFM-style MIL behaving correctly on a published benchmark; losing that gate means every downstream AUC/AP number is on shakier empirical ground. 1-2 days of TDD implementation is cheap relative to the 6-week main-results timeline.

### Task 1 deliverables unaffected

The pooling caches are extracted, verified, and ready for Tasks 4 (phase4_pooling queue). Whenever the architectural block is resolved, Task 1's output is ready.

### Deviations applied during Task 2

**[Rule 3 - Blocking] `wandb.mode: disabled` + `--no-preflight`**
- **Found:** wandb preflight exited 2 (no API key); blocks queue open.
- **Fix:** Set `wandb.mode: disabled` in `configs/rtfm_i3d.yaml` + invoked `run_ablations.py` with `--no-preflight`. Plan Step A Option C is the documented fallback path.
- **Files modified:** `configs/rtfm_i3d.yaml`.

**[Rule 4 - Architectural] xd_i3d training path not implemented**
- **Found:** `src/data/loaders.py::build_dataloaders` unconditionally reads `paths["skeleton_features"]`, raising `KeyError` for `configs/rtfm_i3d.yaml` (no skel/clip paths by design).
- **Disposition:** STOPPED. Returning to orchestrator for researcher decision between Option A (implement D-36 properly), Option B (defer RTFM gate), Option C (rescope to UCF).

---

## DECISION (2026-04-15): Option B — EVAL-01 Deferred to Phase 4b

**Selected:** Option B (drop RTFM gate from Phase 4, run UCF-only remainder).

**Rationale:**
- D-01 already scopes Phase 4 as UCF-only. Plan 04-07 already carves out Phase 4b for XD-Violence main results.
- Implementing the xd_i3d training dispatch (Option A) would cost 1-2 days of TDD — the 12-week thesis timeline is already mid-April and the main UCF results are the critical path.
- Option C (rescope to UCF RTFM) trades the cross-implementation sanity check for zero engineering but diverges from RTFM's standard I3D-on-XD benchmark.

**EVAL-01 deferred to Phase 4b.** Phase 4b will own both the XD main results work AND the RTFM XD-I3D gate. The training dispatch (new `build_dataloaders_i3d` + `train_one_epoch_i3d` + Wu et al. annotation parser) is a prerequisite that Phase 4b must implement.

**Phase 4 Success Criteria impact:**
- SC #1 (EVAL-01) → deferred (not required for Phase 4 completion)
- SC #2..#5 remain in scope and must pass per plan 04-06 Tasks 3-5

**Resume signal:** Continue to Task 3 (`run_ablations.py --queue phase4_main`) with the 4 UCF main variants at seed 42. HUMAN-UAT Checkpoint 1 is WAIVED (RTFM gate deferred).

---

## Task 3 — phase4_main queue (completed 2026-04-16)

**Status:** COMPLETE — 4 of 4 runs succeeded. Queue exit code 0.

**wandb mode:** `disabled` (Rule 3 fallback; same mechanism used in Task 2). `scripts/wandb_preflight.py` exited 2 (no API key / no ~/.netrc). Applied `wandb.mode: disabled` to all 4 UCF variant YAMLs (`configs/skeleton_only.yaml`, `configs/clip_only.yaml`, `configs/late_fusion.yaml`, `configs/gated_fusion.yaml`) and invoked `run_ablations.py --queue phase4_main --no-preflight`. Training still logs to `train_log.csv` and `eval_metrics.json` (CSV-only mode). `wandb_run_id=null` in all four eval_metrics.json.

**Per-variant results (seed 42, UCF test split, n_videos=254, n_frames=1,010,560):**

| Variant | AUC | AP | snippet_auc | video_auc |
|---|---|---|---|---|
| skeleton_only | 0.7180 | 0.1527 | 0.7301 | 0.7209 |
| clip_only | 0.8169 | 0.2234 | 0.8198 | 0.9214 |
| late_fusion | 0.7871 | 0.1874 | 0.8052 | 0.8801 |
| gated_fusion | 0.8227 | 0.2315 | 0.8300 | 0.9268 |

**Run-level errors:** None. `results/runner-errors.log` not written (queue clean).

**Empirical observations for HUMAN-UAT Checkpoint 2:**

1. **SC #2 gate (AUC >= 0.83): MISS** — `gated_fusion` AUC = 0.8227 is 0.73 percentage points below the 0.83 threshold. Documented miss; researcher judgment required (resume signal "gated fusion below target" expects the 80-83% band per plan Checkpoint 2 wording; 82.27% sits in that band).
2. **Variant ordering partial inversion:** Expected `skeleton_only < clip_only < late_fusion <= gated_fusion`. Observed `skeleton_only(0.7180) < late_fusion(0.7871) < clip_only(0.8169) < gated_fusion(0.8227)`. **Late Fusion falls below CLIP-only by ~3 points.** This matches D-08's explicit contingency ("switch to 'learned' only if equal underperforms"). Equal-weighted averaging drags the stronger CLIP branch down with the weaker skeleton branch; recommend switching Late Fusion to `alpha: learned` and re-running, OR documenting this as the thesis's empirical justification for Gated Fusion over Late Fusion.
3. **Gated Fusion is the top variant (as expected)** but the margin over `clip_only` is only +0.58 points (0.8227 vs 0.8169). Fusion architecture is working but gains are modest. Possible Phase 5 levers: threshold calibration, LayerNorm placement sweep (D-07), shared_dim sweep.
4. **Per-category breakdown (gated_fusion):** `Fighting` AUC = 0.9550 and `Assault` AUC = 0.9846 — both far above the full-test AUC of 0.8227, confirming the model's violence-specific signal is strong (EVAL-04 / SC #5 preview: PASS). The drop in full-test AUC comes from non-violence categories (`Arrest` 0.817, `Shoplifting` 0.877, `RoadAccidents` 0.888), which are noisier MIL targets.
5. **snippet_auc > auc in every variant** — consistent with TRN/EVAL contract (snippet_auc is computed before frame-level expansion, so the extra variance from frame-level reconstruction lowers the headline AUC slightly).
6. **config_hash matches between runs only when configs match** — all four hashes differ as expected (one per variant YAML).

**Artifacts verified:**
- `results/ucf_skeleton_only_s42/.done`
- `results/ucf_clip_only_s42/.done`
- `results/ucf_late_fusion_s42/.done`
- `results/ucf_gated_fusion_s42/.done`
- `results/ucf_gated_fusion_s42/best_model.pth` (Phase 5 TTA handoff)
- `results/ucf_gated_fusion_s42/per_category.csv`
- `results/results-index.csv` — 4 rows (header + 4; RTFM row absent per Option B deferral)

**Timing:** Queue wall-clock ~2h (first epoch 01:43:45, last epoch 02:49:38; far faster than the plan's 4-6h estimate because training is CPU-I/O-bound on the small UCF-Crime feature cache and most variants early-stopped between epoch 8-13). Per-run wall clocks: skeleton_only ~1m41s, clip_only ~1m32s, late_fusion ~1m31s, gated_fusion ~1m06s.

**HUMAN-UAT Checkpoint 2 — AWAITING RESEARCHER DECISION.**
