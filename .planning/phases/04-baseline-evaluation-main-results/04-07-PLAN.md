---
phase: 04-baseline-evaluation-main-results
plan: 07
type: execute
wave: 6
depends_on:
  - "04-06"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
threat_model: see_threat_model_section

must_haves:
  truths:
    - "ROADMAP.md has a new Phase 4b entry inserted between Phase 4 and Phase 5"
    - "Phase 4b inherits Phase 4 SC #2-#5 (adapted for XD) and does NOT repeat SC #1 (RTFM gate already passed in Phase 4 per D-03)"
    - "Phase 4b is explicitly gated on XD skeleton+CLIP feature completion in E:/features/xd/"
    - "Phase 4b Requirements column lists EVAL-02, EVAL-03, EVAL-04, EVAL-05 (not EVAL-01 — the RTFM gate is a Phase 4 deliverable per D-03)"
    - "Phase 4 row in the Progress Table shows 7/7 plans complete after Plan 07"
    - "REQUIREMENTS.md traceability table is consistent — no orphaned EVAL-* rows"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4b entry with inherited SCs"
      contains: "Phase 4b:"
  key_links:
    - from: ".planning/ROADMAP.md Phase 4b entry"
      to: "Phase 4 code artifacts (evaluate.py, run_ablations.py)"
      via: "Phase 4b Depends on: Phase 4"
      pattern: "Depends on.*Phase 4"
    - from: ".planning/ROADMAP.md Phase 4b activation criterion"
      to: "E:/features/xd/{skeleton,clip}/ population"
      via: "empirical gate — Phase 4b starts only when features land"
      pattern: "E:/features/xd"
---

<objective>
Insert the Phase 4b ROADMAP entry that scopes the XD-Violence main results work (D-01 + D-02 + D-25). This plan is pure documentation — it closes the planning loop for Phase 4 and leaves a clear restart point for when XD skeleton+CLIP features finish extracting "elsewhere" (per D-02).

Purpose: Per CONTEXT.md D-01, Phase 4 is UCF-only; XD main results explicitly move to a new Phase 4b. This plan is the place where that split becomes visible to the project state machine. After Plan 07, the ROADMAP reflects the decision, and anyone returning to the project in 2 weeks can activate Phase 4b with `/gsd-plan-phase 4b`.

Output:
- `.planning/ROADMAP.md` extended with a new `### Phase 4b: XD-Violence Main Results` section
- Progress Table updated to reflect Phase 4 as 7/7 complete
- Requirement coverage section — no new requirements added (Phase 4b reuses EVAL-02..EVAL-05 since the code is dataset-portable)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md
@.planning/phases/04-baseline-evaluation-main-results/04-06-SUMMARY.md

<interfaces>
From CONTEXT.md (locked decisions driving this plan):
- **D-01:** Phase 4 = UCF-only. XD main results move to new Phase 4b inserted after Phase 4.
- **D-02:** XD skeleton+CLIP extraction continues out-of-band. Phase 4 does not wait/monitor. Phase 4b activates when `E:/features/xd/` is populated.
- **D-25:** Pooling ablations re-extracted UCF only in Phase 4. XD pooling -> Phase 4b.

From RESEARCH.md §Open Questions 4 (answered):
> Phase 4b SC #2 = "Gated Fusion AP >= 80% on XD test"
> SC #3 = XD ablation table
> SC #4 = XD 3-seed std < 0.5%
> SC #5 = XD violence subset breakdown (Fighting+Abuse+Riot per EVAL-04)
> SC #1 (RTFM gate) NOT repeated in Phase 4b

Current ROADMAP.md structure (line numbering is illustrative; the file may drift):
- Lines 13-18: Phase bullet list
- Lines 24-35: Phase 1 detail block
- Lines 38-52: Phase 2 detail block
- Lines 55-72: Phase 3 detail block
- Lines 74-84: Phase 4 detail block (just finished)
- Lines 87-95: Phase 5 detail block (to be shifted down OR left alone; see Step A)
- Lines 97-105: Phase 6 detail block
- Lines 109-120: Progress Table
- Lines 122-167: Requirement Coverage table

The planner preserves all existing numbering. Phase 4b is inserted BETWEEN Phase 4 and Phase 5 WITHOUT renumbering. The phase bullet list at the top gets one new line: "- [ ] **Phase 4b: XD-Violence Main Results** - ...".

XD activation criterion (D-02): `E:/features/xd/skeleton/` has >= 4500 .npy files (out of 4754 total XD videos) AND `E:/features/xd/clip/` has >= 4500 .npy files. Phase 2 DATA-10 completion signal is a concrete artifact, not a date.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Phase 4b entry into ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (entire file — preserve all existing content)
    - .planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md (D-01, D-02, D-25 + deferred ideas)
    - .planning/phases/04-baseline-evaluation-main-results/04-06-SUMMARY.md (Phase 4 results — referenced by Phase 4b "Depends on")
  </read_first>
  <action>
    Step A — Read `.planning/ROADMAP.md` in full to determine current line structure.

    Step B — Three edits:

    **Edit 1: Phase bullet list at top of file.** Find the "## Phases" section (lines ~11-18 in the current file). Insert a new bullet AFTER Phase 4's bullet (which reads: `- [ ] **Phase 4: Baseline Evaluation & Main Results** - ...`) and BEFORE Phase 5's bullet:

    ```
    - [ ] **Phase 4b: XD-Violence Main Results** - Gated Fusion + ablations + 3-seed stability on XD-Violence (activated when XD skeleton+CLIP features complete)
    ```

    **Edit 2: Insert full Phase 4b detail block.** Insert BETWEEN the end of the Phase 4 detail block (which concludes at `**Plans**: TBD` or `**Plans:** 7/7 plans complete` after Plan 07) and the start of the Phase 5 detail block (`### Phase 5: TTA Infrastructure & Corruption Experiments`). Use this exact content:

    ```markdown
    ### Phase 4b: XD-Violence Main Results
    **Goal**: Reproduce Phase 4's complete ablation table on XD-Violence — Gated Fusion + pooling ablations + 3-seed stability + per-category breakdown — using the dataset-portable Phase 4 code (evaluate.py, run_ablations.py, extractors with flags).
    **Depends on**: Phase 4 (code artifacts), Phase 2 DATA-10 (XD skeleton+CLIP extraction complete at E:/features/xd/)
    **Activation criterion**: E:/features/xd/skeleton/ and E:/features/xd/clip/ each contain >= 4500 .npy files. Per D-02, Phase 4 does not wait on or monitor this; resume planning with `/gsd-plan-phase 4b` when features land.
    **Requirements**: EVAL-02, EVAL-03, EVAL-04, EVAL-05 (no new requirements; EVAL-01 RTFM gate already closed in Phase 4 per D-03)
    **Success Criteria** (what must be TRUE):
      1. Gated Fusion achieves frame-level AP >= 80% on XD-Violence official test set, reported by `evaluate.py` with no test set used during training
      2. An ablation table exists with results for all 6 XD model variants (Skeleton-Only, CLIP-Only, Late Fusion, Gated Fusion) and 2 pooling/aggregation ablations, all run on the same XD train/val/test split
      3. Gated Fusion AP on XD-Violence is reported as mean +/- std over 3 independent seeds ({42, 123, 2024}), and the standard deviation is below 0.5%
      4. Per-category breakdown (XD-Violence: Fighting+Abuse+Riot) is computed and shows higher AP on violence-specific subsets versus full test set
    **Deferred from Phase 4** (now active):
      - XD skeleton 2-person aggregation re-extraction (`--keep-persons` on XD in extract_ctrgcn.py)
      - XD CLIP mean-only re-extraction (`--pool=mean` on XD in extract_clip.py)
      - XD pooling ablation YAMLs (`configs/gated_fusion_xd_2person.yaml`, `configs/gated_fusion_xd_clip_mean.yaml` — or reuse UCF YAMLs with dataset key swap)
      - XD ablation orchestration queue in `scripts/run_ablations.py` (add `phase4b_main`, `phase4b_pooling`, `phase4b_seeds` sibling queues)
    **Plans**: TBD (plan once XD features land)

    ```

    **Edit 3: Progress Table update.** Find the "## Progress Table" section (lines ~109-118). Update the Phase 4 row to `7/7 | Complete | <date>` and insert a new Phase 4b row BETWEEN Phase 4 and Phase 5:

    Before:
    ```
    | 4. Baseline Evaluation & Main Results | 0/? | Not started | - |
    | 5. TTA Infrastructure & Corruption Experiments | 0/? | Not started | - |
    ```

    After (fill in the actual completion date from `git log -1 --format=%cd .planning/phases/04-baseline-evaluation-main-results/04-06-SUMMARY.md`):
    ```
    | 4. Baseline Evaluation & Main Results | 7/7 | Complete | <YYYY-MM-DD> |
    | 4b. XD-Violence Main Results | 0/? | Blocked on XD features | - |
    | 5. TTA Infrastructure & Corruption Experiments | 0/? | Not started | - |
    ```

    Do NOT renumber Phase 5/6 in any existing table. The 4b entry is a half-step; the main phase numbers stay 1, 2, 3, 4, 5, 6.

    Step C — Do NOT modify REQUIREMENTS.md. Verify the "Traceability" section at the bottom of REQUIREMENTS.md still correctly lists EVAL-01..EVAL-05 under "Phase 4". This is accurate: per D-03, EVAL-01 is retargeted to XD-I3D but remains a Phase 4 deliverable; EVAL-02..EVAL-05 are delivered for UCF in Phase 4 and re-delivered for XD in Phase 4b. The traceability table does not need a new "Phase 4b" column since the requirement is considered satisfied when it's demonstrated on at least one dataset — XD extension in Phase 4b is an expansion, not a new requirement.

    If the `grep -q '| EVAL-02 | Phase 4 |' .planning/REQUIREMENTS.md` check fails (unlikely), align the table but do not add Phase 4b rows.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import re; txt=open('.planning/ROADMAP.md',encoding='utf-8').read(); assert '### Phase 4b: XD-Violence Main Results' in txt, 'Phase 4b detail block missing'; assert 'Phase 4b: XD-Violence Main Results' in txt; assert 'EVAL-02, EVAL-03, EVAL-04, EVAL-05' in txt or ('EVAL-02' in txt and 'EVAL-03' in txt); assert 'E:/features/xd' in txt, 'XD activation criterion missing'; print('Phase 4b entry OK')"</automated>
  </verify>
  <done>
    - `.planning/ROADMAP.md` has a new `### Phase 4b: XD-Violence Main Results` detail block inserted between Phase 4 and Phase 5.
    - The top-of-file Phase bullet list has the new Phase 4b line.
    - Progress Table has Phase 4b row with status "Blocked on XD features".
    - Phase 4 row updated to 7/7 complete.
    - REQUIREMENTS.md traceability unchanged (verified).
  </done>
  <acceptance_criteria>
    - `grep -q '### Phase 4b: XD-Violence Main Results' .planning/ROADMAP.md`
    - `grep -q 'Phase 4b: XD-Violence Main Results' .planning/ROADMAP.md`
    - `grep -q 'E:/features/xd' .planning/ROADMAP.md`
    - `grep -q 'Activation criterion' .planning/ROADMAP.md`
    - `grep -q 'Blocked on XD features' .planning/ROADMAP.md`
    - `grep -q '7/7' .planning/ROADMAP.md`
    - `grep -qE 'EVAL-0(2|3|4|5)' .planning/ROADMAP.md`
    - Phase 5 detail block is still present and unmodified: `grep -q '### Phase 5: TTA Infrastructure & Corruption Experiments' .planning/ROADMAP.md`
    - Phase 6 detail block is still present: `grep -q '### Phase 6: Analysis' .planning/ROADMAP.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Text file edit -> git repo | Low-risk documentation edit; researcher owns the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | Accidentally deleting Phase 5 / Phase 6 detail blocks | mitigate | Acceptance criteria explicitly grep for `### Phase 5: TTA Infrastructure` and `### Phase 6: Analysis` strings — if either is missing after the edit, the task fails. |
| T-04-07-02 | Information Disclosure | ROADMAP edits commit accidentally-sensitive info | accept | Plain documentation; no credentials, no paths to user data. |
| T-04-07-03 | Denial of Service | Phase 4b added but unclear activation -> future confusion | mitigate | "Activation criterion" line in the detail block specifies the exact filesystem condition (>= 4500 .npy files in E:/features/xd/{skeleton,clip}/). Phase 4b Depends-on line references Phase 2 DATA-10. |
</threat_model>

<verification>
- `grep -c '### Phase' .planning/ROADMAP.md` returns 7 (Phase 1, 2, 3, 4, 4b, 5, 6) instead of 6.
- `python -c "with open('.planning/ROADMAP.md', encoding='utf-8') as f: t=f.read(); idx4=t.find('### Phase 4:'); idx4b=t.find('### Phase 4b:'); idx5=t.find('### Phase 5:'); assert idx4 < idx4b < idx5, f'ordering broken: {idx4} {idx4b} {idx5}'; print('order OK')"`
- Progress Table: `grep -E '^\\| 4. Baseline\\|^\\| 4b\\.' .planning/ROADMAP.md | wc -l` returns 2.
</verification>

<success_criteria>
Phase 4 is fully closed when:
1. ROADMAP.md reflects Phase 4 = 7/7 plans complete.
2. Phase 4b is a first-class entry with activation criterion, inherited SCs, and "Blocked on XD features" status.
3. STATE.md update (handled by execute-plan.md infrastructure, not by this plan) shows `current_phase: 5` (or `4b` if the user activates XD features before moving to TTA).
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseline-evaluation-main-results/04-07-SUMMARY.md`:
- files_touched: .planning/ROADMAP.md
- decisions implemented: D-01 (UCF-only Phase 4), D-02 (XD out-of-band), D-25 (XD pooling to 4b)
- Phase 4 completion status
- Phase 4b activation signal
- handoff to /gsd-plan-phase 5 (TTA) — Phase 4b can start in parallel when XD features land
</output>
