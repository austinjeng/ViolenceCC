---
phase: 04-baseline-evaluation-main-results
plan: 06
type: execute
wave: 5
depends_on:
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - .planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md
  - results/results-index.csv
  - results/xd_i3d_rtfm_i3d_s42/
  - results/ucf_skeleton_only_s42/
  - results/ucf_clip_only_s42/
  - results/ucf_late_fusion_s42/
  - results/ucf_gated_fusion_s42/
  - results/ucf_gated_fusion_2person_s42/
  - results/ucf_gated_fusion_clip_mean_s42/
  - results/ucf_gated_fusion_s123/
  - results/ucf_gated_fusion_s2024/
  - E:/features/ucf/skeleton_2person/
  - E:/features/ucf/clip_mean/
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
threat_model: see_threat_model_section

must_haves:
  truths:
    - "RTFM XD-I3D frame-level AP >= 76.81% (77.81% minus 1%) — EVAL-01 gate passed"
    - "Gated Fusion UCF frame-level AUC >= 83% — EVAL-02/SC#2 passed"
    - "results/results-index.csv has 9 rows: 1 RTFM + 4 main + 2 pooling + 2 seed-variation"
    - "3-seed Gated Fusion UCF AUC std < 0.5% — EVAL-05/SC#4 passed"
    - "per_category.csv files show Fighting+Assault AUC > full-test AUC for Gated Fusion — EVAL-04/SC#5 passed"
    - "E:/features/ucf/skeleton_2person/ has ~1728 .npy files, shape [N, 2, 256]"
    - "E:/features/ucf/clip_mean/ has ~1728 .npy files, shape [N, 512]"
    - "Re-running src/evaluate.py on any best_model.pth produces bit-identical eval_metrics.json (excluding timestamp + duration)"
  artifacts:
    - path: "results/results-index.csv"
      provides: "Append-only audit log with 9 rows after Plan 06 completes"
      contains: "run_name,variant,dataset"
    - path: "results/xd_i3d_rtfm_i3d_s42/eval_metrics.json"
      provides: "RTFM gate result with ap value within plus/minus 1% of 77.81%"
      contains: "ap"
    - path: "results/ucf_gated_fusion_s42/best_model.pth"
      provides: "Canonical Gated Fusion checkpoint consumed by Phase 5 TTA"
    - path: "results/ucf_gated_fusion_s42/per_category.csv"
      provides: "Per-category violence subset breakdown for EVAL-04"
      contains: "Fighting"
    - path: "E:/features/ucf/skeleton_2person/"
      provides: "Re-extracted 2-person skeleton cache for pooling ablation"
    - path: "E:/features/ucf/clip_mean/"
      provides: "Re-extracted CLIP mean-only cache for pooling ablation"
  key_links:
    - from: "scripts/run_ablations.py"
      to: "results/results-index.csv"
      via: "9 append operations across 4 queues"
      pattern: "results-index.csv"
    - from: "scripts/run_ablations.py"
      to: "Phase 5 TTA"
      via: "results/ucf_gated_fusion_s42/best_model.pth handoff"
      pattern: "best_model.pth"
---

<objective>
Execute the Phase 4 empirical workload entirely through the Plan 05 orchestrator. Five waves of empirical work:
  (5a) re-extract the two pooling ablation caches on UCF;
  (5b) run the RTFM gate (halt Phase 4 if it fails);
  (5c) run the 4 main UCF variants at seed 42;
  (5d) run the 2 pooling ablations;
  (5e) run the 2 additional Gated Fusion seeds for 3-seed stability.

Purpose: Produce the numbers that populate the thesis ablation table.

**Why this plan is non-autonomous:** Six items in VALIDATION.md's "Manual-Only" section are empirical gates that cannot be unit-tested: RTFM >=76.81% AP, UCF AUC >=83%, 3-seed std <0.5%, per-category > full-test, bit-identical rerun, and wandb preflight fails-fast on missing auth. Plan 06 surfaces these as HUMAN-UAT items per the validation contract enforcement section.

Output:
- 9 completed run dirs with `eval_metrics.json`, `eval_scores.npz`, `per_category.csv`, `.done`
- Populated `results/results-index.csv` (9 rows)
- Populated `E:/features/ucf/skeleton_2person/` + `E:/features/ucf/clip_mean/`
- `.planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md` documenting 6 HUMAN-UAT gates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md
@.planning/phases/04-baseline-evaluation-main-results/04-VALIDATION.md
@.planning/phases/04-baseline-evaluation-main-results/04-02-SUMMARY.md
@.planning/phases/04-baseline-evaluation-main-results/04-03-SUMMARY.md
@.planning/phases/04-baseline-evaluation-main-results/04-04-SUMMARY.md
@.planning/phases/04-baseline-evaluation-main-results/04-05-SUMMARY.md

<interfaces>
All code used by this plan was built in Plans 01-05. Execution invocations only.

Extractors (Plan 04):
- `C:/Anaconda/envs/vcc-ctrgcn/python.exe scripts/extract_ctrgcn.py --dataset ucf --split all --keep-persons`
- `C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split all --pool mean`

Sanity verifier (Plan 04):
- `C:/Anaconda/envs/vcc-main/python.exe scripts/verify_pooling_caches.py --dataset ucf --cache clip_mean --cache-root E:/features/ucf/clip_mean --baseline-root E:/features/ucf/clip`
- same with cache=skeleton_2person + matching roots

Orchestrator (Plan 05):
- `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue rtfm_gate`
- same with queue in {phase4_main, phase4_pooling, phase4_seeds}

Expected empirical ranges:
- RTFM XD-I3D AP:         76.81-78.81 (gate >=76.81, target 77.81 plus/minus 1)
- Skeleton-Only UCF AUC:  likely 75-80%
- CLIP-Only UCF AUC:      likely 80-84%
- Late Fusion UCF AUC:    likely 82-84%
- Gated Fusion UCF AUC:   >=83% (target 85-87%)
- 3-seed std:             <0.5%

Total wall-clock estimate: ~10-15 hours (may span multiple sessions; .done markers support resume).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-extract the two pooling ablation caches plus verifier sanity check</name>
  <files>E:/features/ucf/skeleton_2person/, E:/features/ucf/clip_mean/</files>
  <read_first>
    - .planning/phases/04-baseline-evaluation-main-results/04-04-SUMMARY.md
    - scripts/extract_ctrgcn.py (confirm --keep-persons branch present)
    - scripts/extract_clip.py (confirm --pool branch present)
    - scripts/verify_pooling_caches.py
  </read_first>
  <action>
    Step A. CTR-GCN 2-person re-extraction. In a visible terminal (per user memory "No headless processes"):
    - cd D:/ViolenceCC
    - `C:/Anaconda/envs/vcc-ctrgcn/python.exe scripts/extract_ctrgcn.py --dataset ucf --split all --keep-persons`

    Expected ~20-40 min on RTX 4090. Output: `E:/features/ucf/skeleton_2person/`. Expected ~1728 files.

    Step B. CLIP mean-only re-extraction. Visible terminal:
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split all --pool mean`

    Expected ~20-40 min. Output: `E:/features/ucf/clip_mean/`. Expected ~1728 files.

    Step C. Run sanity verifiers on both caches. Both MUST exit 0 with FAIL:0:
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/verify_pooling_caches.py --dataset ucf --cache clip_mean --cache-root E:/features/ucf/clip_mean --baseline-root E:/features/ucf/clip`
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/verify_pooling_caches.py --dataset ucf --cache skeleton_2person --cache-root E:/features/ucf/skeleton_2person --baseline-root E:/features/ucf/skeleton`

    If either verifier fails, inspect listed failing videos, re-extract those IDs, re-run verifier.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe scripts/verify_pooling_caches.py --dataset ucf --cache clip_mean --cache-root E:/features/ucf/clip_mean --baseline-root E:/features/ucf/clip</automated>
  </verify>
  <done>
    - `E:/features/ucf/skeleton_2person/` has >= 1700 `.npy` files with shape `[N, 2, 256]`.
    - `E:/features/ucf/clip_mean/` has >= 1700 `.npy` files with shape `[N, 512]`.
    - Both verifier invocations exit 0 with `FAIL: 0`.
  </done>
  <acceptance_criteria>
    - `test -d E:/features/ucf/skeleton_2person`
    - `test -d E:/features/ucf/clip_mean`
    - `python scripts/verify_pooling_caches.py --dataset ucf --cache clip_mean --cache-root E:/features/ucf/clip_mean --baseline-root E:/features/ucf/clip` exits 0
    - `python scripts/verify_pooling_caches.py --dataset ucf --cache skeleton_2person --cache-root E:/features/ucf/skeleton_2person --baseline-root E:/features/ucf/skeleton` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run RTFM gate (halt if fails per specifics bullet 8)</name>
  <files>results/xd_i3d_rtfm_i3d_s42/, results/results-index.csv, runner-errors.log</files>
  <read_first>
    - .planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md (D-03, D-05, D-18, D-19, specifics bullet 8)
    - .planning/phases/04-baseline-evaluation-main-results/04-RESEARCH.md (Pitfall 3 batch_size empirical question)
    - scripts/run_ablations.py
    - configs/rtfm_i3d.yaml
  </read_first>
  <action>
    Step A. wandb preflight MUST exit 0 before opening the queue:
    - `cd D:/ViolenceCC`
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/wandb_preflight.py`

    If it fails: run `wandb login` OR set `wandb.mode: disabled` in configs/rtfm_i3d.yaml, then retry.

    Step B. Run the RTFM gate in a visible terminal:
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue rtfm_gate`

    Expected ~60-120 min (3225 train videos × 5 crops → ~16k effective MIL bags at batch_size=3; 50 epochs max with early stopping).

    Step C. Inspect `results/xd_i3d_rtfm_i3d_s42/eval_metrics.json`:

    GATE (D-03, specifics bullet 8):
    - PASS: `ap >= 0.7681`. Proceed to Task 3.
    - FAIL: `ap < 0.7681`. HALT Phase 4. Diagnostics:
      1. Training loss collapsed to 0 fast + per-snippet variance collapsed -> Pitfall 3 (C5 collapse). Recovery: set `data.batch_size: 1` in configs/rtfm_i3d.yaml OR enable crop-averaging during training. Delete `.done` marker and re-run Task 2.
      2. Training loss never decreased -> MIL loss or loader broken. Surface to user.
      3. AP in 74-78% but outside plus/minus 1%: per CONTEXT.md Discretion MTN Aggregate module fallback is Phase 4b scope. Document the miss and continue ONLY after user approval.

    Step D. Append gate report to `.planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md`:
    - "## EVAL-01 (RTFM XD-I3D) Gate"
    - Config: configs/rtfm_i3d.yaml
    - Run dir: results/xd_i3d_rtfm_i3d_s42/
    - AP: <value>
    - Gate: >= 0.7681 [PASS/FAIL]
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import json,sys; m=json.loads(open('results/xd_i3d_rtfm_i3d_s42/eval_metrics.json').read()); ap=m.get('ap',0.0); print(f'AP={ap:.4f} gate_0_7681_{ap>=0.7681}'); sys.exit(0 if ap>=0.7681 else 1)"</automated>
  </verify>
  <done>
    - `results/xd_i3d_rtfm_i3d_s42/.done` exists.
    - `eval_metrics.json` has `ap >= 0.7681` OR user explicitly approves a documented miss.
    - `04-06-UAT.md` has the EVAL-01 section populated.
  </done>
  <acceptance_criteria>
    - `test -f results/xd_i3d_rtfm_i3d_s42/eval_metrics.json`
    - `test -f results/xd_i3d_rtfm_i3d_s42/.done`
    - `python -c "import json; m=json.loads(open('results/xd_i3d_rtfm_i3d_s42/eval_metrics.json').read()); assert m['ap']>=0.7681"`
    - `grep -q 'EVAL-01' .planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN-UAT Checkpoint 1: RTFM gate empirical result</name>
  <what-built>
    Task 2 trained rtfm_i3d on XD I3D RGB features and produced results/xd_i3d_rtfm_i3d_s42/eval_metrics.json. The automated verify checks the plus/minus 1% threshold, but the researcher must inspect the full metric and training log before authorizing the downstream queues — failure modes like C5 collapse only show up in the training curve, not the single AP value.
  </what-built>
  <files>results/xd_i3d_rtfm_i3d_s42/eval_metrics.json, results/xd_i3d_rtfm_i3d_s42/train_log.csv, results/results-index.csv</files>
  <action>This is a human checkpoint: Claude has completed Task 2 (RTFM training + evaluation). The empirical gate cannot be programmatically verified beyond the single AP threshold check already in Task 2's &lt;automated&gt; verify — the researcher must inspect the training curve for Pitfall 3 collapse signals. No new work is performed in this checkpoint; Claude pauses for the resume signal below.</action>
  <verify><automated>C:/Anaconda/envs/vcc-main/python.exe -c "import os,sys; p='results/xd_i3d_rtfm_i3d_s42/eval_metrics.json'; assert os.path.exists(p); import json; m=json.loads(open(p).read()); sys.exit(0 if m.get('ap',0)&gt;=0.7681 else 0)"</automated></verify>
  <done>Researcher approves one of the resume signals below.</done>
  <how-to-verify>
    1. Inspect `results/xd_i3d_rtfm_i3d_s42/eval_metrics.json` — confirm `ap >= 0.7681`.
    2. Inspect `results/xd_i3d_rtfm_i3d_s42/train_log.csv` — confirm train_loss decreased monotonically over several epochs (not a single-epoch collapse to 0).
    3. If wandb online: inspect the tagged run for per-snippet-score variance over training — variance should NOT monotonically decrease to 0 (sign of Pitfall 3 collapse).
    4. Confirm `results/results-index.csv` has 1 row with run_name = `xd_i3d_rtfm_i3d_s42`.
  </how-to-verify>
  <resume-signal>
    Type "approved" to continue to Task 3.
    Type "retry with batch_size=1" to rerun with the Pitfall 3 fallback (requires editing configs/rtfm_i3d.yaml then deleting .done and rerunning Task 2).
    Type "document miss and continue" if the AP is outside plus/minus 1% but within 74-80% (documented miss; Phase 4b adds MTN Aggregate module as fallback).
    Describe the issue otherwise.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run main UCF queue (4 variants at seed 42)</name>
  <files>results/ucf_skeleton_only_s42/, results/ucf_clip_only_s42/, results/ucf_late_fusion_s42/, results/ucf_gated_fusion_s42/, results/results-index.csv</files>
  <read_first>
    - .planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md (D-27: 3-seed only for gated_fusion; others single-seed)
    - scripts/run_ablations.py (QUEUES phase4_main)
    - configs/skeleton_only.yaml, configs/clip_only.yaml, configs/late_fusion.yaml, configs/gated_fusion.yaml
  </read_first>
  <action>
    Visible terminal:
    - `cd D:/ViolenceCC`
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase4_main`

    Expected ~4-6 hours. Can span multiple sessions — .done markers resume from last-completed run automatically. If any run fails (non-zero subprocess exit), inspect `runner-errors.log` for the error tuple, address the root cause, re-run the queue.

    Upon completion, `results/results-index.csv` has 5 rows total (1 from Task 2 + 4 from this task). Inspect `results/ucf_gated_fusion_s42/eval_metrics.json` — `auc` should be >= 0.83 per SC #2. Surfaced as HUMAN-UAT 2 below.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=5; print('rows', len(df))"</automated>
  </verify>
  <done>
    - `results/ucf_{skeleton_only,clip_only,late_fusion,gated_fusion}_s42/.done` all exist.
    - `results/results-index.csv` has >= 5 rows.
  </done>
  <acceptance_criteria>
    - `test -f results/ucf_skeleton_only_s42/.done`
    - `test -f results/ucf_clip_only_s42/.done`
    - `test -f results/ucf_late_fusion_s42/.done`
    - `test -f results/ucf_gated_fusion_s42/.done`
    - `test -f results/ucf_gated_fusion_s42/best_model.pth`
    - `test -f results/ucf_gated_fusion_s42/per_category.csv`
    - `python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=5, f'expected >=5 rows got {len(df)}'"`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN-UAT Checkpoint 2: Gated Fusion UCF AUC >= 83%</name>
  <what-built>
    Task 3 produced results for all 4 main UCF variants at seed 42. The Gated Fusion run is the canonical Phase 5 TTA checkpoint and must meet SC #2 (AUC >= 83%).
  </what-built>
  <files>results/ucf_gated_fusion_s42/eval_metrics.json, results/ucf_gated_fusion_s42/per_category.csv, results/results-index.csv</files>
  <action>Human checkpoint: Claude completed Task 3 (4 UCF main variants at seed 42). Task 3's &lt;automated&gt; verify already confirms results-index.csv has &gt;=5 rows; the researcher adds empirical judgment on whether the variant ordering (skeleton_only &lt; clip_only &lt; late_fusion &lt;= gated_fusion) matches expectations. No new work in this checkpoint.</action>
  <verify><automated>C:/Anaconda/envs/vcc-main/python.exe -c "import os,sys; assert os.path.exists('results/ucf_gated_fusion_s42/eval_metrics.json')"</automated></verify>
  <done>Researcher approves one of the resume signals below.</done>
  <how-to-verify>
    1. Inspect `results/ucf_gated_fusion_s42/eval_metrics.json[auc]` — confirm >= 0.83.
    2. Inspect `results/ucf_gated_fusion_s42/per_category.csv` — confirm Fighting row exists with a finite AUC (EVAL-04 requires at least Fighting+Assault categories to have anomaly frames in the UCF test set; note Assault may be absent depending on split).
    3. Compare the 4 variants' AUC values in `results-index.csv`: expect `skeleton_only < clip_only < late_fusion <= gated_fusion`. If Gated Fusion is NOT the top variant, something is wrong with the fusion architecture.
    4. `results/results-index.csv` has exactly 5 rows (header + 4 + RTFM).
  </how-to-verify>
  <resume-signal>
    Type "approved" to continue to Task 4.
    Type "gated fusion below target" if the AUC is 80-83% (documented miss; proceed but flag in SUMMARY).
    Describe issue otherwise.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Run pooling ablations (D-22 + D-23, single seed each)</name>
  <files>results/ucf_gated_fusion_2person_s42/, results/ucf_gated_fusion_clip_mean_s42/, results/results-index.csv</files>
  <read_first>
    - .planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md (D-22, D-23, D-29)
    - scripts/run_ablations.py (QUEUES phase4_pooling)
    - configs/gated_fusion_2person.yaml, configs/gated_fusion_clip_mean.yaml
  </read_first>
  <action>
    Visible terminal:
    - `cd D:/ViolenceCC`
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase4_pooling`

    Expected ~2-3 hours (2 runs × ~60-90 min each on the cached 2-person and clip_mean features).

    After completion, `results-index.csv` has 7 rows (prev 5 + 2 pooling).

    Per D-29 discretion: if the AUC delta between pooling choice and baseline gated_fusion is < 0.5%, optionally re-run at 3 seeds for significance. Defer this as a judgment call — document the delta in SUMMARY.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=7, f'expected >=7 got {len(df)}'; print('rows', len(df))"</automated>
  </verify>
  <done>
    - Both pooling run dirs have `.done` markers.
    - `results-index.csv` has >= 7 rows.
  </done>
  <acceptance_criteria>
    - `test -f results/ucf_gated_fusion_2person_s42/.done`
    - `test -f results/ucf_gated_fusion_clip_mean_s42/.done`
    - `python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=7"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run 3-seed Gated Fusion stability (seeds 123 and 2024)</name>
  <files>results/ucf_gated_fusion_s123/, results/ucf_gated_fusion_s2024/, results/results-index.csv</files>
  <read_first>
    - .planning/phases/04-baseline-evaluation-main-results/04-CONTEXT.md (D-27, D-28)
    - scripts/run_ablations.py (QUEUES phase4_seeds)
  </read_first>
  <action>
    Visible terminal:
    - `cd D:/ViolenceCC`
    - `C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase4_seeds`

    Expected ~2-3 hours (2 runs on gated_fusion at seeds 123 and 2024 — seed 42 already done in Task 3).

    After completion, `results-index.csv` has 9 rows total. Compute 3-seed mean and std for `gated_fusion` on UCF:

    ```
    python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); gf=df[(df.variant=='gated_fusion')&(df.dataset=='ucf')&(df.cache_variant.isna()|(df.cache_variant==''))]; print('mean AUC:', gf.auc.mean(), 'std:', gf.auc.std()); print('mean AP:', gf.ap.mean(), 'std:', gf.ap.std())"
    ```

    SC #4: std must be < 0.5% (0.005). If not, document the delta in SUMMARY — note whether a 4th seed is worth running for extra stability.

    Append 3-seed summary to `04-06-UAT.md`:
    - "## EVAL-05 3-seed Gated Fusion Stability"
    - Seeds: 42, 123, 2024
    - AUC mean +/- std: <values>
    - Gate: std < 0.005 [PASS/FAIL]
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=9, f'expected >=9 got {len(df)}'; gf=df[(df.variant=='gated_fusion')&(df.dataset=='ucf')&(df.cache_variant.isna()|(df.cache_variant==''))]; print(f'seeds={sorted(gf.seed.tolist())} n={len(gf)} auc_std={gf.auc.std():.4f}')"</automated>
  </verify>
  <done>
    - `results/ucf_gated_fusion_s123/.done` and `results/ucf_gated_fusion_s2024/.done` exist.
    - `results-index.csv` has >= 9 rows.
    - 3-seed std computed; logged to `04-06-UAT.md`.
  </done>
  <acceptance_criteria>
    - `test -f results/ucf_gated_fusion_s123/.done`
    - `test -f results/ucf_gated_fusion_s2024/.done`
    - `python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)>=9"`
    - `grep -q 'EVAL-05' .planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN-UAT Checkpoint 3: 3-seed std, per-category breakdown, bit-identical rerun</name>
  <what-built>
    All 9 ablation runs complete. The remaining verification gates are SC #4 (3-seed std <0.5%), SC #5 (per-category violence AUC > full-test AUC), and the determinism invariant (bit-identical eval rerun).
  </what-built>
  <files>results/ucf_gated_fusion_s42/, results/ucf_gated_fusion_s123/, results/ucf_gated_fusion_s2024/, .planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md</files>
  <action>Human checkpoint: Claude completed all 9 ablation runs (Task 5). The researcher performs the final three manual gates — 3-seed std, per-category breakdown, bit-identical rerun, and wandb preflight check — per the how-to-verify steps below. No new work by Claude in this checkpoint.</action>
  <verify><automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); assert len(df)&gt;=9, f'expected &gt;=9 got {len(df)}'"</automated></verify>
  <done>Researcher approves &quot;all 6 UAT items passed&quot;.</done>
  <how-to-verify>
    1. **EVAL-05 / SC #4 (3-seed std):** open `04-06-UAT.md` and confirm the std value is < 0.005 for AUC. If not, document and proceed (noted as a risk in SUMMARY).
    2. **EVAL-04 / SC #5 (per-category):** open `results/ucf_gated_fusion_s42/per_category.csv`. Confirm rows for `Fighting` and `Assault` exist with AUC values. Compare against `results/ucf_gated_fusion_s42/eval_metrics.json[auc]` (full-test AUC). For at least one of Fighting/Assault, category AUC should be higher than full-test AUC (violence-specific signal).
    3. **Bit-identical rerun (SC supporting C3/C4):** rerun evaluate.py on the canonical checkpoint:
       - `rm results/ucf_gated_fusion_s42/.done`
       - `C:/Anaconda/envs/vcc-main/python.exe src/evaluate.py --run-dir results/ucf_gated_fusion_s42 --split test`
       - Read the new `eval_metrics.json`; compare all numeric keys (auc, ap, snippet_auc, video_auc, per_category, n_videos, n_frames, config_hash, checkpoint_sha) against a saved copy from before the rerun. Only `eval_timestamp` and `eval_duration_s` may differ.
    4. **wandb preflight fails-fast (manual):** temporarily unset `WANDB_API_KEY` AND rename `~/.netrc` to `~/.netrc.bak`. Run `python scripts/wandb_preflight.py` — MUST exit 2 with the actionable error message. Restore `~/.netrc` after.
  </how-to-verify>
  <resume-signal>
    Type "all 6 UAT items passed" to close Phase 4.
    Type "item X failed" to describe which gate missed and how to proceed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess.run -> train.py / evaluate.py | Argv list only (no shell=True, inherited from Plan 05). |
| Filesystem writes -> results/ | Researcher-owned local path. |
| Empirical gate signals -> human judgment | 6 HUMAN-UAT items require researcher interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | A runaway run exhausts disk (multiple ~14-20 MB checkpoints x 9 runs) | accept | ~180 MB total on D: drive; well within slack. Plan 05's 7200s timeout caps any single stuck run. |
| T-04-06-02 | Tampering | Silent AUC regression from a stray code edit between runs | mitigate | `config_hash` and `checkpoint_sha` in every `eval_metrics.json` (Plan 02 D-12) lock the exact state. results-index.csv records config_hash per row — if `gated_fusion_s42` and `gated_fusion_s123` have different config hashes unexpectedly, something changed mid-queue. |
| T-04-06-03 | Information Disclosure | Thesis-level numbers leak to wandb cloud | accept | Researcher's own wandb project; no PII in training data. Document in `04-06-UAT.md` whether runs used wandb online vs offline. |
| T-04-06-04 | Denial of Service | RTFM gate fails -> all downstream numbers suspect | mitigate | Task 2 HALTS the plan on gate failure. HUMAN-UAT 1 explicitly requires approval before Task 3 opens the main queue. |
</threat_model>

<verification>
- After all 5 tasks: `python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); print(df[['run_name','variant','dataset','auc','ap']].to_string())"` prints 9 rows.
- `results/ucf_gated_fusion_s42/best_model.pth` exists (Phase 5 TTA handoff).
- 3 HUMAN-UAT checkpoints passed (RTFM gate, Gated Fusion SC #2, 3-seed / per-category / bit-identical / preflight bundle).
- `04-06-UAT.md` documents all 6 Manual-Only gates from VALIDATION.md with PASS/FAIL status.
</verification>

<success_criteria>
Phase 4 closes when ALL of these are true (maps directly to the 5 ROADMAP SCs):
1. **SC #1 (EVAL-01):** RTFM XD-I3D AP >= 76.81% (gate passed in Task 2 + HUMAN-UAT 1).
2. **SC #2 (EVAL-02/SC):** Gated Fusion UCF AUC >= 83% (HUMAN-UAT 2).
3. **SC #3 (EVAL-03):** 6 variants in results-index.csv (4 main + 2 pooling) + RTFM on XD. (Phase 4 = UCF-only main results per D-01; XD main table is Phase 4b.)
4. **SC #4 (EVAL-05):** 3-seed Gated Fusion std < 0.5% (HUMAN-UAT 3 item 1).
5. **SC #5 (EVAL-04):** per_category.csv shows Fighting+Assault AUC > full-test AUC (HUMAN-UAT 3 item 2).

`results/ucf_gated_fusion_s42/best_model.pth` is the canonical Phase 5 TTA adaptation base (handoff unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseline-evaluation-main-results/04-06-SUMMARY.md`:
- files_touched
- all 9 empirical results (run_name, variant, dataset, seed, AUC, AP)
- 3-seed mean +/- std for Gated Fusion
- per-category Fighting+Assault breakdown vs full-test
- HUMAN-UAT statuses for all 6 Manual-Only items
- decisions validated empirically (D-03 anchor choice, D-22 concat vs mean/max, D-28 seed choice)
- open issues / Phase 4b backlog items (XD main results pending feature completion)

Also update `.planning/phases/04-baseline-evaluation-main-results/04-06-UAT.md` with final PASS/FAIL table covering all 6 manual-only items.
</output>
