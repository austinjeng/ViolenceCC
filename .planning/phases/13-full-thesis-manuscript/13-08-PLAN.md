---
phase: 13-full-thesis-manuscript
plan: 08
type: execute
wave: 5
depends_on: [13-05, 13-06, 13-07]
files_modified: []
autonomous: false
requirements: [SC1, SC2]
must_haves:
  truths:
    - "All nine chapters build together clean with zero undefined refs/citations"
    - "No PLACEHOLDER-W0 marker survives in any chapter"
    - "User has reviewed and approved the chapter drafts before appendices/verification start"
  artifacts: []
  key_links: []
---

<objective>
Wave-1 exit gate (spec §9): full clean build over all nine drafted chapters, placeholder/structure
scan, then the USER CHECKPOINT on chapter drafts.

Purpose: spec-mandated checkpoint "after Wave 1 (chapter drafts)"; catches cross-chapter build
breakage from parallel drafting before Wave-2 work stacks on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-05-SUMMARY.md
@.planning/phases/13-full-thesis-manuscript/13-06-SUMMARY.md
@.planning/phases/13-full-thesis-manuscript/13-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full clean build + placeholder and forbidden-value scan over ch01-ch09</name>
  <files>(no edits expected — fix-forward only if the merged build breaks)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 forbidden-sources list (grep tokens)
    - thesis/main.log (after the build — scan beyond the script's own checks)
  </read_first>
  <action>
    Run `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean` over the merged
    9-chapter document. If parallel drafting introduced conflicts (duplicate labels, missing figures), fix
    minimally and rebuild. Then run the scan set:
    - grep -rc "PLACEHOLDER-W0" thesis/chapters/ → 0 across all files
    - grep -rc "\\\\todo" thesis/ → 0
    - forbidden values in chapters: grep -rn "83\.6\|71\.8\|13\.9\|0\.9200" thesis/chapters/ → investigate every
      hit; legitimate exceptions only per RESEARCH §2 (e.g. 0.8227/0.7192 with historical labeling in ch05)
    - headline spot-check: grep -rc "82.5" and "78.7" appear in ch01, ch05/ch06, ch09
    - RE-VERIFY inventory: grep -rc "RE-VERIFY" thesis/chapters/ → 13 (all in ch07; carried for the gate)
    - pytest: C:/Anaconda/envs/vcc-main/python.exe -m pytest -q → green
    Record the interim page count (target trajectory: chapters alone ~75-90pp of the 90-120 total).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); grep -rc "PLACEHOLDER-W0" thesis/chapters/ (0 total); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green)</automated>
  </verify>
  <acceptance_criteria>
    - clean build exit 0; main.log free of undefined refs/citations and "!" errors
    - zero PLACEHOLDER-W0 / \todo in chapters; RE-VERIFY count exactly 13 (ch07 only)
    - every forbidden-value hit dispositioned (fixed or documented as a permitted labeled-historical exception)
    - pytest green
  </acceptance_criteria>
  <done>Merged chapter build green; scans clean; draft PDF ready for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: USER CHECKPOINT — Wave 1 chapter drafts</name>
  <read_first>(none — presentation task)</read_first>
  <what-built>All nine thesis chapters drafted placeholder-free (~75-90pp): Intro with RQ1-3, Related-Work survey, Methodology with verbatim equations + full disc_reweight derivation, Experimental Setup with real corruption params, both Results chapters with all tables/figures, Discussion with the full 20-method SOTA positioning, Limitations with plain gate-miss disclosure, Conclusion.</what-built>
  <files>(none — user verification)</files>
  <action>Present the merged 9-chapter draft PDF to the user with per-chapter page counts and the scan results. Pause for approval before Wave 2.</action>
  <how-to-verify>
    1. Run: powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Open
    2. Skim each chapter for voice/coverage; spot-check Ch5 Tables 1/2 against the paper; check Ch3 architecture figure legibility
    3. Note anything to redo before appendices/frontmatter/verification start (Wave 2)
  </how-to-verify>
  <acceptance_criteria>User types "approved" or lists per-chapter revision requests.</acceptance_criteria>
  <verify>User response received ("approved" or revision list)</verify>
  <done>User approved the chapter drafts (or all requested revisions applied and re-approved).</done>
  <resume-signal>Type "approved" to start Wave 2 (appendices + frontmatter + SOTA verification), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Repudiation | draft approval | mitigate | blocking human checkpoint records approval before downstream work |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Clean build + pytest + scan set (13-VALIDATION per-wave gate)
</verification>

<success_criteria>
- Wave 1 exits with user-approved chapter drafts and a green merged build
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-08-SUMMARY.md` when done (include interim page count and any user revision requests + how they were resolved).
</output>
