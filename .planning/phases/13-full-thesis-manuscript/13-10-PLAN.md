---
phase: 13-full-thesis-manuscript
plan: 10
type: execute
wave: 6
depends_on: [13-08]
files_modified:
  - thesis/appendices/appD_engineering.tex
  - thesis/appendices/appE_reproducibility.tex
  - thesis/appendices/appF_figures.tex
  - thesis/frontmatter/titlepage.tex
  - thesis/frontmatter/abstract.tex
  - thesis/frontmatter/acknowledgments.tex
  - thesis/frontmatter/notation.tex
autonomous: true
requirements: [SC2, SC5]
must_haves:
  truths:
    - "App E walks a stranger from clean checkout to reproduced tables/figures (discharges review item T6-2)"
    - "The abstract is restructured (fixes T5-2 density) and keeps every honesty framing"
    - "App F includes only Class-V-verified figures from the 13-04 disposition list"
  artifacts:
    - path: "thesis/appendices/appE_reproducibility.tex"
      provides: "reproducibility guide"
      contains: "results-index.csv"
    - path: "thesis/frontmatter/abstract.tex"
      provides: "restructured abstract"
      contains: "82.5"
    - path: "thesis/frontmatter/notation.tex"
      provides: "abbreviations + symbol table"
      contains: "LayerNorm"
  key_links:
    - from: "thesis/frontmatter/abstract.tex"
      to: "thesis/chapters/ch09_conclusion.tex"
      via: "byte-consistent headlines"
      pattern: "82.5"
---

<objective>
Draft appendices D-F (engineering notes, reproducibility guide, additional figures) and the four
frontmatter files (titlepage, restructured abstract, acknowledgments placeholder, notation table)
per spec §4, placeholder-free (acknowledgments exempt).

Output: three appendices + frontmatter (~12-15 pages combined).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§1 App D-F + Frontmatter rows; §2 anchors)
@.planning/phases/13-full-thesis-manuscript/13-04-SUMMARY.md  (phase-6 disposition list — App F may use only INCLUDED/REGENERATED assets)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§4 map — binding)
</context>

<guardrails>
NUMBER GUARDRAILS (binding): canonical anchors only (RESEARCH §2); abstract headlines byte-identical 82.5 / 78.7;
honesty framings in the abstract (no SOTA claim, points not %, "small but consistent" complementarity,
corrupted-AUC-only TTA gains). Delete PLACEHOLDER-W0 except acknowledgments (permanent user-fill placeholder,
exempt per spec §11.2 — but replace the PLACEHOLDER-W0 token with a clearly-marked user-fill note so the token
grep still returns 0). Incremental build after each task.
</guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Draft appD_engineering.tex + appE_reproducibility.tex</name>
  <files>thesis/appendices/appD_engineering.tex, thesis/appendices/appE_reproducibility.tex</files>
  <read_first>
    - .planning/STATE.md lines 89-141 (Key Decisions log) and 154-165 (critical pitfalls C1-C5, M5-M7)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §1 App D row (Windows quirks list: PyTorch 1.12.1 conda WinError 182 → pip cu113; mmpose/PYSKL --no-deps chumpy; cuDNN 9 DLL paths; numpy<2 in vcc-ctrgcn; cv2 absent in vcc-main → PIL; cp950 cosmetic) and App E row (command entry points)
    - data/ucf_total_frames.json (manifest to document) and results/results-index.csv header (index to document)
  </read_first>
  <action>
    App D (~4pp): two/three-environment strategy (mmcv 1.x vs PyTorch 2.x conflict → vcc-skeleton / vcc-ctrgcn /
    vcc-main split, version matrix), Windows-specific quirks (the six from RESEARCH §1), extraction throughput
    (~21.6 FPS, 99.3% inference-bound), pitfall register (C1-C5, M5-M7 from STATE.md), anti-pattern register
    highlights (04-VERIFICATION WR-01..WR-04). Narrative sourced from tracked STATE.md — % Source comments.

    App E (~5pp, discharges review item T6-2): repo map; env setup → skeleton/CLIP extraction → training →
    evaluation → table/figure generation walkthrough with the exact tracked entry points: src/train.py via
    configs; src/evaluate.py; src/tta/evaluate_tta.py --method disc_reweight; scripts/generate_phase7_charts.py;
    scripts/generate_pub_figures.py; scripts/thesis_per_category_tables.py; scripts/build_thesis.ps1. Seeds
    {42,123,2024}; manifests (data/ucf_total_frames.json — git-reproducible UCF full-length eval); per-run
    config_snapshot.json; results/results-index.csv as the master index; the bit-identical rerun invariant.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "PLACEHOLDER-W0" thesis/appendices/appD_engineering.tex thesis/appendices/appE_reproducibility.tex (0 each); grep -c "ucf_total_frames.json" thesis/appendices/appE_reproducibility.tex (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - App D contains "21.6" and "99.3" and the three env names; App E contains all 7 command entry points
    - App E states the bit-identical rerun invariant and the seed set
    - build green
  </acceptance_criteria>
  <done>Engineering + reproducibility appendices complete; T6-2 discharged.</done>
</task>

<task type="auto">
  <name>Task 2: Draft appF_figures.tex</name>
  <files>thesis/appendices/appF_figures.tex</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-04-SUMMARY.md (disposition list — the ONLY admission ticket for phase-6 assets)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §1 App F row (B-series XD-only limitation wording)
  </read_first>
  <action>
    Write App F using ONLY figures that 13-04 dispositioned INCLUDED-V or REGENERATED-V (plus pri9_score_hist if
    Ch5 used a different asset): B-series skeleton overlays (XD-only BY NECESSITY — document that UCF source
    frames are 64×64 pre-sampled PNGs, too small for meaningful overlays), extra temporal curves (A-series
    survivors), D-series gate histograms not used in Ch5, any F-series survivors. Every figure caption names the
    verification basis (matching its PROVENANCE.md Class-V note). If a spec-suggested figure was DROPPED by
    13-04, note the omission in one sentence rather than resurrecting stale data.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "PLACEHOLDER-W0" thesis/appendices/appF_figures.tex (0); grep -c "64" thesis/appendices/appF_figures.tex (>=1 — the 64x64 limitation documented)</automated>
  </verify>
  <acceptance_criteria>
    - every \includegraphics in appF resolves to a file present in thesis/figures/ that appears in PROVENANCE.md
    - the XD-only B-series limitation is documented
    - zero C-series assets referenced; build green
  </acceptance_criteria>
  <done>App F complete using only provenance-verified figures.</done>
</task>

<task type="auto">
  <name>Task 3: Frontmatter — titlepage, restructured abstract, acknowledgments placeholder, notation</name>
  <files>thesis/frontmatter/titlepage.tex, thesis/frontmatter/abstract.tex, thesis/frontmatter/acknowledgments.tex, thesis/frontmatter/notation.tex</files>
  <read_first>
    - paper/main.tex lines 48-53 (abstract — the T5-2-dense original to restructure) and 32-46 (author block facts)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §1 Frontmatter row (T5-2 fix: trim, defer inline TTA mechanism to body)
    - thesis/chapters/ch03_methodology.tex (equation symbols to harvest for notation.tex)
  </read_first>
  <action>
    titlepage.tex: finalize the generic report titlepage — National Taiwan University of Science and Technology,
    Master's thesis, author Wei-Han Jeng, advisor Chuan-Kai Yang, title matching the paper's, year 2026.
    abstract.tex: restructure the paper abstract (fixes T5-2): break the ~285-word dense paragraph into 2-3
    paragraphs (context+method / results / TTA+takeaway); defer the inline TTA mechanism detail to Ch3; keep
    headlines byte-identical (82.5, 78.7) and every honesty framing (no SOTA claim, points not %, "small but
    consistent", corrupted-AUC-only TTA gains, +13.2 single most-degraded condition if mentioned).
    acknowledgments.tex: replace PLACEHOLDER-W0 with a clearly-marked user-fill note ("[To be written by the
    author]") — the ONLY permanent placeholder (spec §11.2 exempt).
    notation.tex: abbreviations table (VAD, WSVAD, MIL, TTA, LN/BN, GCN, CTR-GCN, VLM, CLIP, SigLIP2, AUC, AP,
    RQ, etc.) + symbol table harvested from the ch03 equations (T, k, λ1, λ2, g, ŝ, v̂, w_vl, s, w, a_t, L_rank...).
    Contents are Claude's discretion (CONTEXT) — cover every symbol used by 2+ chapters.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); grep -c "PLACEHOLDER-W0" thesis/frontmatter/*.tex (0 total); grep -c "82.5" thesis/frontmatter/abstract.tex (1); grep -c "78.7" thesis/frontmatter/abstract.tex (1)</automated>
  </verify>
  <acceptance_criteria>
    - abstract is 2-3 paragraphs, < 300 words total, headlines byte-identical to ch09
    - titlepage renders with NTUST / Wei-Han Jeng / Chuan-Kai Yang
    - notation.tex has both an abbreviations and a symbols section; acknowledgments carries the user-fill note
    - full clean build green
  </acceptance_criteria>
  <done>Frontmatter complete; abstract restructured with framings intact.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Tampering | abstract headline drift | mitigate | byte-consistency greps (82.5/78.7) vs ch09; Wave-3 headline-consistency check re-verifies |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Clean build after Task 3; zero undefined refs
- PLACEHOLDER-W0 count across thesis/ = 0 after this plan (acknowledgments uses the user-fill note instead)
</verification>

<success_criteria>
- Appendices D-F + frontmatter complete per spec §4 (SC2); T6-2 and T5-2 discharged; framings intact (SC5)
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-10-SUMMARY.md` when done.
</output>
