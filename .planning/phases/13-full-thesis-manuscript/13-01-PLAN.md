---
phase: 13-full-thesis-manuscript
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thesis/main.tex
  - thesis/preamble.tex
  - thesis/frontmatter/titlepage.tex
  - thesis/frontmatter/abstract.tex
  - thesis/frontmatter/acknowledgments.tex
  - thesis/frontmatter/notation.tex
  - thesis/chapters/ch01_introduction.tex
  - thesis/chapters/ch02_related_work.tex
  - thesis/chapters/ch03_methodology.tex
  - thesis/chapters/ch04_experimental_setup.tex
  - thesis/chapters/ch05_results_fusion.tex
  - thesis/chapters/ch06_results_tta.tex
  - thesis/chapters/ch07_discussion.tex
  - thesis/chapters/ch08_limitations.tex
  - thesis/chapters/ch09_conclusion.tex
  - thesis/appendices/appA_rtfm.tex
  - thesis/appendices/appB_per_category.tex
  - thesis/appendices/appC_sweep.tex
  - thesis/appendices/appD_engineering.tex
  - thesis/appendices/appE_reproducibility.tex
  - thesis/appendices/appF_figures.tex
  - thesis/figures/fig_architecture.tex
  - thesis/IEEEtranN.bst
  - thesis/.latexmkrc
  - thesis/references.bib
  - scripts/build_thesis.ps1
  - .gitignore
autonomous: true
requirements: [SC1]
must_haves:
  truths:
    - "Running scripts/build_thesis.ps1 -Clean from repo root produces thesis/main.pdf with zero LaTeX errors and zero undefined refs/citations"
    - "The build is non-interactive: no PDF viewer opens unless -Open is passed"
    - "BibTeX resolves via the vendored thesis/IEEEtranN.bst, not any MiKTeX-installed copy"
    - "git status stays clean of thesis build artifacts after a build"
  artifacts:
    - path: "thesis/main.tex"
      provides: "report-class root with \\include chain for 9 chapters + 6 appendices + frontmatter"
      contains: "\\documentclass[12pt,a4paper,oneside]{report}"
    - path: "thesis/IEEEtranN.bst"
      provides: "vendored natbib-compatible bibliography style"
      contains: "IEEEtranN.bst"
    - path: "scripts/build_thesis.ps1"
      provides: "non-interactive latexmk wrapper with preflight + log scan"
      contains: "IEEEtranN.bst"
    - path: "thesis/references.bib"
      provides: "bib superset: 27 paper entries + 11 SOTA + 4 survey-support"
      contains: "sultani2018ucfcrime"
  key_links:
    - from: "scripts/build_thesis.ps1"
      to: "thesis/main.tex"
      via: "latexmk run inside thesis/ with .latexmkrc"
      pattern: "latexmk"
    - from: "thesis/main.tex"
      to: "thesis/chapters/ch01_introduction.tex"
      via: "\\include chain"
      pattern: "include\\{chapters/ch01"
---

<objective>
Create the complete thesis/ LaTeX skeleton (spec Wave 0, part 1): report-class root + preamble,
placeholder chapters/appendices/frontmatter, vendored IEEEtranN.bst, thesis/.latexmkrc,
non-interactive scripts/build_thesis.ps1 with preflight + log scan, .gitignore extensions
(thesis artifacts + results/* rewrite), and the references.bib superset. Ends with a clean
skeleton build.

Purpose: every later plan compiles against this skeleton; build gate (13-VALIDATION) requires it.
Output: compiling thesis/ tree + build tooling + bib superset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§7 LaTeX build notes, §6 bib plan, §3 gitignore mechanics)
@.planning/phases/13-full-thesis-manuscript/13-PATTERNS.md   (main.tex/preamble/latexmkrc/build-script/gitignore/bib patterns)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§3 deliverable layout — binding)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create thesis/ skeleton tree with vendored .bst and architecture-figure smoke test</name>
  <files>thesis/main.tex, thesis/preamble.tex, thesis/frontmatter/titlepage.tex, thesis/frontmatter/abstract.tex, thesis/frontmatter/acknowledgments.tex, thesis/frontmatter/notation.tex, thesis/chapters/ch01_introduction.tex ... ch09_conclusion.tex, thesis/appendices/appA_rtfm.tex ... appF_figures.tex, thesis/figures/fig_architecture.tex, thesis/IEEEtranN.bst</files>
  <read_first>
    - docs/superpowers/specs/2026-07-05-full-thesis-design.md §3 (lines 30-79: exact layout, file names)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §7 (preamble pairing, report-class specifics, TikZ reuse, vendored .bst provenance)
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md "thesis/main.tex + thesis/preamble.tex" section (package block to carry, root structure, comment banner convention)
    - paper/main.tex lines 23-28 (package block) and 113-118 (figure* + resizebox pattern)
    - paper/figures/fig_architecture.tex (source to copy verbatim)
  </read_first>
  <action>
    Create the thesis/ tree exactly per spec §3. main.tex: `\documentclass[12pt,a4paper,oneside]{report}`,
    `\input{preamble}`, frontmatter (titlepage, `\chapter*{Abstract}` + `\addcontentsline{toc}{chapter}{Abstract}`,
    acknowledgments, `\tableofcontents`, `\listoffigures`, `\listoftables`, notation), then
    `\include{chapters/ch01_introduction}` ... `\include{chapters/ch09_conclusion}`, then `\appendix` +
    `\include{appendices/appA_rtfm}` ... `\include{appendices/appF_figures}`, then
    `\bibliographystyle{IEEEtranN}` + `\bibliography{references}`. Use `%%` banner comments per paper convention.
    Exact chapter filenames: ch01_introduction, ch02_related_work, ch03_methodology, ch04_experimental_setup,
    ch05_results_fusion, ch06_results_tta, ch07_discussion, ch08_limitations, ch09_conclusion. Appendices:
    appA_rtfm, appB_per_category, appC_sweep, appD_engineering, appE_reproducibility, appF_figures.

    preamble.tex packages, in this order (RESEARCH §7): geometry (margin=2.5cm), amsmath, amssymb, booktabs,
    multirow, graphicx, array (needed by the SOTA fragment column setups — Pitfall 14), tikz +
    `\usetikzlibrary{positioning,arrows.meta,fit,calc}`, setspace + `\onehalfspacing`, caption, subcaption,
    microtype, `\usepackage[numbers,sort&compress]{natbib}`, hyperref LAST with bookmarksnumbered + hidelinks.
    IEEEtranN REQUIRES natbib — never remove it.

    Each placeholder chapter/appendix: `\chapter{<real title per spec §4>}` + one paragraph containing the
    literal marker `PLACEHOLDER-W0` (Wave-1/2 writers must delete it; acceptance greps depend on this exact token).
    Each frontmatter placeholder likewise, EXCEPT acknowledgments which keeps a permanent user-fill placeholder
    (exempt per spec §11.2). titlepage.tex placeholder must already carry the real facts: National Taiwan
    University of Science and Technology (NTUST), author Wei-Han Jeng, advisor Chuan-Kai Yang, Master's thesis.

    Copy paper/figures/fig_architecture.tex verbatim to thesis/figures/fig_architecture.tex (never \input across
    into frozen paper/). In ch03_methodology.tex placeholder, include a smoke test:
    `\begin{figure}[t]\centering\resizebox{\textwidth}{!}{\input{figures/fig_architecture}}\caption{PLACEHOLDER-W0 architecture smoke test}\label{fig:architecture}\end{figure}`
    — this exercises TikZ + the one-column width question (RESEARCH A5) at Wave 0.

    Vendor the .bst: copy C:/Users/Austin/AppData/Local/Programs/MiKTeX/bibtex/bst/feupphdteses/IEEEtranN.bst
    to thesis/IEEEtranN.bst byte-identical (do NOT edit the file). Provenance note lands in PROVENANCE.md (plan 13-02).
  </action>
  <verify>
    <automated>ls thesis/chapters | grep -c "^ch0" (returns 9); ls thesis/appendices | grep -c "^app" (returns 6); cmp "C:/Users/Austin/AppData/Local/Programs/MiKTeX/bibtex/bst/feupphdteses/IEEEtranN.bst" thesis/IEEEtranN.bst (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - thesis/main.tex contains `\documentclass[12pt,a4paper,oneside]{report}` and 9 `\include{chapters/` lines and 6 `\include{appendices/` lines
    - thesis/IEEEtranN.bst is byte-identical to the MiKTeX feupphdteses copy (cmp exit 0) and its header contains "Version 1.13"
    - thesis/preamble.tex loads natbib with [numbers,sort&compress] and hyperref is the last \usepackage
    - grep -c "PLACEHOLDER-W0" on each of the 15 chapter/appendix files returns >= 1
    - thesis/figures/fig_architecture.tex is byte-identical to paper/figures/fig_architecture.tex (cmp exit 0)
    - thesis/frontmatter/titlepage.tex contains "Wei-Han Jeng" and "Chuan-Kai Yang"
  </acceptance_criteria>
  <done>Complete thesis/ tree exists with correct filenames, vendored .bst, and TikZ smoke test wired into ch03.</done>
</task>

<task type="auto">
  <name>Task 2: build_thesis.ps1 + thesis/.latexmkrc + .gitignore extensions</name>
  <files>scripts/build_thesis.ps1, thesis/.latexmkrc, .gitignore</files>
  <read_first>
    - scripts/build_paper.ps1 (full 118 lines — the model; line 111 Invoke-Item is the anti-pattern to exclude)
    - paper/.latexmkrc (copy nearly verbatim)
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md "scripts/build_thesis.ps1" and ".gitignore (edit)" sections
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §3 "Gitignore mechanics" + §7 "scripts/build_thesis.ps1" (COPY/CHANGE list)
    - .gitignore (current state; line 9 is `results/`, lines 46-57 the paper artifact block)
  </read_first>
  <action>
    thesis/.latexmkrc: copy paper/.latexmkrc content verbatim ($pdf_mode=1, $bibtex_use=2, pdflatex/bibtex
    lines with --enable-installer, @default_files=('main.tex')); update header comment to reference the thesis.

    scripts/build_thesis.ps1 modeled on build_paper.ps1: copy Find-Exe probe, latexmk probe at
    $env:LOCALAPPDATA\Programs\MiKTeX\miktex\bin\x64, PATH injection, Strawberry Perl probe C:\Strawberry\perl\bin,
    Push-Location/Pop-Location around latexmk, -Clean → `latexmk -C` then full build, exit-code handling,
    message prefix `[build_thesis]`. CHANGES (per D-spec §3 / RESEARCH §7):
    (1) target thesis/main.tex; NO Invoke-Item by default; add `[switch]$Open` gating `Invoke-Item $pdf`.
    (2) Preflight BEFORE build: HARD-assert thesis/IEEEtranN.bst exists (exit 1 with clear message if absent);
        REPORT-only loop `kpsewhich <pkg>.sty` over: natbib, booktabs, multirow, graphicx, geometry, setspace,
        caption, subcaption, microtype, amsmath, amssymb, array, tikz (pgf) — absence is NOT an error (MiKTeX
        installs on demand); print resolution status per package for diagnosability.
    (3) Post-build log scan (NEW — build_paper.ps1 has none): exit nonzero if thesis/main.log contains
        "There were undefined references", "Citation .* undefined", "Reference .* undefined", or lines starting
        with "!" (LaTeX errors).

    .gitignore (single owner of this file in wave 1 — plan 13-02 must NOT edit it):
    (a) Add a thesis artifact block mirroring the paper block at lines 46-57: thesis/main.pdf, thesis/*.aux,
        thesis/*.bbl, thesis/*.blg, thesis/*.out, thesis/*.fls, thesis/*.fdb_latexmk, thesis/*.toc,
        thesis/*.synctex.gz, thesis/*.cut, thesis/*.lof, thesis/*.lot (global *.log rule already covers main.log).
    (b) Rewrite line 9 `results/` → `results/*` and add self-documenting un-ignore rules (discharges review item
        C7-3): `!results/results-index.csv`, `!results/benchmark_models.csv`, `!results/backbone_bench_combined.csv`,
        `!results/_analysis_2026-06-10/` + `!results/_analysis_2026-06-10/**`. For subdirs holding a mix of wanted
        and unwanted files (_coral_derisk, _tta_rerun_continual, h1_recompute, phase10_charts, and the 128 run dirs)
        DO NOT add broad un-ignore rules — their already-force-tracked files stay tracked regardless of gitignore,
        and plan 13-02 adds new files there via `git add -f` (spec §7a explicitly permits either mechanism; hybrid
        keeps `git status` free of ~880 untracked TTA JSONs). Add a comment block explaining this policy inline.
    After the rewrite assert nothing previously tracked became ignored: `git status --porcelain` shows no
    unexpected deletions and `git ls-files results/ | wc -l` still returns 28.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0 — run after Task 3's bib lands; here assert file existence + `git ls-files results/ | wc -l` returns 28)</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build_thesis.ps1 contains "[switch]$Open" and does NOT contain an ungated "Invoke-Item" (Invoke-Item appears only inside the `if ($Open)` block)
    - scripts/build_thesis.ps1 contains "IEEEtranN.bst" (preflight hard assert) and "There were undefined references" (log scan)
    - thesis/.latexmkrc contains "--enable-installer" twice (pdflatex + bibtex lines)
    - .gitignore contains "results/*" and "!results/_analysis_2026-06-10/" and "thesis/*.fdb_latexmk"
    - git ls-files results/ | grep -v '^$' | wc -l returns 28 (no tracked file lost by the rewrite)
  </acceptance_criteria>
  <done>Build tooling exists; gitignore covers thesis artifacts and results/* un-ignore policy is self-documenting.</done>
</task>

<task type="auto">
  <name>Task 3: references.bib superset + first clean skeleton build</name>
  <files>thesis/references.bib</files>
  <read_first>
    - paper/references.bib (27 entries — copy verbatim as base; note banner-block organization and entry style at lines 1-17)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §6 (bib plan: 11 SOTA keys table + 4 survey-support entries + key convention)
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md §8 lines 150-188 (author/venue strings for the 11 new entries)
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md "thesis/references.bib" section (entry style; the `and others` anti-pattern)
  </read_first>
  <action>
    Copy paper/references.bib → thesis/references.bib verbatim (27 entries), then append two banner blocks:

    (1) `%% THESIS SOTA` block — 11 entries with exactly these keys (RESEARCH §6 table): zhang2024holmesvad,
    zhang2025holmesvau, wu2024stprompt, song2025fdpn, yang2024tpwng, pu2024pel4vad, leng2026piercingeye,
    zanella2024anomalyclip, chen2023tevad, peng2024hypervd, ghadiya2024crossmodal. Fields from
    12-RESEARCH-sota.md §8 rows (authors/venues/arXiv IDs as staged there). TRAP (Pitfall 4): 12-RESEARCH-sota.md
    §8's Light-WVAD row still lists the fabricated XD 77.3 — bib fields only, never copy result numbers from it.

    (2) `%% THESIS SURVEY` block — 4 entries: yan2018stgcn (Yan, Xiong & Lin, ST-GCN, AAAI 2018),
    hendrycks2019imagenetc (Hendrycks & Dietterich, ImageNet-C, ICLR 2019), sun2016coral (Sun, Feng & Saenko,
    Return of Frustratingly Easy Domain Adaptation, AAAI 2016), schneider2020norm (Schneider et al., covariate
    shift adaptation, NeurIPS 2020).

    Every appended entry gets a trailing comment `% GATE-13: verify in Wave-2 primary-source gate` (these are
    [ASSUMED]/staged fields per RESEARCH A2/A3; plan 13-11 verifies, plan 13-12 removes every GATE-13 marker —
    zero survive to final). Follow the column-aligned entry style; do NOT use `and others` in any new entry.
    Do not add any \cite of the new keys yet (placeholder chapters cite nothing — natbib unused keys are fine).

    Then run the first clean build: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean`.
    Must exit 0 with the log scan passing. Fix any preamble/package issue surfaced (e.g. array/tikz). Confirm
    the architecture-figure smoke test rendered (page count > 15 implies TOC+chapters+figure present). Then run
    `C:/Anaconda/envs/vcc-main/python.exe -m pytest -q` (expect 322 passed baseline) and confirm
    `git status --porcelain` shows no thesis build artifacts.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); grep -c "GATE-13" thesis/references.bib (returns 15); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green)</automated>
  </verify>
  <acceptance_criteria>
    - thesis/references.bib contains all 27 base keys (spot: sultani2018ucfcrime, tian2021rtfm, niu2023sar) AND all 15 new keys listed above
    - grep -c "and others" thesis/references.bib returns exactly 4 — the count inherited from the 4 base entries carrying `and others` (majhi2025pivad, yin2026dsanet, shao2025eventvad, damicantonio2025gsmoe) and no more — no NEW entry uses it
    - build_thesis.ps1 -Clean exits 0; thesis/main.log contains no "undefined" citations/references and no "!" error lines
    - thesis/main.pdf exists; git status --porcelain contains no thesis/*.aux, *.bbl, main.pdf lines
    - pytest exits green (322 passed baseline)
  </acceptance_criteria>
  <done>Skeleton compiles clean end-to-end with the full bib superset; repo hygiene intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MiKTeX/CTAN on-demand install | packages fetched during build (same baseline as existing paper build) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | vendored thesis/IEEEtranN.bst | mitigate | copied byte-identical from local MiKTeX; authentic Michael Shell v1.13 header verified (RESEARCH §7); provenance note recorded in PROVENANCE.md (13-02); cmp assertion in Task 1 |
| T-13-SC | Tampering | package installs | accept | no npm/pip/cargo installs this phase; MiKTeX on-demand = existing paper-build baseline (RESEARCH Package Legitimacy Audit: N/A) |
No HIGH threats — documentation/LaTeX phase, no attack surface.
</threat_model>

<verification>
- `build_thesis.ps1 -Clean` exit 0, log scan pass (13-VALIDATION build gate)
- `git ls-files results/` count unchanged (28)
- pytest green; git status clean of thesis artifacts
</verification>

<success_criteria>
- Skeleton thesis/ tree per spec §3 compiles clean with vendored .bst and non-interactive build script
- bib superset (42 entries) in place with GATE-13 markers for Wave-2 verification
- .gitignore covers thesis artifacts + self-documenting results/* policy (C7-3 discharged)
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-01-SUMMARY.md` when done (include: final package list the preflight reports, page count of skeleton PDF, any TikZ one-column legibility observations for A5).
</output>
