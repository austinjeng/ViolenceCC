---
phase: 13-full-thesis-manuscript
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - scripts/thesis_severity_heatmap.py
  - scripts/thesis_corruption_heatmaps.py
  - scripts/thesis_per_category_tables.py
  - scripts/thesis_tta_rollup.py
  - thesis/figures/fig_severity_heatmap.pdf
  - thesis/figures/fig_corruption_source.pdf
  - thesis/figures/fig_corruption_disc_reweight.pdf
  - thesis/tables/tab_percat_ucf.tex
  - thesis/tables/tab_percat_xd.tex
  - thesis/tables/tab_percat_rtfm.tex
  - results/_tta_rerun_continual/per_condition_rollup.csv
  - thesis/figures/fig_sweep_s01_ucf.png
  - thesis/figures/fig_sweep_s02_xd.png
autonomous: true
requirements: [SC4]
# DISPATCH NOTE (checker W1): execute-phase groups by wave and does not read depends_on.
# This plan MUST be dispatched AFTER 13-02 completes (sequential within wave 2, or rely on
# per-plan worktree isolation). Reason: Task 2's `git add -f` of the rollup CSV races
# 13-02's hard `git ls-files results/ | wc -l == 297` gate in a shared working tree.
must_haves:
  truths:
    - "Every new figure/table asset is Class R: generated by a tracked script from tracked data"
    - "The forbidden phase6 C-series corruption heatmaps are replaced by post-fix continual-data heatmaps"
    - "Per-category tables reflect the current post-lambda0/post-h1 per_category.csv files, 3-seed mean±std"
  artifacts:
    - path: "scripts/thesis_per_category_tables.py"
      provides: "3-seed per-category LaTeX table generator from canonical run dirs"
      contains: "per_category.csv"
    - path: "thesis/tables/tab_percat_ucf.tex"
      provides: "UCF 13-category table for Ch5/App B"
      contains: "\\toprule"
    - path: "results/_tta_rerun_continual/per_condition_rollup.csv"
      provides: "tracked per-condition TENT/SAR rollup (partially discharges C7-1)"
      contains: "gaussian"
  key_links:
    - from: "scripts/thesis_severity_heatmap.py"
      to: "results/_analysis_2026-06-10/pri5_per_condition_heatmap.csv"
      via: "tracked data input"
      pattern: "pri5_per_condition_heatmap"
    - from: "scripts/thesis_corruption_heatmaps.py"
      to: "results/_coral_derisk/r1full_"
      via: "tracked data input"
      pattern: "r1full_"
---

<objective>
Write and run the new Class-R generator scripts (spec Wave 0, part 3): 20×4 severity heatmap,
corruption heatmaps replacing the forbidden C-series, per-category LaTeX tables from canonical run
dirs, and the TENT/SAR per-condition rollup CSV; plus regenerate the Phase-7 sweep charts. All
outputs land in thesis/figures/ + thesis/tables/ with tracked scripts + tracked inputs.

Purpose: Ch5/Ch6/App B/App C writers consume these assets; SC4 requires Class R provenance.
Output: 4 new tracked scripts + their outputs + regenerated sweep charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§4 figure worksheet "Regenerate zero-GPU" table; §2 Pri-5 traps; §8 Pitfalls 3/6/12)
@.planning/phases/13-full-thesis-manuscript/13-PATTERNS.md   ("New figure/table generator scripts" section — patterns A-D are binding)
@scripts/pri5_tta_breakdown.py                                (provenance docstring + LaTeX emission + self-check patterns)
@scripts/generate_phase10_charts.py                           (_seed_mean + run-name parsing patterns)
@scripts/generate_pub_figures.py                              (publication style: 300 DPI, serif, fonttype 42)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write severity-heatmap + corruption-heatmap generators</name>
  <files>scripts/thesis_severity_heatmap.py, scripts/thesis_corruption_heatmaps.py</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md "New figure/table generator scripts" (patterns A: provenance docstring/determinism; D: pub style — copy _setup_style rcParams, PUB_DPI=300, fonttype 42, Agg backend)
    - scripts/pri5_tta_breakdown.py lines 1-38 (docstring model) and 94-96 (self-check assert habit)
    - results/_analysis_2026-06-10/pri5_per_condition_heatmap.csv (input format: family,severity,condition,CLIP ViT-B/16,SigLIP2-base,SigLIP2-SO400M,SigLIP2-giant)
    - results/_coral_derisk/r1full_clip.json (input structure: {summary, per_condition}) and results/_tta_rerun_continual/summary_3seed.json (TENT/SAR/source means)
  </read_first>
  <action>
    scripts/thesis_severity_heatmap.py: read results/_analysis_2026-06-10/pri5_per_condition_heatmap.csv
    (tracked) → render a 20-condition × 4-backbone disc_reweight ΔAUC heatmap →
    thesis/figures/fig_severity_heatmap.pdf. Row order: 4 families × severities 1-5 (gaussian_noise,
    jpeg_compression, brightness, motion_blur). Annotate cell values to 1 decimal. CPU-only, matplotlib Agg,
    provenance docstring naming the exact tracked input, deterministic (no RNG needed; if any, seed 12345).
    TRAP (Pitfall 6): label the colorbar/caption as "disc_reweight ΔAUC (points), 3-seed" — never mix the
    +4.83/+3.31/+8.95/+13.2 aggregations; this figure shows per-condition values only.

    scripts/thesis_corruption_heatmaps.py: replace the FORBIDDEN phase6 C-series. Inputs (all tracked):
    results/_coral_derisk/r1full_{clip,base,so400m,giant}.json + results/_coral_derisk/variants/v_*_{s42_train,s123_test,s2024_test}.json
    (per-condition disc_reweight + source means) and results/_tta_rerun_continual/summary_3seed.json (TENT/SAR
    per-backbone 20-condition means). Emit two figures: thesis/figures/fig_corruption_source.pdf (source-only
    corrupted AUC per condition × backbone — degradation map) and thesis/figures/fig_corruption_disc_reweight.pdf
    (disc_reweight corrupted AUC per condition × backbone). Self-check asserts: recompute per-backbone source-only
    means and assert ≈ 63.71/57.01/58.87/62.83 (CLIP/Base/SO400M/Giant, tolerance 0.05) so stale data fails loudly.
    Do NOT read anything under results/phase6_charts/C_corruption/ (forbidden pre-fix data).
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe scripts/thesis_severity_heatmap.py (exit 0); C:/Anaconda/envs/vcc-main/python.exe scripts/thesis_corruption_heatmaps.py (exit 0); ls thesis/figures/fig_severity_heatmap.pdf thesis/figures/fig_corruption_source.pdf thesis/figures/fig_corruption_disc_reweight.pdf (exist, size > 5KB)</automated>
  </verify>
  <acceptance_criteria>
    - both scripts contain a provenance docstring naming their exact tracked inputs and "CPU-only"
    - thesis_corruption_heatmaps.py contains an assert against 63.71/57.01/58.87/62.83 source means
    - neither script references phase6_charts or C_corruption
    - the 3 output PDFs exist and are non-empty
  </acceptance_criteria>
  <done>Severity + corruption heatmap generators written, run clean, outputs in thesis/figures/.</done>
</task>

<task type="auto">
  <name>Task 2: Write per-category table generator + TENT/SAR rollup script</name>
  <files>scripts/thesis_per_category_tables.py, scripts/thesis_tta_rollup.py, results/_tta_rerun_continual/per_condition_rollup.csv</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md patterns B (LaTeX emission with provenance comment header, pri5_tta_breakdown.py:184-205) and C (_seed_mean + run-name parsing, generate_phase10_charts.py:87-109 + COMPARISON_MAP:59-84)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §3 run-dir enumeration (128 canonical dirs pattern) and §8 Pitfall 9 (per-category regeneration mandatory) + Pitfall 12 (backbone column EMPTY — parse run_name)
    - results/ucf_gated_fusion_giant_s42/per_category.csv (format: category,auc,ap — one row per class)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §3 "Planner decision points" (rollup CSV recommendation — ADOPTED by this plan)
  </read_first>
  <action>
    scripts/thesis_per_category_tables.py: read per_category.csv from the canonical run dirs (Wave-0 tracked by
    13-02; enumerate with the RESEARCH §3 naming pattern) → emit three booktabs LaTeX tables with % Source:
    provenance comment headers (pattern B):
    - thesis/tables/tab_percat_ucf.tex — UCF 13 categories, Gated Fusion Giant (headline variant), 3-seed mean±std
      AUC per category (dirs ucf_gated_fusion_giant_s{42,123,2024})
    - thesis/tables/tab_percat_xd.tex — XD 6 categories, Gated Fusion SO400M, 3-seed mean±std AP
      (dirs xd_gated_fusion_so400m_s{42,123,2024})
    - thesis/tables/tab_percat_rtfm.tex — RTFM per-category from results/xd_i3d_rtfm_i3d_s42/per_category.csv +
      results/xd_i3d_rtfm_i3d_flow_s42/per_category.csv (s42-only — state that in the caption comment)
    TRAP (Pitfall 9): NEVER hardcode Phase-4/4c-era values (Fighting 0.955, Assault 0.985, Riot 88.15, Abuse 44.89);
    the CSVs are the only source. Do NOT extend scripts/generate_latex_tables.py (its Table-3 path is stale — Pitfall 1).

    scripts/thesis_tta_rollup.py: walk results/_tta_rerun_continual/<backbone>/<method>/<condition>/eval_metrics.json
    (~880 untracked JSONs) → emit one tidy CSV results/_tta_rerun_continual/per_condition_rollup.csv with columns
    backbone,method,condition,seed,auc. Self-check: recompute per-backbone 20-condition means from the rollup and
    assert they match summary_3seed.json within 0.01. Then `git add -f results/_tta_rerun_continual/per_condition_rollup.csv`
    + `git ls-files --error-unmatch` assert (ORDERING: this plan depends_on 13-02, so this add -f lands only
    after 13-02's `git ls-files results/ | wc -l` == 297 assertion has passed — no git-index race or index.lock
    contention; after this add the results/ tracked count is 298). This gives Ch 6 a tracked per-condition
    TENT/SAR source and partially
    discharges review item C7-1 (RESEARCH open question 1 — rollup option ADOPTED; scripts/_tmp_r1_full.py promotion
    NOT adopted, tracked pri5/aggregate scripts already give Table-3 provenance).
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe scripts/thesis_per_category_tables.py (exit 0); C:/Anaconda/envs/vcc-main/python.exe scripts/thesis_tta_rollup.py (exit 0); git ls-files --error-unmatch results/_tta_rerun_continual/per_condition_rollup.csv (exit 0); grep -c "toprule" thesis/tables/tab_percat_ucf.tex (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - tab_percat_ucf.tex has 13 category rows; tab_percat_xd.tex has 6; both carry % Source: comment headers naming script + run dirs
    - grep -c "0.955\|0.985\|88.15\|44.89" on the three .tex outputs returns 0 (no Phase-4/4c relics)
    - per_condition_rollup.csv is tracked and its recomputed means match summary_3seed.json (script assert passes)
    - thesis_tta_rollup.py contains the self-check assert against summary_3seed.json
  </acceptance_criteria>
  <done>Per-category tables + rollup CSV generated from current data, tracked, with self-checks green.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate Phase-7 sweep charts and stage thesis copies</name>
  <files>thesis/figures/fig_sweep_s01_ucf.png, thesis/figures/fig_sweep_s02_xd.png (plus S04/S05 copies if used)</files>
  <read_first>
    - scripts/generate_phase7_charts.py lines 55-56 (hardcoded XD_BASELINE_AP=0.7192 / UCF_BASELINE_AUC=0.8227) and 266/283/295/297 (S03 baselines + Delta columns) — the historical-baseline guard (RESEARCH §8 Pitfall 3)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §4 "Regenerate zero-GPU" row 1 (guard wording — binding)
  </read_first>
  <action>
    Run `C:/Anaconda/envs/vcc-main/python.exe scripts/generate_phase7_charts.py` (reads tracked
    results/results-index.csv; recreates the absent results/phase7_charts/ with S01/S02/S04/S05 heatmaps + S03
    confirmation MD). Copy the sweep heatmaps needed by Ch5/App C into thesis/figures/ as
    fig_sweep_s01_ucf.png, fig_sweep_s02_xd.png (and S04/S05 as fig_sweep_s04_*.png/fig_sweep_s05_*.png).
    Record in the SUMMARY, verbatim for downstream writers: "HISTORICAL-BASELINE GUARD: these charts embed
    Delta columns vs the 2026-05 sweep configuration (CLIP backbone, pre-λ0, s42; baselines 0.8227 UCF /
    0.7192 XD and S03 3-seed 81.98±0.29 / 70.98±1.08). Thesis captions/tables must exclude those delta columns
    or label them 'vs historical 2026-05 sweep configuration'; never chain to 82.5/78.7."
    Do NOT track results/phase7_charts/ itself (Class R: script + results-index.csv are both tracked; the
    thesis/figures copies are the committed assets). Leave results/phase7_charts/ as regenerable scratch.
  </action>
  <verify>
    <automated>ls thesis/figures/ | grep -c "fig_sweep_" (>=2); ls results/phase7_charts/ (exists, non-empty); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green — wave-2 per-wave pytest gate)</automated>
  </verify>
  <acceptance_criteria>
    - generate_phase7_charts.py exits 0 against tracked results-index.csv
    - at least fig_sweep_s01_ucf.png and fig_sweep_s02_xd.png exist in thesis/figures/, non-empty
    - SUMMARY contains the historical-baseline guard paragraph verbatim
    - pytest green (322-passed baseline — wave-2 per-wave gate; the new thesis_* scripts introduce no regressions)
  </acceptance_criteria>
  <done>Sweep charts regenerated from tracked data; thesis copies staged; guard documented for writers.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | figure/table data integrity | mitigate | Class-R rule: tracked scripts + tracked inputs only; self-check asserts against tracked summary JSONs fail loudly on stale data |
| T-13-SC | Tampering | package installs | accept | none — vcc-main env already has the plotting stack |
No HIGH threats.
</threat_model>

<verification>
- All 4 scripts run clean under C:/Anaconda/envs/vcc-main/python.exe (shell-default python lacks matplotlib)
- Outputs exist in thesis/figures/ + thesis/tables/; rollup CSV tracked
- Incremental `build_thesis.ps1` still exits 0 (no LaTeX change expected, but run per 13-VALIDATION sampling)
- pytest green (wave-2 per-wave gate per 13-VALIDATION sampling rate)
</verification>

<success_criteria>
- Every Wave-0 gap item from 13-VALIDATION "Wave 0 Requirements" line 5 (Class-R scripts) closed
- Forbidden C-series fully replaced; per-category tables current-era; Pri-5 aggregation labels kept distinct
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-03-SUMMARY.md` when done (MUST include: exact list of generated asset filenames for chapter writers, the historical-baseline guard paragraph, and self-check results).
</output>
