---
phase: 13-full-thesis-manuscript
plan: 13
type: execute
wave: 8
depends_on: [13-12]
files_modified:
  - thesis/PROVENANCE.md
  - .planning/phases/13-full-thesis-manuscript/13-AUDIT-numbers.md
autonomous: false
requirements: [SC1, SC3, SC5]
must_haves:
  truths:
    - "Every numeric claim in thesis/ traces through PROVENANCE.md to a tracked source; the audit found zero untraceable and zero forbidden-source values"
    - "Headlines 82.5 / 78.7 are byte-consistent across abstract, chapters, tables, conclusion"
    - "Honesty framings verified present; no SOTA / online-TTA / clean-AUC-gain claims"
    - "Final PDF builds clean at 90-120 pages; pytest green; user approved the final document"
  artifacts:
    - path: ".planning/phases/13-full-thesis-manuscript/13-AUDIT-numbers.md"
      provides: "adversarial number-audit record (Phase-12 standard)"
      contains: "PASS"
    - path: "thesis/PROVENANCE.md"
      provides: "final complete manifest"
  key_links:
    - from: "thesis/PROVENANCE.md"
      to: "thesis/chapters/"
      via: "audit traces every chapter number to a manifest row"
      pattern: "Class"
---

<objective>
Wave-3 part 2 (phase exit): adversarial number audit against PROVENANCE.md (Phase-12 standard),
overclaim scan, headline-consistency check, final clean build + pytest + page count, and the final
USER CHECKPOINT.

Purpose: SC3 + SC5 + SC1 final certification per spec §8 steps 2-6 and §11.
Output: audit record, finalized PROVENANCE.md, approved thesis/main.pdf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§2 anchors + forbidden list — the audit oracle)
@.planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md   (format analog: the audit standard)
@.planning/phases/13-full-thesis-manuscript/13-VALIDATION.md  (gates map)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adversarial number audit against PROVENANCE.md</name>
  <files>.planning/phases/13-full-thesis-manuscript/13-AUDIT-numbers.md, thesis/PROVENANCE.md</files>
  <read_first>
    - .planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md (the traceability-table format and audit standard to replicate)
    - thesis/PROVENANCE.md (current manifest)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 (anchor + forbidden tables) and §8 (all 16 pitfalls — each is an audit probe)
  </read_first>
  <action>
    Adversarial audit at the Phase-12 standard: extract every numeric claim from thesis/*.tex (chapters,
    appendices, frontmatter, table files) and trace each to a PROVENANCE.md row → tracked source. For every
    manifest source re-assert `git ls-files --error-unmatch`. Probe specifically for the RESEARCH §8 pitfalls:
    tables_generated.tex values (83.0/79.9-as-mean-only, 71.8/40.9), phase-summary relics (83.6, 83.3, 83.28),
    +13.9, wrong p-value (0.008 outside an explicit two-test comparison), Phase-4/4c per-category relics,
    2-Person-as-headline promotion, sweep values chained to current headlines, CLAUDE.md corruption params,
    stored video_auc values, Light-WVAD 77.3. Cross-check every table cell lifted from main.tex against the
    paper source byte-for-byte. Add any missing PROVENANCE.md rows discovered (completeness direction) — the
    audit FAILS on untraceable numbers, forbidden-source values, or untracked manifest sources. Write
    13-AUDIT-numbers.md in the 12-VERIFICATION.md format: traceability table + findings + dispositions + final
    PASS/FAIL. Fix every finding (or route CONTENT rewrites back as fix-forward edits with re-audit) until PASS.
  </action>
  <verify>
    <automated>grep -c "PASS" .planning/phases/13-full-thesis-manuscript/13-AUDIT-numbers.md (>=1 final verdict); grep -rc "77.3\|tables_generated" thesis/chapters/ thesis/appendices/ (0)</automated>
  </verify>
  <acceptance_criteria>
    - audit record covers every chapter/appendix with per-number traces; final verdict PASS
    - every PROVENANCE.md source re-asserted tracked (loop output in the audit record)
    - zero forbidden-source values found (or all found ones fixed and re-audited)
  </acceptance_criteria>
  <done>Number audit PASS with complete manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Overclaim scan + headline-consistency check</name>
  <files>(fix-forward edits only if findings)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 "Honesty framings" (the checklist — each framing must be located in the text)
    - docs/superpowers/specs/2026-07-05-full-thesis-design.md §8 steps 3-4
  </read_first>
  <action>
    Overclaim scan (Phase-12 token approach): grep + contextual read for disallowed claims — "state-of-the-art"/
    "SOTA" applied to OUR method; online/streaming TTA claims (transductive cap); TTA gains framed as clean-AUC
    improvements; "%" used where "points" is required for gains; superlatives contradicting the ~88-91% ceiling
    disclosure. Locate each REQUIRED framing and record file:line — no-SOTA statement; field-ceiling disclosure;
    fair-subset framing (ch07); "+13.2 ... single most-degraded condition" (ch06); transductive disclosure (ch03/
    ch06/ch08); λ2 footnote (ch04); 64-frame disclosures (ch03/ch04); "small but consistent" complementarity
    (ch05). Headline-consistency: grep every occurrence of 82.5 and 78.7 across thesis/ — abstract, ch01, ch05,
    ch06, ch07 (own-result rows), ch09 all byte-identical (82.5 / 78.7, ±0.4 / ±0.9 where std is given); flag any
    82.49/78.72-style drift outside the explicitly-permitted Pri-6 CI context (82.49 allowed ONLY as the s42
    bootstrap point estimate in ch05). Append findings + dispositions to 13-AUDIT-numbers.md.
  </action>
  <verify>
    <automated>grep -rn "82\.5" thesis/frontmatter/abstract.tex thesis/chapters/ch09_conclusion.tex | grep -c "82.5" (>=2); grep -rc "state-of-the-art performance\|achieves SOTA" thesis/chapters/ (0)</automated>
  </verify>
  <acceptance_criteria>
    - every required framing located with file:line in the audit record
    - zero disallowed claims (each grep hit dispositioned)
    - headline byte-consistency verified with occurrence inventory in the audit record
  </acceptance_criteria>
  <done>Overclaim scan clean; framings verified present; headlines byte-consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Final clean build + repo hygiene + page count</name>
  <files>(none — gates only)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-VALIDATION.md (final gate set)
  </read_first>
  <action>
    Final gates: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean` exit 0
    with the log scan green; page count of thesis/main.pdf within 90-120 (extract via the .log "Output written"
    line or pypdf in vcc-main) — if outside the range, report to the user at the checkpoint rather than padding/
    cutting content unilaterally; `C:/Anaconda/envs/vcc-main/python.exe -m pytest -q` green (322-passed baseline);
    `git status --porcelain` clean of build artifacts; final commit of thesis/ + audit records.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green)</automated>
  </verify>
  <acceptance_criteria>
    - clean build exit 0, zero undefined refs/citations; page count recorded (target 90-120)
    - pytest green; git status clean
  </acceptance_criteria>
  <done>All automated phase gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: FINAL USER CHECKPOINT — thesis manuscript</name>
  <read_first>(none — presentation task)</read_first>
  <what-built>The complete oral-defense-ready thesis: 9 chapters + appendices A-F + frontmatter, N pages, clean build, number audit PASS, overclaim scan clean, headlines byte-consistent, zero verification debt, every figure Class R/V, every source tracked.</what-built>
  <files>(none — user verification)</files>
  <action>Present the final PDF (-Open), the 13-AUDIT-numbers.md PASS record, and the page count. Pause for final approval to close Phase 13.</action>
  <how-to-verify>
    1. Run: powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Open
    2. Review the final PDF end-to-end (layout, figures, tables, TOC)
    3. Skim .planning/phases/13-full-thesis-manuscript/13-AUDIT-numbers.md (PASS verdict + framing locations)
    4. Confirm page count is acceptable
  </how-to-verify>
  <acceptance_criteria>User types "approved" — phase complete — or lists final revisions.</acceptance_criteria>
  <verify>User response received ("approved" or final-revision list)</verify>
  <done>User approved the final manuscript (or final revisions applied and re-approved).</done>
  <resume-signal>Type "approved" to close Phase 13, or describe final revisions</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Repudiation/Tampering | final number integrity | mitigate | adversarial audit (Phase-12 standard) + tracked audit record + blocking final human checkpoint |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- 13-AUDIT-numbers.md PASS; all 6 ROADMAP success criteria certifiable from the audit record + build output
</verification>

<success_criteria>
- SC1: clean build, 90-120pp, full structure
- SC3: zero untraceable/forbidden numbers; headlines byte-consistent; all sources tracked
- SC5: framings verified present with locations
- Phase 13 user-approved
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-13-SUMMARY.md` when done (include final page count, audit verdict, and the SC1-SC6 certification table).
</output>
