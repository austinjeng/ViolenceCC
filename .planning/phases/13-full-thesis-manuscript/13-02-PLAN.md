---
phase: 13-full-thesis-manuscript
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - thesis/PROVENANCE.md
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "Every provenance source the thesis will cite is tracked in git (clean checkout reproduces the audit)"
    - "git ls-files --error-unmatch exits 0 for every one of the ~269 newly tracked files"
    - "thesis/PROVENANCE.md maps every canonical number family to a tracked source path"
  artifacts:
    - path: "thesis/PROVENANCE.md"
      provides: "tracked provenance manifest: number families + (later) figures/tables -> tracked sources"
      contains: "82.5"
  key_links:
    - from: "thesis/PROVENANCE.md"
      to: "results/_analysis_2026-06-10/pri5_per_condition_heatmap.csv"
      via: "manifest source row"
      pattern: "pri5_per_condition_heatmap"
---

<objective>
Track every provenance source named by the spec (~269 files per RESEARCH §3 exact list) with hard
per-file assertions, and create the thesis/PROVENANCE.md scaffold with the complete number-family
table (spec Wave 0, part 2).

Purpose: spec §7a is binding — the Wave-3 adversarial number audit FAILS any number whose manifest
source is untracked. This plan makes the audit passable.
Output: ~269 newly tracked files + PROVENANCE.md scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§3 exact Wave-0 file list — do NOT re-derive; §2 canonical numbers)
@.planning/phases/13-full-thesis-manuscript/13-PATTERNS.md   ("thesis/PROVENANCE.md" section — 12-VERIFICATION.md:41-54 table format)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§7a binding provenance rules)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track the exact RESEARCH §3 file list with per-file assertions</name>
  <files>(git index only — ~269 files under results/ added to tracking; no content edits)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §3 (lines 245-297: the EXACT MUST-TRACK list A/B/C + exclusions + assertion protocol — this list is binding, do not re-derive it)
    - .gitignore (state after 13-01: results/* rewrite + un-ignore rules for benchmark CSVs and _analysis_2026-06-10/)
  </read_first>
  <action>
    Track exactly the RESEARCH §3 list:
    A. results/benchmark_models.csv + results/backbone_bench_combined.csv (2 files — un-ignored by 13-01's
       gitignore rules, plain `git add` works).
    B. All 11 files in results/_analysis_2026-06-10/ (pri5_per_condition_heatmap.csv, pri5_tta_breakdown.md,
       pri5_tta_breakdown.tex, pri6_bootstrap_ci.json, pri6_bootstrap_ci.md, pri7_per_class_complementarity.csv,
       pri7_per_class_complementarity.md, pri9_failure_cases.json, pri9_failure_cases.md, pri9_score_hist.png,
       verify_pri5_pri6.json) — un-ignored, plain `git add`.
    C. per_category.csv + eval_metrics.json for exactly the 128 canonical run dirs (256 files) via `git add -f`
       (these dirs are still gitignore-excluded by design — see 13-01 policy comment). Enumerate the dirs
       programmatically from the RESEARCH §3 pattern: UCF 63 dirs (`ucf_skeleton_only_s*` ×3;
       `ucf_clip_only{,_siglip2,_so400m,_giant}_s*` ×12; `ucf_late_fusion{...}_s*` ×12; `ucf_gated_fusion{...}_s*` ×12;
       `ucf_gated_fusion{...}_2person_s*` ×12; `ucf_gated_fusion{...}_clip_mean_s*` ×12), XD 63 dirs (exact `xd_`
       mirror), RTFM 2 dirs (`xd_i3d_rtfm_i3d_s42`, `xd_i3d_rtfm_i3d_flow_s42`); seeds _s42/_s123/_s2024;
       backbone token BEFORE the pooling token. EXCLUDE all `.pre_lam0.bak`/`.pre_h1.bak` files — track only the
       current per_category.csv and eval_metrics.json.

    Assertion protocol (spec §7a — no silent failure): loop over every added path and run
    `git ls-files --error-unmatch <path>`; on any failure run `git check-ignore -v <path>` and fix (add -f or
    gitignore rule) before proceeding. Final count check: `git ls-files results/ | wc -l` returns 28 + 269 = 297.
    Write the full asserted path list into the SUMMARY (13-13's audit re-asserts from PROVENANCE.md rows).
  </action>
  <verify>
    <automated>git ls-files results/ | wc -l (returns 297); git ls-files --error-unmatch results/benchmark_models.csv results/_analysis_2026-06-10/pri9_score_hist.png "results/ucf_gated_fusion_giant_s42/per_category.csv" "results/xd_gated_fusion_so400m_s42/eval_metrics.json" (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - git ls-files results/ | wc -l returns exactly 297
    - git ls-files --error-unmatch exits 0 for every file in list A (2), B (11), and C (256) — full loop, not spot checks
    - git ls-files results/ | grep -c "\.bak" returns 0 (no backup files tracked)
    - the commit contains no eval_metrics.json.pre_h1.bak or .pre_lam0.bak paths
  </acceptance_criteria>
  <done>All 269 provenance sources tracked and individually asserted; count = 297 tracked files under results/.</done>
</task>

<task type="auto">
  <name>Task 2: thesis/PROVENANCE.md scaffold with the complete number-family table</name>
  <files>thesis/PROVENANCE.md</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 (canonical anchor table — every row becomes a manifest row) and §7 line 415 (exact .bst provenance wording)
    - .planning/phases/12-sota-comparison-and-positioning/12-VERIFICATION.md lines 41-54 (traceability-table format analog)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §8 Pitfall 13 (paths that must NEVER appear as sources: results/phase7_charts/ pre-regen, results/phase7_summary.md, results/phase7_rtfm_gap_diagnostic.json, results/phase4_charts/)
  </read_first>
  <action>
    Create thesis/PROVENANCE.md with four sections:

    (1) Header: purpose statement (spec §7a), required-package list for the preflight (natbib, booktabs, multirow,
    graphicx, tikz/pgf, hyperref, geometry, setspace, caption, subcaption, microtype, amsmath, amssymb, array),
    and the vendored-.bst provenance note EXACTLY as RESEARCH §7 words it: "copied from local MiKTeX feupphdteses
    package; authentic Michael Shell header v1.13 2008/09/30".

    (2) Number-family table — one row per RESEARCH §2 anchor-table row, columns: Family | Canonical value |
    Tracked source path(s) | Notes. Rows (values verbatim from RESEARCH §2): UCF headline 82.5±0.4 → paper/main.tex
    tab:ucf-ablation (L226-242); XD headline 78.7±0.9 → tab:xd-ablation (L245-261); skeleton-only 68.8±2.8/40.8±0.6;
    visual-only CLIP 81.2/74.6; visual-only Giant 82.4±0.2/76.8±1.0; late fusion; GF 2-Person 82.6±0.3/79.2±0.3
    (note: NOT headline — Pitfall 10); complementarity 6/8+2 ties p≈0.03; TENT/SAR 3-seed null →
    results/_tta_rerun_continual/summary_3seed.json; disc_reweight +1.21pp per-backbone → paper/main.tex tab:tta +
    results/_coral_derisk/r1full_*.json + variants/v_*.json; Pri-5 aggregations (+4.83/+3.31/+8.95/+13.2 kept
    distinct) → results/_analysis_2026-06-10/pri5_*; Pri-6 CIs → pri6_bootstrap_ci.json; Pri-7 per-class →
    pri7_per_class_complementarity.csv; Pri-9 → pri9_failure_cases.json; sweep (historical) → results/results-index.csv;
    RTFM repro → .planning/phases/04b-xd-violence-main-results-rtfm-xd-i3d-gate/04b-05-SUMMARY.md +
    .planning/phases/07-xd-violence-hyperparameter-sweep/07-VERIFICATION.md; efficiency → results/benchmark_models.csv +
    results/backbone_bench_combined.csv; dataset facts → paper/main.tex L136/L206; PRD gate misses →
    .planning/STATE.md; corruption parameters → scripts/corruption.py:38-42 (NOT CLAUDE.md — Pitfall 5).

    (3) Figures & tables section: empty scaffold with the Class R / Class V column schema (populated by 13-04).

    (4) Forbidden-sources appendix: reproduce the RESEARCH §2 forbidden list so the audit is self-contained.

    Every source path in section 2 must be verified tracked (`git ls-files --error-unmatch`) as part of this task.
  </action>
  <verify>
    <automated>grep -c "82.5" thesis/PROVENANCE.md (>=1); grep -c "summary_3seed.json" thesis/PROVENANCE.md (>=1); grep -c "phase7_summary.md\|phase7_rtfm_gap_diagnostic\|phase4_charts" thesis/PROVENANCE.md (returns 0 outside the forbidden-sources section — verify manually that missing-on-disk paths appear ONLY there)</automated>
  </verify>
  <acceptance_criteria>
    - thesis/PROVENANCE.md exists, is git-tracked, and contains >= 17 number-family rows
    - contains the exact .bst provenance wording "authentic Michael Shell header v1.13"
    - every "Tracked source path" cell passes git ls-files --error-unmatch (loop over all rows)
    - Pitfall-13 missing-on-disk paths appear nowhere as source cells
    - contains the strings "Class R" and "Class V" (figure schema scaffold)
  </acceptance_criteria>
  <done>PROVENANCE.md scaffold complete: all number families mapped to tracked sources; figure section ready for 13-04.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | number provenance chain | mitigate | every manifest source git-tracked + per-file `git ls-files --error-unmatch` assertion; Wave-3 audit fails on untracked sources |
| T-13-SC | Tampering | package installs | accept | none this phase |
No HIGH threats — documentation phase.
</threat_model>

<verification>
- git ls-files results/ count = 297; zero .bak files tracked
- PROVENANCE.md rows all point to tracked paths
- pytest untouched (no code changes)
</verification>

<success_criteria>
- SC3 groundwork complete: clean checkout can trace every canonical number family to a tracked source
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-02-SUMMARY.md` when done (include the exact tracked-file count and any add -f vs gitignore deviations).
</output>
