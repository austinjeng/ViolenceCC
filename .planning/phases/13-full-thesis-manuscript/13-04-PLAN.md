---
phase: 13-full-thesis-manuscript
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - thesis/figures/fig_temporal_scores.pdf
  - thesis/figures/fig_backbone_comparison.pdf
  - thesis/figures/fig_tta_comparison.pdf
  - thesis/figures/fig_gating_distribution.pdf
  - thesis/figures/pri9_score_hist.png
  - thesis/figures/ (phase-6 dispositions: B-series overlays, regenerated A/D/E/F selections)
  - thesis/PROVENANCE.md
autonomous: false
requirements: [SC4, SC1]
must_haves:
  truths:
    - "Every figure in thesis/figures/ has a PROVENANCE.md row of exactly Class R or Class V"
    - "No stale-provenance figure is included; forbidden C-series absent"
    - "Wave 0 exits with a clean build and user approval of the skeleton"
  artifacts:
    - path: "thesis/figures/fig_backbone_comparison.pdf"
      provides: "Ch5 backbone comparison (true 3-seed since 260622-uuu)"
    - path: "thesis/PROVENANCE.md"
      provides: "complete figure/table provenance section"
      contains: "Class V"
  key_links:
    - from: "thesis/PROVENANCE.md"
      to: "thesis/figures/fig_tta_comparison.pdf"
      via: "Class V row with verification note"
      pattern: "fig_tta_comparison"
---

<objective>
Complete the Wave-0 figure set: copy the 6 verified reuse figures, run the Phase-6 PNG
provenance-check/disposition pass (include as Class V / regenerate / drop), write the full
figure+table provenance rows into PROVENANCE.md, and close spec Wave 0 with a clean build +
USER CHECKPOINT (skeleton compiles).

Purpose: SC4 (every figure Class R or Class V, no stale provenance) + spec §9 Wave-0 checkpoint.
Output: complete thesis/figures/ inventory + populated PROVENANCE.md figure section + user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§4 figure provenance worksheet — binding dispositions)
@.planning/phases/13-full-thesis-manuscript/13-03-SUMMARY.md  (generated asset inventory)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§5 figures plan + provenance classes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the 6 reuse figures and draft their Class-V verification notes</name>
  <files>thesis/figures/fig_temporal_scores.pdf, thesis/figures/fig_backbone_comparison.pdf, thesis/figures/fig_tta_comparison.pdf, thesis/figures/fig_gating_distribution.pdf, thesis/figures/pri9_score_hist.png</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §4 "Reuse as-is" table (per-figure verification method column — binding)
    - results/_tta_rerun_continual/summary_3seed.json (to re-verify fig_tta_comparison means)
  </read_first>
  <action>
    Copy paper/figures/{fig_temporal_scores,fig_backbone_comparison,fig_tta_comparison,fig_gating_distribution}.pdf
    and results/_analysis_2026-06-10/pri9_score_hist.png into thesis/figures/ (fig_architecture.tex already copied
    by 13-01). For each, draft the Class-V verification note per the RESEARCH §4 method column:
    - fig_temporal_scores: generated by tracked generate_pub_figures.py from local eval_scores.npz; note curve
      video (RoadAccidents) + caption cross-check vs paper L267-272.
    - fig_backbone_comparison: true 3-seed since 260622-uuu (commit 2e6cb6b); note the existing 24-cell ≤0.05pp
      check vs Tables 1/2 — cite it, no re-measurement needed.
    - fig_tta_comparison: RE-VERIFY now — recompute the per-backbone source/TENT/SAR means from tracked
      summary_3seed.json and confirm the figure's plotted means match (63.7/57.0/58.9/62.8 source-only family).
    - fig_gating_distribution: publication-grade, unused by paper — verify vs D-series data narrative.
    - pri9_score_hist.png: generated by tracked analyze_pri9_failure_cases.py; note giant-s42 provenance.
    Keep the notes in a working file or directly as PROVENANCE.md draft rows (finalized in Task 3).
  </action>
  <verify>
    <automated>ls thesis/figures/fig_temporal_scores.pdf thesis/figures/fig_backbone_comparison.pdf thesis/figures/fig_tta_comparison.pdf thesis/figures/fig_gating_distribution.pdf thesis/figures/pri9_score_hist.png (all exist); cmp paper/figures/fig_backbone_comparison.pdf thesis/figures/fig_backbone_comparison.pdf (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - all 5 files exist in thesis/figures/, byte-identical to their sources (cmp exit 0 each)
    - a drafted verification note exists for each of the 5, naming what it was checked against and when
    - fig_tta_comparison note records the recomputed summary_3seed.json means
  </acceptance_criteria>
  <done>Reuse figures staged with drafted Class-V notes.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-6 PNG disposition pass (include / regenerate / drop)</name>
  <files>thesis/figures/ (selected B/A/D/E/F-series assets)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §4 "Provenance-check before reuse" table (exact per-series inventory + disposition procedure — binding)
    - scripts/generate_phase6_charts.py (regeneration entry point; runs zero-GPU against local checkpoints + cached features in vcc-main)
  </read_first>
  <action>
    Apply the RESEARCH §4 disposition table to results/phase6_charts/:
    - B_skeleton (B01/B02/B03): skeleton overlays don't depend on eval scores — copy to thesis/figures/ as
      Class V with the XD-only note (UCF source frames are 64×64 — document the limitation for App F).
    - A_temporal (A01-A05): UCF items predate the h1 full-length eval — regenerate via generate_phase6_charts.py
      against current checkpoints (vcc-main, zero-GPU) where feasible; verify regenerated curves match canonical
      behavior; include as Class V with regeneration-date note, or DROP any figure whose provenance can't be
      established. App F needs at most 2-3 temporal extras — don't force all 5.
    - C_corruption (C01-C06): FORBIDDEN — do not copy, do not regenerate from that data. (Replacements built in 13-03.)
    - D_gating (D01/D02 ucf+xd): gate distributions depend on checkpoints (λ0 changed XD checkpoints) —
      regenerate/verify; include as Class V (Ch5 uses fig_gating_distribution.pdf as primary; D-series extras go
      to App F only if verified).
    - E_projection (E01 ucf/xd t-SNE): regenerate against current caches/checkpoints or DROP (no underlying data
      files persisted — if regeneration fails, dropping is the correct disposition).
    - F_additional: F04-F06 embed single-seed-era ablation numbers — regenerate from current data or DROP
      (almost certainly stale per RESEARCH). F01-F03: verify score distributions before any inclusion.
    Record per-figure disposition (INCLUDED-V / REGENERATED-V / DROPPED + reason) — this list goes in the SUMMARY
    and PROVENANCE.md. Local checkpoints/E: features are legitimate REGENERATION INPUTS but never provenance
    sources (spec §5 clean-checkout rule).
  </action>
  <verify>
    <automated>ls thesis/figures/ | grep -ci "B0" (>=3 if B-series included); ls thesis/figures/ | grep -ci "C0\|corruption_C" (returns 0 — no C-series leaked)</automated>
  </verify>
  <acceptance_criteria>
    - a complete disposition record exists covering all 20 non-C phase-6 PNGs (5 A + 3 B + 4 D + 2 E + 6 F)
    - zero C-series files copied into thesis/figures/
    - every INCLUDED/REGENERATED figure has a Class-V note naming what it was checked against and the date
    - every DROPPED figure has a one-line reason
  </acceptance_criteria>
  <done>Every Phase-6 PNG has an explicit disposition; only verified figures entered thesis/figures/.</done>
</task>

<task type="auto">
  <name>Task 3: Populate PROVENANCE.md figure/table section + Wave-0 exit build</name>
  <files>thesis/PROVENANCE.md</files>
  <read_first>
    - thesis/PROVENANCE.md (scaffold from 13-02 — figure section schema)
    - .planning/phases/13-full-thesis-manuscript/13-03-SUMMARY.md (Class-R asset list + historical-baseline guard)
  </read_first>
  <action>
    Add one row per asset in thesis/figures/ and thesis/tables/ to PROVENANCE.md:
    - Class R rows (13-01/13-03 assets): fig_architecture.tex (tracked TikZ source, no data inputs);
      fig_severity_heatmap.pdf (scripts/thesis_severity_heatmap.py ← pri5_per_condition_heatmap.csv);
      fig_corruption_source.pdf + fig_corruption_disc_reweight.pdf (scripts/thesis_corruption_heatmaps.py ←
      r1full_*.json + variants + summary_3seed.json); tab_percat_{ucf,xd,rtfm}.tex
      (scripts/thesis_per_category_tables.py ← canonical per_category.csv); fig_sweep_*.png
      (scripts/generate_phase7_charts.py ← results-index.csv, WITH the historical-baseline guard note).
    - Class V rows (Task 1 + Task 2 assets): each with its verification note.
    Assert every source path in every row: `git ls-files --error-unmatch`.
    Then Wave-0 exit gates: `build_thesis.ps1 -Clean` exit 0; `C:/Anaconda/envs/vcc-main/python.exe -m pytest -q`
    green; `git status --porcelain` clean of build artifacts.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green); grep -c "Class" thesis/PROVENANCE.md (>= number of assets in thesis/figures/ + thesis/tables/)</automated>
  </verify>
  <acceptance_criteria>
    - every file in thesis/figures/ and thesis/tables/ has exactly one PROVENANCE.md row marked Class R or Class V
    - every source path in figure rows passes git ls-files --error-unmatch
    - build_thesis.ps1 -Clean exits 0; pytest green; git status clean of thesis artifacts
  </acceptance_criteria>
  <done>Figure provenance complete; Wave-0 gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: USER CHECKPOINT — Wave 0 complete: skeleton compiles</name>
  <read_first>(none — presentation task)</read_first>
  <what-built>Complete thesis skeleton: compiling report-class document (placeholder chapters), vendored .bst, non-interactive build script with preflight + log scan, bib superset (42 entries), ~269 provenance sources tracked, PROVENANCE.md with all number-family + figure rows, full figure inventory (Class R regenerated + Class V verified + phase-6 dispositions).</what-built>
  <files>(none — user verification)</files>
  <action>Present the Wave-0 result to the user: run the build with -Open, walk through the PDF skeleton, PROVENANCE.md, and the phase-6 disposition list. Pause for approval before Wave 1.</action>
  <how-to-verify>
    1. Run: powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean -Open
    2. Confirm thesis/main.pdf opens: title page (NTUST / Wei-Han Jeng / Chuan-Kai Yang), TOC with 9 chapters + 6 appendices, architecture figure renders legibly in ch03 at one-column width
    3. Skim thesis/PROVENANCE.md — number families + figure rows all point at tracked paths
    4. Review the phase-6 disposition list in 13-04-SUMMARY.md (what was included/regenerated/dropped)
  </how-to-verify>
  <acceptance_criteria>User types "approved" or lists issues to fix before Wave 1 chapter drafting begins.</acceptance_criteria>
  <verify>User response received ("approved" or issue list)</verify>
  <done>User approved the Wave-0 skeleton (or all raised issues fixed and re-approved).</done>
  <resume-signal>Type "approved" to start chapter drafting, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | figure provenance (stale single-seed-era PNGs) | mitigate | per-figure disposition pass; Class-V notes record verification target + date; forbidden C-series excluded by grep gate |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Clean build + pytest + git hygiene (Wave-0 exit gates per 13-VALIDATION)
- Zero C-series assets in thesis/figures/
- PROVENANCE.md figure section complete
</verification>

<success_criteria>
- SC4 established at Wave 0: every figure Class R or Class V, no stale provenance
- User checkpoint passed — Wave 1 may start
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-04-SUMMARY.md` when done (MUST include: full figure inventory with classes, phase-6 disposition list, final thesis/figures + thesis/tables filenames for chapter writers).
</output>
