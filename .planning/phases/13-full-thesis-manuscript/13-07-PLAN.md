---
phase: 13-full-thesis-manuscript
plan: 07
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - thesis/chapters/ch07_discussion.tex
  - thesis/chapters/ch08_limitations.tex
  - thesis/chapters/ch09_conclusion.tex
autonomous: true
requirements: [SC2, SC5, SC6]
must_haves:
  truths:
    - "Ch7 contains the full SOTA fragment body with [N] labels rewired to \\cite and Light-WVAD XD cell as ---"
    - "Ch8 states the PRD gate misses plainly (UCF 82.5 < 83; XD 78.7 < 80) and caps TTA claims as transductive"
    - "Ch9 restates findings using only canonical anchors"
  artifacts:
    - path: "thesis/chapters/ch07_discussion.tex"
      provides: "discussion + full 20-method SOTA table + fair subset + P1-P5"
      contains: "tab:sota-full"
    - path: "thesis/chapters/ch08_limitations.tex"
      provides: "limitations + future work with deferred-track pointers"
      contains: "78.7"
  key_links:
    - from: "thesis/chapters/ch07_discussion.tex"
      to: "thesis/references.bib"
      via: "rewired \\cite keys replacing manual [N] labels"
      pattern: "cite\\{zhang2025holmesvau"
---

<objective>
Draft chapters 7-9 (Discussion + full SOTA positioning, Limitations & Future Work, Conclusion) per
spec §4. Ch7 lifts the purpose-built fragment body from paper/sota_comparison_full.tex and rewires
its manual citations; RE-VERIFY flags are carried as comments for the Wave-3 merge (13-12) to resolve.

Output: three complete chapters (~18 pages combined: ch7 ~10pp, ch8 ~5pp, ch9 ~3pp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§1 Ch7-Ch9 rows; §2 canonical numbers; §8 Pitfalls 4, 14)
@.planning/phases/13-full-thesis-manuscript/13-PATTERNS.md   ("ch07 SOTA section (fragment lift)" — marker mechanics + rewiring instructions)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§4 Ch7-Ch9 map — binding)
</context>

<guardrails>
NUMBER GUARDRAILS (binding; RESEARCH §2 / spec §7):
- Canonical anchors ONLY. Headlines 82.5±0.4 / 78.7±0.9. PRD gates: UCF min 83 (target 85-87), XD min 80
  (target 82-85), RTFM 84.30±1% — misses stated plainly.
- FORBIDDEN: Light-WVAD XD 77.3 (fabricated — cell stays `---`); Phase 8/9/10/11 summary numbers; tables_generated.tex.
- HONESTY FRAMINGS: no SOTA claim; ~88-91% field-ceiling; fair-subset framing; compute-honesty argument preserved;
  gains in points; transductive TTA cap (no online-adaptation claim without Pri-4).
- External-method numbers: ONLY values already in the fragment's current state; carry the fragment's 13
  row-level `% RE-VERIFY` comments VERBATIM into the draft (13-12 resolves them after the 13-11 gate — they
  must be findable, so never silently drop or resolve them yourself). Sole exception: the tab:sota-full
  caption's RE-VERIFY workflow sentence (fragment l.93) is dropped/rewritten at lift time per Task 1 — it is
  workflow text, not a flagged row claim.
- Delete PLACEHOLDER-W0; incremental build after each task.
</guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Draft ch07_discussion.tex (five threads + full SOTA fragment lift)</name>
  <files>thesis/chapters/ch07_discussion.tex</files>
  <read_first>
    - paper/main.tex lines 362-374 (§5 five bolded discussion threads; LN-barrier L372; efficiency L374)
    - paper/sota_comparison_full.tex lines 20-33 (rewiring instructions in header) and 56-64 (standalone wrapper — must NOT leak) and 65-329 (FRAGMENT BODY: metric-definitions 69-81; tab:sota-full 85-171 with caveat footnotes a-n at 137-169; tab:sota-fair 176-221; P1-P5 paragraphs 226-314; reproducibility 289-301; manual-references note 318-327)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §6 "Fragment citation rewiring" + §5 (the 13 row-level RE-VERIFY line numbers: 104,106,108,111,112,115,116,117,120,122,123,124,125)
    - thesis/references.bib (the exact keys to rewire onto: zhang2024holmesvad, zhang2025holmesvau, wu2024stprompt, song2025fdpn, yang2024tpwng, pu2024pel4vad, leng2026piercingeye, zanella2024anomalyclip, chen2023tevad, peng2024hypervd, ghadiya2024crossmodal + existing joo2023cliptsa, chen2023mgfn, etc.)
  </read_first>
  <action>
    Expand the paper's five §5 threads into discussion sections (~4pp), then lift the SOTA fragment body
    (~6pp): copy ONLY lines between `%% --- FRAGMENT BODY BEGIN ---` (l.65) and `FRAGMENT BODY END` (l.329) —
    the standalone \documentclass wrapper and \end{document} must NOT leak (Pitfall 14; the preamble already has
    `array`). Rewire every manual bracketed [N] label to `\cite{key}` per the fragment's own header instructions
    (l.20-33) and drop the manual References list at the fragment bottom. Carry ALL caveat footnotes a-n. Carry
    every `% RE-VERIFY` comment VERBATIM onto its row (13 row-level items — 13-12 resolves them). At lift time, DROP
    (or rewrite without the RE-VERIFY token) the tab:sota-full caption's RE-VERIFY workflow sentence (fragment
    line 93: "Rows carrying an inline \texttt{RE-VERIFY} source comment...") — it describes the pre-verification
    workflow state, not thesis content. The fragment body (l.65-329) carries 14 RE-VERIFY occurrences (13 row
    comments + that caption sentence); only the 13 row comments are carried, so the grep-13 gate stays exact.
    Verify the
    Light-WVAD XD cell is `---` (post-260622-ukd state) and its attribution is Wang, Zhou & Guan. In report class
    the fragment tables may need `table` + `\resizebox` or `small` sizing for A4 one-column width — adjust sizing
    only, never cell values. Keep tab:sota-full and tab:sota-fair labels. P1-P5 positioning paragraphs and the
    compute-honesty argument (P5) lift with prose smoothing only.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0 — zero undefined citations); grep -c "RE-VERIFY" thesis/chapters/ch07_discussion.tex (returns 13); grep -c "documentclass\|end{document}" thesis/chapters/ch07_discussion.tex (returns 0); grep -c "77.3" thesis/chapters/ch07_discussion.tex (returns 0)</automated>
  </verify>
  <acceptance_criteria>
    - contains \label{tab:sota-full} and \label{tab:sota-fair}; zero manual "[1]"-style citation labels remain (grep for bracket-digit patterns in prose)
    - exactly 13 % RE-VERIFY comments carried; all footnotes a-n present
    - Light-WVAD row: UCF 84.7, XD cell `---`
    - all \cite keys resolve (zero undefined citations in main.log)
  </acceptance_criteria>
  <done>Discussion chapter complete with fragment lifted, citations rewired, RE-VERIFY flags preserved for the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Draft ch08_limitations.tex</name>
  <files>thesis/chapters/ch08_limitations.tex</files>
  <read_first>
    - paper/main.tex lines 379-429 (§6 base; tab:comparison 387-415 carries 4 % RE-VERIFY comments at L399/400/403/404; GS-MoE footnote 413-414; future directions 423-429)
    - .planning/REVIEW-2026-06-10.md §5 Pri tracker (future-work pointers; Pri-3 +0.57pp: 78.72→79.29 all 3 seeds)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §1 Ch8 row (PRD targets from STATE.md lines 52-60)
  </read_first>
  <action>
    Write ch08 (~5pp) per spec §4: all paper limitations expanded; accepted gate misses stated PLAINLY —
    UCF 82.5 < 83 minimum (target 85-87), XD 78.7 < 80 (target 82-85), RTFM gate 84.30±1% (App A pointer);
    XD seed sensitivity; transductive TTA cap (no online-adaptation claim without Pri-4 — the paper names this
    limitation twice, keep both senses); dataset scope; synthetic corruptions. Future work as forward pointers:
    Pri-3 XD test-score smoothing w=64 (verified +0.57pp: 78.72→79.29 all 3 seeds; deliberately NOT adopted under
    freeze — cite .planning/REVIEW-2026-06-10.md as the tracked source, results/_smoothfix/ stays untracked);
    Pri-2 skeleton-head ensemble routing (+1-2pp est.); Pri-4 streaming/causal w (would discharge the transductive
    limitation); Pri-8 JPEG rescue; Pri-11 temporal module; Pri-13 text-prompt branch; audio for XD; additional
    datasets. NOTE: do NOT duplicate the paper's condensed tab:comparison here — the full SOTA table lives in Ch7
    (Claude's-discretion organization); if any of its rows/claims are reused in prose, carry the corresponding
    % RE-VERIFY comment (main.tex L399/400/403/404 items = EventVAD, LAVAD, CLIP-TSA, MGFN — all already flagged
    in Ch7's fragment rows).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "PLACEHOLDER-W0" thesis/chapters/ch08_limitations.tex (0); grep -c "0.57" thesis/chapters/ch08_limitations.tex (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - contains "83" and "80" as stated minimum targets with the miss framing, and "84.30"
    - contains "transductive" at least twice; no online-adaptation claim
    - Pri-3 framed as deliberately-not-adopted under freeze; 79.29 appears only in that future-work framing
    - build green
  </acceptance_criteria>
  <done>Limitations chapter states misses plainly; future work maps the deferred tracks.</done>
</task>

<task type="auto">
  <name>Task 3: Draft ch09_conclusion.tex</name>
  <files>thesis/chapters/ch09_conclusion.tex</files>
  <read_first>
    - paper/main.tex lines 434-444 (§7 base; code-release promise L444)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 (anchor table — restated numbers must match byte-for-byte)
  </read_first>
  <action>
    Write ch09 (~3pp): restate findings answering RQ1/RQ2/RQ3 from Ch1 with RESEARCH §2 anchor numbers only
    (82.5±0.4, 78.7±0.9, complementarity 6/8+2 ties p≈0.03, disc_reweight +1.21pp corrupted-AUC mean, TENT/SAR
    null < 0.1pp); modularity/efficiency pitch (0.50-1.02M trainable params, <5 min/config, single RTX 4090);
    code availability (lift the L444 promise). Honesty framings intact: no SOTA claim, points not %.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "PLACEHOLDER-W0" thesis/chapters/ch09_conclusion.tex (0); grep -c "82.5" thesis/chapters/ch09_conclusion.tex (>=1); grep -c "78.7" thesis/chapters/ch09_conclusion.tex (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - headlines byte-identical to abstract/ch05 usage (82.5 / 78.7)
    - RQ1/RQ2/RQ3 each explicitly answered
    - build green across the full 9-chapter document
  </acceptance_criteria>
  <done>Conclusion restates the thesis honestly; all nine chapters now placeholder-free.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | external SOTA numbers entering thesis tables | mitigate | fragment lifted verbatim (4-audit-verified); RE-VERIFY flags carried for the 13-11 primary-source gate; unverifiable cells omitted in 13-12 |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Incremental build after each task; zero undefined refs/citations
- RE-VERIFY count in ch07 = 13 (preserved for the gate, not resolved prematurely)
</verification>

<success_criteria>
- Ch7-Ch9 placeholder-free; SOTA fragment lifted + rewired; gate misses disclosed (SC2, SC5, SC6 groundwork)
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-07-SUMMARY.md` when done (include per-chapter page counts and the rewired-key list).
</output>
