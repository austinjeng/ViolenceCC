---
phase: 13-full-thesis-manuscript
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - thesis/chapters/ch01_introduction.tex
  - thesis/chapters/ch02_related_work.tex
  - thesis/chapters/ch03_methodology.tex
autonomous: true
requirements: [SC2, SC5]
must_haves:
  truths:
    - "Ch1 states RQ1/RQ2/RQC3 and the 3 contributions with no SOTA claim"
    - "Ch2 surveys ~20 WSVAD methods + skeleton + VLM + TTA lineages without resurrecting Light-WVAD XD 77.3"
    - "Ch3 contains all 8 paper equations byte-verbatim and the unpacked disc_reweight derivation"
  artifacts:
    - path: "thesis/chapters/ch01_introduction.tex"
      provides: "full introduction with RQs, contributions, roadmap"
      min_lines: 120
    - path: "thesis/chapters/ch03_methodology.tex"
      provides: "expanded methodology, equations verbatim, architecture figure"
      contains: "\\label{eq:loss}"
  key_links:
    - from: "thesis/chapters/ch03_methodology.tex"
      to: "thesis/figures/fig_architecture.tex"
      via: "\\input in figure env"
      pattern: "figures/fig_architecture"
    - from: "thesis/chapters/ch02_related_work.tex"
      to: "thesis/references.bib"
      via: "\\cite of the 11 SOTA + 4 survey keys"
      pattern: "cite\\{"
---

<objective>
Draft chapters 1-3 (Introduction, Related Work, Methodology) as placeholder-free prose expanding
the paper's §1-§3 per spec §4, under the binding number-integrity guardrails.

Output: three complete chapters (~30-35 pages combined: ch1 ~8pp, ch2 ~12pp, ch3 ~15pp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md   (§1 Ch1-Ch3 rows + anchor map; §2 canonical numbers — BINDING)
@.planning/phases/13-full-thesis-manuscript/13-PATTERNS.md   (chapter conventions: labels, figure envs, equation style, prose voice)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md     (§4 Ch1-Ch3 content map — binding)
</context>

<guardrails>
NUMBER GUARDRAILS (binding on every task; RESEARCH §2 / spec §7):
- Canonical anchors ONLY: UCF 82.5±0.4 AUC (Gated Fusion, SigLIP2 Giant); XD 78.7±0.9 AP (Gated Fusion, SO400M);
  any other value must come from the RESEARCH §2 anchor table.
- FORBIDDEN sources: paper/tables_generated.tex; Phase 8/9/10/11 summary numbers (83.6, 83.3±0.3, 83.28/83.23,
  71.8/41.3); REQUIREMENTS.md EVAL rows (0.8227/71.92/0.9200); Light-WVAD XD 77.3 (fabricated); phase6 C-series;
  Phase 4/4c per-category values; CLAUDE.md corruption parameter lists.
- HONESTY FRAMINGS (preserve in meaning): no SOTA claim; ~88-91% field-ceiling disclosure; TTA gains are
  corrupted-AUC robustness only; "+13.2 points in the single most-degraded condition"; gains in "points" not "%";
  transductive assumption disclosed; complementarity "small but consistent" (6/8 strictly positive + 2 ties, p≈0.03).
- Delete the PLACEHOLDER-W0 marker from each chapter; no \todo, no placeholder prose.
- Match the paper's voice; use PATTERNS label/ref conventions (`~` before every \ref/\cite; keep paper label names).
- After EACH task: incremental build `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1` must exit 0.
</guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Draft ch01_introduction.tex</name>
  <files>thesis/chapters/ch01_introduction.tex</files>
  <read_first>
    - paper/main.tex lines 61-83 (§1 base prose; contributions list at 75-83) and lines 48-53 (abstract, for framing consistency)
    - docs/superpowers/specs/2026-07-05-full-thesis-design.md §4 Ch1 map (motivation, problem statement, gaps, RQ1/RQ2/RQ3, 3 contributions, roadmap)
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §2 (anchor table — only permitted numbers)
    - thesis/chapters/ch01_introduction.tex (current placeholder)
  </read_first>
  <action>
    Expand paper §1 into a full introduction chapter (~8pp): surveillance-scale violence detection motivation,
    annotation cost → weak supervision, problem statement, the four gaps per spec §4 (single-modality dominance;
    CLIP methods miss motion dynamics; backbone sensitivity unstudied; BN-targeting TTA vs LayerNorm heads),
    explicit research questions — RQ1 dual-modal fusion value, RQ2 backbone sensitivity, RQ3 entropy-TTA transfer
    to LN — the paper's 3 contributions (lift from L75-83, expand each), and a thesis-organization roadmap
    paragraph naming all 9 chapters + appendices. Headline mentions use exactly 82.5±0.4 / 78.7±0.9. Frame results
    honestly per guardrails (no SOTA claim; the field's fine-tuned leaders reach ~88-91%).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "PLACEHOLDER-W0" thesis/chapters/ch01_introduction.tex (returns 0); grep -c "82.5" thesis/chapters/ch01_introduction.tex (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - chapter contains "RQ1", "RQ2", "RQ3" (or equivalently numbered research-question environment)
    - grep -c "state of the art\|state-of-the-art performance" returns 0 for claims about OUR method (mentions describing the FIELD are allowed — verify context)
    - grep -c "83.6\|71.8\|77.3" thesis/chapters/ch01_introduction.tex returns 0
    - PLACEHOLDER-W0 gone; incremental build exit 0
  </acceptance_criteria>
  <done>Complete introduction with RQs, contributions, roadmap; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Draft ch02_related_work.tex</name>
  <files>thesis/chapters/ch02_related_work.tex</files>
  <read_first>
    - paper/main.tex lines 88-109 (§2 base: §2.1 L90, §2.2 L94, §2.3 TTA L104 — each one-paragraph subsection becomes a survey section)
    - .planning/phases/12-sota-comparison-and-positioning/12-RESEARCH-sota.md (primary expansion source; §8 lines 150-188 per-method verification appendix) — TRAP (Pitfall 4): its Light-WVAD row still lists the FABRICATED XD 77.3; never copy result numbers from §8, use it for author/venue/lineage only
    - .planning/phases/13-full-thesis-manuscript/13-RESEARCH.md §1 Ch2 row (lineage order — binding) and §6 (bib keys available)
    - thesis/references.bib (the 42 available keys — cite with these exact keys)
  </read_first>
  <action>
    Write four survey sections (~12pp) per spec §4 Ch2:
    (1) Weakly supervised VAD lineage: Sultani → RTFM → memory/uncertainty (UR-DMU, MGFN) → CLIP-era (CLIP-TSA,
    VadCLIP, AnomalyCLIP, TPWNG, PEL4VAD) → MLLM/training-free (LAVAD, EventVAD) → recent (PI-VAD, DSANet, GS-MoE).
    ~20 methods; cite via existing bib keys (joo2023cliptsa, wu2024vadclip, zanella2024anomalyclip, yang2024tpwng,
    pu2024pel4vad, zanella2024lavad, shao2025eventvad, majhi2025pivad, yin2026dsanet, damicantonio2025gsmoe, ...).
    (2) Skeleton-based action recognition: ST-GCN lineage (yan2018stgcn) → CTR-GCN (chen2021ctrgcn) + PYSKL
    (yan2022pyskl); skeleton use in anomaly detection (doshi2022skeleton).
    (3) Vision-language backbones: CLIP (radford2021clip), SigLIP (zhai2023siglip), SigLIP2 (tschannen2025siglip2).
    (4) Test-time adaptation: TENT (wang2021tent), SAR (niu2023sar), NORM/statistic-restoration (schneider2020norm),
    CORAL (sun2016coral); the BN-vs-LN structural argument (ioffe2015batchnorm, ba2016layernorm).
    Numeric SOTA claims about other methods: prefer citing WITHOUT specific numbers in Ch2 (the numbers live in
    Ch7's verified tables); where a number is pedagogically needed, use only values that appear in
    paper/sota_comparison_full.tex current state and mark the sentence with `% RE-VERIFY` if the fragment flags
    that method (resolved by 13-12).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0 with zero undefined citations); grep -c "PLACEHOLDER-W0" thesis/chapters/ch02_related_work.tex (returns 0); grep -c "77.3" thesis/chapters/ch02_related_work.tex (returns 0)</automated>
  </verify>
  <acceptance_criteria>
    - all four survey sections present; >= 18 distinct \cite keys used
    - grep -c "cite{yan2018stgcn}\|cite{sun2016coral}\|cite{schneider2020norm}" returns >= 3 occurrences combined
    - zero undefined citations in thesis/main.log after build
    - Light-WVAD XD number absent (77.3 grep = 0)
  </acceptance_criteria>
  <done>Full related-work survey with correct lineages, all citations resolving; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Draft ch03_methodology.tex</name>
  <files>thesis/chapters/ch03_methodology.tex</files>
  <read_first>
    - paper/main.tex lines 111-200 (§3 base — ALL subsections; the 8 equations at L157, L163, L167, L171, L179, L183, L195, L197; 64-frame exclusion sentence at L136; \label{sec:disc} at L190)
    - .knowledge/tta-to-reweighting.html (the §3.7 TTA narrative source: w_vl dispersion ratio, skeleton gate s with fixed factor-2 anchor, saturation to {0,1}, final w = 1-(1-w_vl)s, transductive whole-condition pooling)
    - src/tta/disc_reweight.py, src/tta/tent.py, src/tta/sar.py (code cross-refs for the methods narrative)
    - .planning/phases/13-full-thesis-manuscript/13-PATTERNS.md equation style + figure-with-TikZ pattern
    - thesis/chapters/ch03_methodology.tex (current placeholder incl. the architecture smoke-test figure)
  </read_first>
  <action>
    Expand paper §3 into the methodology chapter (~15pp) per spec §4 Ch3: 3.1 pipeline overview (keep the
    architecture figure from the placeholder, replace its PLACEHOLDER-W0 caption with the paper's caption
    expanded); 3.2 skeleton extraction (YOLOX-m + RTMPose-m, top-2 persons, PreNormalize2D, tensor shapes);
    3.3 CTR-GCN features (4-stream j/b/jm/bm weighted 1.0/1.0/0.5/0.5, 64-frame snippets, GAP, 256-d — lift the
    L136 exclusion sentence VERBATIM: "Videos shorter than 64 frames (172 videos in UCF-Crime, one in
    XD-Violence) are excluded from training and evaluation."); 3.4 VLM features (4 backbones with exact
    open-clip checkpoint names from L141-147, ~1 FPS sampling, mean+max pooling to 2d); 3.5 fusion (late; gated
    with ALL equations, per-dimension gate, residual interpretation (1+g)s_hat + (2-g)v_hat, the three named
    LayerNorms and why); 3.6 MIL training (top-k ranking loss, λ1=8e-3 sparsity, λ2 dataset-dependent,
    shuffled-val rationale); 3.7 TTA methods — TENT/SAR LN-affine protocol (1,536 params = 3 LN modules /
    6 tensors; per-video reset vs continual) AND the full disc_reweight derivation unpacked from L190-199 using
    the .knowledge narrative.
    EQUATIONS: lift all 8 byte-verbatim from main.tex (they are code-verified; NEVER alter the math). L197 is a
    ~1,400-char inline equation paragraph — lift whole. Keep paper labels (eq:loss, sec:disc, etc.).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 (exit 0); grep -c "label{eq:loss}" thesis/chapters/ch03_methodology.tex (returns 1); grep -c "PLACEHOLDER-W0" thesis/chapters/ch03_methodology.tex (returns 0)</automated>
  </verify>
  <acceptance_criteria>
    - all 8 equation bodies match main.tex byte-for-byte (diff the extracted equation environments)
    - the L136 exclusion sentence appears verbatim
    - contains "1{,}536" or "1,536" (adapted params) and the w = 1-(1-w_vl)s formulation
    - architecture figure renders (fig:architecture referenced from prose); build green
  </acceptance_criteria>
  <done>Complete methodology with verbatim equations and full TTA derivation; build green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | number/equation integrity in prose | mitigate | canonical-anchor guardrail block + verbatim-equation rule + forbidden-source greps in acceptance |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Incremental build after each task; zero undefined refs/citations
- Forbidden-value greps (83.6, 71.8, 77.3) return 0 across ch01-ch03
</verification>

<success_criteria>
- Ch1-Ch3 placeholder-free, spec §4 content complete, honesty framings intact (SC2, SC5)
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-05-SUMMARY.md` when done (include per-chapter page counts).
</output>
