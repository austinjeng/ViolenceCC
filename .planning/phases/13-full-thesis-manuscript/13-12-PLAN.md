---
phase: 13-full-thesis-manuscript
plan: 12
type: execute
wave: 7
depends_on: [13-09, 13-10, 13-11]
files_modified:
  - thesis/chapters/ch02_related_work.tex
  - thesis/chapters/ch07_discussion.tex
  - thesis/chapters/ch08_limitations.tex
  - thesis/references.bib
  - thesis/main.tex
  - thesis/frontmatter/notation.tex
autonomous: true
requirements: [SC6, SC1, SC2]
must_haves:
  truths:
    - "Zero % RE-VERIFY comments and zero GATE-13 markers survive anywhere in thesis/"
    - "Every UNVERIFIABLE cell is --- with an omission footnote; every CORRECTED value matches the evidence report"
    - "No `and others` author placeholder survives in thesis/references.bib"
    - "TOC matches spec §4; every figure/table is referenced from prose; cross-references all resolve"
  artifacts:
    - path: "thesis/references.bib"
      provides: "fully verified bibliography, zero TODOs"
  key_links:
    - from: ".planning/phases/13-full-thesis-manuscript/13-SOTA-EVIDENCE.md"
      to: "thesis/chapters/ch07_discussion.tex"
      via: "verdict application"
      pattern: "tab:sota-full"
---

<objective>
Wave-3 part 1: merge the user-approved SOTA verification verdicts into the thesis (RE-VERIFY
resolution: verified → cite normally; corrected → fix value; unverifiable → omit cell as --- with
omission footnote), finalize the bibliography, and run the integration pass (cross-references,
notation consistency, TOC/structure check against spec §4).

Output: verification-debt-free, structurally consistent thesis source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-thesis-manuscript/13-SOTA-EVIDENCE.md   (binding verdicts — user-approved in 13-11)
@.planning/phases/13-full-thesis-manuscript/13-RESEARCH.md        (§5 gate rules; §6 bib plan)
@docs/superpowers/specs/2026-07-05-full-thesis-design.md          (§7 gate rules + §8 steps 2b/5 — binding)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply verdicts — RE-VERIFY resolution + bibliography finalization</name>
  <files>thesis/chapters/ch02_related_work.tex, thesis/chapters/ch07_discussion.tex, thesis/chapters/ch08_limitations.tex, thesis/references.bib</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-SOTA-EVIDENCE.md (every verdict + the "cells to omit" list + corrected bib fields — the ONLY authority for changes here)
    - thesis/chapters/ch07_discussion.tex (the 13 flagged rows as drafted)
    - thesis/references.bib (GATE-13 entries + the FOUR `and others` entries: majhi2025pivad, yin2026dsanet, shao2025eventvad, damicantonio2025gsmoe)
  </read_first>
  <action>
    For each evidence-report item: VERIFIED → delete the % RE-VERIFY comment, keep the value; CORRECTED → replace
    the value with the report's confirmed value, delete the comment; UNVERIFIABLE → set the cell to `---` (or drop
    the row if its headline cells are unverifiable) and add the report's omission footnote, delete the comment.
    Secondary-detail mismatches (venue, author list) corrected in place. Apply corrected bib fields to every
    GATE-13 entry and delete every GATE-13 marker. Replace `and others` in ALL FOUR carrying entries —
    majhi2025pivad, yin2026dsanet, shao2025eventvad, damicantonio2025gsmoe — with the full author lists from the
    evidence report (13-11 records all four); retype majhi2025pivad and yin2026dsanet per their publication-status
    verdicts (checklist #16/#17) — this makes the `grep -c "and others" thesis/references.bib` = 0 gate reachable. Sweep ch02/ch08 prose for any number the report corrected (the same method may be mentioned in
    survey prose). NEVER introduce a value not present in the evidence report or the RESEARCH §2 anchor table.
  </action>
  <verify>
    <automated>grep -rc "RE-VERIFY" thesis/ (returns 0); grep -c "GATE-13" thesis/references.bib (returns 0); grep -c "and others" thesis/references.bib (returns 0); grep -rc "TODO" thesis/ (returns 0)</automated>
  </verify>
  <acceptance_criteria>
    - zero RE-VERIFY / GATE-13 / TODO / `and others` anywhere under thesis/
    - every UNVERIFIABLE cell shows --- and its table carries the omission footnote
    - spot-diff 3 CORRECTED values against the evidence report — exact match
    - build exit 0 with zero undefined citations
  </acceptance_criteria>
  <done>All external-number debt resolved per the approved evidence report; bib final.</done>
</task>

<task type="auto">
  <name>Task 2: Integration pass — cross-refs, notation consistency, structure check</name>
  <files>thesis/main.tex, thesis/frontmatter/notation.tex (minor fixes in any chapter as needed)</files>
  <read_first>
    - docs/superpowers/specs/2026-07-05-full-thesis-design.md §4 (the binding chapter/appendix content map — the structure oracle)
    - thesis/main.log after a clean build (warnings: multiply-defined labels, unreferenced floats)
  </read_first>
  <action>
    (1) Cross-references: build and scan main.log for undefined/multiply-defined labels; verify every chapter's
    forward/backward references (e.g. ch01 roadmap → chapter labels; ch05 → App B/C pointers; ch06 → sec:disc)
    resolve to the intended targets. (2) Notation consistency: every symbol in notation.tex matches usage across
    ch03-ch06 (λ1/λ2, T, k, g, w_vl, s, w); fix drift in prose, never in equations (verbatim rule). (3) Structure
    check vs spec §4: 9 chapters + 6 appendices + 4 frontmatter units all present, section coverage per the map;
    every figure in thesis/figures/ and every table is \ref'd from prose at least once (grep \label vs \ref
    inventory); orphaned assets either get referenced or removed from the document (not from disk). (4) TOC
    sanity: chapter/appendix titles match the spec map wording.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); grep -c "multiply" thesis/main.log (0); grep -c "undefined" thesis/main.log (0)</automated>
  </verify>
  <acceptance_criteria>
    - label/ref inventory shows every figure/table label referenced >= 1 time from prose
    - content-unit inventory complete, each verified present by name: chapters ch01_introduction, ch02_related_work,
      ch03_methodology, ch04_experimental_setup, ch05_results_fusion, ch06_results_tta, ch07_discussion,
      ch08_limitations, ch09_conclusion; appendices appA_rtfm, appB_per_category, appC_sweep, appD_engineering,
      appE_reproducibility, appF_figures; frontmatter titlepage, abstract, acknowledgments (user-fill note),
      notation; TOC/LOF/LOT all render (enumeration replaces the former numeric total)
    - notation.tex symbols consistent with chapter usage (spot-check λ1=8e-3 context, w_vl, gate g)
    - clean build green
  </acceptance_criteria>
  <done>Thesis structurally consistent and cross-referenced per spec §4.</done>
</task>

<task type="auto">
  <name>Task 3: Post-merge gate sweep</name>
  <files>(no new edits expected)</files>
  <read_first>
    - .planning/phases/13-full-thesis-manuscript/13-VALIDATION.md (phase gates → checks map)
  </read_first>
  <action>
    Run the full gate sweep to certify Wave-3 part 1: clean build; grep gates (RE-VERIFY=0, GATE-13=0, TODO=0,
    `and others`=0, PLACEHOLDER-W0=0 outside acknowledgments' user-fill note); pytest green; git status clean of
    artifacts. Record the current page count (must be trending inside 90-120).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_thesis.ps1 -Clean (exit 0); C:/Anaconda/envs/vcc-main/python.exe -m pytest -q (green); grep -rc "RE-VERIFY\|GATE-13" thesis/ (0)</automated>
  </verify>
  <acceptance_criteria>
    - all gate greps return 0; build + pytest green; page count recorded in SUMMARY
  </acceptance_criteria>
  <done>Wave-3 part 1 gates green; ready for the final audits.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Tampering | verdict application fidelity | mitigate | evidence report is the only change authority; spot-diff acceptance; grep gates prove zero surviving debt |
| T-13-SC | Tampering | package installs | accept | none |
No HIGH threats.
</threat_model>

<verification>
- Zero RE-VERIFY / GATE-13 / TODO / `and others` in thesis/
- Clean build + pytest green
</verification>

<success_criteria>
- SC6 fully discharged in source; structure matches spec §4 (SC1/SC2 integration)
</success_criteria>

<output>
Create `.planning/phases/13-full-thesis-manuscript/13-12-SUMMARY.md` when done (include verdict-application tally and page count).
</output>
