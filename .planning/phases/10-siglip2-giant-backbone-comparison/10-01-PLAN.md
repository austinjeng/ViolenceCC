---
phase: 10-siglip2-giant-backbone-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_clip.py
  - configs/clip_only_giant.yaml
  - configs/clip_only_xd_giant.yaml
  - configs/late_fusion_giant.yaml
  - configs/late_fusion_xd_giant.yaml
  - configs/gated_fusion_giant.yaml
  - configs/gated_fusion_xd_giant.yaml
  - configs/gated_fusion_giant_2person.yaml
  - configs/gated_fusion_xd_giant_2person.yaml
  - configs/gated_fusion_giant_clip_mean.yaml
  - configs/gated_fusion_xd_giant_clip_mean.yaml
  - scripts/run_ablations.py
  - tests/test_extract_siglip2.py
  - tests/test_models.py
  - tests/test_run_ablations.py
autonomous: true
requirements:
  - EVAL-02
  - EVAL-03
---

<objective>
Add SigLIP2 Giant-opt backbone support to extraction, training, and ablation infrastructure.

Mirrors Phase 9 (SO400M) exactly with Giant-opt dimensions: embed_dim=1536, clip_dim=3072 (mean+max) or 1536 (mean-only). One new BACKBONE_CONFIGS entry, 10 YAML configs, 6 phase10_* queues with 14 RunSpecs, and test updates for the fourth backbone.
</objective>

<execution_context>
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-01-PLAN.md
@.planning/phases/09-siglip2-so400m-backbone-comparison/09-PATTERNS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add Giant-opt backbone config, create 10 YAML configs, add 6 phase10 queues</name>
  <files>
    scripts/extract_clip.py,
    configs/clip_only_giant.yaml, configs/clip_only_xd_giant.yaml,
    configs/late_fusion_giant.yaml, configs/late_fusion_xd_giant.yaml,
    configs/gated_fusion_giant.yaml, configs/gated_fusion_xd_giant.yaml,
    configs/gated_fusion_giant_2person.yaml, configs/gated_fusion_xd_giant_2person.yaml,
    configs/gated_fusion_giant_clip_mean.yaml, configs/gated_fusion_xd_giant_clip_mean.yaml,
    scripts/run_ablations.py
  </files>
  <read_first>
    scripts/extract_clip.py (BACKBONE_CONFIGS dict — now has 3 entries, add 4th),
    configs/clip_only_so400m.yaml (analog template — substitute giant dimensions),
    configs/gated_fusion_so400m.yaml (analog template),
    configs/gated_fusion_so400m_2person.yaml (analog template),
    configs/gated_fusion_so400m_clip_mean.yaml (analog template),
    configs/late_fusion_so400m.yaml (analog template),
    configs/clip_only_xd_so400m.yaml (XD analog),
    configs/gated_fusion_xd_so400m.yaml (XD analog),
    configs/gated_fusion_xd_so400m_2person.yaml (XD analog),
    configs/gated_fusion_xd_so400m_clip_mean.yaml (XD analog),
    configs/late_fusion_xd_so400m.yaml (XD analog),
    scripts/run_ablations.py (phase9_* queues as pattern for phase10_*)
  </read_first>
  <action>
    1. In scripts/extract_clip.py, add a fourth entry to BACKBONE_CONFIGS after siglip2-so400m:
       - Key: "siglip2-giant"
       - model_name: "ViT-gopt-16-SigLIP2-256"
       - pretrained: "webli"
       - embed_dim: 1536
       - output_subdir: "siglip2_giant"
       - mean_subdir: "siglip2_giant_mean"

    2. Create 10 YAML config files. Each mirrors its Phase 9 SO400M analog exactly, with these substitutions:
       - Header comment: "Phase 10" instead of "Phase 9", "Giant-opt" instead of "SO400M"
       - clip_features path: replace "siglip2_so400m" with "siglip2_giant" (or "siglip2_so400m_mean" with "siglip2_giant_mean")
       - clip_dim: 3072 for mean+max configs; 1536 for mean-only configs
       - wandb tags: replace "phase9" with "phase10" and "so400m" with "giant"

       Config dimension mapping:
       - clip_only_giant.yaml: clip_dim=3072, clip_features=E:/features/ucf/siglip2_giant
       - clip_only_xd_giant.yaml: clip_dim=3072, clip_features=E:/features/xd/siglip2_giant
       - late_fusion_giant.yaml: clip_dim=3072
       - late_fusion_xd_giant.yaml: clip_dim=3072
       - gated_fusion_giant.yaml: clip_dim=3072
       - gated_fusion_xd_giant.yaml: clip_dim=3072
       - gated_fusion_giant_2person.yaml: clip_dim=3072, skel_dim=512, skel_agg=concat
       - gated_fusion_xd_giant_2person.yaml: clip_dim=3072, skel_dim=512, skel_agg=concat
       - gated_fusion_giant_clip_mean.yaml: clip_dim=1536, clip_features=E:/features/ucf/siglip2_giant_mean
       - gated_fusion_xd_giant_clip_mean.yaml: clip_dim=1536, clip_features=E:/features/xd/siglip2_giant_mean

       All other fields (seed, shared_dim=256, proj_dim=512, train hyperparameters) remain identical.

    3. In scripts/run_ablations.py, add 6 phase10_* queues after the Phase 9 queues:
       - phase10_ucf_main: 3 RunSpecs (clip_only/late_fusion/gated_fusion, seed 42, giant configs, cache_variant "giant")
       - phase10_xd_main: 3 RunSpecs (same variants, XD configs, cache_variant "giant")
       - phase10_ucf_pooling: 2 RunSpecs (2person as "giant_2person", clip_mean as "giant_clip_mean")
       - phase10_xd_pooling: 2 RunSpecs (same pattern, XD configs)
       - phase10_ucf_seeds: 2 RunSpecs (seeds 123, 2024, gated_fusion_giant.yaml, cache_variant "giant")
       - phase10_xd_seeds: 2 RunSpecs (seeds 123, 2024, gated_fusion_xd_giant.yaml, cache_variant "giant")
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import sys; sys.path.insert(0,'.'); from scripts.extract_clip import BACKBONE_CONFIGS; assert 'siglip2-giant' in BACKBONE_CONFIGS; cfg=BACKBONE_CONFIGS['siglip2-giant']; assert cfg['embed_dim']==1536; assert cfg['model_name']=='ViT-gopt-16-SigLIP2-256'; print('BACKBONE_CONFIGS OK')"</automated>
  </verify>
  <acceptance_criteria>
    - BACKBONE_CONFIGS has exactly 4 keys: "clip-vit-b-16", "siglip2-base", "siglip2-so400m", "siglip2-giant"
    - siglip2-giant entry: model_name="ViT-gopt-16-SigLIP2-256", pretrained="webli", embed_dim=1536
    - 10 new YAML files in configs/ with correct clip_dim values (3072 for 8 mean+max, 1536 for 2 mean-only)
    - 6 phase10_* queues with 14 total RunSpecs
    - shared_dim=256, proj_dim=512 unchanged in all configs
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update test files for Giant-opt backbone assertions</name>
  <files>
    tests/test_extract_siglip2.py,
    tests/test_models.py,
    tests/test_run_ablations.py
  </files>
  <read_first>
    tests/test_extract_siglip2.py (full file),
    tests/test_models.py (SO400M test functions as pattern),
    tests/test_run_ablations.py (phase9 queue assertions and total count)
  </read_first>
  <action>
    1. tests/test_extract_siglip2.py:
       - Update expected backbone keys set to include "siglip2-giant" (4 keys total)
       - Add test_giant_config_values: assert model_name=="ViT-gopt-16-SigLIP2-256", embed_dim==1536, output_subdir=="siglip2_giant", mean_subdir=="siglip2_giant_mean"
       - Add Giant branches to dimension tests: mean=1536, mean+max=3072
       - Add test_giant_output_subdir

    2. tests/test_models.py:
       - Add test_clip_dim_3072: CLIPProj(clip_dim=3072) forward-pass test
       - Add test_clip_dim_3072_projection_layer: assert in_features==3072, out_features==512
       - Add test_late_fusion_clip_dim_3072
       - Add test_gated_fusion_clip_dim_3072

    3. tests/test_run_ablations.py:
       - Add phase10 queue names to expected queues tuple
       - Add phase10 length assertions (3, 3, 2, 2, 2, 2)
       - Add spot checks for phase10 run_names
       - Update total unique run_names from 252 to 266
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_extract_siglip2.py tests/test_models.py tests/test_run_ablations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass with Giant-opt assertions (4 backbone keys, 1536/3072 dims, 266 total run_names)
    - No existing tests broken
  </acceptance_criteria>
</task>

</tasks>

<verification>
C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_extract_siglip2.py tests/test_models.py tests/test_run_ablations.py -x -q
</verification>

<output>
Create .planning/phases/10-siglip2-giant-backbone-comparison/10-01-SUMMARY.md when done
</output>
