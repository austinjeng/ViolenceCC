---
phase: 10-siglip2-giant-backbone-comparison
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - scripts/generate_phase10_charts.py
autonomous: true
requirements:
  - EVAL-04
---

<objective>
Create the four-way backbone comparison analysis script and generate thesis-ready tables and charts.

Extends Phase 9's three-way comparison to four-way: CLIP ViT-B/16 vs SigLIP2 ViT-B/16-256 vs SigLIP2 SO400M vs SigLIP2 Giant-opt. Leave Phase 8 and Phase 9 chart scripts untouched.

Output: generate_phase10_charts.py, 2 CSV tables (comparison + seed stability), 3 PNG charts.
</objective>

<execution_context>
@scripts/generate_phase9_charts.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create four-way comparison script generate_phase10_charts.py</name>
  <files>scripts/generate_phase10_charts.py</files>
  <read_first>
    scripts/generate_phase9_charts.py (structural analog — extend from 3-column to 4-column),
    results/results-index.csv (column format)
  </read_first>
  <action>
    Create scripts/generate_phase10_charts.py following Phase 9 script structure, extended to 4-column:

    1. COMPARISON_MAP: 12 entries with 4-element tuples (clip_name, sig_name, so400m_name, giant_name)
       Giant run_names follow pattern: {dataset}_{variant}_giant_s42

    2. SEED_MAP: 6 entries with 4-element tuples

    3. build_comparison_table: Add Giant_AUC, Giant_AP columns. Add delta columns for CLIP-vs-Giant and SigLIP2-vs-Giant and SO400M-vs-Giant

    4. build_seed_table: Add Giant columns with mean/std

    5. plot_comparison_bars: 4 grouped bars. Width ~0.2. Colors: "#3B82F6" (CLIP), "#EF4444" (SigLIP2), "#10B981" (SO400M), "#8B5CF6" (Giant). Labels: "CLIP ViT-B/16", "SigLIP2 ViT-B/16", "SigLIP2 SO400M", "SigLIP2 Giant"

    6. plot_seed_stability: 4 backbone bars with error bars

    7. OUT_DIR: results/phase10_charts
    8. Output files: backbone_comparison_4way.csv, seed_stability_4way.csv, comparison_auc_4way.png, comparison_ap_4way.png, seed_stability_4way.png
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import ast; ast.parse(open('scripts/generate_phase10_charts.py').read()); print('Syntax OK')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Generate four-way comparison tables and charts</name>
  <action>
    1. Run: C:/Anaconda/envs/vcc-main/python.exe scripts/generate_phase10_charts.py
    2. Verify outputs exist in results/phase10_charts/
    3. Print comparison table to console
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df=pd.read_csv('results/phase10_charts/backbone_comparison_4way.csv'); assert 'Giant_AUC' in df.columns; print(df.to_string())"</automated>
  </verify>
</task>

</tasks>

<output>
Create .planning/phases/10-siglip2-giant-backbone-comparison/10-04-SUMMARY.md when done
</output>
