---
phase: 10-siglip2-giant-backbone-comparison
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified: []
autonomous: false
requirements:
  - EVAL-02
  - EVAL-03
---

<objective>
Run the full Giant-opt ablation matrix: 8 main variant runs + 6 seed stability runs = 14 total experiments.

Same evaluation harness, hyperparameters, and seed set as all prior backbone ablations.

Output: 14 new rows in results/results-index.csv with giant cache_variant tags.
</objective>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run Phase 10 ablation queues (14 experiments)</name>
  <how-to-verify>
    Run 6 queues with --no-preflight (wandb disabled in all configs):

    python scripts/run_ablations.py --queue phase10_ucf_main --no-preflight
    python scripts/run_ablations.py --queue phase10_xd_main --no-preflight
    python scripts/run_ablations.py --queue phase10_ucf_pooling --no-preflight
    python scripts/run_ablations.py --queue phase10_xd_pooling --no-preflight
    python scripts/run_ablations.py --queue phase10_ucf_seeds --no-preflight
    python scripts/run_ablations.py --queue phase10_xd_seeds --no-preflight

    Verify:
    python -c "import pandas as pd; df=pd.read_csv('results/results-index.csv'); g=df[df['run_name'].str.contains('giant')]; print(f'Giant runs: {len(g)}'); print(g[['run_name','auc','ap']].to_string())"
  </how-to-verify>
  <acceptance_criteria>
    - results/results-index.csv contains exactly 14 new rows with "giant" in run_name
    - All 14 runs have non-zero AUC and AP values
    - No training errors or NaN losses
  </acceptance_criteria>
  <resume-signal>Paste results summary or type "ablations complete"</resume-signal>
</task>

</tasks>

<output>
Create .planning/phases/10-siglip2-giant-backbone-comparison/10-03-SUMMARY.md when done
</output>
