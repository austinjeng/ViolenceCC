---
phase: 10-siglip2-giant-backbone-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - EVAL-02
---

<objective>
Extract SigLIP2 Giant-opt features for all UCF-Crime and XD-Violence videos.

Mirrors Phase 9 Plan 02. Uses the same snippet boundaries and temporal alignment. Must extract train, val, AND test splits (Phase 9 lesson: val split was initially missed). Giant-opt uses ~8 GB VRAM at batch_size=16.

Output: 4 feature directories on E: drive (siglip2_giant and siglip2_giant_mean for both datasets).
</objective>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Extract Giant-opt features for UCF-Crime and XD-Violence (all splits)</name>
  <what-built>Plan 01 added siglip2-giant to BACKBONE_CONFIGS.</what-built>
  <how-to-verify>
    Run these 12 extraction commands (3 splits x 2 datasets x 2 pooling modes):

    UCF mean+max:
    python scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-giant --batch-size 16
    python scripts/extract_clip.py --dataset ucf --split val --backbone siglip2-giant --batch-size 16
    python scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-giant --batch-size 16

    UCF mean-only:
    python scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-giant --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset ucf --split val --backbone siglip2-giant --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-giant --batch-size 16 --pool mean

    XD mean+max:
    python scripts/extract_clip.py --dataset xd --split train --backbone siglip2-giant --batch-size 16
    python scripts/extract_clip.py --dataset xd --split val --backbone siglip2-giant --batch-size 16
    python scripts/extract_clip.py --dataset xd --split test --backbone siglip2-giant --batch-size 16

    XD mean-only:
    python scripts/extract_clip.py --dataset xd --split train --backbone siglip2-giant --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset xd --split val --backbone siglip2-giant --batch-size 16 --pool mean
    python scripts/extract_clip.py --dataset xd --split test --backbone siglip2-giant --batch-size 16 --pool mean

    Verify dimensions:
    python -c "import numpy as np, glob; files=glob.glob('E:/features/ucf/siglip2_giant/*.npy'); f=np.load(files[0]); print(f'UCF mean+max: {len(files)} files, shape={f.shape}, expect dim=3072')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/ucf/siglip2_giant_mean/*.npy'); f=np.load(files[0]); print(f'UCF mean-only: {len(files)} files, shape={f.shape}, expect dim=1536')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/xd/siglip2_giant/*.npy'); f=np.load(files[0]); print(f'XD mean+max: {len(files)} files, shape={f.shape}, expect dim=3072')"
    python -c "import numpy as np, glob; files=glob.glob('E:/features/xd/siglip2_giant_mean/*.npy'); f=np.load(files[0]); print(f'XD mean-only: {len(files)} files, shape={f.shape}, expect dim=1536')"
  </how-to-verify>
  <acceptance_criteria>
    - E:/features/ucf/siglip2_giant/ has 1728 .npy files with shape [N, 3072]
    - E:/features/ucf/siglip2_giant_mean/ has 1728 .npy files with shape [N, 1536]
    - E:/features/xd/siglip2_giant/ has 4752 .npy files with shape [N, 3072]
    - E:/features/xd/siglip2_giant_mean/ has 4752 .npy files with shape [N, 1536]
  </acceptance_criteria>
  <resume-signal>Paste dimension verification outputs or type "extracted"</resume-signal>
</task>

</tasks>

<output>
Create .planning/phases/10-siglip2-giant-backbone-comparison/10-02-SUMMARY.md when done
</output>
