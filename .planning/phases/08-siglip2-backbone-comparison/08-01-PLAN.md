---
phase: 08-siglip2-backbone-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_clip.py
  - tests/test_extract_siglip2.py
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "extract_clip.py --backbone siglip2-giant loads SigLIP2 Giant model via open_clip"
    - "extract_clip.py --backbone clip-vit-b-16 loads CLIP ViT-B/16 (default, backward compatible)"
    - "SigLIP2 extraction produces [N_snippets, 3072] float32 .npy files (mean+max pooling)"
    - "SigLIP2 mean-only extraction produces [N_snippets, 1536] float32 .npy files"
    - "Output subdirectory is 'siglip2' for mean+max, 'siglip2_mean' for mean-only"
  artifacts:
    - path: "scripts/extract_clip.py"
      provides: "Backbone-parameterized extraction with BACKBONE_CONFIGS dict"
      contains: "BACKBONE_CONFIGS"
    - path: "tests/test_extract_siglip2.py"
      provides: "Backbone config validation and dimension assertions"
  key_links:
    - from: "scripts/extract_clip.py"
      to: "open_clip.create_model_and_transforms"
      via: "BACKBONE_CONFIGS model_name + pretrained fields"
      pattern: "BACKBONE_CONFIGS.*siglip2-giant"
---

<objective>
Parameterize extract_clip.py with a --backbone flag that supports both CLIP ViT-B/16 (default) and SigLIP2 Giant, including backbone-aware dimension assertions and output subdirectory routing.

Purpose: Enable feature extraction for the SigLIP2 backbone without duplicating the extraction script. The existing snippet boundary logic, frame sampling, and atomic write patterns are reused; only model loading, preprocessing, output dimension, and output subdirectory change.

Output: Modified extract_clip.py with BACKBONE_CONFIGS registry + new test file validating backbone configs and dimension logic.
</objective>

<execution_context>
@.planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md
@.planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/extract_clip.py

<interfaces>
<!-- Key contracts the executor needs -->

From scripts/extract_clip.py (current model loading, lines 106-123):
  def load_clip_model(device: str = "cuda") -> tuple[model, preprocess]
  - Calls open_clip.create_model_and_transforms("ViT-B-16", pretrained="openai")
  - Returns (model, preprocess)

From scripts/extract_clip.py (dimension assertions, lines 313, 441-445, 581-583):
  expected_dim = 512 if pool == "mean" else 1024
  - Hardcoded in extract_clip_snippet(), extract_video_clip_features(), run_extraction()

From scripts/extract_clip.py (output subdir selection, lines 500-508):
  if corruption_type is not None:
      out_subdir = f"clip_{corruption_type}_{corruption_severity}"
  elif pool == "mean":
      out_subdir = "clip_mean"
  else:
      out_subdir = "clip"

From open_clip (verified 2026-05-19):
  open_clip.create_model_and_transforms("ViT-gopt-16-SigLIP2-384", pretrained="webli")
  - encode_image() output: [B, 1536]
  - Preprocessing: Resize(384,384) squash + Normalize(0.5, 0.5, 0.5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BACKBONE_CONFIGS registry and --backbone CLI flag to extract_clip.py</name>
  <files>scripts/extract_clip.py</files>
  <read_first>
    - scripts/extract_clip.py (full file -- understand load_clip_model at lines 106-123, dimension assertions at lines 313/441-445/581-583, output subdir at lines 500-508, argparse at lines 706-762)
    - .planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md (backbone registry pattern, parameterized dimension pattern, backbone-aware subdir pattern)
    - .planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md (Pattern 1 backbone config, Pitfall 1-6 avoidance)
  </read_first>
  <action>
    Add a module-level BACKBONE_CONFIGS dict near the top of the file (after imports, before load_clip_model) with two entries:
    - Key "clip-vit-b-16": model_name="ViT-B-16", pretrained="openai", embed_dim=512, output_subdir="clip", mean_subdir="clip_mean"
    - Key "siglip2-giant": model_name="ViT-gopt-16-SigLIP2-384", pretrained="webli", embed_dim=1536, output_subdir="siglip2", mean_subdir="siglip2_mean"

    Replace load_clip_model() with load_vision_model(backbone: str, device: str = "cuda") that:
    - Looks up cfg = BACKBONE_CONFIGS[backbone]
    - Calls open_clip.create_model_and_transforms(cfg["model_name"], pretrained=cfg["pretrained"])
    - Returns (model, preprocess, cfg)
    - Logs "Loading {backbone} ({cfg['model_name']}, pretrained='{cfg['pretrained']}')..." at INFO level

    Update all callers of load_clip_model() to use load_vision_model(args.backbone) and thread the cfg dict through.

    Replace all three hardcoded dimension assertions (lines ~313, ~441-445, ~581-583) with:
      embed_dim = cfg["embed_dim"]
      expected_dim = embed_dim if pool == "mean" else embed_dim * 2
    Thread embed_dim through extract_clip_snippet(), extract_video_clip_features(), and run_extraction().

    Replace the output subdir selection block (lines ~500-508) with backbone-aware routing:
      if corruption_type is not None:
          out_subdir = f"{cfg['output_subdir']}_{corruption_type}_{corruption_severity}"
      elif pool == "mean":
          out_subdir = cfg["mean_subdir"]
      else:
          out_subdir = cfg["output_subdir"]

    Add --backbone argparse argument with choices=list(BACKBONE_CONFIGS.keys()), default="clip-vit-b-16", help string describing the two options and their embed dimensions (512 vs 1536).

    Update the script's module docstring to mention the --backbone flag and SigLIP2 support.

    IMPORTANT: Do NOT change any existing behavior when --backbone is omitted or set to "clip-vit-b-16". All existing CLIP extraction must produce byte-identical output. The default must remain backward compatible.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "from scripts.extract_clip import BACKBONE_CONFIGS, load_vision_model; assert set(BACKBONE_CONFIGS.keys()) == {'clip-vit-b-16', 'siglip2-giant'}; assert BACKBONE_CONFIGS['siglip2-giant']['embed_dim'] == 1536; assert BACKBONE_CONFIGS['siglip2-giant']['model_name'] == 'ViT-gopt-16-SigLIP2-384'; print('BACKBONE_CONFIGS OK')"</automated>
  </verify>
  <acceptance_criteria>
    - BACKBONE_CONFIGS dict exists at module level with exactly 2 keys: "clip-vit-b-16" and "siglip2-giant"
    - BACKBONE_CONFIGS["siglip2-giant"]["embed_dim"] == 1536
    - BACKBONE_CONFIGS["siglip2-giant"]["model_name"] == "ViT-gopt-16-SigLIP2-384"
    - BACKBONE_CONFIGS["siglip2-giant"]["pretrained"] == "webli"
    - BACKBONE_CONFIGS["siglip2-giant"]["output_subdir"] == "siglip2"
    - BACKBONE_CONFIGS["siglip2-giant"]["mean_subdir"] == "siglip2_mean"
    - load_vision_model function exists and returns (model, preprocess, cfg) 3-tuple
    - No hardcoded 512 or 1024 dimension values remain in assertion lines (all use embed_dim from cfg)
    - --backbone flag appears in --help output with choices clip-vit-b-16 and siglip2-giant
    - Default --backbone value is "clip-vit-b-16"
    - Output subdir uses cfg["output_subdir"] / cfg["mean_subdir"] instead of hardcoded "clip" / "clip_mean"
  </acceptance_criteria>
  <done>extract_clip.py accepts --backbone flag, loads model from BACKBONE_CONFIGS, parameterizes all dimensions via embed_dim, routes output to backbone-specific subdirectories. Default behavior unchanged for CLIP.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_extract_siglip2.py for backbone config and dimension validation</name>
  <files>tests/test_extract_siglip2.py</files>
  <read_first>
    - scripts/extract_clip.py (the modified version from Task 1 -- BACKBONE_CONFIGS dict, load_vision_model signature)
    - tests/test_models.py (lines 1-80 -- test naming pattern, assertion style)
    - .planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md (test pattern section)
  </read_first>
  <action>
    Create tests/test_extract_siglip2.py with these tests:

    1. test_backbone_configs_keys(): Assert BACKBONE_CONFIGS has exactly the keys {"clip-vit-b-16", "siglip2-giant"}. Import BACKBONE_CONFIGS from scripts.extract_clip.

    2. test_clip_config_values(): Assert the "clip-vit-b-16" entry has model_name="ViT-B-16", pretrained="openai", embed_dim=512, output_subdir="clip", mean_subdir="clip_mean".

    3. test_siglip2_config_values(): Assert the "siglip2-giant" entry has model_name="ViT-gopt-16-SigLIP2-384", pretrained="webli", embed_dim=1536, output_subdir="siglip2", mean_subdir="siglip2_mean".

    4. test_expected_dim_mean_pool(): For each backbone, assert expected_dim with pool="mean" equals embed_dim (512 for CLIP, 1536 for SigLIP2).

    5. test_expected_dim_meanmax_pool(): For each backbone, assert expected_dim with pool="meanmax" equals embed_dim * 2 (1024 for CLIP, 3072 for SigLIP2).

    6. test_siglip2_output_subdir(): Assert cfg["output_subdir"] == "siglip2" and cfg["mean_subdir"] == "siglip2_mean" for the siglip2-giant entry.

    7. test_clip_backward_compat(): Assert cfg["output_subdir"] == "clip" and cfg["mean_subdir"] == "clip_mean" for the clip-vit-b-16 entry, confirming backward compatibility.

    Use the import pattern: sys.path.insert(0, str(Path(__file__).resolve().parent.parent)) then from scripts.extract_clip import BACKBONE_CONFIGS. Follow the existing test_models.py assertion style (direct assert statements, no unittest.TestCase).
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_extract_siglip2.py -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_extract_siglip2.py exists with 7 test functions
    - All 7 tests pass with exit code 0
    - Tests import BACKBONE_CONFIGS from scripts.extract_clip (not hardcoded values)
    - No GPU required (pure dict/config validation)
    - Tests complete in under 10 seconds
  </acceptance_criteria>
  <done>7 backbone config tests pass, validating both CLIP backward compatibility and SigLIP2 config correctness without requiring GPU.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> extraction | --backbone value selects model loading path |
| Filesystem -> features | .npy files consumed by training pipeline must match config clip_dim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | BACKBONE_CONFIGS values | accept | Config is hardcoded in source; no external input |
| T-08-02 | Information Disclosure | Model weights download | accept | First run downloads from HuggingFace Hub over HTTPS; standard ML workflow |
| T-08-03 | Tampering | Feature .npy dimension mismatch | mitigate | Assertion in extraction: feats.shape[1] == expected_dim computed from BACKBONE_CONFIGS |
</threat_model>

<verification>
Run full test suite to confirm no regressions:
  C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/ -x -q --timeout=60
Verify BACKBONE_CONFIGS imports correctly and has expected values.
</verification>

<success_criteria>
- extract_clip.py --backbone siglip2-giant --help shows SigLIP2 option
- extract_clip.py --backbone clip-vit-b-16 behavior is identical to current (backward compatible)
- BACKBONE_CONFIGS and load_vision_model importable and structurally correct
- All 7 new tests pass, existing test suite has no regressions
</success_criteria>

<output>
Create `.planning/phases/08-siglip2-backbone-comparison/08-01-SUMMARY.md` when done
</output>
