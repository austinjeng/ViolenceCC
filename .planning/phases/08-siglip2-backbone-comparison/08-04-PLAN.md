---
phase: 08-siglip2-backbone-comparison
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - scripts/generate_phase8_charts.py
autonomous: false
requirements: [EVAL-02, EVAL-03, EVAL-04]

must_haves:
  truths:
    - "All SigLIP2 model variants (SigLIP2-Only, Late Fusion, Gated Fusion) trained and evaluated on UCF and XD"
    - "Pooling ablations (2person, clip_mean) trained and evaluated with SigLIP2 on UCF and XD"
    - "3-seed stability (42, 123, 2024) completed for SigLIP2 Gated Fusion on both datasets"
    - "Side-by-side CLIP vs SigLIP2 comparison table exists with all ablation rows"
    - "All results logged in results/results-index.csv"
  artifacts:
    - path: "results/results-index.csv"
      provides: "All Phase 8 run metrics (AUC/AP for each SigLIP2 config)"
    - path: "scripts/generate_phase8_charts.py"
      provides: "Backbone comparison table generation script"
    - path: "results/phase8_charts/"
      provides: "CLIP vs SigLIP2 comparison figures"
  key_links:
    - from: "scripts/run_ablations.py"
      to: "results/results-index.csv"
      via: "results_index_append after each run"
      pattern: "results_index_append"
    - from: "scripts/generate_phase8_charts.py"
      to: "results/results-index.csv"
      via: "pandas read_csv + filter by siglip2 tag"
      pattern: "pd.read_csv.*results-index"
---

<objective>
Execute all Phase 8 ablation queues (6 queues, 14 runs total for SigLIP2 + 6 seed confirmation runs = 20 total training runs), then generate a CLIP vs SigLIP2 side-by-side comparison table for the thesis.

Purpose: Produce the controlled backbone comparison that is the thesis contribution of Phase 8. Every SigLIP2 run mirrors an existing CLIP run with only the visual feature source changed.

Output: All SigLIP2 ablation results in results-index.csv, comparison table/chart in results/phase8_charts/.
</objective>

<execution_context>
@.planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md
@.planning/phases/08-siglip2-backbone-comparison/08-02-SUMMARY.md
@.planning/phases/08-siglip2-backbone-comparison/08-03-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/run_ablations.py
@scripts/generate_phase6_charts.py

<interfaces>
<!-- run_ablations.py CLI: -->
  python scripts/run_ablations.py --queue {queue_name} [--dry-run] [--no-preflight]

<!-- Phase 8 queues (from Plan 02): -->
  phase8_ucf_main:    3 runs (clip_only, late_fusion, gated_fusion -- seed 42)
  phase8_xd_main:     3 runs (same variants for XD)
  phase8_ucf_pooling:  2 runs (2person, clip_mean -- seed 42)
  phase8_xd_pooling:   2 runs (same for XD)
  phase8_ucf_seeds:    2 runs (gated_fusion seeds 123, 2024)
  phase8_xd_seeds:     2 runs (same for XD)
  Total: 14 runs

<!-- results-index.csv schema (existing): -->
  Columns: run_name, dataset, variant, seed, auc, ap, config, cache_variant, ...

<!-- Phase 6 chart generation pattern (from scripts/generate_phase6_charts.py): -->
  - Read results-index.csv with pandas
  - Filter by tags/cache_variant
  - Generate matplotlib/seaborn figures
  - Save to results/{phase}_charts/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Phase 8 ablation queues and create comparison chart script</name>
  <files>scripts/generate_phase8_charts.py</files>
  <read_first>
    - scripts/run_ablations.py (Phase 8 queue definitions -- verify they exist and have correct RunSpecs)
    - scripts/generate_phase6_charts.py (chart generation pattern -- imports, style constants, project setup, CSV loading)
    - results/results-index.csv (current content -- understand column names and existing data format)
    - .planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md (generate_phase8_charts.py section -- analog pattern)
  </read_first>
  <action>
    Part A: Execute all 6 Phase 8 queues sequentially. Use the --no-preflight flag (wandb is disabled). Run in this order:

    1. Dry-run first to verify config paths resolve:
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_ucf_main --dry-run
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_xd_main --dry-run

    2. Execute main queues:
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_ucf_main --no-preflight
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_xd_main --no-preflight

    3. Execute pooling queues:
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_ucf_pooling --no-preflight
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_xd_pooling --no-preflight

    4. Execute seed queues:
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_ucf_seeds --no-preflight
       C:/Anaconda/envs/vcc-main/python.exe scripts/run_ablations.py --queue phase8_xd_seeds --no-preflight

    Each queue invocation is a long-running GPU job (~5-10 min per run, ~2-3 hours total for all 14 runs). If any single run fails, the queue continues to the next spec automatically (existing error handling).

    Part B: After all queues complete, create scripts/generate_phase8_charts.py following the Phase 6 chart generation pattern:

    The script should:
    1. Load results/results-index.csv with pandas
    2. Build a comparison DataFrame by matching CLIP runs and SigLIP2 runs:
       - CLIP runs: cache_variant is empty or does not contain "siglip2"
       - SigLIP2 runs: cache_variant contains "siglip2"
       - Match on: dataset + variant + seed (for main/seed runs) or dataset + variant + seed + pooling ablation type (for pooling runs)
    3. Generate a side-by-side comparison table (printed to console and saved as CSV):
       - Columns: Dataset, Variant, Seed, CLIP_AUC, CLIP_AP, SigLIP2_AUC, SigLIP2_AP, Delta_AUC, Delta_AP
       - Include all ablation rows (main variants + pooling + 3-seed)
    4. Generate a grouped bar chart comparing CLIP vs SigLIP2 AUC/AP across all variants
    5. Save outputs to results/phase8_charts/ directory (create if needed)

    Follow the generate_phase6_charts.py pattern for: imports, matplotlib Agg backend, sns.set_theme, DPI/FIG_* constants, PROJECT_ROOT setup. Use the existing style constants verbatim.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import pandas as pd; df = pd.read_csv('results/results-index.csv'); sig = df[df['cache_variant'].str.contains('siglip2', na=False)]; print(f'SigLIP2 runs in results-index.csv: {len(sig)}'); assert len(sig) >= 14, f'Expected >= 14 SigLIP2 runs, got {len(sig)}'"</automated>
  </verify>
  <acceptance_criteria>
    - results-index.csv contains >= 14 rows with cache_variant containing "siglip2"
    - SigLIP2 runs cover: clip_only (UCF+XD), late_fusion (UCF+XD), gated_fusion (UCF+XD), 2person (UCF+XD), clip_mean (UCF+XD), seeds 123+2024 (UCF+XD)
    - scripts/generate_phase8_charts.py exists and runs without error
    - results/phase8_charts/ directory contains comparison CSV and at least one chart PNG
    - No training runs failed (check runner-errors.log is empty or has 0 Phase 8 entries)
  </acceptance_criteria>
  <done>All 14 Phase 8 training runs complete, results logged to results-index.csv, comparison chart script created and executed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 8 SigLIP2 backbone comparison is complete. All ablation runs executed, CLIP vs SigLIP2 comparison table generated.</what-built>
  <how-to-verify>
    1. Review the comparison table at results/phase8_charts/backbone_comparison.csv
    2. Verify all ablation rows are present (6 variants x 2 datasets = 12 main rows + 4 seed rows)
    3. Check that the bar chart at results/phase8_charts/ visually makes sense
    4. Confirm the delta columns show the SigLIP2 improvement/regression vs CLIP
    5. Run the full test suite:
       C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/ -x -q --timeout=60
    6. Verify Skeleton-Only rows are reused from Phase 4/4c (not re-run) in the comparison table
  </how-to-verify>
  <resume-signal>Type "approved" if the comparison table is complete and results look reasonable, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SigLIP2 features -> model training | Feature files loaded from disk into training pipeline |
| Training output -> results-index.csv | Metrics appended to shared CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | Feature dimension mismatch at train time | mitigate | MILFeatureDataset loads clip.shape[1] which must match config clip_dim; mismatch causes immediate crash (not silent failure) |
| T-08-10 | Repudiation | Results provenance | mitigate | results-index.csv includes config path, seed, run_name; git-tracked configs enable reproduction |
</threat_model>

<verification>
After all runs and chart generation:
1. results-index.csv has >= 14 new SigLIP2 rows
2. No failed runs in runner-errors.log
3. Comparison table covers all variant x dataset combinations
4. Full test suite passes
</verification>

<success_criteria>
- All 14 SigLIP2 ablation runs complete with non-NaN AUC/AP values
- 3-seed stability for SigLIP2 Gated Fusion computed on both datasets
- Side-by-side CLIP vs SigLIP2 comparison table exists and is thesis-ready
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/08-siglip2-backbone-comparison/08-04-SUMMARY.md` when done
</output>
