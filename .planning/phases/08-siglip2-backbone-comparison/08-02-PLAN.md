---
phase: 08-siglip2-backbone-comparison
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/clip_only_siglip2.yaml
  - configs/clip_only_xd_siglip2.yaml
  - configs/late_fusion_siglip2.yaml
  - configs/late_fusion_xd_siglip2.yaml
  - configs/gated_fusion_siglip2.yaml
  - configs/gated_fusion_xd_siglip2.yaml
  - configs/gated_fusion_siglip2_2person.yaml
  - configs/gated_fusion_xd_siglip2_2person.yaml
  - configs/gated_fusion_siglip2_clip_mean.yaml
  - configs/gated_fusion_xd_siglip2_clip_mean.yaml
  - scripts/run_ablations.py
  - tests/test_models.py
  - tests/test_run_ablations.py
autonomous: true
requirements: [EVAL-02, EVAL-03, EVAL-04]

must_haves:
  truths:
    - "10 SigLIP2 YAML configs exist, each with clip_dim=3072 (or 1536 for mean-only) and clip_features pointing to siglip2 subdirectory"
    - "6 Phase 8 queues exist in run_ablations.py: phase8_ucf_main, phase8_xd_main, phase8_ucf_pooling, phase8_xd_pooling, phase8_ucf_seeds, phase8_xd_seeds"
    - "CLIPProj, GatedFusion, and LateFusion all accept clip_dim=3072 and forward correctly (tested)"
    - "Queue run_name format includes siglip2 cache_variant"
  artifacts:
    - path: "configs/gated_fusion_siglip2.yaml"
      provides: "UCF Gated Fusion with SigLIP2 features"
      contains: "clip_dim: 3072"
    - path: "configs/gated_fusion_xd_siglip2.yaml"
      provides: "XD Gated Fusion with SigLIP2 features"
      contains: "clip_dim: 3072"
    - path: "scripts/run_ablations.py"
      provides: "Phase 8 queue definitions"
      contains: "phase8_ucf_main"
    - path: "tests/test_models.py"
      provides: "clip_dim=3072 forward pass tests for CLIPProj, GatedFusion, and LateFusion"
      contains: "test_clip_dim_3072"
    - path: "tests/test_run_ablations.py"
      provides: "Phase 8 queue count assertions"
      contains: "phase8_ucf_main"
  key_links:
    - from: "configs/gated_fusion_siglip2.yaml"
      to: "E:/features/ucf/siglip2"
      via: "paths.clip_features"
      pattern: "clip_features.*siglip2"
    - from: "scripts/run_ablations.py"
      to: "configs/gated_fusion_siglip2.yaml"
      via: "RunSpec config path"
      pattern: "configs/gated_fusion_siglip2.yaml"
---

<objective>
Create all 10 SigLIP2 YAML configs, add 6 Phase 8 ablation queues to run_ablations.py, and add model dimension tests for clip_dim=3072.

Purpose: Wire the training and evaluation infrastructure so that once SigLIP2 features are extracted (Plan 03), the full ablation matrix can be executed immediately. This plan has zero file overlap with Plan 01 and runs in parallel.

Output: 10 config files, updated run_ablations.py with Phase 8 queues, updated test_models.py with 3072-d tests for all three model variants, updated test_run_ablations.py with Phase 8 queue assertions.
</objective>

<execution_context>
@.planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md
@.planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@configs/gated_fusion.yaml
@configs/gated_fusion_xd.yaml
@configs/clip_only.yaml
@configs/late_fusion.yaml
@configs/gated_fusion_2person.yaml
@configs/gated_fusion_clip_mean.yaml
@configs/gated_fusion_xd_2person.yaml
@configs/gated_fusion_xd_clip_mean.yaml
@scripts/run_ablations.py
@tests/test_models.py
@tests/test_run_ablations.py

<interfaces>
<!-- Existing config contract (5-section YAML structure): -->
  seed: 42
  dataset: ucf|xd
  paths: {skeleton_features, clip_features, splits_dir, results_dir}
  model: {variant, [skel_dim], clip_dim, [proj_dim], [shared_dim], head_hidden, dropout, [alpha]}
  data: {T, batch_size, num_workers, pin_memory, [skel_agg]}
  train: {lr, weight_decay, epochs, warmup_epochs, patience, k_topk, margin, lam_sparse, lam_smooth}
  wandb: {project, mode, tags}

<!-- RunSpec constructor: -->
  RunSpec(dataset, variant, seed, config, cache_variant="", lr_override=None, k_topk_override=None)
  - run_name property: "{dataset}_{variant}[_{cache_variant}]_s{seed}"
  - wandb_tags(): ["phase4", dataset, variant, f"s{seed}"] + [cache_variant if set]

<!-- CLIPProj constructor (from src/models/clip_only.py): -->
  CLIPProj(clip_dim=1024, proj_dim=512, head_hidden=[128, 32], dropout=0.3)
  - clip_proj: nn.Linear(clip_dim, proj_dim)
  - ln_clip: nn.LayerNorm(proj_dim)

<!-- GatedFusion constructor (from src/models/gated_fusion.py): -->
  GatedFusion(skel_dim=256, clip_dim=1024, shared_dim=256, head_hidden=[128, 32], dropout=0.3)
  - clip_proj: nn.Linear(clip_dim, shared_dim)

<!-- LateFusion constructor (from src/models/late_fusion.py): -->
  LateFusion(skel_dim=256, clip_dim=1024, proj_dim=512, head_hidden=[128, 32], dropout=0.3, alpha="equal")
  - clip_proj: nn.Linear(clip_dim, proj_dim)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 10 SigLIP2 YAML configs</name>
  <files>
    configs/clip_only_siglip2.yaml
    configs/clip_only_xd_siglip2.yaml
    configs/late_fusion_siglip2.yaml
    configs/late_fusion_xd_siglip2.yaml
    configs/gated_fusion_siglip2.yaml
    configs/gated_fusion_xd_siglip2.yaml
    configs/gated_fusion_siglip2_2person.yaml
    configs/gated_fusion_xd_siglip2_2person.yaml
    configs/gated_fusion_siglip2_clip_mean.yaml
    configs/gated_fusion_xd_siglip2_clip_mean.yaml
  </files>
  <read_first>
    - configs/clip_only.yaml (analog for clip_only_siglip2.yaml)
    - configs/clip_only_xd.yaml (analog for clip_only_xd_siglip2.yaml)
    - configs/late_fusion.yaml (analog for late_fusion_siglip2.yaml)
    - configs/late_fusion_xd.yaml (analog for late_fusion_xd_siglip2.yaml)
    - configs/gated_fusion.yaml (analog for gated_fusion_siglip2.yaml)
    - configs/gated_fusion_xd.yaml (analog for gated_fusion_xd_siglip2.yaml)
    - configs/gated_fusion_2person.yaml (analog for gated_fusion_siglip2_2person.yaml)
    - configs/gated_fusion_xd_2person.yaml (analog for gated_fusion_xd_siglip2_2person.yaml)
    - configs/gated_fusion_clip_mean.yaml (analog for gated_fusion_siglip2_clip_mean.yaml)
    - configs/gated_fusion_xd_clip_mean.yaml (analog for gated_fusion_xd_siglip2_clip_mean.yaml)
    - .planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md (exact field changes per config)
  </read_first>
  <action>
    Create each config by copying its analog verbatim and changing exactly the fields specified below. All other fields (seed, dataset, data, train, paths.skeleton_features, paths.splits_dir, paths.results_dir, model.shared_dim, model.head_hidden, model.dropout, model.alpha) MUST remain identical to the analog.

    For clip_only variants (UCF and XD):
    - paths.clip_features: replace "clip" subdir with "siglip2" (e.g. "E:/features/ucf/siglip2")
    - model.clip_dim: 3072 (was 1024)
    - wandb.tags: replace phase tag with "phase8", add "siglip2" tag

    For late_fusion variants (UCF and XD):
    - paths.clip_features: replace "clip" subdir with "siglip2"
    - model.clip_dim: 3072 (was 1024)
    - wandb.tags: replace phase tag with "phase8", add "siglip2" tag

    For gated_fusion variants (UCF and XD):
    - paths.clip_features: replace "clip" subdir with "siglip2"
    - model.clip_dim: 3072 (was 1024)
    - wandb.tags: replace phase tag with "phase8", add "siglip2" tag

    For gated_fusion_2person variants (UCF and XD):
    - paths.clip_features: replace "clip" subdir with "siglip2"
    - model.clip_dim: 3072 (was 1024)
    - model.skel_dim: 512 (unchanged from 2person analog)
    - data.skel_agg: concat (unchanged from 2person analog)
    - wandb.tags: replace phase tag with "phase8", add "siglip2" tag
    - XD variant also has paths.skeleton_features: "E:/features/xd/skeleton_2person" (unchanged)

    For gated_fusion_clip_mean variants (UCF and XD):
    - paths.clip_features: replace "clip_mean" subdir with "siglip2_mean" (e.g. "E:/features/ucf/siglip2_mean")
    - model.clip_dim: 1536 (was 512; SigLIP2 mean-only = 1536, not 3072)
    - wandb.tags: replace phase tag with "phase8", add "siglip2" tag

    Each config file MUST have a header comment: "# configs/{filename}" followed by "# Phase 8: SigLIP2 {variant description}. Mirrors {analog_filename} with SigLIP2 features."

    CRITICAL: clip_mean variants use clip_dim=1536, NOT 3072. Mean-only SigLIP2 is embed_dim=1536. Mean+max SigLIP2 is embed_dim*2=3072.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import yaml, glob; files = glob.glob('configs/*siglip2*.yaml'); assert len(files) == 10, f'Expected 10 SigLIP2 configs, got {len(files)}'; [yaml.safe_load(open(f)) for f in files]; print(f'All {len(files)} SigLIP2 configs parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 10 new YAML files exist matching configs/*siglip2*.yaml
    - All 10 parse with yaml.safe_load without error
    - clip_only_siglip2.yaml: clip_dim=3072, clip_features ends with "/siglip2"
    - clip_only_xd_siglip2.yaml: dataset=xd, clip_dim=3072, clip_features="E:/features/xd/siglip2"
    - late_fusion_siglip2.yaml: clip_dim=3072, alpha=equal, proj_dim=512
    - gated_fusion_siglip2.yaml: clip_dim=3072, shared_dim=256
    - gated_fusion_siglip2_2person.yaml: clip_dim=3072, skel_dim=512, skel_agg=concat
    - gated_fusion_siglip2_clip_mean.yaml: clip_dim=1536, clip_features ends with "/siglip2_mean"
    - gated_fusion_xd_siglip2_clip_mean.yaml: clip_dim=1536, clip_features="E:/features/xd/siglip2_mean"
    - All non-changed fields are identical to their analog configs (seed, train section, data.T, data.batch_size, etc.)
    - Every config has "siglip2" in wandb.tags
  </acceptance_criteria>
  <done>10 SigLIP2 YAML configs exist, correctly parameterized with clip_dim 3072 (mean+max) or 1536 (mean-only) and siglip2/siglip2_mean feature paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 8 queues to run_ablations.py and update all tests</name>
  <files>
    scripts/run_ablations.py
    tests/test_models.py
    tests/test_run_ablations.py
  </files>
  <read_first>
    - scripts/run_ablations.py (lines 50-200 -- RunSpec class, QUEUES dict, Phase 4/4c queue patterns)
    - tests/test_models.py (lines 39-68 -- CLIPProj test pattern; lines 84-141 -- LateFusion test pattern; lines 170-218 -- GatedFusion test pattern)
    - tests/test_run_ablations.py (lines 30-82 -- help queue listing, queue count assertions, run_name spot checks)
    - .planning/phases/08-siglip2-backbone-comparison/08-PATTERNS.md (queue definitions section, test assertions section)
  </read_first>
  <action>
    In scripts/run_ablations.py, add 6 Phase 8 queue entries to the QUEUES dict, after the existing Phase 7 queues. Follow the exact RunSpec pattern from Phase 4/4c queues. All Phase 8 RunSpecs use cache_variant="siglip2" (or "siglip2_2person" / "siglip2_clip_mean" for pooling variants).

    Add these queues:
    - "phase8_ucf_main": 3 entries (clip_only, late_fusion, gated_fusion for UCF with siglip2 configs, seed=42, cache_variant="siglip2"). NOTE: skeleton_only is NOT included -- it uses no visual features.
    - "phase8_xd_main": 3 entries (same pattern for XD dataset)
    - "phase8_ucf_pooling": 2 entries (gated_fusion with 2person and clip_mean siglip2 configs for UCF, seed=42, cache_variant="siglip2_2person" and "siglip2_clip_mean")
    - "phase8_xd_pooling": 2 entries (same pattern for XD)
    - "phase8_ucf_seeds": 2 entries (gated_fusion siglip2 for UCF, seeds 123 and 2024, cache_variant="siglip2")
    - "phase8_xd_seeds": 2 entries (same pattern for XD)

    Total: 14 new RunSpec entries across 6 queues.

    In tests/test_models.py, add these test functions:

    After the existing CLIPProj tests (after test_clip_only_raises_without_clip):
    - test_clip_dim_3072(): Construct CLIPProj(clip_dim=3072), forward with torch.randn(2, 32, 3072), assert output shape (2, 32), assert torch.isfinite, assert values in [0, 1]
    - test_clip_dim_3072_projection_layer(): Construct CLIPProj(clip_dim=3072), assert model.clip_proj.in_features == 3072, assert model.clip_proj.out_features == 512

    After the existing GatedFusion shape tests (after test_gated_fusion_shapes, near line 183):
    - test_gated_fusion_clip_dim_3072(): Construct GatedFusion(skel_dim=256, clip_dim=3072, shared_dim=256), forward with skel=torch.randn(2, 32, 256) and clip=torch.randn(2, 32, 3072), assert output shape (2, 32), assert torch.isfinite(out).all(), assert (out >= 0).all() and (out <= 1).all(). Use torch.manual_seed(0) for reproducibility.

    After the existing LateFusion tests (after test_late_fusion_invalid_alpha_raises, near line 141):
    - test_late_fusion_clip_dim_3072(): Construct LateFusion(skel_dim=256, clip_dim=3072, proj_dim=512, alpha="equal"), forward with skel=torch.randn(2, 32, 256) and clip=torch.randn(2, 32, 3072), assert output shape (2, 32), assert torch.isfinite(out).all(), assert (out >= 0).all() and (out <= 1).all(). Use torch.manual_seed(0) for reproducibility.

    These tests validate that ALL three model variants that receive clip_dim from SigLIP2 configs (CLIPProj, GatedFusion, LateFusion) accept 3072-d input without runtime error, preventing failures during Plan 04 training after hours of extraction.

    In tests/test_run_ablations.py:
    - In test_help_has_expected_queues(): Add all 6 Phase 8 queue names to the assertion loop
    - In test_queue_definitions(): Add Phase 8 queue length assertions (3, 3, 2, 2, 2, 2) and run_name spot checks:
      * QUEUES["phase8_ucf_main"][2].run_name == "ucf_gated_fusion_siglip2_s42"
      * QUEUES["phase8_xd_seeds"][0].run_name == "xd_gated_fusion_siglip2_s123"
    - Update the total unique run_names count from 224 to 238 (224 + 14 new)
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/test_models.py tests/test_run_ablations.py -v --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - QUEUES["phase8_ucf_main"] has exactly 3 entries (no skeleton_only)
    - QUEUES["phase8_xd_main"] has exactly 3 entries
    - QUEUES["phase8_ucf_pooling"] has exactly 2 entries
    - QUEUES["phase8_xd_pooling"] has exactly 2 entries
    - QUEUES["phase8_ucf_seeds"] has exactly 2 entries
    - QUEUES["phase8_xd_seeds"] has exactly 2 entries
    - QUEUES["phase8_ucf_main"][2].run_name == "ucf_gated_fusion_siglip2_s42"
    - QUEUES["phase8_xd_seeds"][0].run_name == "xd_gated_fusion_siglip2_s123"
    - Total unique run_names across all queues == 238
    - test_clip_dim_3072 passes (CLIPProj accepts 3072-d input)
    - test_clip_dim_3072_projection_layer passes (in_features=3072, out_features=512)
    - test_gated_fusion_clip_dim_3072 passes (GatedFusion accepts clip_dim=3072, output shape (2, 32), finite, in [0, 1])
    - test_late_fusion_clip_dim_3072 passes (LateFusion accepts clip_dim=3072, output shape (2, 32), finite, in [0, 1])
    - All existing tests in test_models.py and test_run_ablations.py still pass
  </acceptance_criteria>
  <done>6 Phase 8 queues with 14 RunSpecs are defined, CLIPProj/GatedFusion/LateFusion all accept clip_dim=3072, and all tests pass including new Phase 8 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config YAML -> model constructor | clip_dim value flows from config to nn.Linear in_features |
| Config YAML -> feature path | clip_features path must match actual extraction output directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Config clip_features path mismatch | mitigate | All configs are version-controlled; path correctness verified by feature existence check at train.py load time |
| T-08-05 | Information Disclosure | wandb tags leak experiment structure | accept | Tags are project-internal; wandb is disabled by default |
</threat_model>

<verification>
Run full test suite:
  C:/Anaconda/envs/vcc-main/python.exe -m pytest tests/ -x -q --timeout=60
Verify all 10 configs parse and all 6 queues exist.
</verification>

<success_criteria>
- 10 SigLIP2 YAML configs exist and parse correctly
- 6 Phase 8 queues in run_ablations.py with 14 total RunSpecs
- CLIPProj(clip_dim=3072) forward pass produces correct shape
- GatedFusion(clip_dim=3072) forward pass produces correct shape
- LateFusion(clip_dim=3072) forward pass produces correct shape
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/08-siglip2-backbone-comparison/08-02-SUMMARY.md` when done
</output>
