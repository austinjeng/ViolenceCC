---
phase: 08-siglip2-backbone-comparison
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "SigLIP2 mean+max features exist at E:/features/ucf/siglip2/ for all extractable UCF videos"
    - "SigLIP2 mean+max features exist at E:/features/xd/siglip2/ for all XD-Violence videos"
    - "SigLIP2 mean-only features exist at E:/features/ucf/siglip2_mean/ for all extractable UCF videos"
    - "SigLIP2 mean-only features exist at E:/features/xd/siglip2_mean/ for all XD-Violence videos"
    - "Every .npy file has shape [N_snippets, 3072] (mean+max) or [N_snippets, 1536] (mean-only)"
    - "Snippet count matches existing CLIP extraction (same boundaries reused)"
  artifacts:
    - path: "E:/features/ucf/siglip2/"
      provides: "UCF SigLIP2 mean+max features"
    - path: "E:/features/xd/siglip2/"
      provides: "XD SigLIP2 mean+max features"
    - path: "E:/features/ucf/siglip2_mean/"
      provides: "UCF SigLIP2 mean-only features"
    - path: "E:/features/xd/siglip2_mean/"
      provides: "XD SigLIP2 mean-only features"
  key_links:
    - from: "E:/features/ucf/siglip2/*.npy"
      to: "E:/features/ucf/clip/*.npy"
      via: "Same snippet boundary JSON files"
      pattern: "N_snippets equal per video"
---

<objective>
Execute SigLIP2 feature extraction for both UCF-Crime and XD-Violence datasets using the --backbone siglip2-giant flag added in Plan 01. Run 4 extraction passes: mean+max and mean-only for each dataset.

Purpose: Produce the feature caches that the ablation matrix (Plan 04) will consume. Extraction is GPU-bound (~2.5 hours total) and must be human-supervised in a visible terminal per project conventions.

Output: Feature .npy files in E:/features/{dataset}/siglip2/ and E:/features/{dataset}/siglip2_mean/ directories.
</objective>

<execution_context>
@.planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md
@.planning/phases/08-siglip2-backbone-comparison/08-01-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/extract_clip.py

<interfaces>
<!-- Extraction CLI (after Plan 01 modifications): -->
  python scripts/extract_clip.py --dataset {ucf|xd} --split {train|test} --backbone {clip-vit-b-16|siglip2-giant} [--pool mean] [--batch-size N]

<!-- SigLIP2 throughput (verified benchmarks): -->
  Batch 16: 68 img/s, peak VRAM 10,374 MB (within 24GB RTX 4090)

<!-- Estimated extraction times: -->
  UCF mean+max: ~17 min, UCF mean-only: ~17 min
  XD mean+max: ~131 min, XD mean-only: ~131 min
  Total: ~5 hours (including mean-only passes)

<!-- Feature file counts (from Phase 2 extraction): -->
  UCF extractable videos: 1728 (of 1900; 172 sub-64-frame videos skipped)
  XD-Violence videos: 4754 (all extractable, min 120 frames)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run SigLIP2 feature extraction for UCF-Crime (mean+max and mean-only)</name>
  <files></files>
  <read_first>
    - scripts/extract_clip.py (verify --backbone flag exists and BACKBONE_CONFIGS is correct)
    - .planning/phases/08-siglip2-backbone-comparison/08-RESEARCH.md (Pitfall 5: offline loading bug, Pitfall 6: VRAM pressure)
  </read_first>
  <action>
    Run a smoke test first: extract a single UCF video with SigLIP2 to verify the pipeline works end-to-end before committing to the full run.

    Smoke test command (limit=1):
      C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-giant --batch-size 16 --limit 1

    Verify the smoke test output:
    - Check that E:/features/ucf/siglip2/ contains exactly 1 .npy file
    - Load the .npy and assert shape[1] == 3072
    - Verify the extraction log shows "Loading siglip2-giant" (not "Loading CLIP")
    - Verify the preprocessing transform includes Resize(384, 384) and Normalize(0.5) -- log this from the extraction script or inspect at runtime

    After smoke test passes, run the 4 full extraction passes. Because these are long-running GPU jobs, print the commands for the user to execute in a visible terminal (per project convention: no headless processes).

    Extraction commands (run sequentially in one terminal):
      1. UCF mean+max (train+test):
         C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-giant --batch-size 16
         C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-giant --batch-size 16

      2. UCF mean-only (train+test):
         C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split train --backbone siglip2-giant --batch-size 16 --pool mean
         C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset ucf --split test --backbone siglip2-giant --batch-size 16 --pool mean

    After UCF extraction completes, verify file counts:
    - E:/features/ucf/siglip2/*.npy should have ~1728 files (same as E:/features/ucf/clip/*.npy)
    - E:/features/ucf/siglip2_mean/*.npy should have ~1728 files

    Spot-check dimension consistency: load 5 random .npy files from siglip2/ and assert shape[1]==3072; load 5 from siglip2_mean/ and assert shape[1]==1536.
  </action>
  <verify>
    <automated>C:/Anaconda/envs/vcc-main/python.exe -c "import numpy as np, glob; files = glob.glob('E:/features/ucf/siglip2/*.npy'); print(f'UCF siglip2 files: {len(files)}'); assert len(files) >= 1700, f'Expected ~1728, got {len(files)}'; x = np.load(files[0]); assert x.shape[1] == 3072, f'Expected dim 3072, got {x.shape[1]}'; files_m = glob.glob('E:/features/ucf/siglip2_mean/*.npy'); print(f'UCF siglip2_mean files: {len(files_m)}'); assert len(files_m) >= 1700; y = np.load(files_m[0]); assert y.shape[1] == 1536; print('UCF SigLIP2 extraction PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - E:/features/ucf/siglip2/ contains >= 1700 .npy files
    - E:/features/ucf/siglip2_mean/ contains >= 1700 .npy files
    - Every sampled .npy in siglip2/ has shape [N, 3072] with dtype float32
    - Every sampled .npy in siglip2_mean/ has shape [N, 1536] with dtype float32
    - File count matches E:/features/ucf/clip/*.npy count (same videos extracted)
    - No NaN or Inf values in sampled features
  </acceptance_criteria>
  <done>UCF-Crime SigLIP2 features extracted for both mean+max (3072-d) and mean-only (1536-d) pooling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>UCF-Crime SigLIP2 feature extraction completed. The user needs to run XD-Violence extraction (~4.5 hours) in a visible terminal.</what-built>
  <how-to-verify>
    1. Confirm UCF extraction passed the automated verification above
    2. Run XD-Violence extraction in a visible terminal (4 commands, ~4.5 hours total):

       Command 1 - XD mean+max train:
       C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset xd --split train --backbone siglip2-giant --batch-size 16

       Command 2 - XD mean+max test:
       C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset xd --split test --backbone siglip2-giant --batch-size 16

       Command 3 - XD mean-only train:
       C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset xd --split train --backbone siglip2-giant --batch-size 16 --pool mean

       Command 4 - XD mean-only test:
       C:/Anaconda/envs/vcc-main/python.exe scripts/extract_clip.py --dataset xd --split test --backbone siglip2-giant --batch-size 16 --pool mean

    3. After all 4 commands complete, verify counts:
       - E:/features/xd/siglip2/*.npy should have ~4754 files
       - E:/features/xd/siglip2_mean/*.npy should have ~4754 files

    4. Run the verification command:
       C:/Anaconda/envs/vcc-main/python.exe -c "import numpy as np, glob; files = glob.glob('E:/features/xd/siglip2/*.npy'); print(f'XD siglip2: {len(files)}'); assert len(files) >= 4700; x = np.load(files[0]); assert x.shape[1] == 3072; files_m = glob.glob('E:/features/xd/siglip2_mean/*.npy'); print(f'XD siglip2_mean: {len(files_m)}'); assert len(files_m) >= 4700; y = np.load(files_m[0]); assert y.shape[1] == 1536; print('XD SigLIP2 extraction PASS')"
  </how-to-verify>
  <resume-signal>Type "extraction complete" when all 4 XD passes finish and verification passes, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace Hub -> local model cache | First SigLIP2 download (~4GB) from external server |
| Video frames -> model inference | Untrusted video content processed by frozen model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Model weights from HuggingFace | accept | Standard ML workflow; HuggingFace uses SHA256 checksums on model files |
| T-08-07 | Denial of Service | GPU OOM during extraction | mitigate | Use batch_size=16 (peak ~10.4 GB, well within 24 GB RTX 4090) |
| T-08-08 | Tampering | Corrupted .npy from interrupted extraction | mitigate | Atomic write pattern (tmp.npy -> rename) already in extract_clip.py |
</threat_model>

<verification>
After all extraction completes:
  - UCF siglip2: ~1728 files, shape [N, 3072]
  - UCF siglip2_mean: ~1728 files, shape [N, 1536]
  - XD siglip2: ~4754 files, shape [N, 3072]
  - XD siglip2_mean: ~4754 files, shape [N, 1536]
  - Snippet counts match existing CLIP extraction per video
</verification>

<success_criteria>
- All 4 feature directories populated with correct file counts
- Dimension assertions pass for all directories
- No extraction errors (0 failed videos, or < 0.1% failure rate)
</success_criteria>

<output>
Create `.planning/phases/08-siglip2-backbone-comparison/08-03-SUMMARY.md` when done
</output>
