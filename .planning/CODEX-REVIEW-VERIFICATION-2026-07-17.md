# Codex Review Verification — 2026-07-17

**Task:** Austin received an external (Codex) review of thesis/main.pdf claiming "do not submit yet"
with 5 must-address methodological items + 9 presentation items. Every claim was verified against the
repo, thesis sources, built PDF, git history, run artifacts (config_snapshot.json git SHAs, checkpoint
hashes), and external sources — by one investigator per claim plus an adversarial cross-check arguing
the OPPOSITE direction of the investigator's verdict (12 agents, wf_bbe67192-82d).

## Bottom line

Codex's "not ready" verdict is directionally right but ~half its reasoning is wrong — it missed most of
the thesis's existing disclosures. The genuinely actionable set is small: two disclosure defects it
partially caught (one it missed entirely), one open internal item it independently rediscovered (D3),
and a batch of wording/presentation fixes. NOTHING requires re-running the UCF experiment grid.

## Verdict table

| Claim | Verdict (inv / xcheck) | What's real | What Codex got wrong |
|---|---|---|---|
| C1 smoothness mismatch | PARTIALLY_REAL / agree | Legacy-axis training of UCF runs is real; NEW: footnote misdescribes the seed MIXTURE (see below) | ch03 "exactly as implemented" refers to released code (true); grid partially re-run already; legacy code archived in git |
| C2 eval protocol | PARTIALLY_REAL / agree | 64x64 PNG inputs CONFIRMED ON DISK; disclosure was lost when Appendix D was archived (2026-07-14) | 254/290 + 3FPS/20s fully disclosed; zero absolute leaderboard claims; caption defines "apples-to-apples" scope |
| C3 corruption asymmetry | ALREADY_ADDRESSED / **overturned to PARTIALLY_REAL** | Clean-cache reuse for gaussian/brightness is real AND disclosed verbatim ("rescue, not robustness"); but the RTMPose-robustness justification is asserted, never measured = open internal item D3 | Not hidden: dedicated ch04 subsection + per-family table column + ch06 honest caveat; blur/JPEG skeletons WERE re-extracted (10/20 conditions) |
| C4 TTA selection leakage | PARTIALLY_REAL / agree | Six strategies screened on the same UCF-C test conditions -> residual selection risk worth one honest caveat | Search openly narrated in ch06; CORAL killed by held-out LOCO check; winner reproduced on unseen axes; LN-barrier already scoped + ch02 cites SAR's LN results |
| C5 gate confound | PARTIALLY_REAL / agree | Learned-scalar late fusion exists (late_fusion.py:55 nn.Parameter) but never run; 3 sentences over-attribute | "proves/confirms" misread; video-level bootstrap EXISTS (B=2000); capacity confound points the WRONG way (gated 498K < late 633K params, disclosed) |
| C6 presentation (9 items) | PARTIALLY_REAL / agree | (c) GF 2-Person definition false, (e) Fig 5.4 illegible, (f) Fig 6.2 caption, (g) "clear leader" x3, (h) empty PDF metadata, (i) Carreira bib bug, (a) acknowledgments | (b) approval forms are self-documented pre-defense placeholders; (d) Table 7.1 borderline (deliberate full-page float); (f) narrower than claimed |

## The finding Codex missed (C1)

The ch04:247-252 footnote says UCF numbers "were produced with an earlier implementation" — as a blanket
statement this is itself WRONG. Verified via config_snapshot git SHAs + checkpoint hashes: all Gated
Fusion rows (incl. the 82.5 headline, May 22, pre-fix d71865ca) and all seed-42 runs are legacy-trained;
but seeds 123/2024 of 5 of the 6 UCF table rows were trained June 9-10 AFTER fix f89910b with the
corrected temporal implementation at the same lambda=8e-4. The footnote must describe this mixture.
Silver lining: the mixture is multi-config empirical evidence the axis change is sub-seed-noise (mixed
rows' stds 0.1-0.8 are indistinguishable from the homogeneous-legacy headline row's 0.4).

## Action plan (recommended)

**Wording batch (zero GPU, one session):**
1. C1: rewrite ch04 footnote to describe the legacy/corrected seed mixture precisely + note legacy code recoverable from git (e5ab412/f89910b, tests/test_smoothness_axis.py).
2. C2: restore the 64x64 disclosure to ch04 (adapt archived appD_engineering.tex:128-132); fix ch03:76 "medium-resolution"; add one protocol-note sentence to tab:sota-fair caption.
3. C4: add 4th honest caveat (selection on the reported test conditions; mitigations: label-free/tuning-free constraint, LOCO check killed CORAL, reproduction on unseen backbones/severities/seeds) to ch06 + mirror in 7.7.4.
4. C5: soften ch01:197-198, ch09:27-28, ch07:43-44 (gate attribution).
5. C6: fix GF 2-Person definition (per-person concat vs default top-2 mean-pool); reword Fig 6.2 caption (cite Table 6.1); soften "clear leader" x3; \hypersetup pdftitle/pdfauthor/pdfkeywords; fix Jo{\~a}o Carreira in references.bib; regenerate fig_gate_by_category at print-size fonts.

**GPU jobs worth running before the defense (both small):**
6. C3/D3 prep run: re-extract RTMPose on a SAMPLE of test videos under gaussian s3-s5 + brightness s5; measure keypoint deviation (OKS/confidence drop) vs clean cache; one sentence + table row in ch04. Converts the load-bearing unmeasured assertion into a measurement. Directly answers committee question "would the gain survive pose from corrupted frames?"
7. C5 learned-scalar late-fusion baseline: already implemented, cached features, ~minutes per config (8 configs x 3 seeds). Defuses "why was learned-weight late fusion not evaluated?"

**Author-only:** acknowledgments text (unchanged blocker).

**Explicitly rejected:** re-running the UCF grid (C1) — the mixture + o55 A/B already bound the effect; disclosure is the correct instrument. Renaming the fair-subset table — caption already defines its scope; optional style.

## Per-claim evidence


### C1-smoothness — PARTIALLY_REAL (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/LOW | effort wording-hours

**Evidence:**
- Legacy axis confirmed: tests/test_smoothness_axis.py:4-6 documents the bug as 'shifted dim 0 (arr2[:-1] = arr[1:])' i.e. the batch/video axis; current src/losses/mil_loss.py:51-54 shifts dim 1 (temporal): arr2[:, :-1] = scores_abn[:, 1:].
- Fix commit exists and archives the legacy code: git f89910b (2026-06-01 17:33) diff shows exact legacy 'arr2[:-1] = arr[1:]'; original implementation at parent commit e5ab412 (2026-04-14). Legacy objective is fully recoverable from git.
- Thesis disclosure exists: thesis/chapters/ch04_experimental_setup.tex:247-252 footnote (PDF body p.31 = viewer p.49, verified by text extraction): 'The reported UCF-Crime numbers were produced with an earlier implementation of the smoothness term; the released code applies the penalty strictly along the temporal (adjacent-snippet) axis. We verified that this change shifts the headline UCF-Crime AUC by at most 0.13 percentage points...so the UCF-Crime results are reported as run.'
- ch03 exactness claim: thesis/chapters/ch03_methodology.tex:13-14 says 'All equations in this chapter are stated exactly as implemented in the released code' — TRUE as written: Eq eq:loss (ch03:254) shows temporal smoothness sum_i (a_i - a_{i+1})^2, which matches the released mil_loss.py:51-54. The claim's referent is the released code, not the code that trained UCF results; the ch04 footnote covers that gap.
- o55 scope confirmed as ONE config, ONE seed: .planning/quick/260601-o55-.../260601-o55-SUMMARY.md:62-83 — seed-42 control-vs-fixed A/B on gated_fusion_giant UCF only; AUC -0.13pp (negligible), AP -1.23pp (material); control reproduced canonical 0.8249 bit-for-bit.
- Headline row is homogeneously legacy: results/ucf_gated_fusion_giant_s42 best_model.pth SHA 6f988683... identical to best_model.pth.pre_lam0.bak (May 22) and matches eval_metrics.json checkpoint_sha; eval_timestamp 2026-05-22T15:36:38, git d71865ca (pre-fix). s123 eval_ts 2026-05-22T15:57:19; all Gated Fusion rows trained Apr 16-May 22 (git 10d95a3f/1c89d48d/d71865ca), all pre-fix.
- NEW FINDING (footnote scope is factually wrong the other way): seeds 123/2024 of 5 of the 6 UCF table rows were trained AFTER the fix with the CORRECTED implementation at lam=8e-4 — results/ucf_late_fusion_s123 (eval_ts 2026-06-10, git 3bf52487), ucf_skeleton_only_s123 (2026-06-10, git 3f484555), ucf_gated_fusion_giant_2person_s123 (2026-06-10, git 3f484555), ucf_clip_only_giant_s123 + ucf_clip_only_s2024 (2026-06-09, git d9df6db4); all config_snapshot.json show lam_smooth 0.0008. So UCF rows MIX legacy (s42) and corrected (s123/s2024) objectives, undisclosed; the footnote's blanket 'produced with an earlier implementation' is inaccurate for ~34 of the UCF runs.
- The mixing accidentally provides multi-config evidence the AUC impact is sub-noise: mixed-implementation rows in thesis/chapters/ch05_results_fusion.tex:44-50 show seed stds 0.1-2.0 (e.g. Visual Only Giant 82.4+/-0.2, GF 2-Person Giant 82.6+/-0.3), indistinguishable from the homogeneous-legacy Gated Fusion row (82.5+/-0.4) — a material implementation effect would inflate these stds.
- The material AP effect (-1.23pp) never surfaces in reported UCF numbers: ch05_results_fusion.tex:37 caption — UCF ablation table reports 'AUC (%)' only; UCF per-category analysis (ch05:595-597) is AUC; no UCF AP is tabled.
- XD has zero legacy exposure: all XD fusion runs retrained at lam_smooth=0.0 post-fix (results/xd_gated_fusion_so400m_s42/config_snapshot.json lam_smooth 0.0, eval_ts 2026-06-01T23:59:18, git 83403d01; xd_late_fusion_s123 eval_ts 2026-06-10). With lam=0 the smoothness term is multiplied by zero, so the axis bug is mathematically irrelevant to every XD number.
- Decision provenance: .planning/quick/260602-339-stage3-xd-lam0-paper-propagation/260602-339-SUMMARY.md:16-23 — 'XD canonical = lam_smooth 0 (retrained, kept), UCF canonical = lam_smooth 8e-4 (frozen)'; 'UCF = disclose-and-keep (no retrain; one-sentence footnote)'. The lam=0 UCF experiment (Jun 1 22:30) was run and then UCF checkpoints were restored bit-identical from .pre_lam0.bak (hash-verified).
- Ch6 TTA results are built on the headline gated_fusion_giant checkpoints (all three seeds legacy, May 22) — covered by the footnote's 'reported as run'; ch07_discussion.tex:540-543 only references the smoothness setting for XD variance, no additional exposure.

**Codex errors:**
- Codex's proposed fix 'remove claims that the reported equations exactly match the released implementation' misreads ch03:14 — the thesis claims equations match the RELEASED code (true: Eq at ch03:254 = mil_loss.py:51-54); it never claims the UCF results were produced by the released code — the ch04:247-252 footnote explicitly states the opposite.
- 'Rerun the UCF grid' is overstated: Codex missed that seeds 123/2024 of 5 of 6 UCF rows were ALREADY trained with the corrected implementation (config_snapshot.json git shas 3f484555/3bf52487/d9df6db4 post-date fix f89910b), so the grid is partially re-run and the consistent within-row stds (0.1-0.8 for most rows) empirically bound the AUC effect below seed noise across multiple configs, not just one.
- 'Archive its code' is already satisfied: the legacy implementation is preserved in git (e5ab412, exact diff in f89910b) and its formula is documented verbatim in tests/test_smoothness_axis.py:4-6 — no additional archival needed, at most a pointer in the thesis.
- Codex frames the situation as 'reported results ... do not match released implementation' uniformly; in fact the mismatch is heterogeneous — only the Gated Fusion rows (incl. headline) and all seed-42 runs are legacy-trained; the June-9/10 seeds match the released code exactly. The real defect is that the footnote misdescribes this mixture, which Codex did not identify.

**Thesis already discloses:** ch04_experimental_setup.tex:247-252 (PDF viewer p.49, body p.31) footnote: "The reported UCF-Crime numbers were produced with an earlier implementation of the smoothness term; the released code applies the penalty strictly along the temporal (adjacent-snippet) axis. We verified that this change shifts the headline UCF-Crime AUC by at most 0.13 percentage points---within the 0.1--0.4% seed variance reported in Chapter 5---so the UCF-Crime results are reported as run." This correctly discloses and quantifies the headline-config mismatch, and ch03:254's Eq shows the temporal form matching released code. What it does NOT disclose: (a) the -0.13pp came from a single-config, single-seed A/B (Codex's valid point), (b) the corrected implementation also lowered UCF AP by 1.23pp on that config (immaterial to the thesis since no UCF AP is tabled), and (c) the footnote's blanket wording is factually wrong for seeds 123/2024 of the Visual-Only/Late-Fusion/GF-2-Person/GF-Mean-Only/Skeleton-Only rows, which were trained 2026-06-09/10 with the corrected temporal implementation — UCF rows silently mix the two objectives across seeds.

**Recommended action:** Rewrite the ch04 footnote for precision — no retraining. State: (1) all seed-42 UCF runs and all three seeds of the Gated Fusion rows (including the 82.5+/-0.4 headline) were trained with the earlier implementation, which penalized score differences between bag-adjacent videos (dim 0) rather than temporally adjacent snippets — one clause suffices to 'document the legacy objective precisely'; (2) seeds 123/2024 of the remaining UCF rows were trained after the correction with the released temporal implementation at identical lambda_2=8e-4, and their per-row seed stds (0.1-0.8 for most rows) are indistinguishable from the homogeneous rows, corroborating the headline A/B finding that the change is below seed variance for AUC (-0.13pp on the headline configuration); (3) optionally note the legacy code is recoverable at the parent of fix commit f89910b and characterized by tests/test_smoothness_axis.py. Also prepare a one-line defense answer for 'why not rerun the grid': measured effect sub-noise for AUC, UCF tables report AUC only, XD fully retrained at lambda=0 where the term vanishes. ch03:14 can stand as written since it refers to the released code, but adding 'see the Chapter 4 footnote for the one training-provenance caveat on UCF-Crime' would close the residual tension.


### C2-protocol — PARTIALLY_REAL (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/MEDIUM | effort wording-hours

**Evidence:**
- 254/290 exclusion IS disclosed in a dedicated subsection: 'We disclose this exclusion prominently because it makes our UCF-Crime evaluation cohort slightly smaller than the nominal 290-video test set used by methods that operate on per-frame or 16-frame features' — thesis/chapters/ch04_experimental_setup.tex:70-81; also ch04:28-30 and ch05_results_fusion.tex:506. Codex arithmetic correct: 290-254=36, 36/290=12.41%.
- 64x64 resolution is TRUE, verified on disk: E:\UCF_crime_dataset\test\Abuse\Abuse028_x264_0.png measures 64x64 px (PowerShell System.Drawing check, this session).
- Both streams consume the 64x64 PNGs for UCF: CLIP extraction reads E:/UCF_crime_dataset/{Train|test}/.../{video_id}_x264_{N}.png via PIL then open_clip preprocess — scripts/extract_clip.py:20, 85-86, 260; skeleton extraction reads the same PNG tree — scripts/extract_skeletons.py:76-77, 161, 188.
- XD-Violence is NOT affected: decoded from native .mp4 via decord at native ~24 FPS — scripts/extract_clip.py:21, 45; thesis ch04:45-46.
- The 64x64 disclosure existed ONLY in appendices now archived out of the build: 'The UCF-Crime distribution provides frames as 64x64-pixel PNGs pre-sampled at roughly 3 FPS; this resolution is sufficient for CLIP/SigLIP2 preprocessing (which resizes anyway) and for pose estimation' — thesis/appendices_archived/appD_engineering.tex:128-131; also appF_figures.tex:43, 73.
- Appendices A-F were removed from the build on 2026-07-14, commit f860753 ('archive appendices A-F out of the build'); thesis/main.tex:60-61 confirms no appendix is included. Grep for '64\times64|64x64|pixel|resolution' across thesis/chapters/ finds NO spatial-resolution disclosure in the current build; ch03_methodology.tex:76 even says 'medium-resolution surveillance frames'.
- The ~3 FPS / ~20 s snippet span IS disclosed twice: 'a 64-frame snippet spans about 20 s on UCF-Crime versus ~2.7 s on XD-Violence' — ch04:122-125; and ch03:119-126, which adds 'the 20-second effective snippet duration on UCF-Crime stretches this encoder well outside its pretraining regime, an honest caveat'. Distribution FPS at ch04:31-33 ('frames pre-sampled at roughly 3 FPS, every tenth frame of its ~30 FPS source video'). Codex arithmetic fine (64/3~21 s; thesis says ~20 s, exclusion threshold 'under ~21 seconds' ch04:81).
- Table 7.2 caption literally uses the phrase: 'Fair-subset (apples-to-apples) comparison. Subset definition: frozen features, no backbone fine-tuning, no learnable text/prompt branch, visual-only RGB (or RGB+skeleton), and a single-GPU-class feature head' — ch07_discussion.tex:314-323; aux confirms tab:sota-fair = Table 7.2, PDF p.64 (thesis/chapters/ch07_discussion.aux newlabel {7.2}{64}). The caption scopes fairness by REGIME, not evaluation protocol; the 254-cohort / 3-FPS / 64x64 caveats are not repeated in any ch07 table note (grep '254|cohort|exclusion' in ch07: no hits near the tables).
- Table 7.1 (tab:sota-full) already hedges comparability: 'it is never a bare head-to-head, because the Setting column makes each row's regime explicit' (ch07:224-226) and bolded 'We do not claim state-of-the-art' in the table notes (ch07:267-270).
- No absolute leaderboard claims exist: 'We do not claim state-of-the-art performance; the honest claim is competitiveness within the constrained frozen / visual-only / single-GPU regime' — ch07:214-216; 'presented below as supporting evidence, not as a claim to the leaderboard' — ch07:362-366; 'a reasonable mid-pack entry on XD-Violence rather than an outlier' — ch07:430-432; 'We do not claim state-of-the-art benchmark performance' — ch09_conclusion.tex:74. The fair-subset read itself concedes UCF 82.5 'trails the frozen-feature pack' — ch07:348-350.
- CLIP-TSA differences already acknowledged: 'The closest single comparator, CLIP-TSA, uses CLIP visual features with a temporal self-attention module and multi-crop-style aggregation that this work deliberately omits; we hypothesize the residual gap to it stems from that temporal-modeling and aggregation machinery... but we do not isolate this experimentally' — ch07:350-355 and ch07:425-430.
- Minor wording issue found during verification: ch04:31-32 says 'The dataset is distributed as frames pre-sampled at roughly 3 FPS' — this describes the specific frame re-distribution the project used, not the original Sultani et al. video release the compared methods use; combined with the missing 64x64 sentence, the current ch04 dataset description understates the input-fidelity difference vs published pipelines.

**Codex errors:**
- Codex implies the exclusion and temporal-sampling conditions are undisclosed; in fact the 254/290 exclusion has a dedicated subsection with explicit cross-method comparability framing (ch04:70-81), and the ~3 FPS / ~20 s snippet span is disclosed twice with an 'honest caveat' sentence (ch03:119-126, ch04:122-125).
- 'Avoid absolute leaderboard claims' targets a non-existent problem: the thesis disclaims SOTA in four places (ch07:214-216, ch07:267, ch07:362-366, ch09:74) and self-describes as 'mid-pack' (ch07:431); Table 7.2 is used to show the work TRAILS the frozen-feature pack on UCF (ch07:348-350), not to claim a ranking.
- The rename suggestion overlooks that the Table 7.2 caption immediately defines its 'apples-to-apples' scope ('Subset definition: frozen features, no backbone fine-tuning, no learnable text/prompt branch...', ch07:314-316) — the phrase is scoped to training regime, so renaming is a style preference; the substantive gap is the missing protocol note, not the name.

**Thesis already discloses:** 254/290 exclusion: fully disclosed with comparability framing (ch04:70-81 'We disclose this exclusion prominently... slightly smaller than the nominal 290-video test set'; ch04:28-30; ch05:506). ~3 FPS distribution and ~20 s snippet span: fully disclosed (ch04:31-33, ch04:122-125, ch03:119-126 including the NTU-pretraining-regime caveat). SOTA hedging: 'not a bare head-to-head' (ch07:224), 'We do not claim state-of-the-art' (ch07:214-216, 267), 'not as a claim to the leaderboard' (ch07:362-366), CLIP-TSA architectural differences hypothesized as gap source (ch07:350-355). NOT disclosed in the current build: the 64x64-pixel input resolution — that sentence lived only in Appendix D (appendices_archived/appD_engineering.tex:128-131) and Appendix F, which were archived out of the build on 2026-07-14 (commit f860753; main.tex:60-61).

**Recommended action:** Two wording fixes, no experiments. (1) Restore the 64x64 disclosure into the built body: add one sentence to ch04 UCF-Crime dataset description (near ch04:31-34), adapted from archived appD_engineering.tex:128-131 — the distribution used provides 64x64-pixel PNG frames pre-sampled at ~3 FPS; CLIP/SigLIP2 preprocessing resizes to the backbone input size and RTMPose operates on these frames; note this input fidelity is lower than the native-resolution video used by published pipelines. Also soften ch03:76 'medium-resolution surveillance frames' or scope it to XD. (2) Add a short protocol note to the Table 7.1/7.2 notes (ch07:263-270 and ch07:341-343): this work's UCF numbers are computed on the 254-video cohort (Sec 4.1.4) from the 64x64 / ~3 FPS frame distribution, so cross-paper absolute comparison carries protocol differences beyond the regime taxonomy. Renaming 'apples-to-apples' to 'regime-matched subset' is optional style — the caption already defines its scope; adding the protocol cross-reference is what actually closes the gap. No change needed for the exclusion, FPS/snippet-span, or leaderboard-claim sub-parts (already addressed).


### C3-corruption-asymmetry — ALREADY_ADDRESSED (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/HIGH | effort analysis-day

**Evidence:**
- scripts/corruption.py:28-31 — SKELETON_REEXTRACT_TYPES = ("motion_blur", "jpeg_compression"); comment states RTMPose 'produces effectively identical keypoints' on gaussian/brightness
- src/tta/evaluate_tta.py:112-139 — evaluation loads corrupted skeleton dirs (skeleton_{type}_{severity}) for blur/JPEG, clean 'skeleton/' cache for gaussian_noise and brightness
- data/features/ucf/ directory listing — skeleton_motion_blur_1..5 and skeleton_jpeg_compression_1..5 exist with 255 test-video .npy files each; NO skeleton_gaussian_noise_* or skeleton_brightness_* dirs exist (verified via ls + wc)
- scripts/extract_skeletons.py:212,448,847 — corruption applied to frames BEFORE RTMPose inference for the re-extracted families; scripts/batch_extract_corrupted.py:28,105-129 ran skeleton+CTR-GCN for exactly those 10 conditions
- thesis/chapters/ch04_experimental_setup.tex:190-215 — dedicated 'Feature re-extraction policy' subsection: skeleton re-extracted ONLY for motion blur + JPEG; clean cache reused for gaussian/brightness; explicitly states the asymmetry 'creates the clean-skeleton/corrupted-visual asymmetry that the test-time reweighting mechanism of Chapter 6 exploits'
- thesis/chapters/ch04_experimental_setup.tex:170-184 — tab:corruption-params carries a per-family 'Skeleton re-extract' column (gaussian no / JPEG yes / brightness no / blur yes)
- thesis/chapters/ch06_results_tta.tex:286-291 (tab:tta_breakdown) — gains +2.67/+5.99/+8.95/+1.69 under gaussian only; 0.00 for motion blur and JPEG; +0.01/0.00 for brightness; family average +4.83 — Codex's 'all substantive gain is gaussian' is TRUE and printed in the thesis's own table
- thesis/chapters/ch06_results_tta.tex:330-342 — text states 'The gain is concentrated almost entirely on Gaussian noise' and explains why: under JPEG the skeleton cache is itself re-extracted from corrupted frames and degraded, so no robust stream remains to route toward
- thesis/chapters/ch06_results_tta.tex:349-354 — 'for Gaussian noise and brightness the skeleton cache is the clean one, reused unchanged; Chapter 4' ... 'The gain there is therefore recovery by routing to an unaffected modality, not intrinsic robustness of the fused features' — the exact narrowed claim Codex demands
- thesis/chapters/ch06_results_tta.tex:367-371 — 'Honest caveats' item 1: 'it is rescue, not robustness' — verbatim the framing Codex says the thesis should adopt
- thesis/chapters/ch09_conclusion.tex:121-127 and 135-140 — the two future-work statements reconcile with no contradiction: 'clean cache remains intact' refers to the gaussian/brightness families; 'corrupted skeleton caches for this family already exist' refers to JPEG (which WAS re-extracted)
- thesis/chapters/ch04_experimental_setup.tex:199-209 — the justification for clean-cache reuse ('RTMPose produces effectively identical keypoints on the corrupted frames') is asserted as fact, not backed by a measurement in the thesis
- .planning/research/PITFALLS.md:250-262 — pitfall M7 anticipated this exact issue; prevention item 4 (run RTMPose on 50 corrupted frames per type, measure keypoint-confidence drop) has NO execution artifact anywhere in .planning/, scripts/, or results/ (grep for keypoint confidence/OKS/sensitivity check returns only the plan text)
- .planning/THESIS-REVIEW-2026-07-11.md:70 (item D3) — the project's own internal review already flags this verbatim: 'asserted, never measured... 100% of the headline robustness gain lives exactly in those conditions; the z_skel≈z0 gate check is circular for reused-cache conditions' with a recommended keypoint-delta prep run; per project memory D3 is still OPEN
- thesis/frontmatter/abstract.tex:43-44 and thesis/chapters/ch01_introduction.tex:221 — headline phrase 'corruption-robust skeleton stream' compresses the disclosure and leans on the unmeasured assumption (minor wording exposure; ch04/ch06 fully qualify it)

**Codex errors:**
- Codex's 'artificial advantage' framing implies hidden bias, but the asymmetry is disclosed in a dedicated ch04 subsection (ch04:190-215), a per-family table column (ch04:170-184), and re-disclosed at the point of interpretation in ch06 (ch06:349-354)
- Codex's recommended remedy 'narrow the claim to asymmetric modality corruption with a clean cached skeleton stream' is already implemented: ch06:353-354 calls the gain 'recovery by routing to an unaffected modality, not intrinsic robustness' and Honest caveats #1 (ch06:369-371) is literally 'it is rescue, not robustness'
- Codex omits that skeletons WERE re-extracted from corrupted frames for motion blur and JPEG (10 of 20 conditions) — corruption.py:31, extract_skeletons.py:212/448, and populated data/features/ucf/skeleton_{motion_blur,jpeg_compression}_1..5 caches (255 test files each) prove the asymmetry is family-specific by disclosed policy, not benchmark-wide
- Codex implies the gaussian-only concentration of the gain is something the reader must discover; the thesis prints it in tab:tta_breakdown and states it in prose ('concentrated almost entirely on Gaussian noise', ch06:330-331) with a mechanism explanation for why JPEG/blur are zero (ch06:339-342, 356-365)

**Thesis already discloses:** Fully. Ch04 §'Feature re-extraction policy' (ch04:190-215) states skeleton features are re-extracted only for motion blur + JPEG and that the clean cache is reused for gaussian/brightness, flags it as a cost decision, and says the asymmetry 'is not merely an engineering footnote: it creates the clean-skeleton/corrupted-visual asymmetry that the test-time reweighting mechanism of Chapter 6 exploits.' The severity table carries a per-family 'Skeleton re-extract' yes/no column. Ch06 re-discloses at the point of interpretation (ch06:349-354) and scopes the result exactly as Codex demands: 'recovery by routing to an unaffected modality, not intrinsic robustness'; Honest caveats #1 is 'it is rescue, not robustness' (ch06:369-371). The abstract claims only corrupted-input AUC under a transductive protocol with no clean-data gain (abstract.tex:45-48). The ch09 future-work statements reconcile (clean cache = gaussian/brightness families; existing corrupted skeleton caches = JPEG family). Residual gap (real, internally known): the JUSTIFICATION for clean-cache reuse — 'RTMPose produces effectively identical keypoints' (ch04:202-204) — is asserted, never measured; the project's own review tracks this as open defense-prep item D3 (.planning/THESIS-REVIEW-2026-07-11.md:70), noting the skeleton-trust gate is circular for reused-cache conditions. Also 'corruption-robust skeleton stream' in abstract:44/ch01:221 compresses the disclosure.

**Recommended action:** Reject Codex's premise that the claim needs narrowing — the thesis already scopes it verbatim as 'rescue, not robustness' with full policy disclosure. The real residual is internal item D3: (1) run the small sampled validation already recommended there — re-extract skeletons on a sample of test videos under gaussian s3-s5 (and brightness s5), measure keypoint deviation (OKS / mean confidence drop) vs the clean cache, and ideally re-run disc_reweight on those cells with re-extracted skeletons; add one sentence + a small table row to ch04 §reextraction citing the measurement, which converts the 'effectively identical keypoints' assertion into evidence and makes the +8.95/+13.2 cells defensible. (2) Hours-level wording polish: soften 'corruption-robust skeleton stream' in abstract.tex:44 and ch01:221 to reference the disclosed re-extraction policy (e.g. 'the skeleton stream, unaffected under these corruption families per the Chapter 4 re-extraction policy'). If the sampled run shows material keypoint drift at gaussian s4-s5, escalate to full gaussian-family skeleton re-extraction (~1-2 GPU days per the Phase 5 D-02 budget) before the defense.

**Cross-check overturn rationale:** Overturn ALREADY_ADDRESSED to PARTIALLY_REAL: Codex's non-disclosure framing is wrong (keep those rebuttals), but the substantive core survives and the project's own review already tracks it as open HIGH item D3 (THESIS-REVIEW-2026-07-11.md:70, 3/3 verifiers, 'cannot be fully fixed by wording alone') — a concern cannot be graded already-addressed while the project's internal review holds it open. Before the defense: (1) EXPERIMENT (the D3 prep run, small GPU job): re-extract RTMPose skeletons on a sample of test videos under gaussian s3-s5 and brightness s5, measure keypoint deviation (OKS / mean-confidence delta) against the clean cache, and re-run disc_reweight on those cells with re-extracted skeletons; add the measurement as one sentence + a table row in ch04 §reextraction, converting the ch04:202 assertion into evidence and making the +8.95/+13.2 cells defensible. If material drift appears at s4-s5, escalate to full gaussian-family skeleton re-extraction (~1-2 GPU days) and update tab:tta_breakdown. (2) WORDING (hours): qualify 'corruption-robust skeleton stream' at abstract:44, ch01:221, ch03:360/364, ch06:422, ch07:166 (e.g., 'the skeleton stream, unaffected under these corruption families per the Chapter 4 re-extraction policy'); add the clean-cache assumption as a third scope cap in ch07 sec:lim-tta:563; add one clause in ch03:382-393 stating explicitly that for reused-cache conditions the gate check is satisfied by construction, so gate behavior there is a design consequence, not an empirical safety verification.


### C4-tta-selection — PARTIALLY_REAL (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/MEDIUM | effort wording-hours

**Evidence:**
- scripts/_tmp_strat_template.py:5-12 — screening protocol: each strategy run per backbone (clip-vit-b-16, siglip2-so400m) over '4 families x sev{3,5} = 8 conditions'; HONESTY RULE requires label-free/tuning-free transforms and asserts 'Then the reported test-C delta is automatically honest (no val-C needed)' — i.e., candidates were scored on corrupted TEST AUC with no validation split, by design.
- scripts/_tmp_strat_template.py:44-51 — the screen loop loads each UCF-Crime-C test condition and records src_auc vs strat_auc deltas; results/_coral_derisk/strat_S1..S6_{clip,so400m}.json hold these test-set screen results (e.g. strat_S4_fusegate_clip.json summary mean_delta=0.383, per-condition gaussian_noise_5 +1.76).
- results/_coral_derisk/r1full_*.json, r2full_*.json, s4full_*.json — three finalist methods were all run on the FULL reported grid (4 backbones x 20 UCF-Crime-C test conditions, seed 42); computed mean deltas: R1 +0.727/+0.270/+1.966/+0.351 vs R2 +0.680/+0.221/+1.972/+0.353 vs S4 +0.256/-0.117/+0.252/+0.170 (clip/base/so400m/giant) — R1 (disc_reweight) was selected by comparing test-benchmark results.
- scripts/_tmp_r2_full.py:1-13 — R2 was an R1 variant ('R1 + clip-skel rank-AGREEMENT gate... Reports R1 and R2 side by side. All 4 bb x 20 cond.'), confirming candidate comparison happened on the full test grid before adoption.
- results/_coral_derisk/variants/v_{bb}_s{123,2024}_test.json + v_{bb}_s42_train.json — post-selection held-out axes: seeds 123/2024 and a clean-TRAIN-reference variant were run AFTER adoption; screening itself saw only seed 42, 2 of 4 backbones, and severities {3,5}. All 12 backbone-x-seed cells positive (thesis ch06:264-266).
- thesis/chapters/ch06_results_tta.tex:208-220 — the thesis DISCLOSES the development process: 'We explored this through a six-strategy search (shift-gated blending, channel selection, projection-space correction, fusion-gate modulation, skeleton-score blending, and an open-ended track), whose surviving candidate is the discriminative-reliability reweighting... Before adoption it was validated adversarially: ... its search-time gains reproduced exactly on the production pipeline.' It does NOT state the search was scored on the same reported test benchmark.
- thesis/chapters/ch06_results_tta.tex:196-206 — the rejected CORAL path WAS killed by held-out validation ('a leave-one-condition-out evaluation---the label-free operating-point selection a deployment would actually face---scored -1.62 points with zero of four backbones positive'), showing held-out checks were applied to methods with free parameters but not to the final winner's selection.
- thesis/chapters/ch06_results_tta.tex:367-383 'Honest caveats' — lists exactly three limitations (rescue-not-robustness; dispersion proxy over-routing; transductive pooling); selection-on-the-test-benchmark is NOT among them. Same for ch07_discussion.tex:553-576 (sec:lim-tta lists only transductive + over-routing caps).
- thesis/chapters/ch05_results_fusion.tex:158-166 — the thesis elsewhere explicitly flags 'selecting the per-cell maximum across ablation variants after seeing the results would constitute a mild form of test-set selection', proving the authors apply this standard — but never apply it to the TTA strategy search.
- thesis/chapters/ch04_experimental_setup.tex:133 — corruptions are applied 'to the UCF-Crime test set, yielding 20 corruption conditions in total'; no corrupted validation split and no second corrupted dataset (XD-Violence-C) exists, so no unseen family/dataset was available for selection.
- thesis/chapters/ch02_related_work.tex:330-337 — the thesis ALREADY concedes Codex's SAR point verbatim: 'SAR itself reports results on LayerNorm-based vision transformers, where the affine parameters remain the adaptation surface. What happens when that surface is confined to a small frozen-model fusion head---and the evaluation metric is a rank statistic (AUC) invariant to monotone score changes---has not been examined in the VAD literature.'
- github.com/mr-eggplant/SAR README (fetched 2026-07-17) — results table includes 'VitBase (LN)' (58.0±0.5) and 'ResNet-50 (GN)' models (vitbase_timm, resnet50_gn_timm), confirming SAR successfully adapts LN/GN models in full-network classification — consistent with what ch02:333-335 already discloses.
- Barrier wording is consistently scoped, not general: abstract.tex:38-42 ('restricted by the architecture to the fusion head's LayerNorm parameters... a structural null result'); ch01_introduction.tex:212-215 ('We argue this is structural rather than a tuning artifact'); ch06:415-418 and ch07:151-162 ('adaptation confined to LayerNorm affine parameters cannot move a frozen, rank-based detector'; '1,536-parameter affine correction... has almost no capacity'); ch09:46-52 ('restricted by the architecture to the 1,536 LayerNorm affine scalars of the fusion head... under the three-seed continual protocol').
- thesis/chapters/ch06_results_tta.tex:229-245 (tab:tta) — reported headline (+0.67/+1.50/+2.24/+0.42, mean +1.21, 3 seeds) is computed on the SAME 20 UCF-Crime-C test conditions used for the strategy screen and finalist comparison; results/_coral_derisk/r1full_*.json + variants are cited as the table's source (comment ch06:225-227).
- thesis/chapters/ch06_results_tta.tex:277-294 + 356-365 — per-family zeros on motion blur/JPEG/brightness are produced by the gate 'by construction', a mechanism-level (not fitted) property that partially mitigates the selection concern: the gain lives only where the routing story predicts it (Gaussian noise, +4.83 family mean).

**Codex errors:**
- Codex implies the method is presented 'as if designed a priori' — false: ch06_results_tta.tex:208-220 explicitly narrates the failed CORAL path and the six-strategy search, naming all six tracks and calling disc_reweight the 'surviving candidate'.
- Codex implies the LayerNorm-barrier claim is an unscoped general structural claim needing softening — but every barrier statement is already conditioned on the frozen fusion head's 1,536 affine scalars + rank-based AUC + frozen upstream features (abstract.tex:40-41; ch01:212-215 hedged as 'We argue'; ch06:415-418; ch07:151-162; ch09:47-52), which is precisely the 'null under this frozen-head protocol' framing Codex asks for.
- Codex implies the thesis is unaware SAR considers group/layer norm — false: ch02_related_work.tex:333-335 states 'SAR itself reports results on LayerNorm-based vision transformers, where the affine parameters remain the adaptation surface' and scopes the thesis question to the unexamined frozen-head + rank-metric configuration.
- 'Selection should have used a validation split' overlooks documented mitigations in the record: a leave-one-condition-out held-out protocol WAS used and killed the CORAL candidate (ch06:200-202), each screened strategy was constrained label-free/tuning-free with knobs fixed by stated principles (scripts/_tmp_strat_template.py:9-12), and the winner reproduced on axes unseen at screen time (2 backbones, severities 1/2/4, seeds 123/2024) — though none of this substitutes for an unseen corruption family, so the core critique stands.

**Thesis already discloses:** The six-strategy search itself and disc_reweight's origin as its 'surviving candidate' (ch06:210-214); the adversarial pre-adoption checks (bit-identical at w=1, no labels, production reproduction, ch06:218-220); CORAL's rejection via held-out leave-one-condition-out validation (ch06:200-202); three honest caveats — rescue-not-robustness, over-routing, transductive pooling (ch06:367-383; ch07:563-576); SAR reporting results on LayerNorm ViTs, with the thesis gap scoped to a frozen fusion head + rank metric (ch02:333-337); and barrier claims consistently scoped to the 1,536 frozen affine scalars and rank-based AUC (abstract:38-42, ch01:212-215, ch06:415-418, ch07:151-162, ch09:46-52). NOT disclosed: that the six strategies and the R1/R2/S4 finalists were scored on the same UCF-Crime-C test conditions later reported as the headline (+1.21pp), and that no corrupted validation split, unseen family, or second dataset was held out for method selection — this specific caveat is absent from both limitation sections, despite ch05:164 flagging the analogous 'mild test-set selection' risk for fusion variants.

**Recommended action:** Split the response. (1) Selection-leakage sub-claim: concede-and-disclose with a wording fix, no new experiments. Add one caveat (fourth item) to ch06 'Honest caveats' (after line ~383) and mirror it in ch07 sec:lim-tta: the six-strategy search and finalist comparison were scored on the same 20 UCF-Crime-C test conditions reported in Table tab:tta, so a residual researcher-selection risk remains even though each candidate was label-free and tuning-free with knobs fixed by stated principles; mitigation: the adopted method's gains reproduced on backbones (Base, Giant), severities (1,2,4), and seeds (123, 2024) unseen at screen time, its zeros on three families are produced by the gate by construction, and the rejected CORAL path shows held-out validation was decisive where free parameters existed; no unseen corruption family or second corrupted dataset was available, which bounds the claim. Prepare the matching defense answer for 'how did you choose disc_reweight?'. (2) LayerNorm-barrier sub-claim: no softening of the scoped claims needed (they already say frozen + rank-based + 1,536 affine params, and ch02 concedes SAR-on-LN-ViT); optionally add one preemptive half-sentence inside ch06 sec:ln-barrier (~line 401) cross-referencing ch02: 'SAR itself adapts LN affine parameters successfully in full-network classification; the barrier here is the conjunction of a frozen 1,536-parameter surface with a rank metric, not LayerNorm per se.' Total effort: a few sentences + rebuild.


### C5-gate-confound — PARTIALLY_REAL (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/MEDIUM | effort analysis-day

**Evidence:**
- src/models/late_fusion.py:33 is the constructor arg `alpha: str = "equal"`; the learned scalar mode exists at lines 53-55 (`self.alpha_logit = nn.Parameter(torch.zeros(1))`) — Codex is right the capability exists in-repo
- All 8 late-fusion configs pin equal weighting: configs/late_fusion*.yaml:19-20 `alpha: equal # D-08 default; switch to 'learned' only if equal underperforms`; grep of results/ for learned|alpha returns zero hits — the learned baseline was never trained
- .planning/phases/03-model-architecture-training-infrastructure/03-CONTEXT.md:50 pre-registered 'learned scalar α∈[0,1] if equal weighting underperforms either single-modal baseline'; ch05_results_fusion.tex:120-124 shows equal-weight late fusion DID underperform visual-only (78.9 vs 81.2 UCF) — the trigger fired but the variant was never run and no planning doc revisits the decision
- tests/test_models.py:160-177 unit-tests the learned-alpha mode (init=0.5, trainable) — implemented and tested, never trained: a genuine missing-baseline gap
- Capacity confound runs OPPOSITE to Codex's implication: results/benchmark_models.csv shows Gated Fusion 498,113 trainable params < Late Fusion 633,218 < CLIP-Only 595,649; thesis ch04_experimental_setup.tex:390-394 explicitly states 'the gated fusion head is *smaller* than the late-fusion head (498K vs 633K...) while performing substantially better'
- GF Mean-Only and GF 2-Person are pooling/aggregation ablations under the same gated architecture (ch05:140-166, ch03:160-162), not gate-off or capacity controls — Codex correct that no additive/residual feature-fusion control exists
- No zeroed/shuffled-skeleton ablation exists anywhere; zeroed-modality forwards under the gated architecture exist only as TTA solo-score machinery (scripts/_tmp_r2_full.py:31 zeroes the skeleton projection; scripts/_tmp_disc_signal.py:6), not as a complementarity control — and mock-committee Q5 (.planning/THESIS-REVIEW-2026-07-11.md:105) already notes these solo scores are off-manifold
- Video-level bootstrap DOES exist in the thesis: ch05_results_fusion.tex:316-356 (§Statistical Robustness, B=2000, video-level resampling, UCF CI [75.63,88.02] width 12.39pp) with explicit cross-method-restraint warnings — only the PAIRED delta bootstrap is absent
- Paired bootstrap was probed and deliberately not adopted: .planning/REVIEW-2026-06-10.md:155,186 — Pri-10 'Wilcoxon over paired bootstraps' MAYBE-tier, 'Single-config probe not significant (P(>0)=0.599)... paper already hedges correctly'; the thesis's own mock-committee re-flagged it (.planning/THESIS-REVIEW-2026-07-11.md:89)
- Complementarity language is already hedged: ch05:93-97 ('consistency indicates', sign test p≈0.03 with correlation caveat), ch05:245-252 ('a consistency check rather than a claim of eight independent replications'; 'no individual configuration's gain exceeds its own seed std by a large factor'), ch07:36-54 recovery-vs-extraction triangulation ('None of the three is decisive alone; together they indicate'), ch07:59-65 per-class 'directionally supportive but mixed and noisy', abstract.tex:29-31 'small but consistent'
- 'proves' is NEVER attached to gate/complementarity claims — the only body 'proves' is ch03_methodology.tex:347 'restoration proves rank-disruptive' (TTA topic, idiomatic 'proves to be'); 'requires' appears only in RQ1's question form (ch01:157) and efficiency statements (ch09:65,72; ch07:100)
- Three sentences DO over-attribute to input-dependence when a learned STATIC alpha was never tested: ch01_introduction.tex:197-198 'establishing that learned, input-dependent gating is what allows a weak modality to help'; ch09_conclusion.tex:27-30 'The value of the modality is only realized through learned, input-dependent gating... confirming that how the streams are combined matters'; ch07_discussion.tex:43-44 'equal-weight late fusion underperforms precisely because it has no input-dependent way to perform that suppression' — a learned static α could also perform the suppression that accounts for 10.7 of the 12.6 points (ch07:41-43)
- ch05:461 and ch05:490 'confirming' attach to directly measured gate-activation variation by category (input-dependence in operation, a legitimate direct observation); the gap attribution itself is self-labeled as attribution ('We attribute this to', ch05:109; 'to which Section~5.1 attributes', ch05:490-491)
- The missing learned-alpha baseline is cheap, not GPU-days: ch04:378-383 'training completes in under 5 minutes per configuration on the single RTX 4090' with all features cached — 8 configs x 3 seeds ≈ 24 runs ≈ 2-3 GPU-hours plus one table row
- ch07:508-511 shows the thesis already has a disclosure template for non-isolated attributions ('we do not isolate this experimentally and leave a controlled ablation to future work' re CLIP-TSA gap) — the same clause is absent for the gate-mechanism attribution

**Codex errors:**
- 'replace proves/requires/confirms' misreads the text: 'proves' never modifies any gate/complementarity claim (only ch03:347, a TTA restoration result, idiomatic usage); 'requires' appears only as RQ1's question phrasing (ch01:157) and in efficiency statements — the complementarity claims already use 'attribute/suggests/indicates/consistency check' (ch05:96,109,123,248-249; ch07:52-53)
- Implies no bootstrap analysis exists: the thesis has a dedicated video-level bootstrap CI section (ch05:316-356, B=2000, video-level resampling) — only the paired-delta variant is missing, and an internal probe already showed it non-significant for a single config (P(>0)=0.599, .planning/REVIEW-2026-06-10.md:186), an outcome the thesis text is already scoped to survive
- The implied capacity confound points the wrong way for gated-vs-late: the gated head has FEWER trainable parameters than the late-fusion head (498K vs 633K) and than CLIP-Only (596K), and the thesis discloses this explicitly (ch04:390-394; results/benchmark_models.csv) — 'gated wins because it is bigger' is contradicted on the benchmarked configuration
- Line-number nit: late_fusion.py:33 is the string-mode constructor argument; the learned nn.Parameter is at line 55 (substance of the point stands)
- Overlooks that the gate's input-dependence is directly measured (gate activations vary by category/dataset, fig_gate_by_category, ch05:459-462) — 'confirms' there attaches to an observed property, not an inferred cause

**Thesis already discloses:** The thesis already discloses most of what Codex asks the language to concede: the complementarity gain is framed as 'small but consistent' with per-config gains 'individually within seed variance' (abstract:29-31; ch05:95-96, 250-251); the sign test is explicitly downgraded to 'a consistency check rather than a claim of eight independent replications' with a correlation caveat (ch05:245-249); ch07:36-54 decomposes the 12.6-pt gated-vs-late gap into ~10.7 pts of suppression-recovery vs ~1.9 pts of extraction and states 'None of the three [evidence lines] is decisive alone'; the per-class mechanism check is called 'directionally supportive but mixed and noisy' (ch07:59-65); a video-level bootstrap CI section warns that 1-2-pt cross-method gaps 'should be read with restraint, including where they favor us' (ch05:316-356); and ch04:390-394 pre-empts the capacity confound for gated-vs-late (gated head smaller yet better). NOT disclosed: that a learned-scalar late-fusion variant exists in code behind a pre-registered switch rule that was arguably triggered but never run, and that no gate-off/zeroed-skeleton control was run — plus three residual sentences (ch01:197-198, ch09:27-30, ch07:43-44) that attribute the effect specifically to 'input-dependent' gating, which the run matrix cannot separate from a learned static weight.

**Recommended action:** Two-part fix. (1) Wording (hours): soften the three over-attributing sentences — ch01:197-198 'establishing that learned, input-dependent gating is what allows' -> 'indicating that a learned combination — here, input-dependent gating — is what allows'; ch09:27-28 'only realized through learned, input-dependent gating' -> 'only realized through learned gating (the mechanism tested here)'; ch07:43-44 'underperforms precisely because it has no input-dependent way' -> 'consistent with its lack of any learned way'; optionally add one sentence to ch07 Limitations mirroring the existing ch07:508-511 template: a learned-scalar late-fusion weight (implemented in the released code) and a gate-ablated additive control were not run, so the attribution to input-dependence specifically is not experimentally isolated. (2) Optional cheap defense insurance (~2-3 GPU-hours, NOT days): flip `alpha: learned` in the 8 late-fusion configs and run 3 seeds each (head-only, <5 min/run per ch04:383, features cached) — if learned-static alpha recovers most of late fusion's deficit the softened wording is required; if it does not, the input-dependence claim is vindicated and one table row/footnote strengthens the defense. Do NOT run the paired video-level bootstrap as a headline statistic: the internal Pri-10 probe (P(>0)=0.599) predicts non-significance per config, which the text already concedes; if the committee asks, answer from ch05 §Statistical Robustness plus the seed-variance concessions. For 'zeroed-skeleton' questions, note the eval-time zeroed-skeleton forward already exists as the TTA solo-score machinery (scripts/_tmp_r2_full.py:31) and can be scripted as an eval control in minutes.


### C6-presentation — PARTIALLY_REAL (cross-check: PARTIALLY_REAL) | defense risk MEDIUM/MEDIUM | effort wording-hours

**Evidence:**
- (a) CONFIRMED-DELIBERATE: PDF p.7 shows '[Acknowledgments to be written by the author.]' — thesis/frontmatter/acknowledgments.tex:10; lines 3-5 self-document it as a 'Permanent user-fill placeholder: exempt from the no-placeholder rule (spec S11.2)'. Real but intentional; must be filled by the author before binding.
- (b) ALREADY_ADDRESSED: PDF pp.3-4 are the standard pre-defense placeholder pages. recommendation.tex:2-4 and approval.tex:2-4 self-document 'Replace this whole page with the officially signed ... form before binding'; the rendered pages themselves print that instruction in gray (PDF p.3: 'replace with the signed advisor recommendation form before binding'; p.4 same for the committee approval form).
- (c) REAL (wording, hours): Codex's mechanics are accurate. Default pipeline retains top-2 persons (ch03_methodology.tex:79) and mean-pools over the person dim M (ch03:119; scripts/extract_ctrgcn.py:241-245: default feat = out.mean(dim=[1,3,4]) over M=2, keep_persons pools only T'/V' giving [2,256]). The '2-Person' ablation keeps per-person features and concatenates [N,2,256]->[N,512] (configs/gated_fusion_2person.yaml:2-5,29 skel_agg: concat). But ch05_results_fusion.tex:29-30 defines GF 2-Person as replacing 'the default single-person aggregation' — factually wrong (default is 2-person mean-pooled, not single-person) and contradicts ch03:79/119; nowhere does the thesis state the variant is per-person-concat. The name 'GF 2-Person' is genuinely misleading since the default also uses 2 people.
- (d) PARTIALLY REAL (borderline objective): Table 7.1 (tab:sota-full) is on PDF p.81; 24 data rows + header x 6 columns, set with \small + \resizebox{\textwidth} (ch07_discussion.tex:228-231). Measured via PyMuPDF span sizes on p.81: table body renders at 6.6pt effective (1964 chars), superscripts 4.8-6.0pt, caveat notes ~7.9-8.0pt over ~30 lines. 6.6pt is objectively below the ~8pt floor most print guidelines assume, so it is more than pure style preference — but it is a deliberate full-page [p] float and remains legible on screen.
- (e) REAL (presentation, hours): Figure 5.4 = fig:gate-by-category (main.lof entry 5.4, PDF p.61; ch05:448-464), two 0.48\textwidth PNG subfigures each rendered 2.83 in wide (bboxes ~204pt; sources 2074x1038 and 1516x1025 px -> 732/535 effective dpi), so in-figure titles, axis and 14 UCF category labels render at roughly 2pt equivalent — unreadable in a 150-dpi page render. The caption (ch05:459-462) and prose (ch05:488-491) rest the claim 'gate values vary systematically by category / vary by category and dataset rather than saturating' on exactly the per-category detail that is illegible at this size (UCF boxes look flat at rendered scale).
- (f) REAL (caption wording, hours): Figure 6.2 = fig:tta-comparison (ch06_results_tta.tex:250-260). Caption states 'Entropy-minimization TTA (TENT, SAR) is indistinguishable from source-only', but text extracted from thesis/figures/fig_tta_comparison.pdf shows only two series — legend 'Source-Only' and 'Ours', title 'Reweighting vs Source-Only (3 seeds, 20 corruptions)' — no TENT/SAR bars. The TENT/SAR null result exists in the thesis but as Table 6.1 (PDF printed p.49; ch06 tab caption 'worst per-backbone magnitude is 0.082 points'), not in this figure. Codex says 'curves' (it is a bar chart) but the substance holds.
- (g) CONFIRMED phrase, 3 occurrences: ch01_introduction.tex:205 'SO400M emerges as the clear leader only under gated fusion'; ch05:180-181 (Fig 5.1 caption) 'SigLIP2 SO400M is the clear leader under gated fusion---a dataset-specific preference that emerges through fusion'; ch05:220 'resolves a clear leader (SO400M, 78.7%)'. Against Table 5.2 (ch05:72): gated XD SO400M 78.7±0.9 vs Giant 76.8±2.8 — Giant's ±2.8 band overlaps SO400M's mean, so 'clear' is quantitatively contestable; the prose partially hedges ('std 0.9% AP for three of the four', ch05:219).
- (h) REAL (trivial, minutes): fitz doc.metadata on thesis/main.pdf: title='', author='', subject='', keywords='' (creator 'LaTeX with hyperref'). preamble.tex:65 loads hyperref as \usepackage[bookmarksnumbered,hidelinks]{hyperref} with no pdftitle/pdfauthor and no \hypersetup anywhere (grep of preamble.tex + main.tex).
- (i) REAL (bib fix, minutes): references.bib:149 'author = {Jo\~{a}o Carreira and Andrew Zisserman}' breaks BibTeX/IEEEtranN name abbreviation: main.bbl:265-266 emits '\bibitem[{a}o Carreira and Zisserman(2017)]' and 'J.~{a}o Carreira'; the built PDF (p.94, entry [38]) visually renders 'J. ao Carreira' (verified from a 200-dpi render of the region). Not PDF mojibake — a BibTeX special-character bug; fix is 'Jo{\~a}o' or 'Carreira, Jo{\~a}o'.

**Codex errors:**
- (b) Framing 'unsigned approval forms' as a defect: pp.3-4 are self-documented placeholder pages ('replace with the signed ... form before binding', recommendation.tex:12-13 / approval.tex:12-13, printed on the pages themselves) — standard pre-defense state, not an error.
- (f) 'curves that are not shown' — Figure 6.2 is a bar chart, not curves; and the TENT/SAR result is presented in the thesis (Table 6.1, worst |delta| 0.082pp), just not in this figure. The caption defect is real but narrower than Codex implies.
- (d) 'objective print-size problem' is only half-right: the table is a deliberate single-page [p] float; the measured 6.6pt effective body font is small but the page is complete and legible at zoom — this sits on the border with style preference rather than being an unambiguous violation.

**Thesis already discloses:** Items (a) and (b) are self-documented placeholders: acknowledgments.tex:3-5 marks itself a permanent user-fill placeholder, and recommendation.tex/approval.tex print 'replace with the signed form before binding' on the pages themselves. The TENT/SAR null result behind the Figure 6.2 caption IS disclosed in Table 6.1 (worst |delta| 0.082pp) — the defect is only that the figure itself omits those series. Ch03:79/119 correctly discloses the default top-2-person mean-pooled aggregation; the contradiction is confined to ch05:29-30's 'single-person aggregation' wording. The 'clear leader' claim is partially hedged at ch05:218-220 (visual-only within seed variance; stability claimed only for 'three of the four' backbones).

**Recommended action:** One zero-GPU wording/presentation quick task before the defense: (1) fix ch05:29-30 — rename 'GF 2-Person' to something like 'GF Per-Person (concat)' or keep the name but define it correctly ('preserves the two per-person CTR-GCN features and concatenates them to 512-d, in place of the default mean-pool over the two retained persons'), removing the false 'single-person aggregation' phrase; (2) regenerate fig_gate_by_category_{ucf,xd}.png with print-size fonts (scripts/generate_phase6_charts.py exists) or stack them full-width; (3) reword the Fig 6.2 caption to cite Table 6.1 for the TENT/SAR null ('...is indistinguishable from source-only (Table 6.1)') or add TENT/SAR bars; (4) add \\hypersetup{pdftitle=...,pdfauthor=...,pdfkeywords=...} to preamble.tex; (5) fix references.bib:149 to Jo{\\~a}o and rebuild (bbl regenerates); (6) optionally soften 'clear leader' at ch01:205, ch05:180, ch05:220 (e.g. 'leads under gated fusion') given Giant's ±2.8 overlap; (7) optionally split Table 7.1 or narrow the Setting column to lift body type above ~8pt. Leave (a)/(b) as-is until post-defense binding (fill acknowledgments then).

